name=ICMP CLI
description=This plugin provides a command line interface (CLI) for Internet Control Message Protocol (ICMP).
category=Common
quality=test

requiredApis=command-interpreter2,icmp,stack

icmp-cli.c

setup(commandLineConfiguration) {
  icmp listen, icmpListenCommand, b
  icmp ping,   icmpPingCommand,   bv*
}
