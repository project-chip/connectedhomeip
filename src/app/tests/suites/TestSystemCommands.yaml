# Copyright (c) 2022 Project CHIP Authors
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

name: System Commands Tests

config:
    nodeId: 0x12344321
    cluster: "SystemCommands"
    endpoint: 0
    payload:
        type: CHAR_STRING
        defaultValue: "MT:-24J0IX4122-.548G00" # This value needs to be generated

tests:
    - label: "Wait for the commissioned device to be retrieved"
      cluster: "DelayCommands"
      command: "WaitForCommissionee"
      arguments:
          values:
              - name: "nodeId"
                value: nodeId

    - label: "Stop the default accessory"
      command: "Stop"
      arguments:
          values:
              - name: "registerKey"
                value: "default"

    - label: "Start the default accessory with no command line options"
      command: "Start"
      arguments:
          values:
              - name: "registerKey"
                value: "default"

    - label: "Stop the default accessory"
      command: "Stop"
      arguments:
          values:
              - name: "registerKey"
                value: "default"

    - label:
          "Start the default accessory with discriminator command line option"
      command: "Start"
      arguments:
          values:
              - name: "registerKey"
                value: "default"
              - name: "discriminator"
                value: 1111

    - label: "Stop the default accessory"
      command: "Stop"
      arguments:
          values:
              - name: "registerKey"
                value: "default"

    - label:
          "Start the default accessory with discriminator and port command line
          options"
      command: "Start"
      arguments:
          values:
              - name: "registerKey"
                value: "default"
              - name: "discriminator"
                value: 1111
              - name: "port"
                value: 5560

    - label: "Stop the default accessory"
      command: "Stop"
      arguments:
          values:
              - name: "registerKey"
                value: "default"

    - label: "Start the default accessory with all command line options"
      command: "Start"
      arguments:
          values:
              - name: "registerKey"
                value: "default"
              - name: "discriminator"
                value: 1111
              - name: "port"
                value: 5560
              - name: "kvs"
                value: "/tmp/chip_kvs_default"

    - label: "Start a second accessory with all command line options"
      command: "Start"
      arguments:
          values:
              - name: "registerKey"
                value: "chip-lock-app"
              - name: "discriminator"
                value: 50
              - name: "port"
                value: 5561
              - name: "kvs"
                value: "/tmp/chip_kvs_lock"

    - label: "Commission second accessory from alpha"
      identity: "alpha"
      cluster: "CommissionerCommands"
      command: "PairWithQRCode"
      arguments:
          values:
              - name: "nodeId"
                value: 0xDEADBEEF
              - name: "payload"
                value: payload

    - label: "Wait for the second commissioned device to be retrieved for alpha"
      identity: "alpha"
      cluster: "DelayCommands"
      command: "WaitForCommissionee"
      arguments:
          values:
              - name: "nodeId"
                value: 0xDEADBEEF

    - label: "Reboot the second accessory with all command line options"
      command: "Reboot"
      arguments:
          values:
              - name: "registerKey"
                value: "chip-lock-app"
              - name: "discriminator"
                value: 50
              - name: "port"
                value: 5561
              - name: "kvs"
                value: "/tmp/chip_kvs_lock"

    - label: "Stop the second accessory"
      command: "Stop"
      arguments:
          values:
              - name: "registerKey"
                value: "chip-lock-app"

    - label: "Reboot the default accessory with no command line options"
      command: "Reboot"
      arguments:
          values:
              - name: "registerKey"
                value: "default"

    - label:
          "Reboot the default accessory with discriminator command line option"
      command: "Reboot"
      arguments:
          values:
              - name: "registerKey"
                value: "default"
              - name: "discriminator"
                value: 2222

    - label:
          "Reboot the default accessory with discriminator and port command line
          options"
      command: "Reboot"
      arguments:
          values:
              - name: "registerKey"
                value: "default"
              - name: "discriminator"
                value: 2222
              - name: "port"
                value: 5565

    - label: "Reboot the default accessory with all command line options"
      command: "Reboot"
      arguments:
          values:
              - name: "registerKey"
                value: "default"
              - name: "discriminator"
                value: 2222
              - name: "port"
                value: 5565
              - name: "kvs"
                value: "/tmp/chip_kvs_default"

    - label: "Factory Reset the default accessory"
      command: "FactoryReset"
      arguments:
          values:
              - name: "registerKey"
                value: "default"

    - label: "Factory Reset the second accessory"
      command: "FactoryReset"
      arguments:
          values:
              - name: "registerKey"
                value: "chip-lock-app"
