# Copyright (c) 2021 Project CHIP Authors
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
# Auto-generated scripts for harness use only, please review before automation. The endpoints and cluster names are currently set to default

name:
    100.2.13. [TC-DL-2.13] Verification for the following Commands - Set User,
    Get User, Get User Response, Clear User [DUT-Server]

config:
    nodeId: 0x12344321
    cluster: "Basic"
    endpoint: 0

tests:
    - label:
          "TH reads NumberOfTotalUsersSupported attribute and saves for future
          use."
      verification: |
          https://github.com/project-chip/connectedhomeip/issues/15776
      disabled: true

    - label:
          "TH sends Set User Command to DUT with the following values:
          OperationType as 0 UserIndex as 2 UserName as xxx UserUniqueID as 6452
          UserStatus as 1 UserType as 0 CredentialRule as 0"
      verification: |

      disabled: true

    - label:
          "TH sends Set User Command to DUT with the following values:
          OperationType as 0 UserIndex as 2 UserName as xxx UserUniqueID as 6452
          UserStatus as 1 UserType as 10 CredentialRule as 3"
      verification: |

      disabled: true

    - label:
          "TH sends Set User Command to DUT with the following values:
          OperationType as 0 UserIndex as 2 UserName as xxx UserUniqueID as 8965
          UserStatus as 1 UserType as 0 CredentialRule as 0"
      verification: |

      disabled: true

    - label:
          "TH sends Set User Command to DUT with the following values:
          OperationType as 0 UserIndex as 26 UserName as xxx UserUniqueID as
          6452 UserStatus as 1 UserType as 1 CredentialRule as 0"
      verification: |

      disabled: true

    - label:
          "TH sends Set User Command to DUT with the following values:
          OperationType as 0 UserIndex as 2 UserName as NULL UserUniqueID as
          NULL UserStatus as NULL UserType as NULL CredentialRule as NULL"
      verification: |

      disabled: true

    - label:
          "TH sends Set User Command to DUT with the following values:
          OperationType as 2 UserIndex as 2 UserName as NULL UserUniqueID as
          NULL UserStatus as NULL UserType as NULL CredentialRule as NULL"
      verification: |

      disabled: true

    - label: "TH sends Get User Command to DUT"
      verification: |

      disabled: true

    - label: "TH sends Clear User Command to DUT"
      verification: |

      disabled: true

    - label: "TH sends Get User Command to DUT"
      verification: |

      disabled: true
