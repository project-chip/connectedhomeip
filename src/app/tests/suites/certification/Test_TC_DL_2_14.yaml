# Copyright (c) 2021 Project CHIP Authors
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
# Auto-generated scripts for harness use only, please review before automation. The endpoints and cluster names are currently set to default

name:
    100.2.14. [TC-DL-2.14] Verification for the following Commands - Set
    Credential , Set Credential Response, Get Credential Status, Get Credential
    Status Response, Clear Credential [DUT-Server]

config:
    nodeId: 0x12344321
    cluster: "Basic"
    endpoint: 0

tests:
    - label:
          "TH reads NumberOfTotalUsersSupported attribute and saves for future
          use."
      verification: |
          https://github.com/project-chip/connectedhomeip/issues/15776
      disabled: true

    - label:
          "TH sends Set Credential Command to DUT with the following fields:
          OperationType as 0 Credential as 1 1 CredentialData as xxxx UserIndex
          as 2 UserStatus as 0 UserType as 0"
      verification: |

      disabled: true

    - label:
          "TH sends Set Credential Command to DUT with the following fields:
          OperationType as 0 Credential as 1 1 CredentialData as xxxx UserIndex
          as 2 UserStatus as 5 UserType as 10"
      verification: |

      disabled: true

    - label:
          "TH sends Set Credential Command to DUT with the following fields:
          OperationType as 0 Credential as 1 1 CredentialData as xxxx UserIndex
          as 2 UserStatus as 0 UserType as 0"
      verification: |

      disabled: true

    - label:
          "TH sends Set Credential Command to DUT with the following fields:
          OperationType as 0 Credential as 1 1 CredentialData as xxxx UserIndex
          as 2 UserStatus as 0 UserType as 0"
      verification: |

      disabled: true

    - label:
          "TH sends Set Credential Command to DUT with the following fields:
          OperationType as 0 Credential as 1 2 CredentialData as xxxx UserIndex
          as 3 UserStatus as 0 UserType as 0"
      verification: |

      disabled: true

    - label:
          "TH sends Set Credential Command to DUT with the following fields:
          OperationType as 2 Credential as 1 3 CredentialData as xxxx UserIndex
          as 3 UserStatus as 0 UserType as 0"
      verification: |

      disabled: true

    - label: "TH sends Get Credential Status Command"
      verification: |

      disabled: true

    - label:
          "TH sends Clear Credential Command to DUT with the following fields:
          CredentialType as 1 CredentialIndex as 2"
      verification: |

      disabled: true

    - label:
          "TH sends Set Credential Command to DUT with the following fields:
          OperationType as 0 Credential as 1 1 CredentialData as xxxx UserIndex
          as 2 UserStatus as 0 UserType as 0"
      verification: |

      disabled: true

    - label:
          "TH sends Clear Credential Command to DUT with the following fields:
          CredentialType as 1 CredentialIndex as 0xFFFE"
      verification: |

      disabled: true

    - label:
          "TH sends Set Credential Command to DUT with the following fields:
          OperationType as 0 Credential as 1 1 CredentialData as xxxx UserIndex
          as 2 UserStatus as 0 UserType as 0"
      verification: |

      disabled: true

    - label: "TH sends Clear Credential Command to DUT"
      verification: |

      disabled: true

    - label: "TH sends Get Credential Status Command"
      verification: |

      disabled: true

    - label:
          "TH sends Set Credential Command to DUT with the following fields:
          OperationType as 0 Credential as 1 1 CredentialData as xxxx UserIndex
          as 2 UserStatus as 0 UserType as 0"
      verification: |

      disabled: true

    - label:
          "TH sends Clear Credential Command to DUT with the following fields:
          CredentialType as 8 CredentialIndex as 2"
      verification: |

      disabled: true
