name=Critical Message Queue

category=Works With All Hubs

# This plugin is part of the Works With All Hubs release
released=wwah

qualityString=Alpha Release

quality=development

description=Silicon Labs' implementation of a critical message queue

sourceFiles=critical-message-queue.c,critical-message-queue-cli.c

includedByDefault=false

dependsOnClusterServer=sl works with all hubs

providedApis=critical-message-queue

implementedCallbacks=emberAfWwahAppEventRetryManagerConfigBackoffParamsCallback,emberAfWwahAppEventRetryManagerSetBackoffParamsToDefault

events=retry
