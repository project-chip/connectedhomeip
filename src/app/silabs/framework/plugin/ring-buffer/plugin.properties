name=Ring Buffer
description=This plugin manages a ring buffer of binary entries.  Each entry has a 1 or 2-byte length field that is recorded to manage the buffer.  This ring buffer can be used by multiple entities as the buffer space for the ring buffer is allocated by the caller.  
category=Utility
quality=development
qualityString=In Development

# This plugin is part of the Works With All Hubs release
released=wwah

includedByDefault=false

providedApis=ring-buffer
sourceFiles=ring-buffer.c

