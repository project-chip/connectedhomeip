name=OTA Bootload Cluster Common Code
category=ZigBee OTA Bootloading

# Any string is allowable here.  Generally it is either: Production Ready, Test Tool, or Requires Extending
qualityString=Production Ready
# This must be one of the following:  productionReady, testTool, extensionNeeded
quality=production

introducedIn=

description=Common code for Ember's implementation of the Zigbee Over-the-air Bootload Cluster Client and Server plugins (a multi-hop, application bootloader).

sourceFiles=ota-common.c

# Commented out until App. Builder changes to allow 'emAf' prefixes.
#implementedCallbacks=emAfOtaCreateOtaImageIdStruct, emAfOtaParseImageIdFromMessage, emAfPrintPercentageSetStartAndEnd, emAfPrintPercentageUpdate

dependsOnClusterServer=over the air bootloading
dependsOnClusterClient=over the air bootloading

# If any of the above dependencies are satisfied, it can be turned on.
allClusterDependenciesRequired=FALSE

includedByDefault=TRUE
