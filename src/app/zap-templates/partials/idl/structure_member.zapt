{{~#if isOptional~}} optional {{/if~}}
{{~#if isNullable~}} nullable {{/if~}}

{{type}} {{~#if (isString type)~}}
  {{~#if maxLength~}} 
    <{{maxLength}}> 
  {{~/if~}}
{{~/if}} {{asLowerCamelCase label~}}

{{~#if isArray~}}
  []
{{~/if}} = {{fieldIdentifier}};