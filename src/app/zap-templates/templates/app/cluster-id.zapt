{{> header}}

// Prevent multiple inclusion
#pragma once

#include <app/util/basic-types.h>

{{#zcl_clusters}}

// Definitions for cluster: {{label}}
static constexpr chip::ClusterId ZCL_{{asDelimitedMacro define}}_ID = {{asHex code 4}};

{{/zcl_clusters}}
