mobly
pyasn1
pyasn1_modules
zeroconf
ifaddr
langcodes
requests
pycountry
validators
nest-asyncio
pyserial

# support_modules/pqc_support.py verifies ML-DSA attestation signatures in Python rather than
# through the SDK's C++ crypto, so the harness stays an independent check on the stack. That needs
# hazmat.primitives.asymmetric.mldsa, first importable in cryptography 47.0.0 (46.0.0 and 46.0.7
# do not provide it); the wheel bundles an OpenSSL new enough to back it. Below this floor
# _ml_dsa_module() returns None on the ImportError, so the ML-DSA cases in
# test_testing/test_pqc_support.py skip rather than run and the suite still reports green.
cryptography>=47
