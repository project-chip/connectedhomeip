#include "Project.xcconfig"

ENABLE_NS_ASSERTIONS = NO
VALIDATE_PRODUCT = YES

// TODO: remove, only adding whitespace to match existing value
GCC_PREPROCESSOR_DEFINITIONS = $(nothing) $(GCC_PREPROCESSOR_DEFINITIONS)
