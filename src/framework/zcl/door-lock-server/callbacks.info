#include PLATFORM_HEADER

/** @brief Activate Door Lock
 *
 * This function is provided by the door lock server plugin.
 *
 * @param activate True if the lock should move to the locked position, false
 * if it should move to the unlocked position Ver.: always
 *
 * @return true if the callback was able to activate/deactivate the Lock.
 */
bool chipAfPluginDoorLockServerActivateDoorLockCallback(bool activate)
{
  return true;
}
