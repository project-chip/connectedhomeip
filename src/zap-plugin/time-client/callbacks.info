#include PLATFORM_HEADER

/** @brief Calls back user when a Network time server available
 *
 * This function is called from the client when a Time Server is available on the network.
 *
 * @param timeServer Pointer to the destination of the Time Server  Ver.: always
 *
 * @return FALSE if the application is not handling the update of Time Servers on this node.
 */
bool emberZclClusterTimeClientTimeSyncCallback(EmberZclDestination_t * timeServer)
{
  return FALSE;
}
