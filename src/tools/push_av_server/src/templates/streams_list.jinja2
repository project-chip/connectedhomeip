<!DOCTYPE html>
<html>

<head>
    <title>Push AV Server</title>
    <link href="{{ url_for('static', path='/styles.css') }}" rel="stylesheet">
</head>

<body>
    <nav class="top-level">
        <a href="/ui/streams">Streams</a>
        <a href="/ui/certificates">Certificates</a>
    </nav>
    <div class="left-bar">
        <h2>Streams</h2>

        <ul>
            {% for stream in streams %}
            <li> Stream {{stream.id}}
                <ul>
                    <li>Error{% if stream.error_uploads|length > 1 %}s{% endif %}: {{stream.error_uploads|length}}</li>
                    <li>Session{% if stream.sessions|length > 1 %}s{% endif %}: {{stream.sessions|length}}</li>
                    <li><a href="/ui/streams/{{stream.id}}">Open details</a></li>
                </ul>
            </li>
            {% endfor %}
        </ul>
        <div class="center">
        </div>
</body>

</html>