TestBeds:
    # A test bed where pw_rpc will find a CHIP device
    - Name: SampleTestBed
      Controllers:
          ChipDevice:
              - device_tty: /dev/ttyACM0
                baud: 115200
