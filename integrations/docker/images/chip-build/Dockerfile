# start with Ubuntu 18.04LTS
FROM ubuntu:bionic

RUN set -x \
 && apt-get update \
 && apt install \
 && apt-get install -fy \
      git \
      curl \
      jq \
      make \
      autoconf \
      automake \
      libtool \
      g++ \
      clang-9 \
      clang-format-9 \
      clang-tidy-9 \
      lcov \
      shellcheck \
