language: minimal

services:
  - docker

before_install:
- ./integrations/ci-tools/pre_build.sh

script:
- ./integrations/ci-tools/build.sh

matrix:
  include:
    - name: "Main Build"
      env: TASK="build-linux"
      
    - name: "Shell Check"
      env: TASK="preflight-shell-check"
    
