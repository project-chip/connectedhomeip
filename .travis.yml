language: generic

services:
  - docker

script:
- ./integrations/travis/build.sh

matrix:
  include:
    - name: "Main Build"
      env: TASK="build-linux"
      
    - name: "Shell Check"
      env: TASK="preflight-shell-check"
    
