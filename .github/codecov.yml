coverage:
  status:
    project:
      # default is the status check's name, not default settings
      default:
        if_ci_failed: success
    patch:
      # default is the status check's name, not default settings
      default:
        if_ci_failed: success

parsers:
  cobertura:
    partials_as_hits: true