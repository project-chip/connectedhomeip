name: World Build

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Run Build
        run: ./bootstrap && make -f Makefile-Standalone distcheck
