/*
 * This file is generated by gdbus-codegen, do not modify it.
 *
 * The license of this code is the same as for the D-Bus interface description
 * it was derived from. Note that it links to GLib, so must comply with the
 * LGPL linking clauses.
 */

#ifdef HAVE_CONFIG_H
#  include "BuildConfig.h"
#endif

#include "DbusBluez.h"

#include <string.h>
#ifdef G_OS_UNIX
#  include <gio/gunixfdlist.h>
#endif

typedef struct
{
  GDBusArgInfo parent_struct;
  gboolean use_gvariant;
} _ExtendedGDBusArgInfo;

typedef struct
{
  GDBusMethodInfo parent_struct;
  const gchar *signal_name;
  gboolean pass_fdlist;
} _ExtendedGDBusMethodInfo;

typedef struct
{
  GDBusSignalInfo parent_struct;
  const gchar *signal_name;
} _ExtendedGDBusSignalInfo;

typedef struct
{
  GDBusPropertyInfo parent_struct;
  const gchar *hyphen_name;
  guint use_gvariant : 1;
  guint emits_changed_signal : 1;
} _ExtendedGDBusPropertyInfo;

typedef struct
{
  GDBusInterfaceInfo parent_struct;
  const gchar *hyphen_name;
} _ExtendedGDBusInterfaceInfo;

typedef struct
{
  const _ExtendedGDBusPropertyInfo *info;
  guint prop_id;
  GValue orig_value; /* the value before the change */
} ChangedProperty;

static void
_changed_property_free (ChangedProperty *data)
{
  g_value_unset (&data->orig_value);
  g_free (data);
}

static gboolean
_g_strv_equal0 (gchar **a, gchar **b)
{
  gboolean ret = FALSE;
  guint n;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  if (g_strv_length (a) != g_strv_length (b))
    goto out;
  for (n = 0; a[n] != NULL; n++)
    if (g_strcmp0 (a[n], b[n]) != 0)
      goto out;
  ret = TRUE;
out:
  return ret;
}

static gboolean
_g_variant_equal0 (GVariant *a, GVariant *b)
{
  gboolean ret = FALSE;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  ret = g_variant_equal (a, b);
out:
  return ret;
}

G_GNUC_UNUSED static gboolean
_g_value_equal (const GValue *a, const GValue *b)
{
  gboolean ret = FALSE;
  g_assert (G_VALUE_TYPE (a) == G_VALUE_TYPE (b));
  switch (G_VALUE_TYPE (a))
    {
      case G_TYPE_BOOLEAN:
        ret = (g_value_get_boolean (a) == g_value_get_boolean (b));
        break;
      case G_TYPE_UCHAR:
        ret = (g_value_get_uchar (a) == g_value_get_uchar (b));
        break;
      case G_TYPE_INT:
        ret = (g_value_get_int (a) == g_value_get_int (b));
        break;
      case G_TYPE_UINT:
        ret = (g_value_get_uint (a) == g_value_get_uint (b));
        break;
      case G_TYPE_INT64:
        ret = (g_value_get_int64 (a) == g_value_get_int64 (b));
        break;
      case G_TYPE_UINT64:
        ret = (g_value_get_uint64 (a) == g_value_get_uint64 (b));
        break;
      case G_TYPE_DOUBLE:
        {
          /* Avoid -Wfloat-equal warnings by doing a direct bit compare */
          gdouble da = g_value_get_double (a);
          gdouble db = g_value_get_double (b);
          ret = memcmp (&da, &db, sizeof (gdouble)) == 0;
        }
        break;
      case G_TYPE_STRING:
        ret = (g_strcmp0 (g_value_get_string (a), g_value_get_string (b)) == 0);
        break;
      case G_TYPE_VARIANT:
        ret = _g_variant_equal0 (g_value_get_variant (a), g_value_get_variant (b));
        break;
      default:
        if (G_VALUE_TYPE (a) == G_TYPE_STRV)
          ret = _g_strv_equal0 (g_value_get_boxed (a), g_value_get_boxed (b));
        else
          g_critical ("_g_value_equal() does not handle type %s", g_type_name (G_VALUE_TYPE (a)));
        break;
    }
  return ret;
}

/* ------------------------------------------------------------------------
 * Code for interface org.bluez.Adapter1
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:BluezAdapter1
 * @title: BluezAdapter1
 * @short_description: Generated C code for the org.bluez.Adapter1 D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-bluez-Adapter1.top_of_page">org.bluez.Adapter1</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.bluez.Adapter1 ---- */

static const _ExtendedGDBusMethodInfo _bluez_adapter1_method_info_start_discovery =
{
  {
    -1,
    (gchar *) "StartDiscovery",
    NULL,
    NULL,
    NULL
  },
  "handle-start-discovery",
  FALSE
};

static const _ExtendedGDBusMethodInfo _bluez_adapter1_method_info_stop_discovery =
{
  {
    -1,
    (gchar *) "StopDiscovery",
    NULL,
    NULL,
    NULL
  },
  "handle-stop-discovery",
  FALSE
};

static const _ExtendedGDBusArgInfo _bluez_adapter1_method_info_set_discovery_filter_IN_ARG_properties =
{
  {
    -1,
    (gchar *) "properties",
    (gchar *) "a{sv}",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _bluez_adapter1_method_info_set_discovery_filter_IN_ARG_pointers[] =
{
  &_bluez_adapter1_method_info_set_discovery_filter_IN_ARG_properties.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _bluez_adapter1_method_info_set_discovery_filter =
{
  {
    -1,
    (gchar *) "SetDiscoveryFilter",
    (GDBusArgInfo **) &_bluez_adapter1_method_info_set_discovery_filter_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-set-discovery-filter",
  FALSE
};

static const _ExtendedGDBusArgInfo _bluez_adapter1_method_info_remove_device_IN_ARG_device =
{
  {
    -1,
    (gchar *) "device",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _bluez_adapter1_method_info_remove_device_IN_ARG_pointers[] =
{
  &_bluez_adapter1_method_info_remove_device_IN_ARG_device.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _bluez_adapter1_method_info_remove_device =
{
  {
    -1,
    (gchar *) "RemoveDevice",
    (GDBusArgInfo **) &_bluez_adapter1_method_info_remove_device_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-remove-device",
  FALSE
};

static const GDBusMethodInfo * const _bluez_adapter1_method_info_pointers[] =
{
  &_bluez_adapter1_method_info_start_discovery.parent_struct,
  &_bluez_adapter1_method_info_stop_discovery.parent_struct,
  &_bluez_adapter1_method_info_set_discovery_filter.parent_struct,
  &_bluez_adapter1_method_info_remove_device.parent_struct,
  NULL
};

static const _ExtendedGDBusPropertyInfo _bluez_adapter1_property_info_address =
{
  {
    -1,
    (gchar *) "Address",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "address",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _bluez_adapter1_property_info_name =
{
  {
    -1,
    (gchar *) "Name",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "name",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _bluez_adapter1_property_info_alias =
{
  {
    -1,
    (gchar *) "Alias",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "alias",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _bluez_adapter1_property_info_class =
{
  {
    -1,
    (gchar *) "Class",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "class",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _bluez_adapter1_property_info_powered =
{
  {
    -1,
    (gchar *) "Powered",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "powered",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _bluez_adapter1_property_info_discoverable =
{
  {
    -1,
    (gchar *) "Discoverable",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "discoverable",
  FALSE,
  TRUE
};

static const GDBusPropertyInfo * const _bluez_adapter1_property_info_pointers[] =
{
  &_bluez_adapter1_property_info_address.parent_struct,
  &_bluez_adapter1_property_info_name.parent_struct,
  &_bluez_adapter1_property_info_alias.parent_struct,
  &_bluez_adapter1_property_info_class.parent_struct,
  &_bluez_adapter1_property_info_powered.parent_struct,
  &_bluez_adapter1_property_info_discoverable.parent_struct,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _bluez_adapter1_interface_info =
{
  {
    -1,
    (gchar *) "org.bluez.Adapter1",
    (GDBusMethodInfo **) &_bluez_adapter1_method_info_pointers,
    NULL,
    (GDBusPropertyInfo **) &_bluez_adapter1_property_info_pointers,
    NULL
  },
  "adapter1",
};


/**
 * bluez_adapter1_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-bluez-Adapter1.top_of_page">org.bluez.Adapter1</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
bluez_adapter1_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_bluez_adapter1_interface_info.parent_struct;
}

/**
 * bluez_adapter1_override_properties:
 * @klass: The class structure for a #GObject derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #BluezAdapter1 interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
bluez_adapter1_override_properties (GObjectClass *klass, guint property_id_begin)
{
  g_object_class_override_property (klass, property_id_begin++, "address");
  g_object_class_override_property (klass, property_id_begin++, "name");
  g_object_class_override_property (klass, property_id_begin++, "alias");
  g_object_class_override_property (klass, property_id_begin++, "class");
  g_object_class_override_property (klass, property_id_begin++, "powered");
  g_object_class_override_property (klass, property_id_begin++, "discoverable");
  return property_id_begin - 1;
}



/**
 * BluezAdapter1:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-bluez-Adapter1.top_of_page">org.bluez.Adapter1</link>.
 */

/**
 * BluezAdapter1Iface:
 * @parent_iface: The parent interface.
 * @handle_remove_device: Handler for the #BluezAdapter1::handle-remove-device signal.
 * @handle_set_discovery_filter: Handler for the #BluezAdapter1::handle-set-discovery-filter signal.
 * @handle_start_discovery: Handler for the #BluezAdapter1::handle-start-discovery signal.
 * @handle_stop_discovery: Handler for the #BluezAdapter1::handle-stop-discovery signal.
 * @get_address: Getter for the #BluezAdapter1:address property.
 * @get_alias: Getter for the #BluezAdapter1:alias property.
 * @get_class: Getter for the #BluezAdapter1:class property.
 * @get_discoverable: Getter for the #BluezAdapter1:discoverable property.
 * @get_name: Getter for the #BluezAdapter1:name property.
 * @get_powered: Getter for the #BluezAdapter1:powered property.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-bluez-Adapter1.top_of_page">org.bluez.Adapter1</link>.
 */

typedef BluezAdapter1Iface BluezAdapter1Interface;
G_DEFINE_INTERFACE (BluezAdapter1, bluez_adapter1, G_TYPE_OBJECT)

static void
bluez_adapter1_default_init (BluezAdapter1Iface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * BluezAdapter1::handle-start-discovery:
   * @object: A #BluezAdapter1.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-bluez-Adapter1.StartDiscovery">StartDiscovery()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call bluez_adapter1_complete_start_discovery() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-start-discovery",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (BluezAdapter1Iface, handle_start_discovery),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * BluezAdapter1::handle-stop-discovery:
   * @object: A #BluezAdapter1.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-bluez-Adapter1.StopDiscovery">StopDiscovery()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call bluez_adapter1_complete_stop_discovery() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-stop-discovery",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (BluezAdapter1Iface, handle_stop_discovery),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * BluezAdapter1::handle-set-discovery-filter:
   * @object: A #BluezAdapter1.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_properties: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-bluez-Adapter1.SetDiscoveryFilter">SetDiscoveryFilter()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call bluez_adapter1_complete_set_discovery_filter() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-set-discovery-filter",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (BluezAdapter1Iface, handle_set_discovery_filter),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_VARIANT);

  /**
   * BluezAdapter1::handle-remove-device:
   * @object: A #BluezAdapter1.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_device: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-bluez-Adapter1.RemoveDevice">RemoveDevice()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call bluez_adapter1_complete_remove_device() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-remove-device",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (BluezAdapter1Iface, handle_remove_device),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /* GObject properties for D-Bus properties: */
  /**
   * BluezAdapter1:address:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Adapter1.Address">"Address"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("address", "Address", "Address", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * BluezAdapter1:name:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Adapter1.Name">"Name"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("name", "Name", "Name", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * BluezAdapter1:alias:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Adapter1.Alias">"Alias"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("alias", "Alias", "Alias", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * BluezAdapter1:class:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Adapter1.Class">"Class"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("class", "Class", "Class", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * BluezAdapter1:powered:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Adapter1.Powered">"Powered"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("powered", "Powered", "Powered", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * BluezAdapter1:discoverable:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Adapter1.Discoverable">"Discoverable"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("discoverable", "Discoverable", "Discoverable", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

/**
 * bluez_adapter1_get_address: (skip)
 * @object: A #BluezAdapter1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Adapter1.Address">"Address"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use bluez_adapter1_dup_address() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
bluez_adapter1_get_address (BluezAdapter1 *object)
{
  return BLUEZ_ADAPTER1_GET_IFACE (object)->get_address (object);
}

/**
 * bluez_adapter1_dup_address: (skip)
 * @object: A #BluezAdapter1.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-Adapter1.Address">"Address"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
bluez_adapter1_dup_address (BluezAdapter1 *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "address", &value, NULL);
  return value;
}

/**
 * bluez_adapter1_set_address: (skip)
 * @object: A #BluezAdapter1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Adapter1.Address">"Address"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_adapter1_set_address (BluezAdapter1 *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "address", value, NULL);
}

/**
 * bluez_adapter1_get_name: (skip)
 * @object: A #BluezAdapter1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Adapter1.Name">"Name"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use bluez_adapter1_dup_name() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
bluez_adapter1_get_name (BluezAdapter1 *object)
{
  return BLUEZ_ADAPTER1_GET_IFACE (object)->get_name (object);
}

/**
 * bluez_adapter1_dup_name: (skip)
 * @object: A #BluezAdapter1.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-Adapter1.Name">"Name"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
bluez_adapter1_dup_name (BluezAdapter1 *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "name", &value, NULL);
  return value;
}

/**
 * bluez_adapter1_set_name: (skip)
 * @object: A #BluezAdapter1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Adapter1.Name">"Name"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_adapter1_set_name (BluezAdapter1 *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "name", value, NULL);
}

/**
 * bluez_adapter1_get_alias: (skip)
 * @object: A #BluezAdapter1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Adapter1.Alias">"Alias"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use bluez_adapter1_dup_alias() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
bluez_adapter1_get_alias (BluezAdapter1 *object)
{
  return BLUEZ_ADAPTER1_GET_IFACE (object)->get_alias (object);
}

/**
 * bluez_adapter1_dup_alias: (skip)
 * @object: A #BluezAdapter1.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-Adapter1.Alias">"Alias"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
bluez_adapter1_dup_alias (BluezAdapter1 *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "alias", &value, NULL);
  return value;
}

/**
 * bluez_adapter1_set_alias: (skip)
 * @object: A #BluezAdapter1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Adapter1.Alias">"Alias"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
bluez_adapter1_set_alias (BluezAdapter1 *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "alias", value, NULL);
}

/**
 * bluez_adapter1_get_class: (skip)
 * @object: A #BluezAdapter1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Adapter1.Class">"Class"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
bluez_adapter1_get_class (BluezAdapter1 *object)
{
  return BLUEZ_ADAPTER1_GET_IFACE (object)->get_class (object);
}

/**
 * bluez_adapter1_set_class: (skip)
 * @object: A #BluezAdapter1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Adapter1.Class">"Class"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_adapter1_set_class (BluezAdapter1 *object, guint value)
{
  g_object_set (G_OBJECT (object), "class", value, NULL);
}

/**
 * bluez_adapter1_get_powered: (skip)
 * @object: A #BluezAdapter1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Adapter1.Powered">"Powered"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
bluez_adapter1_get_powered (BluezAdapter1 *object)
{
  return BLUEZ_ADAPTER1_GET_IFACE (object)->get_powered (object);
}

/**
 * bluez_adapter1_set_powered: (skip)
 * @object: A #BluezAdapter1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Adapter1.Powered">"Powered"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
bluez_adapter1_set_powered (BluezAdapter1 *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "powered", value, NULL);
}

/**
 * bluez_adapter1_get_discoverable: (skip)
 * @object: A #BluezAdapter1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Adapter1.Discoverable">"Discoverable"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
bluez_adapter1_get_discoverable (BluezAdapter1 *object)
{
  return BLUEZ_ADAPTER1_GET_IFACE (object)->get_discoverable (object);
}

/**
 * bluez_adapter1_set_discoverable: (skip)
 * @object: A #BluezAdapter1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Adapter1.Discoverable">"Discoverable"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
bluez_adapter1_set_discoverable (BluezAdapter1 *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "discoverable", value, NULL);
}

/**
 * bluez_adapter1_call_start_discovery:
 * @proxy: A #BluezAdapter1Proxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-bluez-Adapter1.StartDiscovery">StartDiscovery()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call bluez_adapter1_call_start_discovery_finish() to get the result of the operation.
 *
 * See bluez_adapter1_call_start_discovery_sync() for the synchronous, blocking version of this method.
 */
void
bluez_adapter1_call_start_discovery (
    BluezAdapter1 *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "StartDiscovery",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * bluez_adapter1_call_start_discovery_finish:
 * @proxy: A #BluezAdapter1Proxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_adapter1_call_start_discovery().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with bluez_adapter1_call_start_discovery().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_adapter1_call_start_discovery_finish (
    BluezAdapter1 *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_adapter1_call_start_discovery_sync:
 * @proxy: A #BluezAdapter1Proxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-bluez-Adapter1.StartDiscovery">StartDiscovery()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See bluez_adapter1_call_start_discovery() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_adapter1_call_start_discovery_sync (
    BluezAdapter1 *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "StartDiscovery",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_adapter1_call_stop_discovery:
 * @proxy: A #BluezAdapter1Proxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-bluez-Adapter1.StopDiscovery">StopDiscovery()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call bluez_adapter1_call_stop_discovery_finish() to get the result of the operation.
 *
 * See bluez_adapter1_call_stop_discovery_sync() for the synchronous, blocking version of this method.
 */
void
bluez_adapter1_call_stop_discovery (
    BluezAdapter1 *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "StopDiscovery",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * bluez_adapter1_call_stop_discovery_finish:
 * @proxy: A #BluezAdapter1Proxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_adapter1_call_stop_discovery().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with bluez_adapter1_call_stop_discovery().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_adapter1_call_stop_discovery_finish (
    BluezAdapter1 *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_adapter1_call_stop_discovery_sync:
 * @proxy: A #BluezAdapter1Proxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-bluez-Adapter1.StopDiscovery">StopDiscovery()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See bluez_adapter1_call_stop_discovery() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_adapter1_call_stop_discovery_sync (
    BluezAdapter1 *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "StopDiscovery",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_adapter1_call_set_discovery_filter:
 * @proxy: A #BluezAdapter1Proxy.
 * @arg_properties: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-bluez-Adapter1.SetDiscoveryFilter">SetDiscoveryFilter()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call bluez_adapter1_call_set_discovery_filter_finish() to get the result of the operation.
 *
 * See bluez_adapter1_call_set_discovery_filter_sync() for the synchronous, blocking version of this method.
 */
void
bluez_adapter1_call_set_discovery_filter (
    BluezAdapter1 *proxy,
    GVariant *arg_properties,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "SetDiscoveryFilter",
    g_variant_new ("(@a{sv})",
                   arg_properties),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * bluez_adapter1_call_set_discovery_filter_finish:
 * @proxy: A #BluezAdapter1Proxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_adapter1_call_set_discovery_filter().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with bluez_adapter1_call_set_discovery_filter().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_adapter1_call_set_discovery_filter_finish (
    BluezAdapter1 *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_adapter1_call_set_discovery_filter_sync:
 * @proxy: A #BluezAdapter1Proxy.
 * @arg_properties: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-bluez-Adapter1.SetDiscoveryFilter">SetDiscoveryFilter()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See bluez_adapter1_call_set_discovery_filter() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_adapter1_call_set_discovery_filter_sync (
    BluezAdapter1 *proxy,
    GVariant *arg_properties,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "SetDiscoveryFilter",
    g_variant_new ("(@a{sv})",
                   arg_properties),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_adapter1_call_remove_device:
 * @proxy: A #BluezAdapter1Proxy.
 * @arg_device: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-bluez-Adapter1.RemoveDevice">RemoveDevice()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call bluez_adapter1_call_remove_device_finish() to get the result of the operation.
 *
 * See bluez_adapter1_call_remove_device_sync() for the synchronous, blocking version of this method.
 */
void
bluez_adapter1_call_remove_device (
    BluezAdapter1 *proxy,
    const gchar *arg_device,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "RemoveDevice",
    g_variant_new ("(o)",
                   arg_device),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * bluez_adapter1_call_remove_device_finish:
 * @proxy: A #BluezAdapter1Proxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_adapter1_call_remove_device().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with bluez_adapter1_call_remove_device().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_adapter1_call_remove_device_finish (
    BluezAdapter1 *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_adapter1_call_remove_device_sync:
 * @proxy: A #BluezAdapter1Proxy.
 * @arg_device: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-bluez-Adapter1.RemoveDevice">RemoveDevice()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See bluez_adapter1_call_remove_device() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_adapter1_call_remove_device_sync (
    BluezAdapter1 *proxy,
    const gchar *arg_device,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "RemoveDevice",
    g_variant_new ("(o)",
                   arg_device),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_adapter1_complete_start_discovery:
 * @object: A #BluezAdapter1.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-bluez-Adapter1.StartDiscovery">StartDiscovery()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
bluez_adapter1_complete_start_discovery (
    BluezAdapter1 *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * bluez_adapter1_complete_stop_discovery:
 * @object: A #BluezAdapter1.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-bluez-Adapter1.StopDiscovery">StopDiscovery()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
bluez_adapter1_complete_stop_discovery (
    BluezAdapter1 *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * bluez_adapter1_complete_set_discovery_filter:
 * @object: A #BluezAdapter1.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-bluez-Adapter1.SetDiscoveryFilter">SetDiscoveryFilter()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
bluez_adapter1_complete_set_discovery_filter (
    BluezAdapter1 *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * bluez_adapter1_complete_remove_device:
 * @object: A #BluezAdapter1.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-bluez-Adapter1.RemoveDevice">RemoveDevice()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
bluez_adapter1_complete_remove_device (
    BluezAdapter1 *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * BluezAdapter1Proxy:
 *
 * The #BluezAdapter1Proxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * BluezAdapter1ProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #BluezAdapter1Proxy.
 */

struct _BluezAdapter1ProxyPrivate
{
  GData *qdata;
};

static void bluez_adapter1_proxy_iface_init (BluezAdapter1Iface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (BluezAdapter1Proxy, bluez_adapter1_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (BluezAdapter1Proxy)
                         G_IMPLEMENT_INTERFACE (BLUEZ_TYPE_ADAPTER1, bluez_adapter1_proxy_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (BluezAdapter1Proxy, bluez_adapter1_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (BLUEZ_TYPE_ADAPTER1, bluez_adapter1_proxy_iface_init))

#endif
static void
bluez_adapter1_proxy_finalize (GObject *object)
{
  BluezAdapter1Proxy *proxy = BLUEZ_ADAPTER1_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (bluez_adapter1_proxy_parent_class)->finalize (object);
}

static void
bluez_adapter1_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 6);
  info = (const _ExtendedGDBusPropertyInfo *) _bluez_adapter1_property_info_pointers[prop_id - 1];
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
  if (info->use_gvariant)
    {
      g_value_set_variant (value, variant);
    }
  else
    {
      if (variant != NULL)
        g_dbus_gvariant_to_gvalue (variant, value);
    }
  if (variant != NULL)
    g_variant_unref (variant);
}

static void
bluez_adapter1_proxy_set_property_cb (GDBusProxy *proxy,
  GAsyncResult *res,
  gpointer      user_data)
{
  const _ExtendedGDBusPropertyInfo *info = user_data;
  GError *error;
  GVariant *_ret;
  error = NULL;
  _ret = g_dbus_proxy_call_finish (proxy, res, &error);
  if (!_ret)
    {
      g_warning ("Error setting property '%s' on interface org.bluez.Adapter1: %s (%s, %d)",
                 info->parent_struct.name, 
                 error->message, g_quark_to_string (error->domain), error->code);
      g_error_free (error);
    }
  else
    {
      g_variant_unref (_ret);
    }
}

static void
bluez_adapter1_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 6);
  info = (const _ExtendedGDBusPropertyInfo *) _bluez_adapter1_property_info_pointers[prop_id - 1];
  variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
  g_dbus_proxy_call (G_DBUS_PROXY (object),
    "org.freedesktop.DBus.Properties.Set",
    g_variant_new ("(ssv)", "org.bluez.Adapter1", info->parent_struct.name, variant),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    NULL, (GAsyncReadyCallback) bluez_adapter1_proxy_set_property_cb, (GDBusPropertyInfo *) &info->parent_struct);
  g_variant_unref (variant);
}

static void
bluez_adapter1_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  gsize n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_bluez_adapter1_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], BLUEZ_TYPE_ADAPTER1);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, BLUEZ_TYPE_ADAPTER1);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
bluez_adapter1_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  BluezAdapter1Proxy *proxy = BLUEZ_ADAPTER1_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_adapter1_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_adapter1_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static const gchar *
bluez_adapter1_proxy_get_address (BluezAdapter1 *object)
{
  BluezAdapter1Proxy *proxy = BLUEZ_ADAPTER1_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Address");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
bluez_adapter1_proxy_get_name (BluezAdapter1 *object)
{
  BluezAdapter1Proxy *proxy = BLUEZ_ADAPTER1_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Name");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
bluez_adapter1_proxy_get_alias (BluezAdapter1 *object)
{
  BluezAdapter1Proxy *proxy = BLUEZ_ADAPTER1_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Alias");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static guint 
bluez_adapter1_proxy_get_class (BluezAdapter1 *object)
{
  BluezAdapter1Proxy *proxy = BLUEZ_ADAPTER1_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Class");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
bluez_adapter1_proxy_get_powered (BluezAdapter1 *object)
{
  BluezAdapter1Proxy *proxy = BLUEZ_ADAPTER1_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Powered");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
bluez_adapter1_proxy_get_discoverable (BluezAdapter1 *object)
{
  BluezAdapter1Proxy *proxy = BLUEZ_ADAPTER1_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Discoverable");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static void
bluez_adapter1_proxy_init (BluezAdapter1Proxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = bluez_adapter1_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, BLUEZ_TYPE_ADAPTER1_PROXY, BluezAdapter1ProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), bluez_adapter1_interface_info ());
}

static void
bluez_adapter1_proxy_class_init (BluezAdapter1ProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = bluez_adapter1_proxy_finalize;
  gobject_class->get_property = bluez_adapter1_proxy_get_property;
  gobject_class->set_property = bluez_adapter1_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = bluez_adapter1_proxy_g_signal;
  proxy_class->g_properties_changed = bluez_adapter1_proxy_g_properties_changed;

  bluez_adapter1_override_properties (gobject_class, 1);

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (BluezAdapter1ProxyPrivate));
#endif
}

static void
bluez_adapter1_proxy_iface_init (BluezAdapter1Iface *iface)
{
  iface->get_address = bluez_adapter1_proxy_get_address;
  iface->get_name = bluez_adapter1_proxy_get_name;
  iface->get_alias = bluez_adapter1_proxy_get_alias;
  iface->get_class = bluez_adapter1_proxy_get_class;
  iface->get_powered = bluez_adapter1_proxy_get_powered;
  iface->get_discoverable = bluez_adapter1_proxy_get_discoverable;
}

/**
 * bluez_adapter1_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-bluez-Adapter1.top_of_page">org.bluez.Adapter1</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call bluez_adapter1_proxy_new_finish() to get the result of the operation.
 *
 * See bluez_adapter1_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
bluez_adapter1_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (BLUEZ_TYPE_ADAPTER1_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.bluez.Adapter1", NULL);
}

/**
 * bluez_adapter1_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_adapter1_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with bluez_adapter1_proxy_new().
 *
 * Returns: (transfer full) (type BluezAdapter1Proxy): The constructed proxy object or %NULL if @error is set.
 */
BluezAdapter1 *
bluez_adapter1_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return BLUEZ_ADAPTER1 (ret);
  else
    return NULL;
}

/**
 * bluez_adapter1_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-bluez-Adapter1.top_of_page">org.bluez.Adapter1</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See bluez_adapter1_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type BluezAdapter1Proxy): The constructed proxy object or %NULL if @error is set.
 */
BluezAdapter1 *
bluez_adapter1_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (BLUEZ_TYPE_ADAPTER1_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.bluez.Adapter1", NULL);
  if (ret != NULL)
    return BLUEZ_ADAPTER1 (ret);
  else
    return NULL;
}


/**
 * bluez_adapter1_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like bluez_adapter1_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call bluez_adapter1_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See bluez_adapter1_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
bluez_adapter1_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (BLUEZ_TYPE_ADAPTER1_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.bluez.Adapter1", NULL);
}

/**
 * bluez_adapter1_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_adapter1_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with bluez_adapter1_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type BluezAdapter1Proxy): The constructed proxy object or %NULL if @error is set.
 */
BluezAdapter1 *
bluez_adapter1_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return BLUEZ_ADAPTER1 (ret);
  else
    return NULL;
}

/**
 * bluez_adapter1_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like bluez_adapter1_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See bluez_adapter1_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type BluezAdapter1Proxy): The constructed proxy object or %NULL if @error is set.
 */
BluezAdapter1 *
bluez_adapter1_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (BLUEZ_TYPE_ADAPTER1_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.bluez.Adapter1", NULL);
  if (ret != NULL)
    return BLUEZ_ADAPTER1 (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * BluezAdapter1Skeleton:
 *
 * The #BluezAdapter1Skeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * BluezAdapter1SkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #BluezAdapter1Skeleton.
 */

struct _BluezAdapter1SkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_bluez_adapter1_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  BluezAdapter1Skeleton *skeleton = BLUEZ_ADAPTER1_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  guint num_extra;
  gsize n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], BLUEZ_TYPE_ADAPTER1);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, BLUEZ_TYPE_ADAPTER1);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_bluez_adapter1_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  BluezAdapter1Skeleton *skeleton = BLUEZ_ADAPTER1_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_adapter1_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_bluez_adapter1_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  BluezAdapter1Skeleton *skeleton = BLUEZ_ADAPTER1_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_adapter1_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _bluez_adapter1_skeleton_vtable =
{
  _bluez_adapter1_skeleton_handle_method_call,
  _bluez_adapter1_skeleton_handle_get_property,
  _bluez_adapter1_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
bluez_adapter1_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return bluez_adapter1_interface_info ();
}

static GDBusInterfaceVTable *
bluez_adapter1_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_bluez_adapter1_skeleton_vtable;
}

static GVariant *
bluez_adapter1_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  BluezAdapter1Skeleton *skeleton = BLUEZ_ADAPTER1_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_bluez_adapter1_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _bluez_adapter1_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _bluez_adapter1_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _bluez_adapter1_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.bluez.Adapter1", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static gboolean _bluez_adapter1_emit_changed (gpointer user_data);

static void
bluez_adapter1_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
  BluezAdapter1Skeleton *skeleton = BLUEZ_ADAPTER1_SKELETON (_skeleton);
  gboolean emit_changed = FALSE;

  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    {
      g_source_destroy (skeleton->priv->changed_properties_idle_source);
      skeleton->priv->changed_properties_idle_source = NULL;
      emit_changed = TRUE;
    }
  g_mutex_unlock (&skeleton->priv->lock);

  if (emit_changed)
    _bluez_adapter1_emit_changed (skeleton);
}

static void bluez_adapter1_skeleton_iface_init (BluezAdapter1Iface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (BluezAdapter1Skeleton, bluez_adapter1_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (BluezAdapter1Skeleton)
                         G_IMPLEMENT_INTERFACE (BLUEZ_TYPE_ADAPTER1, bluez_adapter1_skeleton_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (BluezAdapter1Skeleton, bluez_adapter1_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (BLUEZ_TYPE_ADAPTER1, bluez_adapter1_skeleton_iface_init))

#endif
static void
bluez_adapter1_skeleton_finalize (GObject *object)
{
  BluezAdapter1Skeleton *skeleton = BLUEZ_ADAPTER1_SKELETON (object);
  guint n;
  for (n = 0; n < 6; n++)
    g_value_unset (&skeleton->priv->properties[n]);
  g_free (skeleton->priv->properties);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (bluez_adapter1_skeleton_parent_class)->finalize (object);
}

static void
bluez_adapter1_skeleton_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  BluezAdapter1Skeleton *skeleton = BLUEZ_ADAPTER1_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 6);
  g_mutex_lock (&skeleton->priv->lock);
  g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
  g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
_bluez_adapter1_emit_changed (gpointer user_data)
{
  BluezAdapter1Skeleton *skeleton = BLUEZ_ADAPTER1_SKELETON (user_data);
  GList *l;
  GVariantBuilder builder;
  GVariantBuilder invalidated_builder;
  guint num_changes;

  g_mutex_lock (&skeleton->priv->lock);
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
  for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
    {
      ChangedProperty *cp = l->data;
      GVariant *variant;
      const GValue *cur_value;

      cur_value = &skeleton->priv->properties[cp->prop_id - 1];
      if (!_g_value_equal (cur_value, &cp->orig_value))
        {
          variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
          g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
          g_variant_unref (variant);
          num_changes++;
        }
    }
  if (num_changes > 0)
    {
      GList *connections, *ll;
      GVariant *signal_variant;
      signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "org.bluez.Adapter1",
                                           &builder, &invalidated_builder));
      connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
      for (ll = connections; ll != NULL; ll = ll->next)
        {
          GDBusConnection *connection = ll->data;

          g_dbus_connection_emit_signal (connection,
                                         NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
                                         "org.freedesktop.DBus.Properties",
                                         "PropertiesChanged",
                                         signal_variant,
                                         NULL);
        }
      g_variant_unref (signal_variant);
      g_list_free_full (connections, g_object_unref);
    }
  else
    {
      g_variant_builder_clear (&builder);
      g_variant_builder_clear (&invalidated_builder);
    }
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  skeleton->priv->changed_properties = NULL;
  skeleton->priv->changed_properties_idle_source = NULL;
  g_mutex_unlock (&skeleton->priv->lock);
  return FALSE;
}

static void
_bluez_adapter1_schedule_emit_changed (BluezAdapter1Skeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
  ChangedProperty *cp;
  GList *l;
  cp = NULL;
  for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
    {
      ChangedProperty *i_cp = l->data;
      if (i_cp->info == info)
        {
          cp = i_cp;
          break;
        }
    }
  if (cp == NULL)
    {
      cp = g_new0 (ChangedProperty, 1);
      cp->prop_id = prop_id;
      cp->info = info;
      skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
      g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
      g_value_copy (orig_value, &cp->orig_value);
    }
}

static void
bluez_adapter1_skeleton_notify (GObject      *object,
  GParamSpec *pspec G_GNUC_UNUSED)
{
  BluezAdapter1Skeleton *skeleton = BLUEZ_ADAPTER1_SKELETON (object);
  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties != NULL &&
      skeleton->priv->changed_properties_idle_source == NULL)
    {
      skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
      g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
      g_source_set_callback (skeleton->priv->changed_properties_idle_source, _bluez_adapter1_emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
      g_source_set_name (skeleton->priv->changed_properties_idle_source, "[generated] _bluez_adapter1_emit_changed");
      g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
      g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
  g_mutex_unlock (&skeleton->priv->lock);
}

static void
bluez_adapter1_skeleton_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  const _ExtendedGDBusPropertyInfo *info;
  BluezAdapter1Skeleton *skeleton = BLUEZ_ADAPTER1_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 6);
  info = (const _ExtendedGDBusPropertyInfo *) _bluez_adapter1_property_info_pointers[prop_id - 1];
  g_mutex_lock (&skeleton->priv->lock);
  g_object_freeze_notify (object);
  if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
      if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL &&
          info->emits_changed_signal)
        _bluez_adapter1_schedule_emit_changed (skeleton, info, prop_id, &skeleton->priv->properties[prop_id - 1]);
      g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
      g_object_notify_by_pspec (object, pspec);
    }
  g_mutex_unlock (&skeleton->priv->lock);
  g_object_thaw_notify (object);
}

static void
bluez_adapter1_skeleton_init (BluezAdapter1Skeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = bluez_adapter1_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, BLUEZ_TYPE_ADAPTER1_SKELETON, BluezAdapter1SkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
  skeleton->priv->properties = g_new0 (GValue, 6);
  g_value_init (&skeleton->priv->properties[0], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[1], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[2], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[3], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[4], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[5], G_TYPE_BOOLEAN);
}

static const gchar *
bluez_adapter1_skeleton_get_address (BluezAdapter1 *object)
{
  BluezAdapter1Skeleton *skeleton = BLUEZ_ADAPTER1_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[0]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
bluez_adapter1_skeleton_get_name (BluezAdapter1 *object)
{
  BluezAdapter1Skeleton *skeleton = BLUEZ_ADAPTER1_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[1]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
bluez_adapter1_skeleton_get_alias (BluezAdapter1 *object)
{
  BluezAdapter1Skeleton *skeleton = BLUEZ_ADAPTER1_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[2]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint 
bluez_adapter1_skeleton_get_class (BluezAdapter1 *object)
{
  BluezAdapter1Skeleton *skeleton = BLUEZ_ADAPTER1_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[3]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
bluez_adapter1_skeleton_get_powered (BluezAdapter1 *object)
{
  BluezAdapter1Skeleton *skeleton = BLUEZ_ADAPTER1_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[4]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
bluez_adapter1_skeleton_get_discoverable (BluezAdapter1 *object)
{
  BluezAdapter1Skeleton *skeleton = BLUEZ_ADAPTER1_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[5]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static void
bluez_adapter1_skeleton_class_init (BluezAdapter1SkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = bluez_adapter1_skeleton_finalize;
  gobject_class->get_property = bluez_adapter1_skeleton_get_property;
  gobject_class->set_property = bluez_adapter1_skeleton_set_property;
  gobject_class->notify       = bluez_adapter1_skeleton_notify;


  bluez_adapter1_override_properties (gobject_class, 1);

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = bluez_adapter1_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = bluez_adapter1_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = bluez_adapter1_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = bluez_adapter1_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (BluezAdapter1SkeletonPrivate));
#endif
}

static void
bluez_adapter1_skeleton_iface_init (BluezAdapter1Iface *iface)
{
  iface->get_address = bluez_adapter1_skeleton_get_address;
  iface->get_name = bluez_adapter1_skeleton_get_name;
  iface->get_alias = bluez_adapter1_skeleton_get_alias;
  iface->get_class = bluez_adapter1_skeleton_get_class;
  iface->get_powered = bluez_adapter1_skeleton_get_powered;
  iface->get_discoverable = bluez_adapter1_skeleton_get_discoverable;
}

/**
 * bluez_adapter1_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-bluez-Adapter1.top_of_page">org.bluez.Adapter1</link>.
 *
 * Returns: (transfer full) (type BluezAdapter1Skeleton): The skeleton object.
 */
BluezAdapter1 *
bluez_adapter1_skeleton_new (void)
{
  return BLUEZ_ADAPTER1 (g_object_new (BLUEZ_TYPE_ADAPTER1_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface org.bluez.Device1
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:BluezDevice1
 * @title: BluezDevice1
 * @short_description: Generated C code for the org.bluez.Device1 D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-bluez-Device1.top_of_page">org.bluez.Device1</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.bluez.Device1 ---- */

static const _ExtendedGDBusMethodInfo _bluez_device1_method_info_disconnect =
{
  {
    -1,
    (gchar *) "Disconnect",
    NULL,
    NULL,
    NULL
  },
  "handle-disconnect",
  FALSE
};

static const _ExtendedGDBusMethodInfo _bluez_device1_method_info_connect =
{
  {
    -1,
    (gchar *) "Connect",
    NULL,
    NULL,
    NULL
  },
  "handle-connect",
  FALSE
};

static const GDBusMethodInfo * const _bluez_device1_method_info_pointers[] =
{
  &_bluez_device1_method_info_disconnect.parent_struct,
  &_bluez_device1_method_info_connect.parent_struct,
  NULL
};

static const _ExtendedGDBusPropertyInfo _bluez_device1_property_info_address =
{
  {
    -1,
    (gchar *) "Address",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "address",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _bluez_device1_property_info_connected =
{
  {
    -1,
    (gchar *) "Connected",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "connected",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _bluez_device1_property_info_adapter =
{
  {
    -1,
    (gchar *) "Adapter",
    (gchar *) "o",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "adapter",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _bluez_device1_property_info_service_data =
{
  {
    -1,
    (gchar *) "ServiceData",
    (gchar *) "a{sv}",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "service-data",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _bluez_device1_property_info_services_resolved =
{
  {
    -1,
    (gchar *) "ServicesResolved",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "services-resolved",
  FALSE,
  TRUE
};

static const GDBusPropertyInfo * const _bluez_device1_property_info_pointers[] =
{
  &_bluez_device1_property_info_address.parent_struct,
  &_bluez_device1_property_info_connected.parent_struct,
  &_bluez_device1_property_info_adapter.parent_struct,
  &_bluez_device1_property_info_service_data.parent_struct,
  &_bluez_device1_property_info_services_resolved.parent_struct,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _bluez_device1_interface_info =
{
  {
    -1,
    (gchar *) "org.bluez.Device1",
    (GDBusMethodInfo **) &_bluez_device1_method_info_pointers,
    NULL,
    (GDBusPropertyInfo **) &_bluez_device1_property_info_pointers,
    NULL
  },
  "device1",
};


/**
 * bluez_device1_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-bluez-Device1.top_of_page">org.bluez.Device1</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
bluez_device1_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_bluez_device1_interface_info.parent_struct;
}

/**
 * bluez_device1_override_properties:
 * @klass: The class structure for a #GObject derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #BluezDevice1 interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
bluez_device1_override_properties (GObjectClass *klass, guint property_id_begin)
{
  g_object_class_override_property (klass, property_id_begin++, "address");
  g_object_class_override_property (klass, property_id_begin++, "connected");
  g_object_class_override_property (klass, property_id_begin++, "adapter");
  g_object_class_override_property (klass, property_id_begin++, "service-data");
  g_object_class_override_property (klass, property_id_begin++, "services-resolved");
  return property_id_begin - 1;
}



/**
 * BluezDevice1:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-bluez-Device1.top_of_page">org.bluez.Device1</link>.
 */

/**
 * BluezDevice1Iface:
 * @parent_iface: The parent interface.
 * @handle_connect: Handler for the #BluezDevice1::handle-connect signal.
 * @handle_disconnect: Handler for the #BluezDevice1::handle-disconnect signal.
 * @get_adapter: Getter for the #BluezDevice1:adapter property.
 * @get_address: Getter for the #BluezDevice1:address property.
 * @get_connected: Getter for the #BluezDevice1:connected property.
 * @get_service_data: Getter for the #BluezDevice1:service-data property.
 * @get_services_resolved: Getter for the #BluezDevice1:services-resolved property.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-bluez-Device1.top_of_page">org.bluez.Device1</link>.
 */

typedef BluezDevice1Iface BluezDevice1Interface;
G_DEFINE_INTERFACE (BluezDevice1, bluez_device1, G_TYPE_OBJECT)

static void
bluez_device1_default_init (BluezDevice1Iface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * BluezDevice1::handle-disconnect:
   * @object: A #BluezDevice1.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-bluez-Device1.Disconnect">Disconnect()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call bluez_device1_complete_disconnect() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-disconnect",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (BluezDevice1Iface, handle_disconnect),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * BluezDevice1::handle-connect:
   * @object: A #BluezDevice1.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-bluez-Device1.Connect">Connect()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call bluez_device1_complete_connect() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-connect",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (BluezDevice1Iface, handle_connect),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /* GObject properties for D-Bus properties: */
  /**
   * BluezDevice1:address:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.Address">"Address"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("address", "Address", "Address", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * BluezDevice1:connected:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.Connected">"Connected"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("connected", "Connected", "Connected", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * BluezDevice1:adapter:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.Adapter">"Adapter"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("adapter", "Adapter", "Adapter", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * BluezDevice1:service-data:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.ServiceData">"ServiceData"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_variant ("service-data", "ServiceData", "ServiceData", G_VARIANT_TYPE ("a{sv}"), NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * BluezDevice1:services-resolved:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-Device1.ServicesResolved">"ServicesResolved"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("services-resolved", "ServicesResolved", "ServicesResolved", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

/**
 * bluez_device1_get_address: (skip)
 * @object: A #BluezDevice1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.Address">"Address"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use bluez_device1_dup_address() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
bluez_device1_get_address (BluezDevice1 *object)
{
  return BLUEZ_DEVICE1_GET_IFACE (object)->get_address (object);
}

/**
 * bluez_device1_dup_address: (skip)
 * @object: A #BluezDevice1.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-Device1.Address">"Address"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
bluez_device1_dup_address (BluezDevice1 *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "address", &value, NULL);
  return value;
}

/**
 * bluez_device1_set_address: (skip)
 * @object: A #BluezDevice1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.Address">"Address"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_device1_set_address (BluezDevice1 *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "address", value, NULL);
}

/**
 * bluez_device1_get_connected: (skip)
 * @object: A #BluezDevice1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.Connected">"Connected"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
bluez_device1_get_connected (BluezDevice1 *object)
{
  return BLUEZ_DEVICE1_GET_IFACE (object)->get_connected (object);
}

/**
 * bluez_device1_set_connected: (skip)
 * @object: A #BluezDevice1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.Connected">"Connected"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_device1_set_connected (BluezDevice1 *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "connected", value, NULL);
}

/**
 * bluez_device1_get_adapter: (skip)
 * @object: A #BluezDevice1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.Adapter">"Adapter"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use bluez_device1_dup_adapter() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
bluez_device1_get_adapter (BluezDevice1 *object)
{
  return BLUEZ_DEVICE1_GET_IFACE (object)->get_adapter (object);
}

/**
 * bluez_device1_dup_adapter: (skip)
 * @object: A #BluezDevice1.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-Device1.Adapter">"Adapter"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
bluez_device1_dup_adapter (BluezDevice1 *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "adapter", &value, NULL);
  return value;
}

/**
 * bluez_device1_set_adapter: (skip)
 * @object: A #BluezDevice1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.Adapter">"Adapter"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_device1_set_adapter (BluezDevice1 *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "adapter", value, NULL);
}

/**
 * bluez_device1_get_service_data: (skip)
 * @object: A #BluezDevice1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.ServiceData">"ServiceData"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use bluez_device1_dup_service_data() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
GVariant *
bluez_device1_get_service_data (BluezDevice1 *object)
{
  return BLUEZ_DEVICE1_GET_IFACE (object)->get_service_data (object);
}

/**
 * bluez_device1_dup_service_data: (skip)
 * @object: A #BluezDevice1.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-Device1.ServiceData">"ServiceData"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_variant_unref().
 */
GVariant *
bluez_device1_dup_service_data (BluezDevice1 *object)
{
  GVariant *value;
  g_object_get (G_OBJECT (object), "service-data", &value, NULL);
  return value;
}

/**
 * bluez_device1_set_service_data: (skip)
 * @object: A #BluezDevice1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.ServiceData">"ServiceData"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_device1_set_service_data (BluezDevice1 *object, GVariant *value)
{
  g_object_set (G_OBJECT (object), "service-data", value, NULL);
}

/**
 * bluez_device1_get_services_resolved: (skip)
 * @object: A #BluezDevice1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-Device1.ServicesResolved">"ServicesResolved"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
bluez_device1_get_services_resolved (BluezDevice1 *object)
{
  return BLUEZ_DEVICE1_GET_IFACE (object)->get_services_resolved (object);
}

/**
 * bluez_device1_set_services_resolved: (skip)
 * @object: A #BluezDevice1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-Device1.ServicesResolved">"ServicesResolved"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_device1_set_services_resolved (BluezDevice1 *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "services-resolved", value, NULL);
}

/**
 * bluez_device1_call_disconnect:
 * @proxy: A #BluezDevice1Proxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-bluez-Device1.Disconnect">Disconnect()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call bluez_device1_call_disconnect_finish() to get the result of the operation.
 *
 * See bluez_device1_call_disconnect_sync() for the synchronous, blocking version of this method.
 */
void
bluez_device1_call_disconnect (
    BluezDevice1 *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Disconnect",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * bluez_device1_call_disconnect_finish:
 * @proxy: A #BluezDevice1Proxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_device1_call_disconnect().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with bluez_device1_call_disconnect().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_device1_call_disconnect_finish (
    BluezDevice1 *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_device1_call_disconnect_sync:
 * @proxy: A #BluezDevice1Proxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-bluez-Device1.Disconnect">Disconnect()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See bluez_device1_call_disconnect() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_device1_call_disconnect_sync (
    BluezDevice1 *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Disconnect",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_device1_call_connect:
 * @proxy: A #BluezDevice1Proxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-bluez-Device1.Connect">Connect()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call bluez_device1_call_connect_finish() to get the result of the operation.
 *
 * See bluez_device1_call_connect_sync() for the synchronous, blocking version of this method.
 */
void
bluez_device1_call_connect (
    BluezDevice1 *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Connect",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * bluez_device1_call_connect_finish:
 * @proxy: A #BluezDevice1Proxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_device1_call_connect().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with bluez_device1_call_connect().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_device1_call_connect_finish (
    BluezDevice1 *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_device1_call_connect_sync:
 * @proxy: A #BluezDevice1Proxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-bluez-Device1.Connect">Connect()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See bluez_device1_call_connect() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_device1_call_connect_sync (
    BluezDevice1 *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Connect",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_device1_complete_disconnect:
 * @object: A #BluezDevice1.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-bluez-Device1.Disconnect">Disconnect()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
bluez_device1_complete_disconnect (
    BluezDevice1 *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * bluez_device1_complete_connect:
 * @object: A #BluezDevice1.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-bluez-Device1.Connect">Connect()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
bluez_device1_complete_connect (
    BluezDevice1 *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * BluezDevice1Proxy:
 *
 * The #BluezDevice1Proxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * BluezDevice1ProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #BluezDevice1Proxy.
 */

struct _BluezDevice1ProxyPrivate
{
  GData *qdata;
};

static void bluez_device1_proxy_iface_init (BluezDevice1Iface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (BluezDevice1Proxy, bluez_device1_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (BluezDevice1Proxy)
                         G_IMPLEMENT_INTERFACE (BLUEZ_TYPE_DEVICE1, bluez_device1_proxy_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (BluezDevice1Proxy, bluez_device1_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (BLUEZ_TYPE_DEVICE1, bluez_device1_proxy_iface_init))

#endif
static void
bluez_device1_proxy_finalize (GObject *object)
{
  BluezDevice1Proxy *proxy = BLUEZ_DEVICE1_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (bluez_device1_proxy_parent_class)->finalize (object);
}

static void
bluez_device1_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 5);
  info = (const _ExtendedGDBusPropertyInfo *) _bluez_device1_property_info_pointers[prop_id - 1];
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
  if (info->use_gvariant)
    {
      g_value_set_variant (value, variant);
    }
  else
    {
      if (variant != NULL)
        g_dbus_gvariant_to_gvalue (variant, value);
    }
  if (variant != NULL)
    g_variant_unref (variant);
}

static void
bluez_device1_proxy_set_property_cb (GDBusProxy *proxy,
  GAsyncResult *res,
  gpointer      user_data)
{
  const _ExtendedGDBusPropertyInfo *info = user_data;
  GError *error;
  GVariant *_ret;
  error = NULL;
  _ret = g_dbus_proxy_call_finish (proxy, res, &error);
  if (!_ret)
    {
      g_warning ("Error setting property '%s' on interface org.bluez.Device1: %s (%s, %d)",
                 info->parent_struct.name, 
                 error->message, g_quark_to_string (error->domain), error->code);
      g_error_free (error);
    }
  else
    {
      g_variant_unref (_ret);
    }
}

static void
bluez_device1_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 5);
  info = (const _ExtendedGDBusPropertyInfo *) _bluez_device1_property_info_pointers[prop_id - 1];
  variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
  g_dbus_proxy_call (G_DBUS_PROXY (object),
    "org.freedesktop.DBus.Properties.Set",
    g_variant_new ("(ssv)", "org.bluez.Device1", info->parent_struct.name, variant),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    NULL, (GAsyncReadyCallback) bluez_device1_proxy_set_property_cb, (GDBusPropertyInfo *) &info->parent_struct);
  g_variant_unref (variant);
}

static void
bluez_device1_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  gsize n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_bluez_device1_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], BLUEZ_TYPE_DEVICE1);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, BLUEZ_TYPE_DEVICE1);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
bluez_device1_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  BluezDevice1Proxy *proxy = BLUEZ_DEVICE1_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_device1_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_device1_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static const gchar *
bluez_device1_proxy_get_address (BluezDevice1 *object)
{
  BluezDevice1Proxy *proxy = BLUEZ_DEVICE1_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Address");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
bluez_device1_proxy_get_connected (BluezDevice1 *object)
{
  BluezDevice1Proxy *proxy = BLUEZ_DEVICE1_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Connected");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
bluez_device1_proxy_get_adapter (BluezDevice1 *object)
{
  BluezDevice1Proxy *proxy = BLUEZ_DEVICE1_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Adapter");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static GVariant *
bluez_device1_proxy_get_service_data (BluezDevice1 *object)
{
  BluezDevice1Proxy *proxy = BLUEZ_DEVICE1_PROXY (object);
  GVariant *variant;
  GVariant *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "ServiceData");
  value = variant;
  if (variant != NULL)
    g_variant_unref (variant);
  return value;
}

static gboolean 
bluez_device1_proxy_get_services_resolved (BluezDevice1 *object)
{
  BluezDevice1Proxy *proxy = BLUEZ_DEVICE1_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "ServicesResolved");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static void
bluez_device1_proxy_init (BluezDevice1Proxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = bluez_device1_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, BLUEZ_TYPE_DEVICE1_PROXY, BluezDevice1ProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), bluez_device1_interface_info ());
}

static void
bluez_device1_proxy_class_init (BluezDevice1ProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = bluez_device1_proxy_finalize;
  gobject_class->get_property = bluez_device1_proxy_get_property;
  gobject_class->set_property = bluez_device1_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = bluez_device1_proxy_g_signal;
  proxy_class->g_properties_changed = bluez_device1_proxy_g_properties_changed;

  bluez_device1_override_properties (gobject_class, 1);

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (BluezDevice1ProxyPrivate));
#endif
}

static void
bluez_device1_proxy_iface_init (BluezDevice1Iface *iface)
{
  iface->get_address = bluez_device1_proxy_get_address;
  iface->get_connected = bluez_device1_proxy_get_connected;
  iface->get_adapter = bluez_device1_proxy_get_adapter;
  iface->get_service_data = bluez_device1_proxy_get_service_data;
  iface->get_services_resolved = bluez_device1_proxy_get_services_resolved;
}

/**
 * bluez_device1_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-bluez-Device1.top_of_page">org.bluez.Device1</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call bluez_device1_proxy_new_finish() to get the result of the operation.
 *
 * See bluez_device1_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
bluez_device1_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (BLUEZ_TYPE_DEVICE1_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.bluez.Device1", NULL);
}

/**
 * bluez_device1_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_device1_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with bluez_device1_proxy_new().
 *
 * Returns: (transfer full) (type BluezDevice1Proxy): The constructed proxy object or %NULL if @error is set.
 */
BluezDevice1 *
bluez_device1_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return BLUEZ_DEVICE1 (ret);
  else
    return NULL;
}

/**
 * bluez_device1_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-bluez-Device1.top_of_page">org.bluez.Device1</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See bluez_device1_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type BluezDevice1Proxy): The constructed proxy object or %NULL if @error is set.
 */
BluezDevice1 *
bluez_device1_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (BLUEZ_TYPE_DEVICE1_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.bluez.Device1", NULL);
  if (ret != NULL)
    return BLUEZ_DEVICE1 (ret);
  else
    return NULL;
}


/**
 * bluez_device1_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like bluez_device1_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call bluez_device1_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See bluez_device1_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
bluez_device1_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (BLUEZ_TYPE_DEVICE1_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.bluez.Device1", NULL);
}

/**
 * bluez_device1_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_device1_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with bluez_device1_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type BluezDevice1Proxy): The constructed proxy object or %NULL if @error is set.
 */
BluezDevice1 *
bluez_device1_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return BLUEZ_DEVICE1 (ret);
  else
    return NULL;
}

/**
 * bluez_device1_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like bluez_device1_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See bluez_device1_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type BluezDevice1Proxy): The constructed proxy object or %NULL if @error is set.
 */
BluezDevice1 *
bluez_device1_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (BLUEZ_TYPE_DEVICE1_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.bluez.Device1", NULL);
  if (ret != NULL)
    return BLUEZ_DEVICE1 (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * BluezDevice1Skeleton:
 *
 * The #BluezDevice1Skeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * BluezDevice1SkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #BluezDevice1Skeleton.
 */

struct _BluezDevice1SkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_bluez_device1_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  BluezDevice1Skeleton *skeleton = BLUEZ_DEVICE1_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  guint num_extra;
  gsize n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], BLUEZ_TYPE_DEVICE1);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, BLUEZ_TYPE_DEVICE1);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_bluez_device1_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  BluezDevice1Skeleton *skeleton = BLUEZ_DEVICE1_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_device1_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_bluez_device1_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  BluezDevice1Skeleton *skeleton = BLUEZ_DEVICE1_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_device1_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _bluez_device1_skeleton_vtable =
{
  _bluez_device1_skeleton_handle_method_call,
  _bluez_device1_skeleton_handle_get_property,
  _bluez_device1_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
bluez_device1_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return bluez_device1_interface_info ();
}

static GDBusInterfaceVTable *
bluez_device1_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_bluez_device1_skeleton_vtable;
}

static GVariant *
bluez_device1_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  BluezDevice1Skeleton *skeleton = BLUEZ_DEVICE1_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_bluez_device1_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _bluez_device1_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _bluez_device1_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _bluez_device1_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.bluez.Device1", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static gboolean _bluez_device1_emit_changed (gpointer user_data);

static void
bluez_device1_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
  BluezDevice1Skeleton *skeleton = BLUEZ_DEVICE1_SKELETON (_skeleton);
  gboolean emit_changed = FALSE;

  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    {
      g_source_destroy (skeleton->priv->changed_properties_idle_source);
      skeleton->priv->changed_properties_idle_source = NULL;
      emit_changed = TRUE;
    }
  g_mutex_unlock (&skeleton->priv->lock);

  if (emit_changed)
    _bluez_device1_emit_changed (skeleton);
}

static void bluez_device1_skeleton_iface_init (BluezDevice1Iface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (BluezDevice1Skeleton, bluez_device1_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (BluezDevice1Skeleton)
                         G_IMPLEMENT_INTERFACE (BLUEZ_TYPE_DEVICE1, bluez_device1_skeleton_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (BluezDevice1Skeleton, bluez_device1_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (BLUEZ_TYPE_DEVICE1, bluez_device1_skeleton_iface_init))

#endif
static void
bluez_device1_skeleton_finalize (GObject *object)
{
  BluezDevice1Skeleton *skeleton = BLUEZ_DEVICE1_SKELETON (object);
  guint n;
  for (n = 0; n < 5; n++)
    g_value_unset (&skeleton->priv->properties[n]);
  g_free (skeleton->priv->properties);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (bluez_device1_skeleton_parent_class)->finalize (object);
}

static void
bluez_device1_skeleton_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  BluezDevice1Skeleton *skeleton = BLUEZ_DEVICE1_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 5);
  g_mutex_lock (&skeleton->priv->lock);
  g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
  g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
_bluez_device1_emit_changed (gpointer user_data)
{
  BluezDevice1Skeleton *skeleton = BLUEZ_DEVICE1_SKELETON (user_data);
  GList *l;
  GVariantBuilder builder;
  GVariantBuilder invalidated_builder;
  guint num_changes;

  g_mutex_lock (&skeleton->priv->lock);
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
  for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
    {
      ChangedProperty *cp = l->data;
      GVariant *variant;
      const GValue *cur_value;

      cur_value = &skeleton->priv->properties[cp->prop_id - 1];
      if (!_g_value_equal (cur_value, &cp->orig_value))
        {
          variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
          g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
          g_variant_unref (variant);
          num_changes++;
        }
    }
  if (num_changes > 0)
    {
      GList *connections, *ll;
      GVariant *signal_variant;
      signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "org.bluez.Device1",
                                           &builder, &invalidated_builder));
      connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
      for (ll = connections; ll != NULL; ll = ll->next)
        {
          GDBusConnection *connection = ll->data;

          g_dbus_connection_emit_signal (connection,
                                         NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
                                         "org.freedesktop.DBus.Properties",
                                         "PropertiesChanged",
                                         signal_variant,
                                         NULL);
        }
      g_variant_unref (signal_variant);
      g_list_free_full (connections, g_object_unref);
    }
  else
    {
      g_variant_builder_clear (&builder);
      g_variant_builder_clear (&invalidated_builder);
    }
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  skeleton->priv->changed_properties = NULL;
  skeleton->priv->changed_properties_idle_source = NULL;
  g_mutex_unlock (&skeleton->priv->lock);
  return FALSE;
}

static void
_bluez_device1_schedule_emit_changed (BluezDevice1Skeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
  ChangedProperty *cp;
  GList *l;
  cp = NULL;
  for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
    {
      ChangedProperty *i_cp = l->data;
      if (i_cp->info == info)
        {
          cp = i_cp;
          break;
        }
    }
  if (cp == NULL)
    {
      cp = g_new0 (ChangedProperty, 1);
      cp->prop_id = prop_id;
      cp->info = info;
      skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
      g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
      g_value_copy (orig_value, &cp->orig_value);
    }
}

static void
bluez_device1_skeleton_notify (GObject      *object,
  GParamSpec *pspec G_GNUC_UNUSED)
{
  BluezDevice1Skeleton *skeleton = BLUEZ_DEVICE1_SKELETON (object);
  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties != NULL &&
      skeleton->priv->changed_properties_idle_source == NULL)
    {
      skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
      g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
      g_source_set_callback (skeleton->priv->changed_properties_idle_source, _bluez_device1_emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
      g_source_set_name (skeleton->priv->changed_properties_idle_source, "[generated] _bluez_device1_emit_changed");
      g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
      g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
  g_mutex_unlock (&skeleton->priv->lock);
}

static void
bluez_device1_skeleton_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  const _ExtendedGDBusPropertyInfo *info;
  BluezDevice1Skeleton *skeleton = BLUEZ_DEVICE1_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 5);
  info = (const _ExtendedGDBusPropertyInfo *) _bluez_device1_property_info_pointers[prop_id - 1];
  g_mutex_lock (&skeleton->priv->lock);
  g_object_freeze_notify (object);
  if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
      if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL &&
          info->emits_changed_signal)
        _bluez_device1_schedule_emit_changed (skeleton, info, prop_id, &skeleton->priv->properties[prop_id - 1]);
      g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
      g_object_notify_by_pspec (object, pspec);
    }
  g_mutex_unlock (&skeleton->priv->lock);
  g_object_thaw_notify (object);
}

static void
bluez_device1_skeleton_init (BluezDevice1Skeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = bluez_device1_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, BLUEZ_TYPE_DEVICE1_SKELETON, BluezDevice1SkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
  skeleton->priv->properties = g_new0 (GValue, 5);
  g_value_init (&skeleton->priv->properties[0], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[1], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[2], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[3], G_TYPE_VARIANT);
  g_value_init (&skeleton->priv->properties[4], G_TYPE_BOOLEAN);
}

static const gchar *
bluez_device1_skeleton_get_address (BluezDevice1 *object)
{
  BluezDevice1Skeleton *skeleton = BLUEZ_DEVICE1_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[0]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
bluez_device1_skeleton_get_connected (BluezDevice1 *object)
{
  BluezDevice1Skeleton *skeleton = BLUEZ_DEVICE1_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[1]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
bluez_device1_skeleton_get_adapter (BluezDevice1 *object)
{
  BluezDevice1Skeleton *skeleton = BLUEZ_DEVICE1_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[2]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static GVariant *
bluez_device1_skeleton_get_service_data (BluezDevice1 *object)
{
  BluezDevice1Skeleton *skeleton = BLUEZ_DEVICE1_SKELETON (object);
  GVariant *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_variant (&(skeleton->priv->properties[3]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
bluez_device1_skeleton_get_services_resolved (BluezDevice1 *object)
{
  BluezDevice1Skeleton *skeleton = BLUEZ_DEVICE1_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[4]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static void
bluez_device1_skeleton_class_init (BluezDevice1SkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = bluez_device1_skeleton_finalize;
  gobject_class->get_property = bluez_device1_skeleton_get_property;
  gobject_class->set_property = bluez_device1_skeleton_set_property;
  gobject_class->notify       = bluez_device1_skeleton_notify;


  bluez_device1_override_properties (gobject_class, 1);

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = bluez_device1_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = bluez_device1_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = bluez_device1_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = bluez_device1_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (BluezDevice1SkeletonPrivate));
#endif
}

static void
bluez_device1_skeleton_iface_init (BluezDevice1Iface *iface)
{
  iface->get_address = bluez_device1_skeleton_get_address;
  iface->get_connected = bluez_device1_skeleton_get_connected;
  iface->get_adapter = bluez_device1_skeleton_get_adapter;
  iface->get_service_data = bluez_device1_skeleton_get_service_data;
  iface->get_services_resolved = bluez_device1_skeleton_get_services_resolved;
}

/**
 * bluez_device1_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-bluez-Device1.top_of_page">org.bluez.Device1</link>.
 *
 * Returns: (transfer full) (type BluezDevice1Skeleton): The skeleton object.
 */
BluezDevice1 *
bluez_device1_skeleton_new (void)
{
  return BLUEZ_DEVICE1 (g_object_new (BLUEZ_TYPE_DEVICE1_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface org.bluez.GattManager1
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:BluezGattManager1
 * @title: BluezGattManager1
 * @short_description: Generated C code for the org.bluez.GattManager1 D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-bluez-GattManager1.top_of_page">org.bluez.GattManager1</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.bluez.GattManager1 ---- */

static const _ExtendedGDBusArgInfo _bluez_gatt_manager1_method_info_register_application_IN_ARG_application =
{
  {
    -1,
    (gchar *) "application",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _bluez_gatt_manager1_method_info_register_application_IN_ARG_options =
{
  {
    -1,
    (gchar *) "options",
    (gchar *) "a{sv}",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _bluez_gatt_manager1_method_info_register_application_IN_ARG_pointers[] =
{
  &_bluez_gatt_manager1_method_info_register_application_IN_ARG_application.parent_struct,
  &_bluez_gatt_manager1_method_info_register_application_IN_ARG_options.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _bluez_gatt_manager1_method_info_register_application =
{
  {
    -1,
    (gchar *) "RegisterApplication",
    (GDBusArgInfo **) &_bluez_gatt_manager1_method_info_register_application_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-register-application",
  FALSE
};

static const _ExtendedGDBusArgInfo _bluez_gatt_manager1_method_info_unregister_application_IN_ARG_application =
{
  {
    -1,
    (gchar *) "application",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _bluez_gatt_manager1_method_info_unregister_application_IN_ARG_pointers[] =
{
  &_bluez_gatt_manager1_method_info_unregister_application_IN_ARG_application.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _bluez_gatt_manager1_method_info_unregister_application =
{
  {
    -1,
    (gchar *) "UnregisterApplication",
    (GDBusArgInfo **) &_bluez_gatt_manager1_method_info_unregister_application_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-unregister-application",
  FALSE
};

static const GDBusMethodInfo * const _bluez_gatt_manager1_method_info_pointers[] =
{
  &_bluez_gatt_manager1_method_info_register_application.parent_struct,
  &_bluez_gatt_manager1_method_info_unregister_application.parent_struct,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _bluez_gatt_manager1_interface_info =
{
  {
    -1,
    (gchar *) "org.bluez.GattManager1",
    (GDBusMethodInfo **) &_bluez_gatt_manager1_method_info_pointers,
    NULL,
    NULL,
    NULL
  },
  "gatt-manager1",
};


/**
 * bluez_gatt_manager1_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-bluez-GattManager1.top_of_page">org.bluez.GattManager1</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
bluez_gatt_manager1_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_bluez_gatt_manager1_interface_info.parent_struct;
}

/**
 * bluez_gatt_manager1_override_properties:
 * @klass: The class structure for a #GObject derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #BluezGattManager1 interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
bluez_gatt_manager1_override_properties (GObjectClass *klass G_GNUC_UNUSED, guint property_id_begin)
{
  return property_id_begin - 1;
}



/**
 * BluezGattManager1:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-bluez-GattManager1.top_of_page">org.bluez.GattManager1</link>.
 */

/**
 * BluezGattManager1Iface:
 * @parent_iface: The parent interface.
 * @handle_register_application: Handler for the #BluezGattManager1::handle-register-application signal.
 * @handle_unregister_application: Handler for the #BluezGattManager1::handle-unregister-application signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-bluez-GattManager1.top_of_page">org.bluez.GattManager1</link>.
 */

typedef BluezGattManager1Iface BluezGattManager1Interface;
G_DEFINE_INTERFACE (BluezGattManager1, bluez_gatt_manager1, G_TYPE_OBJECT)

static void
bluez_gatt_manager1_default_init (BluezGattManager1Iface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * BluezGattManager1::handle-register-application:
   * @object: A #BluezGattManager1.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_application: Argument passed by remote caller.
   * @arg_options: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-bluez-GattManager1.RegisterApplication">RegisterApplication()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call bluez_gatt_manager1_complete_register_application() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-register-application",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (BluezGattManager1Iface, handle_register_application),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_VARIANT);

  /**
   * BluezGattManager1::handle-unregister-application:
   * @object: A #BluezGattManager1.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_application: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-bluez-GattManager1.UnregisterApplication">UnregisterApplication()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call bluez_gatt_manager1_complete_unregister_application() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-unregister-application",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (BluezGattManager1Iface, handle_unregister_application),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

}

/**
 * bluez_gatt_manager1_call_register_application:
 * @proxy: A #BluezGattManager1Proxy.
 * @arg_application: Argument to pass with the method invocation.
 * @arg_options: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-bluez-GattManager1.RegisterApplication">RegisterApplication()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call bluez_gatt_manager1_call_register_application_finish() to get the result of the operation.
 *
 * See bluez_gatt_manager1_call_register_application_sync() for the synchronous, blocking version of this method.
 */
void
bluez_gatt_manager1_call_register_application (
    BluezGattManager1 *proxy,
    const gchar *arg_application,
    GVariant *arg_options,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "RegisterApplication",
    g_variant_new ("(o@a{sv})",
                   arg_application,
                   arg_options),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * bluez_gatt_manager1_call_register_application_finish:
 * @proxy: A #BluezGattManager1Proxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_gatt_manager1_call_register_application().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with bluez_gatt_manager1_call_register_application().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_gatt_manager1_call_register_application_finish (
    BluezGattManager1 *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_gatt_manager1_call_register_application_sync:
 * @proxy: A #BluezGattManager1Proxy.
 * @arg_application: Argument to pass with the method invocation.
 * @arg_options: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-bluez-GattManager1.RegisterApplication">RegisterApplication()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See bluez_gatt_manager1_call_register_application() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_gatt_manager1_call_register_application_sync (
    BluezGattManager1 *proxy,
    const gchar *arg_application,
    GVariant *arg_options,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "RegisterApplication",
    g_variant_new ("(o@a{sv})",
                   arg_application,
                   arg_options),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_gatt_manager1_call_unregister_application:
 * @proxy: A #BluezGattManager1Proxy.
 * @arg_application: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-bluez-GattManager1.UnregisterApplication">UnregisterApplication()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call bluez_gatt_manager1_call_unregister_application_finish() to get the result of the operation.
 *
 * See bluez_gatt_manager1_call_unregister_application_sync() for the synchronous, blocking version of this method.
 */
void
bluez_gatt_manager1_call_unregister_application (
    BluezGattManager1 *proxy,
    const gchar *arg_application,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "UnregisterApplication",
    g_variant_new ("(o)",
                   arg_application),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * bluez_gatt_manager1_call_unregister_application_finish:
 * @proxy: A #BluezGattManager1Proxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_gatt_manager1_call_unregister_application().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with bluez_gatt_manager1_call_unregister_application().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_gatt_manager1_call_unregister_application_finish (
    BluezGattManager1 *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_gatt_manager1_call_unregister_application_sync:
 * @proxy: A #BluezGattManager1Proxy.
 * @arg_application: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-bluez-GattManager1.UnregisterApplication">UnregisterApplication()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See bluez_gatt_manager1_call_unregister_application() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_gatt_manager1_call_unregister_application_sync (
    BluezGattManager1 *proxy,
    const gchar *arg_application,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "UnregisterApplication",
    g_variant_new ("(o)",
                   arg_application),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_gatt_manager1_complete_register_application:
 * @object: A #BluezGattManager1.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-bluez-GattManager1.RegisterApplication">RegisterApplication()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
bluez_gatt_manager1_complete_register_application (
    BluezGattManager1 *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * bluez_gatt_manager1_complete_unregister_application:
 * @object: A #BluezGattManager1.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-bluez-GattManager1.UnregisterApplication">UnregisterApplication()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
bluez_gatt_manager1_complete_unregister_application (
    BluezGattManager1 *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * BluezGattManager1Proxy:
 *
 * The #BluezGattManager1Proxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * BluezGattManager1ProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #BluezGattManager1Proxy.
 */

struct _BluezGattManager1ProxyPrivate
{
  GData *qdata;
};

static void bluez_gatt_manager1_proxy_iface_init (BluezGattManager1Iface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (BluezGattManager1Proxy, bluez_gatt_manager1_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (BluezGattManager1Proxy)
                         G_IMPLEMENT_INTERFACE (BLUEZ_TYPE_GATT_MANAGER1, bluez_gatt_manager1_proxy_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (BluezGattManager1Proxy, bluez_gatt_manager1_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (BLUEZ_TYPE_GATT_MANAGER1, bluez_gatt_manager1_proxy_iface_init))

#endif
static void
bluez_gatt_manager1_proxy_finalize (GObject *object)
{
  BluezGattManager1Proxy *proxy = BLUEZ_GATT_MANAGER1_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (bluez_gatt_manager1_proxy_parent_class)->finalize (object);
}

static void
bluez_gatt_manager1_proxy_get_property (GObject      *object G_GNUC_UNUSED,
  guint         prop_id G_GNUC_UNUSED,
  GValue       *value G_GNUC_UNUSED,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
bluez_gatt_manager1_proxy_set_property (GObject      *object G_GNUC_UNUSED,
  guint         prop_id G_GNUC_UNUSED,
  const GValue *value G_GNUC_UNUSED,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
bluez_gatt_manager1_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  gsize n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_bluez_gatt_manager1_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], BLUEZ_TYPE_GATT_MANAGER1);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, BLUEZ_TYPE_GATT_MANAGER1);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
bluez_gatt_manager1_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  BluezGattManager1Proxy *proxy = BLUEZ_GATT_MANAGER1_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_gatt_manager1_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_gatt_manager1_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
bluez_gatt_manager1_proxy_init (BluezGattManager1Proxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = bluez_gatt_manager1_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, BLUEZ_TYPE_GATT_MANAGER1_PROXY, BluezGattManager1ProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), bluez_gatt_manager1_interface_info ());
}

static void
bluez_gatt_manager1_proxy_class_init (BluezGattManager1ProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = bluez_gatt_manager1_proxy_finalize;
  gobject_class->get_property = bluez_gatt_manager1_proxy_get_property;
  gobject_class->set_property = bluez_gatt_manager1_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = bluez_gatt_manager1_proxy_g_signal;
  proxy_class->g_properties_changed = bluez_gatt_manager1_proxy_g_properties_changed;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (BluezGattManager1ProxyPrivate));
#endif
}

static void
bluez_gatt_manager1_proxy_iface_init (BluezGattManager1Iface *iface G_GNUC_UNUSED)
{
}

/**
 * bluez_gatt_manager1_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-bluez-GattManager1.top_of_page">org.bluez.GattManager1</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call bluez_gatt_manager1_proxy_new_finish() to get the result of the operation.
 *
 * See bluez_gatt_manager1_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
bluez_gatt_manager1_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (BLUEZ_TYPE_GATT_MANAGER1_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.bluez.GattManager1", NULL);
}

/**
 * bluez_gatt_manager1_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_gatt_manager1_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with bluez_gatt_manager1_proxy_new().
 *
 * Returns: (transfer full) (type BluezGattManager1Proxy): The constructed proxy object or %NULL if @error is set.
 */
BluezGattManager1 *
bluez_gatt_manager1_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return BLUEZ_GATT_MANAGER1 (ret);
  else
    return NULL;
}

/**
 * bluez_gatt_manager1_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-bluez-GattManager1.top_of_page">org.bluez.GattManager1</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See bluez_gatt_manager1_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type BluezGattManager1Proxy): The constructed proxy object or %NULL if @error is set.
 */
BluezGattManager1 *
bluez_gatt_manager1_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (BLUEZ_TYPE_GATT_MANAGER1_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.bluez.GattManager1", NULL);
  if (ret != NULL)
    return BLUEZ_GATT_MANAGER1 (ret);
  else
    return NULL;
}


/**
 * bluez_gatt_manager1_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like bluez_gatt_manager1_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call bluez_gatt_manager1_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See bluez_gatt_manager1_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
bluez_gatt_manager1_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (BLUEZ_TYPE_GATT_MANAGER1_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.bluez.GattManager1", NULL);
}

/**
 * bluez_gatt_manager1_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_gatt_manager1_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with bluez_gatt_manager1_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type BluezGattManager1Proxy): The constructed proxy object or %NULL if @error is set.
 */
BluezGattManager1 *
bluez_gatt_manager1_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return BLUEZ_GATT_MANAGER1 (ret);
  else
    return NULL;
}

/**
 * bluez_gatt_manager1_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like bluez_gatt_manager1_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See bluez_gatt_manager1_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type BluezGattManager1Proxy): The constructed proxy object or %NULL if @error is set.
 */
BluezGattManager1 *
bluez_gatt_manager1_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (BLUEZ_TYPE_GATT_MANAGER1_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.bluez.GattManager1", NULL);
  if (ret != NULL)
    return BLUEZ_GATT_MANAGER1 (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * BluezGattManager1Skeleton:
 *
 * The #BluezGattManager1Skeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * BluezGattManager1SkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #BluezGattManager1Skeleton.
 */

struct _BluezGattManager1SkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_bluez_gatt_manager1_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  BluezGattManager1Skeleton *skeleton = BLUEZ_GATT_MANAGER1_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  guint num_extra;
  gsize n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], BLUEZ_TYPE_GATT_MANAGER1);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, BLUEZ_TYPE_GATT_MANAGER1);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_bluez_gatt_manager1_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  BluezGattManager1Skeleton *skeleton = BLUEZ_GATT_MANAGER1_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_gatt_manager1_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_bluez_gatt_manager1_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  BluezGattManager1Skeleton *skeleton = BLUEZ_GATT_MANAGER1_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_gatt_manager1_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _bluez_gatt_manager1_skeleton_vtable =
{
  _bluez_gatt_manager1_skeleton_handle_method_call,
  _bluez_gatt_manager1_skeleton_handle_get_property,
  _bluez_gatt_manager1_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
bluez_gatt_manager1_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return bluez_gatt_manager1_interface_info ();
}

static GDBusInterfaceVTable *
bluez_gatt_manager1_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_bluez_gatt_manager1_skeleton_vtable;
}

static GVariant *
bluez_gatt_manager1_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  BluezGattManager1Skeleton *skeleton = BLUEZ_GATT_MANAGER1_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_bluez_gatt_manager1_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _bluez_gatt_manager1_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _bluez_gatt_manager1_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _bluez_gatt_manager1_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.bluez.GattManager1", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static void
bluez_gatt_manager1_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton G_GNUC_UNUSED)
{
}

static void bluez_gatt_manager1_skeleton_iface_init (BluezGattManager1Iface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (BluezGattManager1Skeleton, bluez_gatt_manager1_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (BluezGattManager1Skeleton)
                         G_IMPLEMENT_INTERFACE (BLUEZ_TYPE_GATT_MANAGER1, bluez_gatt_manager1_skeleton_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (BluezGattManager1Skeleton, bluez_gatt_manager1_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (BLUEZ_TYPE_GATT_MANAGER1, bluez_gatt_manager1_skeleton_iface_init))

#endif
static void
bluez_gatt_manager1_skeleton_finalize (GObject *object)
{
  BluezGattManager1Skeleton *skeleton = BLUEZ_GATT_MANAGER1_SKELETON (object);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (bluez_gatt_manager1_skeleton_parent_class)->finalize (object);
}

static void
bluez_gatt_manager1_skeleton_init (BluezGattManager1Skeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = bluez_gatt_manager1_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, BLUEZ_TYPE_GATT_MANAGER1_SKELETON, BluezGattManager1SkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
bluez_gatt_manager1_skeleton_class_init (BluezGattManager1SkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = bluez_gatt_manager1_skeleton_finalize;

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = bluez_gatt_manager1_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = bluez_gatt_manager1_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = bluez_gatt_manager1_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = bluez_gatt_manager1_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (BluezGattManager1SkeletonPrivate));
#endif
}

static void
bluez_gatt_manager1_skeleton_iface_init (BluezGattManager1Iface *iface G_GNUC_UNUSED)
{
}

/**
 * bluez_gatt_manager1_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-bluez-GattManager1.top_of_page">org.bluez.GattManager1</link>.
 *
 * Returns: (transfer full) (type BluezGattManager1Skeleton): The skeleton object.
 */
BluezGattManager1 *
bluez_gatt_manager1_skeleton_new (void)
{
  return BLUEZ_GATT_MANAGER1 (g_object_new (BLUEZ_TYPE_GATT_MANAGER1_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface org.bluez.GattService1
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:BluezGattService1
 * @title: BluezGattService1
 * @short_description: Generated C code for the org.bluez.GattService1 D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-bluez-GattService1.top_of_page">org.bluez.GattService1</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.bluez.GattService1 ---- */

static const _ExtendedGDBusPropertyInfo _bluez_gatt_service1_property_info_uuid =
{
  {
    -1,
    (gchar *) "UUID",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "uuid",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _bluez_gatt_service1_property_info_device =
{
  {
    -1,
    (gchar *) "Device",
    (gchar *) "o",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "device",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _bluez_gatt_service1_property_info_primary =
{
  {
    -1,
    (gchar *) "Primary",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "primary",
  FALSE,
  TRUE
};

static const GDBusPropertyInfo * const _bluez_gatt_service1_property_info_pointers[] =
{
  &_bluez_gatt_service1_property_info_uuid.parent_struct,
  &_bluez_gatt_service1_property_info_device.parent_struct,
  &_bluez_gatt_service1_property_info_primary.parent_struct,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _bluez_gatt_service1_interface_info =
{
  {
    -1,
    (gchar *) "org.bluez.GattService1",
    NULL,
    NULL,
    (GDBusPropertyInfo **) &_bluez_gatt_service1_property_info_pointers,
    NULL
  },
  "gatt-service1",
};


/**
 * bluez_gatt_service1_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-bluez-GattService1.top_of_page">org.bluez.GattService1</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
bluez_gatt_service1_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_bluez_gatt_service1_interface_info.parent_struct;
}

/**
 * bluez_gatt_service1_override_properties:
 * @klass: The class structure for a #GObject derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #BluezGattService1 interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
bluez_gatt_service1_override_properties (GObjectClass *klass, guint property_id_begin)
{
  g_object_class_override_property (klass, property_id_begin++, "uuid");
  g_object_class_override_property (klass, property_id_begin++, "device");
  g_object_class_override_property (klass, property_id_begin++, "primary");
  return property_id_begin - 1;
}



/**
 * BluezGattService1:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-bluez-GattService1.top_of_page">org.bluez.GattService1</link>.
 */

/**
 * BluezGattService1Iface:
 * @parent_iface: The parent interface.
 * @get_device: Getter for the #BluezGattService1:device property.
 * @get_primary: Getter for the #BluezGattService1:primary property.
 * @get_uuid: Getter for the #BluezGattService1:uuid property.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-bluez-GattService1.top_of_page">org.bluez.GattService1</link>.
 */

typedef BluezGattService1Iface BluezGattService1Interface;
G_DEFINE_INTERFACE (BluezGattService1, bluez_gatt_service1, G_TYPE_OBJECT)

static void
bluez_gatt_service1_default_init (BluezGattService1Iface *iface)
{
  /* GObject properties for D-Bus properties: */
  /**
   * BluezGattService1:uuid:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-GattService1.UUID">"UUID"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("uuid", "UUID", "UUID", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * BluezGattService1:device:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-GattService1.Device">"Device"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("device", "Device", "Device", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * BluezGattService1:primary:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-GattService1.Primary">"Primary"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("primary", "Primary", "Primary", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

/**
 * bluez_gatt_service1_get_uuid: (skip)
 * @object: A #BluezGattService1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-GattService1.UUID">"UUID"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use bluez_gatt_service1_dup_uuid() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
bluez_gatt_service1_get_uuid (BluezGattService1 *object)
{
  return BLUEZ_GATT_SERVICE1_GET_IFACE (object)->get_uuid (object);
}

/**
 * bluez_gatt_service1_dup_uuid: (skip)
 * @object: A #BluezGattService1.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-GattService1.UUID">"UUID"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
bluez_gatt_service1_dup_uuid (BluezGattService1 *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "uuid", &value, NULL);
  return value;
}

/**
 * bluez_gatt_service1_set_uuid: (skip)
 * @object: A #BluezGattService1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-GattService1.UUID">"UUID"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_gatt_service1_set_uuid (BluezGattService1 *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "uuid", value, NULL);
}

/**
 * bluez_gatt_service1_get_device: (skip)
 * @object: A #BluezGattService1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-GattService1.Device">"Device"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use bluez_gatt_service1_dup_device() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
bluez_gatt_service1_get_device (BluezGattService1 *object)
{
  return BLUEZ_GATT_SERVICE1_GET_IFACE (object)->get_device (object);
}

/**
 * bluez_gatt_service1_dup_device: (skip)
 * @object: A #BluezGattService1.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-GattService1.Device">"Device"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
bluez_gatt_service1_dup_device (BluezGattService1 *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "device", &value, NULL);
  return value;
}

/**
 * bluez_gatt_service1_set_device: (skip)
 * @object: A #BluezGattService1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-GattService1.Device">"Device"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_gatt_service1_set_device (BluezGattService1 *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "device", value, NULL);
}

/**
 * bluez_gatt_service1_get_primary: (skip)
 * @object: A #BluezGattService1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-GattService1.Primary">"Primary"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
bluez_gatt_service1_get_primary (BluezGattService1 *object)
{
  return BLUEZ_GATT_SERVICE1_GET_IFACE (object)->get_primary (object);
}

/**
 * bluez_gatt_service1_set_primary: (skip)
 * @object: A #BluezGattService1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-GattService1.Primary">"Primary"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_gatt_service1_set_primary (BluezGattService1 *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "primary", value, NULL);
}

/* ------------------------------------------------------------------------ */

/**
 * BluezGattService1Proxy:
 *
 * The #BluezGattService1Proxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * BluezGattService1ProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #BluezGattService1Proxy.
 */

struct _BluezGattService1ProxyPrivate
{
  GData *qdata;
};

static void bluez_gatt_service1_proxy_iface_init (BluezGattService1Iface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (BluezGattService1Proxy, bluez_gatt_service1_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (BluezGattService1Proxy)
                         G_IMPLEMENT_INTERFACE (BLUEZ_TYPE_GATT_SERVICE1, bluez_gatt_service1_proxy_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (BluezGattService1Proxy, bluez_gatt_service1_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (BLUEZ_TYPE_GATT_SERVICE1, bluez_gatt_service1_proxy_iface_init))

#endif
static void
bluez_gatt_service1_proxy_finalize (GObject *object)
{
  BluezGattService1Proxy *proxy = BLUEZ_GATT_SERVICE1_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (bluez_gatt_service1_proxy_parent_class)->finalize (object);
}

static void
bluez_gatt_service1_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 3);
  info = (const _ExtendedGDBusPropertyInfo *) _bluez_gatt_service1_property_info_pointers[prop_id - 1];
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
  if (info->use_gvariant)
    {
      g_value_set_variant (value, variant);
    }
  else
    {
      if (variant != NULL)
        g_dbus_gvariant_to_gvalue (variant, value);
    }
  if (variant != NULL)
    g_variant_unref (variant);
}

static void
bluez_gatt_service1_proxy_set_property_cb (GDBusProxy *proxy,
  GAsyncResult *res,
  gpointer      user_data)
{
  const _ExtendedGDBusPropertyInfo *info = user_data;
  GError *error;
  GVariant *_ret;
  error = NULL;
  _ret = g_dbus_proxy_call_finish (proxy, res, &error);
  if (!_ret)
    {
      g_warning ("Error setting property '%s' on interface org.bluez.GattService1: %s (%s, %d)",
                 info->parent_struct.name, 
                 error->message, g_quark_to_string (error->domain), error->code);
      g_error_free (error);
    }
  else
    {
      g_variant_unref (_ret);
    }
}

static void
bluez_gatt_service1_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 3);
  info = (const _ExtendedGDBusPropertyInfo *) _bluez_gatt_service1_property_info_pointers[prop_id - 1];
  variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
  g_dbus_proxy_call (G_DBUS_PROXY (object),
    "org.freedesktop.DBus.Properties.Set",
    g_variant_new ("(ssv)", "org.bluez.GattService1", info->parent_struct.name, variant),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    NULL, (GAsyncReadyCallback) bluez_gatt_service1_proxy_set_property_cb, (GDBusPropertyInfo *) &info->parent_struct);
  g_variant_unref (variant);
}

static void
bluez_gatt_service1_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  gsize n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_bluez_gatt_service1_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], BLUEZ_TYPE_GATT_SERVICE1);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, BLUEZ_TYPE_GATT_SERVICE1);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
bluez_gatt_service1_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  BluezGattService1Proxy *proxy = BLUEZ_GATT_SERVICE1_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_gatt_service1_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_gatt_service1_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static const gchar *
bluez_gatt_service1_proxy_get_uuid (BluezGattService1 *object)
{
  BluezGattService1Proxy *proxy = BLUEZ_GATT_SERVICE1_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "UUID");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
bluez_gatt_service1_proxy_get_device (BluezGattService1 *object)
{
  BluezGattService1Proxy *proxy = BLUEZ_GATT_SERVICE1_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Device");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
bluez_gatt_service1_proxy_get_primary (BluezGattService1 *object)
{
  BluezGattService1Proxy *proxy = BLUEZ_GATT_SERVICE1_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Primary");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static void
bluez_gatt_service1_proxy_init (BluezGattService1Proxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = bluez_gatt_service1_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, BLUEZ_TYPE_GATT_SERVICE1_PROXY, BluezGattService1ProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), bluez_gatt_service1_interface_info ());
}

static void
bluez_gatt_service1_proxy_class_init (BluezGattService1ProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = bluez_gatt_service1_proxy_finalize;
  gobject_class->get_property = bluez_gatt_service1_proxy_get_property;
  gobject_class->set_property = bluez_gatt_service1_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = bluez_gatt_service1_proxy_g_signal;
  proxy_class->g_properties_changed = bluez_gatt_service1_proxy_g_properties_changed;

  bluez_gatt_service1_override_properties (gobject_class, 1);

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (BluezGattService1ProxyPrivate));
#endif
}

static void
bluez_gatt_service1_proxy_iface_init (BluezGattService1Iface *iface)
{
  iface->get_uuid = bluez_gatt_service1_proxy_get_uuid;
  iface->get_device = bluez_gatt_service1_proxy_get_device;
  iface->get_primary = bluez_gatt_service1_proxy_get_primary;
}

/**
 * bluez_gatt_service1_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-bluez-GattService1.top_of_page">org.bluez.GattService1</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call bluez_gatt_service1_proxy_new_finish() to get the result of the operation.
 *
 * See bluez_gatt_service1_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
bluez_gatt_service1_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (BLUEZ_TYPE_GATT_SERVICE1_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.bluez.GattService1", NULL);
}

/**
 * bluez_gatt_service1_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_gatt_service1_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with bluez_gatt_service1_proxy_new().
 *
 * Returns: (transfer full) (type BluezGattService1Proxy): The constructed proxy object or %NULL if @error is set.
 */
BluezGattService1 *
bluez_gatt_service1_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return BLUEZ_GATT_SERVICE1 (ret);
  else
    return NULL;
}

/**
 * bluez_gatt_service1_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-bluez-GattService1.top_of_page">org.bluez.GattService1</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See bluez_gatt_service1_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type BluezGattService1Proxy): The constructed proxy object or %NULL if @error is set.
 */
BluezGattService1 *
bluez_gatt_service1_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (BLUEZ_TYPE_GATT_SERVICE1_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.bluez.GattService1", NULL);
  if (ret != NULL)
    return BLUEZ_GATT_SERVICE1 (ret);
  else
    return NULL;
}


/**
 * bluez_gatt_service1_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like bluez_gatt_service1_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call bluez_gatt_service1_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See bluez_gatt_service1_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
bluez_gatt_service1_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (BLUEZ_TYPE_GATT_SERVICE1_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.bluez.GattService1", NULL);
}

/**
 * bluez_gatt_service1_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_gatt_service1_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with bluez_gatt_service1_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type BluezGattService1Proxy): The constructed proxy object or %NULL if @error is set.
 */
BluezGattService1 *
bluez_gatt_service1_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return BLUEZ_GATT_SERVICE1 (ret);
  else
    return NULL;
}

/**
 * bluez_gatt_service1_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like bluez_gatt_service1_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See bluez_gatt_service1_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type BluezGattService1Proxy): The constructed proxy object or %NULL if @error is set.
 */
BluezGattService1 *
bluez_gatt_service1_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (BLUEZ_TYPE_GATT_SERVICE1_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.bluez.GattService1", NULL);
  if (ret != NULL)
    return BLUEZ_GATT_SERVICE1 (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * BluezGattService1Skeleton:
 *
 * The #BluezGattService1Skeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * BluezGattService1SkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #BluezGattService1Skeleton.
 */

struct _BluezGattService1SkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_bluez_gatt_service1_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  BluezGattService1Skeleton *skeleton = BLUEZ_GATT_SERVICE1_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  guint num_extra;
  gsize n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], BLUEZ_TYPE_GATT_SERVICE1);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, BLUEZ_TYPE_GATT_SERVICE1);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_bluez_gatt_service1_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  BluezGattService1Skeleton *skeleton = BLUEZ_GATT_SERVICE1_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_gatt_service1_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_bluez_gatt_service1_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  BluezGattService1Skeleton *skeleton = BLUEZ_GATT_SERVICE1_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_gatt_service1_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _bluez_gatt_service1_skeleton_vtable =
{
  _bluez_gatt_service1_skeleton_handle_method_call,
  _bluez_gatt_service1_skeleton_handle_get_property,
  _bluez_gatt_service1_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
bluez_gatt_service1_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return bluez_gatt_service1_interface_info ();
}

static GDBusInterfaceVTable *
bluez_gatt_service1_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_bluez_gatt_service1_skeleton_vtable;
}

static GVariant *
bluez_gatt_service1_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  BluezGattService1Skeleton *skeleton = BLUEZ_GATT_SERVICE1_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_bluez_gatt_service1_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _bluez_gatt_service1_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _bluez_gatt_service1_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _bluez_gatt_service1_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.bluez.GattService1", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static gboolean _bluez_gatt_service1_emit_changed (gpointer user_data);

static void
bluez_gatt_service1_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
  BluezGattService1Skeleton *skeleton = BLUEZ_GATT_SERVICE1_SKELETON (_skeleton);
  gboolean emit_changed = FALSE;

  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    {
      g_source_destroy (skeleton->priv->changed_properties_idle_source);
      skeleton->priv->changed_properties_idle_source = NULL;
      emit_changed = TRUE;
    }
  g_mutex_unlock (&skeleton->priv->lock);

  if (emit_changed)
    _bluez_gatt_service1_emit_changed (skeleton);
}

static void bluez_gatt_service1_skeleton_iface_init (BluezGattService1Iface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (BluezGattService1Skeleton, bluez_gatt_service1_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (BluezGattService1Skeleton)
                         G_IMPLEMENT_INTERFACE (BLUEZ_TYPE_GATT_SERVICE1, bluez_gatt_service1_skeleton_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (BluezGattService1Skeleton, bluez_gatt_service1_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (BLUEZ_TYPE_GATT_SERVICE1, bluez_gatt_service1_skeleton_iface_init))

#endif
static void
bluez_gatt_service1_skeleton_finalize (GObject *object)
{
  BluezGattService1Skeleton *skeleton = BLUEZ_GATT_SERVICE1_SKELETON (object);
  guint n;
  for (n = 0; n < 3; n++)
    g_value_unset (&skeleton->priv->properties[n]);
  g_free (skeleton->priv->properties);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (bluez_gatt_service1_skeleton_parent_class)->finalize (object);
}

static void
bluez_gatt_service1_skeleton_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  BluezGattService1Skeleton *skeleton = BLUEZ_GATT_SERVICE1_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 3);
  g_mutex_lock (&skeleton->priv->lock);
  g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
  g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
_bluez_gatt_service1_emit_changed (gpointer user_data)
{
  BluezGattService1Skeleton *skeleton = BLUEZ_GATT_SERVICE1_SKELETON (user_data);
  GList *l;
  GVariantBuilder builder;
  GVariantBuilder invalidated_builder;
  guint num_changes;

  g_mutex_lock (&skeleton->priv->lock);
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
  for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
    {
      ChangedProperty *cp = l->data;
      GVariant *variant;
      const GValue *cur_value;

      cur_value = &skeleton->priv->properties[cp->prop_id - 1];
      if (!_g_value_equal (cur_value, &cp->orig_value))
        {
          variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
          g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
          g_variant_unref (variant);
          num_changes++;
        }
    }
  if (num_changes > 0)
    {
      GList *connections, *ll;
      GVariant *signal_variant;
      signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "org.bluez.GattService1",
                                           &builder, &invalidated_builder));
      connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
      for (ll = connections; ll != NULL; ll = ll->next)
        {
          GDBusConnection *connection = ll->data;

          g_dbus_connection_emit_signal (connection,
                                         NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
                                         "org.freedesktop.DBus.Properties",
                                         "PropertiesChanged",
                                         signal_variant,
                                         NULL);
        }
      g_variant_unref (signal_variant);
      g_list_free_full (connections, g_object_unref);
    }
  else
    {
      g_variant_builder_clear (&builder);
      g_variant_builder_clear (&invalidated_builder);
    }
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  skeleton->priv->changed_properties = NULL;
  skeleton->priv->changed_properties_idle_source = NULL;
  g_mutex_unlock (&skeleton->priv->lock);
  return FALSE;
}

static void
_bluez_gatt_service1_schedule_emit_changed (BluezGattService1Skeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
  ChangedProperty *cp;
  GList *l;
  cp = NULL;
  for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
    {
      ChangedProperty *i_cp = l->data;
      if (i_cp->info == info)
        {
          cp = i_cp;
          break;
        }
    }
  if (cp == NULL)
    {
      cp = g_new0 (ChangedProperty, 1);
      cp->prop_id = prop_id;
      cp->info = info;
      skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
      g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
      g_value_copy (orig_value, &cp->orig_value);
    }
}

static void
bluez_gatt_service1_skeleton_notify (GObject      *object,
  GParamSpec *pspec G_GNUC_UNUSED)
{
  BluezGattService1Skeleton *skeleton = BLUEZ_GATT_SERVICE1_SKELETON (object);
  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties != NULL &&
      skeleton->priv->changed_properties_idle_source == NULL)
    {
      skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
      g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
      g_source_set_callback (skeleton->priv->changed_properties_idle_source, _bluez_gatt_service1_emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
      g_source_set_name (skeleton->priv->changed_properties_idle_source, "[generated] _bluez_gatt_service1_emit_changed");
      g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
      g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
  g_mutex_unlock (&skeleton->priv->lock);
}

static void
bluez_gatt_service1_skeleton_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  const _ExtendedGDBusPropertyInfo *info;
  BluezGattService1Skeleton *skeleton = BLUEZ_GATT_SERVICE1_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 3);
  info = (const _ExtendedGDBusPropertyInfo *) _bluez_gatt_service1_property_info_pointers[prop_id - 1];
  g_mutex_lock (&skeleton->priv->lock);
  g_object_freeze_notify (object);
  if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
      if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL &&
          info->emits_changed_signal)
        _bluez_gatt_service1_schedule_emit_changed (skeleton, info, prop_id, &skeleton->priv->properties[prop_id - 1]);
      g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
      g_object_notify_by_pspec (object, pspec);
    }
  g_mutex_unlock (&skeleton->priv->lock);
  g_object_thaw_notify (object);
}

static void
bluez_gatt_service1_skeleton_init (BluezGattService1Skeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = bluez_gatt_service1_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, BLUEZ_TYPE_GATT_SERVICE1_SKELETON, BluezGattService1SkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
  skeleton->priv->properties = g_new0 (GValue, 3);
  g_value_init (&skeleton->priv->properties[0], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[1], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[2], G_TYPE_BOOLEAN);
}

static const gchar *
bluez_gatt_service1_skeleton_get_uuid (BluezGattService1 *object)
{
  BluezGattService1Skeleton *skeleton = BLUEZ_GATT_SERVICE1_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[0]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
bluez_gatt_service1_skeleton_get_device (BluezGattService1 *object)
{
  BluezGattService1Skeleton *skeleton = BLUEZ_GATT_SERVICE1_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[1]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
bluez_gatt_service1_skeleton_get_primary (BluezGattService1 *object)
{
  BluezGattService1Skeleton *skeleton = BLUEZ_GATT_SERVICE1_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[2]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static void
bluez_gatt_service1_skeleton_class_init (BluezGattService1SkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = bluez_gatt_service1_skeleton_finalize;
  gobject_class->get_property = bluez_gatt_service1_skeleton_get_property;
  gobject_class->set_property = bluez_gatt_service1_skeleton_set_property;
  gobject_class->notify       = bluez_gatt_service1_skeleton_notify;


  bluez_gatt_service1_override_properties (gobject_class, 1);

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = bluez_gatt_service1_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = bluez_gatt_service1_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = bluez_gatt_service1_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = bluez_gatt_service1_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (BluezGattService1SkeletonPrivate));
#endif
}

static void
bluez_gatt_service1_skeleton_iface_init (BluezGattService1Iface *iface)
{
  iface->get_uuid = bluez_gatt_service1_skeleton_get_uuid;
  iface->get_device = bluez_gatt_service1_skeleton_get_device;
  iface->get_primary = bluez_gatt_service1_skeleton_get_primary;
}

/**
 * bluez_gatt_service1_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-bluez-GattService1.top_of_page">org.bluez.GattService1</link>.
 *
 * Returns: (transfer full) (type BluezGattService1Skeleton): The skeleton object.
 */
BluezGattService1 *
bluez_gatt_service1_skeleton_new (void)
{
  return BLUEZ_GATT_SERVICE1 (g_object_new (BLUEZ_TYPE_GATT_SERVICE1_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface org.bluez.GattCharacteristic1
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:BluezGattCharacteristic1
 * @title: BluezGattCharacteristic1
 * @short_description: Generated C code for the org.bluez.GattCharacteristic1 D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-bluez-GattCharacteristic1.top_of_page">org.bluez.GattCharacteristic1</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.bluez.GattCharacteristic1 ---- */

static const _ExtendedGDBusArgInfo _bluez_gatt_characteristic1_method_info_read_value_IN_ARG_options =
{
  {
    -1,
    (gchar *) "options",
    (gchar *) "a{sv}",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _bluez_gatt_characteristic1_method_info_read_value_IN_ARG_pointers[] =
{
  &_bluez_gatt_characteristic1_method_info_read_value_IN_ARG_options.parent_struct,
  NULL
};

static const _ExtendedGDBusArgInfo _bluez_gatt_characteristic1_method_info_read_value_OUT_ARG_value =
{
  {
    -1,
    (gchar *) "value",
    (gchar *) "ay",
    NULL
  },
  TRUE
};

static const GDBusArgInfo * const _bluez_gatt_characteristic1_method_info_read_value_OUT_ARG_pointers[] =
{
  &_bluez_gatt_characteristic1_method_info_read_value_OUT_ARG_value.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _bluez_gatt_characteristic1_method_info_read_value =
{
  {
    -1,
    (gchar *) "ReadValue",
    (GDBusArgInfo **) &_bluez_gatt_characteristic1_method_info_read_value_IN_ARG_pointers,
    (GDBusArgInfo **) &_bluez_gatt_characteristic1_method_info_read_value_OUT_ARG_pointers,
    NULL
  },
  "handle-read-value",
  FALSE
};

static const _ExtendedGDBusArgInfo _bluez_gatt_characteristic1_method_info_write_value_IN_ARG_value =
{
  {
    -1,
    (gchar *) "value",
    (gchar *) "ay",
    NULL
  },
  TRUE
};

static const _ExtendedGDBusArgInfo _bluez_gatt_characteristic1_method_info_write_value_IN_ARG_options =
{
  {
    -1,
    (gchar *) "options",
    (gchar *) "a{sv}",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _bluez_gatt_characteristic1_method_info_write_value_IN_ARG_pointers[] =
{
  &_bluez_gatt_characteristic1_method_info_write_value_IN_ARG_value.parent_struct,
  &_bluez_gatt_characteristic1_method_info_write_value_IN_ARG_options.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _bluez_gatt_characteristic1_method_info_write_value =
{
  {
    -1,
    (gchar *) "WriteValue",
    (GDBusArgInfo **) &_bluez_gatt_characteristic1_method_info_write_value_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-write-value",
  FALSE
};

static const _ExtendedGDBusArgInfo _bluez_gatt_characteristic1_method_info_acquire_write_IN_ARG_options =
{
  {
    -1,
    (gchar *) "options",
    (gchar *) "a{sv}",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _bluez_gatt_characteristic1_method_info_acquire_write_IN_ARG_pointers[] =
{
  &_bluez_gatt_characteristic1_method_info_acquire_write_IN_ARG_options.parent_struct,
  NULL
};

static const _ExtendedGDBusArgInfo _bluez_gatt_characteristic1_method_info_acquire_write_OUT_ARG_fd =
{
  {
    -1,
    (gchar *) "fd",
    (gchar *) "h",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _bluez_gatt_characteristic1_method_info_acquire_write_OUT_ARG_mtu =
{
  {
    -1,
    (gchar *) "mtu",
    (gchar *) "q",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _bluez_gatt_characteristic1_method_info_acquire_write_OUT_ARG_pointers[] =
{
  &_bluez_gatt_characteristic1_method_info_acquire_write_OUT_ARG_fd.parent_struct,
  &_bluez_gatt_characteristic1_method_info_acquire_write_OUT_ARG_mtu.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _bluez_gatt_characteristic1_method_info_acquire_write =
{
  {
    -1,
    (gchar *) "AcquireWrite",
    (GDBusArgInfo **) &_bluez_gatt_characteristic1_method_info_acquire_write_IN_ARG_pointers,
    (GDBusArgInfo **) &_bluez_gatt_characteristic1_method_info_acquire_write_OUT_ARG_pointers,
    NULL
  },
  "handle-acquire-write",
  TRUE
};

static const _ExtendedGDBusArgInfo _bluez_gatt_characteristic1_method_info_acquire_notify_IN_ARG_options =
{
  {
    -1,
    (gchar *) "options",
    (gchar *) "a{sv}",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _bluez_gatt_characteristic1_method_info_acquire_notify_IN_ARG_pointers[] =
{
  &_bluez_gatt_characteristic1_method_info_acquire_notify_IN_ARG_options.parent_struct,
  NULL
};

static const _ExtendedGDBusArgInfo _bluez_gatt_characteristic1_method_info_acquire_notify_OUT_ARG_fd =
{
  {
    -1,
    (gchar *) "fd",
    (gchar *) "h",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _bluez_gatt_characteristic1_method_info_acquire_notify_OUT_ARG_mtu =
{
  {
    -1,
    (gchar *) "mtu",
    (gchar *) "q",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _bluez_gatt_characteristic1_method_info_acquire_notify_OUT_ARG_pointers[] =
{
  &_bluez_gatt_characteristic1_method_info_acquire_notify_OUT_ARG_fd.parent_struct,
  &_bluez_gatt_characteristic1_method_info_acquire_notify_OUT_ARG_mtu.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _bluez_gatt_characteristic1_method_info_acquire_notify =
{
  {
    -1,
    (gchar *) "AcquireNotify",
    (GDBusArgInfo **) &_bluez_gatt_characteristic1_method_info_acquire_notify_IN_ARG_pointers,
    (GDBusArgInfo **) &_bluez_gatt_characteristic1_method_info_acquire_notify_OUT_ARG_pointers,
    NULL
  },
  "handle-acquire-notify",
  TRUE
};

static const _ExtendedGDBusMethodInfo _bluez_gatt_characteristic1_method_info_start_notify =
{
  {
    -1,
    (gchar *) "StartNotify",
    NULL,
    NULL,
    NULL
  },
  "handle-start-notify",
  FALSE
};

static const _ExtendedGDBusMethodInfo _bluez_gatt_characteristic1_method_info_stop_notify =
{
  {
    -1,
    (gchar *) "StopNotify",
    NULL,
    NULL,
    NULL
  },
  "handle-stop-notify",
  FALSE
};

static const _ExtendedGDBusMethodInfo _bluez_gatt_characteristic1_method_info_confirm =
{
  {
    -1,
    (gchar *) "Confirm",
    NULL,
    NULL,
    NULL
  },
  "handle-confirm",
  FALSE
};

static const GDBusMethodInfo * const _bluez_gatt_characteristic1_method_info_pointers[] =
{
  &_bluez_gatt_characteristic1_method_info_read_value.parent_struct,
  &_bluez_gatt_characteristic1_method_info_write_value.parent_struct,
  &_bluez_gatt_characteristic1_method_info_acquire_write.parent_struct,
  &_bluez_gatt_characteristic1_method_info_acquire_notify.parent_struct,
  &_bluez_gatt_characteristic1_method_info_start_notify.parent_struct,
  &_bluez_gatt_characteristic1_method_info_stop_notify.parent_struct,
  &_bluez_gatt_characteristic1_method_info_confirm.parent_struct,
  NULL
};

static const _ExtendedGDBusPropertyInfo _bluez_gatt_characteristic1_property_info_uuid =
{
  {
    -1,
    (gchar *) "UUID",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "uuid",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _bluez_gatt_characteristic1_property_info_service =
{
  {
    -1,
    (gchar *) "Service",
    (gchar *) "o",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "service",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _bluez_gatt_characteristic1_property_info_value =
{
  {
    -1,
    (gchar *) "Value",
    (gchar *) "ay",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "value",
  TRUE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _bluez_gatt_characteristic1_property_info_notifying =
{
  {
    -1,
    (gchar *) "Notifying",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "notifying",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _bluez_gatt_characteristic1_property_info_flags =
{
  {
    -1,
    (gchar *) "Flags",
    (gchar *) "as",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "flags",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _bluez_gatt_characteristic1_property_info_write_acquired =
{
  {
    -1,
    (gchar *) "WriteAcquired",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "write-acquired",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _bluez_gatt_characteristic1_property_info_notify_acquired =
{
  {
    -1,
    (gchar *) "NotifyAcquired",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "notify-acquired",
  FALSE,
  TRUE
};

static const GDBusPropertyInfo * const _bluez_gatt_characteristic1_property_info_pointers[] =
{
  &_bluez_gatt_characteristic1_property_info_uuid.parent_struct,
  &_bluez_gatt_characteristic1_property_info_service.parent_struct,
  &_bluez_gatt_characteristic1_property_info_value.parent_struct,
  &_bluez_gatt_characteristic1_property_info_notifying.parent_struct,
  &_bluez_gatt_characteristic1_property_info_flags.parent_struct,
  &_bluez_gatt_characteristic1_property_info_write_acquired.parent_struct,
  &_bluez_gatt_characteristic1_property_info_notify_acquired.parent_struct,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _bluez_gatt_characteristic1_interface_info =
{
  {
    -1,
    (gchar *) "org.bluez.GattCharacteristic1",
    (GDBusMethodInfo **) &_bluez_gatt_characteristic1_method_info_pointers,
    NULL,
    (GDBusPropertyInfo **) &_bluez_gatt_characteristic1_property_info_pointers,
    NULL
  },
  "gatt-characteristic1",
};


/**
 * bluez_gatt_characteristic1_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-bluez-GattCharacteristic1.top_of_page">org.bluez.GattCharacteristic1</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
bluez_gatt_characteristic1_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_bluez_gatt_characteristic1_interface_info.parent_struct;
}

/**
 * bluez_gatt_characteristic1_override_properties:
 * @klass: The class structure for a #GObject derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #BluezGattCharacteristic1 interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
bluez_gatt_characteristic1_override_properties (GObjectClass *klass, guint property_id_begin)
{
  g_object_class_override_property (klass, property_id_begin++, "uuid");
  g_object_class_override_property (klass, property_id_begin++, "service");
  g_object_class_override_property (klass, property_id_begin++, "value");
  g_object_class_override_property (klass, property_id_begin++, "notifying");
  g_object_class_override_property (klass, property_id_begin++, "flags");
  g_object_class_override_property (klass, property_id_begin++, "write-acquired");
  g_object_class_override_property (klass, property_id_begin++, "notify-acquired");
  return property_id_begin - 1;
}



/**
 * BluezGattCharacteristic1:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-bluez-GattCharacteristic1.top_of_page">org.bluez.GattCharacteristic1</link>.
 */

/**
 * BluezGattCharacteristic1Iface:
 * @parent_iface: The parent interface.
 * @handle_acquire_notify: Handler for the #BluezGattCharacteristic1::handle-acquire-notify signal.
 * @handle_acquire_write: Handler for the #BluezGattCharacteristic1::handle-acquire-write signal.
 * @handle_confirm: Handler for the #BluezGattCharacteristic1::handle-confirm signal.
 * @handle_read_value: Handler for the #BluezGattCharacteristic1::handle-read-value signal.
 * @handle_start_notify: Handler for the #BluezGattCharacteristic1::handle-start-notify signal.
 * @handle_stop_notify: Handler for the #BluezGattCharacteristic1::handle-stop-notify signal.
 * @handle_write_value: Handler for the #BluezGattCharacteristic1::handle-write-value signal.
 * @get_flags: Getter for the #BluezGattCharacteristic1:flags property.
 * @get_notify_acquired: Getter for the #BluezGattCharacteristic1:notify-acquired property.
 * @get_notifying: Getter for the #BluezGattCharacteristic1:notifying property.
 * @get_service: Getter for the #BluezGattCharacteristic1:service property.
 * @get_uuid: Getter for the #BluezGattCharacteristic1:uuid property.
 * @get_value: Getter for the #BluezGattCharacteristic1:value property.
 * @get_write_acquired: Getter for the #BluezGattCharacteristic1:write-acquired property.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-bluez-GattCharacteristic1.top_of_page">org.bluez.GattCharacteristic1</link>.
 */

typedef BluezGattCharacteristic1Iface BluezGattCharacteristic1Interface;
G_DEFINE_INTERFACE (BluezGattCharacteristic1, bluez_gatt_characteristic1, G_TYPE_OBJECT)

static void
bluez_gatt_characteristic1_default_init (BluezGattCharacteristic1Iface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * BluezGattCharacteristic1::handle-read-value:
   * @object: A #BluezGattCharacteristic1.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_options: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-bluez-GattCharacteristic1.ReadValue">ReadValue()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call bluez_gatt_characteristic1_complete_read_value() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-read-value",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (BluezGattCharacteristic1Iface, handle_read_value),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_VARIANT);

  /**
   * BluezGattCharacteristic1::handle-write-value:
   * @object: A #BluezGattCharacteristic1.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_value: Argument passed by remote caller.
   * @arg_options: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-bluez-GattCharacteristic1.WriteValue">WriteValue()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call bluez_gatt_characteristic1_complete_write_value() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-write-value",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (BluezGattCharacteristic1Iface, handle_write_value),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_VARIANT, G_TYPE_VARIANT);

  /**
   * BluezGattCharacteristic1::handle-acquire-write:
   * @object: A #BluezGattCharacteristic1.
   * @invocation: A #GDBusMethodInvocation.
   * @fd_list: (nullable): A #GUnixFDList or %NULL.
   * @arg_options: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-bluez-GattCharacteristic1.AcquireWrite">AcquireWrite()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call bluez_gatt_characteristic1_complete_acquire_write() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-acquire-write",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (BluezGattCharacteristic1Iface, handle_acquire_write),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_UNIX_FD_LIST, G_TYPE_VARIANT);

  /**
   * BluezGattCharacteristic1::handle-acquire-notify:
   * @object: A #BluezGattCharacteristic1.
   * @invocation: A #GDBusMethodInvocation.
   * @fd_list: (nullable): A #GUnixFDList or %NULL.
   * @arg_options: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-bluez-GattCharacteristic1.AcquireNotify">AcquireNotify()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call bluez_gatt_characteristic1_complete_acquire_notify() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-acquire-notify",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (BluezGattCharacteristic1Iface, handle_acquire_notify),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_UNIX_FD_LIST, G_TYPE_VARIANT);

  /**
   * BluezGattCharacteristic1::handle-start-notify:
   * @object: A #BluezGattCharacteristic1.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-bluez-GattCharacteristic1.StartNotify">StartNotify()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call bluez_gatt_characteristic1_complete_start_notify() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-start-notify",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (BluezGattCharacteristic1Iface, handle_start_notify),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * BluezGattCharacteristic1::handle-stop-notify:
   * @object: A #BluezGattCharacteristic1.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-bluez-GattCharacteristic1.StopNotify">StopNotify()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call bluez_gatt_characteristic1_complete_stop_notify() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-stop-notify",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (BluezGattCharacteristic1Iface, handle_stop_notify),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * BluezGattCharacteristic1::handle-confirm:
   * @object: A #BluezGattCharacteristic1.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-bluez-GattCharacteristic1.Confirm">Confirm()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call bluez_gatt_characteristic1_complete_confirm() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-confirm",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (BluezGattCharacteristic1Iface, handle_confirm),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /* GObject properties for D-Bus properties: */
  /**
   * BluezGattCharacteristic1:uuid:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-GattCharacteristic1.UUID">"UUID"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("uuid", "UUID", "UUID", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * BluezGattCharacteristic1:service:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-GattCharacteristic1.Service">"Service"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("service", "Service", "Service", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * BluezGattCharacteristic1:value:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-GattCharacteristic1.Value">"Value"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_variant ("value", "Value", "Value", G_VARIANT_TYPE ("ay"), NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * BluezGattCharacteristic1:notifying:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-GattCharacteristic1.Notifying">"Notifying"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("notifying", "Notifying", "Notifying", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * BluezGattCharacteristic1:flags:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-GattCharacteristic1.Flags">"Flags"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boxed ("flags", "Flags", "Flags", G_TYPE_STRV, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * BluezGattCharacteristic1:write-acquired:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-GattCharacteristic1.WriteAcquired">"WriteAcquired"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("write-acquired", "WriteAcquired", "WriteAcquired", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * BluezGattCharacteristic1:notify-acquired:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-GattCharacteristic1.NotifyAcquired">"NotifyAcquired"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("notify-acquired", "NotifyAcquired", "NotifyAcquired", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

/**
 * bluez_gatt_characteristic1_get_uuid: (skip)
 * @object: A #BluezGattCharacteristic1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-GattCharacteristic1.UUID">"UUID"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use bluez_gatt_characteristic1_dup_uuid() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
bluez_gatt_characteristic1_get_uuid (BluezGattCharacteristic1 *object)
{
  return BLUEZ_GATT_CHARACTERISTIC1_GET_IFACE (object)->get_uuid (object);
}

/**
 * bluez_gatt_characteristic1_dup_uuid: (skip)
 * @object: A #BluezGattCharacteristic1.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-GattCharacteristic1.UUID">"UUID"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
bluez_gatt_characteristic1_dup_uuid (BluezGattCharacteristic1 *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "uuid", &value, NULL);
  return value;
}

/**
 * bluez_gatt_characteristic1_set_uuid: (skip)
 * @object: A #BluezGattCharacteristic1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-GattCharacteristic1.UUID">"UUID"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_gatt_characteristic1_set_uuid (BluezGattCharacteristic1 *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "uuid", value, NULL);
}

/**
 * bluez_gatt_characteristic1_get_service: (skip)
 * @object: A #BluezGattCharacteristic1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-GattCharacteristic1.Service">"Service"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use bluez_gatt_characteristic1_dup_service() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
bluez_gatt_characteristic1_get_service (BluezGattCharacteristic1 *object)
{
  return BLUEZ_GATT_CHARACTERISTIC1_GET_IFACE (object)->get_service (object);
}

/**
 * bluez_gatt_characteristic1_dup_service: (skip)
 * @object: A #BluezGattCharacteristic1.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-GattCharacteristic1.Service">"Service"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
bluez_gatt_characteristic1_dup_service (BluezGattCharacteristic1 *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "service", &value, NULL);
  return value;
}

/**
 * bluez_gatt_characteristic1_set_service: (skip)
 * @object: A #BluezGattCharacteristic1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-GattCharacteristic1.Service">"Service"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_gatt_characteristic1_set_service (BluezGattCharacteristic1 *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "service", value, NULL);
}

/**
 * bluez_gatt_characteristic1_get_value: (skip)
 * @object: A #BluezGattCharacteristic1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-GattCharacteristic1.Value">"Value"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use bluez_gatt_characteristic1_dup_value() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
GVariant *
bluez_gatt_characteristic1_get_value (BluezGattCharacteristic1 *object)
{
  return BLUEZ_GATT_CHARACTERISTIC1_GET_IFACE (object)->get_value (object);
}

/**
 * bluez_gatt_characteristic1_dup_value: (skip)
 * @object: A #BluezGattCharacteristic1.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-GattCharacteristic1.Value">"Value"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_variant_unref().
 */
GVariant *
bluez_gatt_characteristic1_dup_value (BluezGattCharacteristic1 *object)
{
  GVariant *value;
  g_object_get (G_OBJECT (object), "value", &value, NULL);
  return value;
}

/**
 * bluez_gatt_characteristic1_set_value: (skip)
 * @object: A #BluezGattCharacteristic1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-GattCharacteristic1.Value">"Value"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_gatt_characteristic1_set_value (BluezGattCharacteristic1 *object, GVariant *value)
{
  g_object_set (G_OBJECT (object), "value", value, NULL);
}

/**
 * bluez_gatt_characteristic1_get_notifying: (skip)
 * @object: A #BluezGattCharacteristic1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-GattCharacteristic1.Notifying">"Notifying"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
bluez_gatt_characteristic1_get_notifying (BluezGattCharacteristic1 *object)
{
  return BLUEZ_GATT_CHARACTERISTIC1_GET_IFACE (object)->get_notifying (object);
}

/**
 * bluez_gatt_characteristic1_set_notifying: (skip)
 * @object: A #BluezGattCharacteristic1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-GattCharacteristic1.Notifying">"Notifying"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_gatt_characteristic1_set_notifying (BluezGattCharacteristic1 *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "notifying", value, NULL);
}

/**
 * bluez_gatt_characteristic1_get_flags: (skip)
 * @object: A #BluezGattCharacteristic1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-GattCharacteristic1.Flags">"Flags"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use bluez_gatt_characteristic1_dup_flags() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *const *
bluez_gatt_characteristic1_get_flags (BluezGattCharacteristic1 *object)
{
  return BLUEZ_GATT_CHARACTERISTIC1_GET_IFACE (object)->get_flags (object);
}

/**
 * bluez_gatt_characteristic1_dup_flags: (skip)
 * @object: A #BluezGattCharacteristic1.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-GattCharacteristic1.Flags">"Flags"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_strfreev().
 */
gchar **
bluez_gatt_characteristic1_dup_flags (BluezGattCharacteristic1 *object)
{
  gchar **value;
  g_object_get (G_OBJECT (object), "flags", &value, NULL);
  return value;
}

/**
 * bluez_gatt_characteristic1_set_flags: (skip)
 * @object: A #BluezGattCharacteristic1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-GattCharacteristic1.Flags">"Flags"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_gatt_characteristic1_set_flags (BluezGattCharacteristic1 *object, const gchar *const *value)
{
  g_object_set (G_OBJECT (object), "flags", value, NULL);
}

/**
 * bluez_gatt_characteristic1_get_write_acquired: (skip)
 * @object: A #BluezGattCharacteristic1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-GattCharacteristic1.WriteAcquired">"WriteAcquired"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
bluez_gatt_characteristic1_get_write_acquired (BluezGattCharacteristic1 *object)
{
  return BLUEZ_GATT_CHARACTERISTIC1_GET_IFACE (object)->get_write_acquired (object);
}

/**
 * bluez_gatt_characteristic1_set_write_acquired: (skip)
 * @object: A #BluezGattCharacteristic1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-GattCharacteristic1.WriteAcquired">"WriteAcquired"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_gatt_characteristic1_set_write_acquired (BluezGattCharacteristic1 *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "write-acquired", value, NULL);
}

/**
 * bluez_gatt_characteristic1_get_notify_acquired: (skip)
 * @object: A #BluezGattCharacteristic1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-GattCharacteristic1.NotifyAcquired">"NotifyAcquired"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
bluez_gatt_characteristic1_get_notify_acquired (BluezGattCharacteristic1 *object)
{
  return BLUEZ_GATT_CHARACTERISTIC1_GET_IFACE (object)->get_notify_acquired (object);
}

/**
 * bluez_gatt_characteristic1_set_notify_acquired: (skip)
 * @object: A #BluezGattCharacteristic1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-GattCharacteristic1.NotifyAcquired">"NotifyAcquired"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_gatt_characteristic1_set_notify_acquired (BluezGattCharacteristic1 *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "notify-acquired", value, NULL);
}

/**
 * bluez_gatt_characteristic1_call_read_value:
 * @proxy: A #BluezGattCharacteristic1Proxy.
 * @arg_options: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-bluez-GattCharacteristic1.ReadValue">ReadValue()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call bluez_gatt_characteristic1_call_read_value_finish() to get the result of the operation.
 *
 * See bluez_gatt_characteristic1_call_read_value_sync() for the synchronous, blocking version of this method.
 */
void
bluez_gatt_characteristic1_call_read_value (
    BluezGattCharacteristic1 *proxy,
    GVariant *arg_options,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "ReadValue",
    g_variant_new ("(@a{sv})",
                   arg_options),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * bluez_gatt_characteristic1_call_read_value_finish:
 * @proxy: A #BluezGattCharacteristic1Proxy.
 * @out_value: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_gatt_characteristic1_call_read_value().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with bluez_gatt_characteristic1_call_read_value().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_gatt_characteristic1_call_read_value_finish (
    BluezGattCharacteristic1 *proxy,
    GVariant **out_value,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@ay)",
                 out_value);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_gatt_characteristic1_call_read_value_sync:
 * @proxy: A #BluezGattCharacteristic1Proxy.
 * @arg_options: Argument to pass with the method invocation.
 * @out_value: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-bluez-GattCharacteristic1.ReadValue">ReadValue()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See bluez_gatt_characteristic1_call_read_value() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_gatt_characteristic1_call_read_value_sync (
    BluezGattCharacteristic1 *proxy,
    GVariant *arg_options,
    GVariant **out_value,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "ReadValue",
    g_variant_new ("(@a{sv})",
                   arg_options),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@ay)",
                 out_value);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_gatt_characteristic1_call_write_value:
 * @proxy: A #BluezGattCharacteristic1Proxy.
 * @arg_value: Argument to pass with the method invocation.
 * @arg_options: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-bluez-GattCharacteristic1.WriteValue">WriteValue()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call bluez_gatt_characteristic1_call_write_value_finish() to get the result of the operation.
 *
 * See bluez_gatt_characteristic1_call_write_value_sync() for the synchronous, blocking version of this method.
 */
void
bluez_gatt_characteristic1_call_write_value (
    BluezGattCharacteristic1 *proxy,
    GVariant *arg_value,
    GVariant *arg_options,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "WriteValue",
    g_variant_new ("(@ay@a{sv})",
                   arg_value,
                   arg_options),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * bluez_gatt_characteristic1_call_write_value_finish:
 * @proxy: A #BluezGattCharacteristic1Proxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_gatt_characteristic1_call_write_value().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with bluez_gatt_characteristic1_call_write_value().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_gatt_characteristic1_call_write_value_finish (
    BluezGattCharacteristic1 *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_gatt_characteristic1_call_write_value_sync:
 * @proxy: A #BluezGattCharacteristic1Proxy.
 * @arg_value: Argument to pass with the method invocation.
 * @arg_options: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-bluez-GattCharacteristic1.WriteValue">WriteValue()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See bluez_gatt_characteristic1_call_write_value() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_gatt_characteristic1_call_write_value_sync (
    BluezGattCharacteristic1 *proxy,
    GVariant *arg_value,
    GVariant *arg_options,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "WriteValue",
    g_variant_new ("(@ay@a{sv})",
                   arg_value,
                   arg_options),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_gatt_characteristic1_call_acquire_write:
 * @proxy: A #BluezGattCharacteristic1Proxy.
 * @arg_options: Argument to pass with the method invocation.
 * @fd_list: (nullable): A #GUnixFDList or %NULL.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-bluez-GattCharacteristic1.AcquireWrite">AcquireWrite()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call bluez_gatt_characteristic1_call_acquire_write_finish() to get the result of the operation.
 *
 * See bluez_gatt_characteristic1_call_acquire_write_sync() for the synchronous, blocking version of this method.
 */
void
bluez_gatt_characteristic1_call_acquire_write (
    BluezGattCharacteristic1 *proxy,
    GVariant *arg_options,
    GUnixFDList *fd_list,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call_with_unix_fd_list (G_DBUS_PROXY (proxy),
    "AcquireWrite",
    g_variant_new ("(@a{sv})",
                   arg_options),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    fd_list,
    cancellable,
    callback,
    user_data);
}

/**
 * bluez_gatt_characteristic1_call_acquire_write_finish:
 * @proxy: A #BluezGattCharacteristic1Proxy.
 * @out_fd: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_mtu: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_fd_list: (out) (optional): Return location for a #GUnixFDList or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_gatt_characteristic1_call_acquire_write().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with bluez_gatt_characteristic1_call_acquire_write().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_gatt_characteristic1_call_acquire_write_finish (
    BluezGattCharacteristic1 *proxy,
    GVariant **out_fd,
    guint16 *out_mtu,
    GUnixFDList **out_fd_list,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_with_unix_fd_list_finish (G_DBUS_PROXY (proxy), out_fd_list, res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@hq)",
                 out_fd,
                 out_mtu);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_gatt_characteristic1_call_acquire_write_sync:
 * @proxy: A #BluezGattCharacteristic1Proxy.
 * @arg_options: Argument to pass with the method invocation.
 * @fd_list: (nullable): A #GUnixFDList or %NULL.
 * @out_fd: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_mtu: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_fd_list: (out): Return location for a #GUnixFDList or %NULL.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-bluez-GattCharacteristic1.AcquireWrite">AcquireWrite()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See bluez_gatt_characteristic1_call_acquire_write() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_gatt_characteristic1_call_acquire_write_sync (
    BluezGattCharacteristic1 *proxy,
    GVariant *arg_options,
    GUnixFDList  *fd_list,
    GVariant **out_fd,
    guint16 *out_mtu,
    GUnixFDList **out_fd_list,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_with_unix_fd_list_sync (G_DBUS_PROXY (proxy),
    "AcquireWrite",
    g_variant_new ("(@a{sv})",
                   arg_options),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    fd_list,
    out_fd_list,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@hq)",
                 out_fd,
                 out_mtu);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_gatt_characteristic1_call_acquire_notify:
 * @proxy: A #BluezGattCharacteristic1Proxy.
 * @arg_options: Argument to pass with the method invocation.
 * @fd_list: (nullable): A #GUnixFDList or %NULL.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-bluez-GattCharacteristic1.AcquireNotify">AcquireNotify()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call bluez_gatt_characteristic1_call_acquire_notify_finish() to get the result of the operation.
 *
 * See bluez_gatt_characteristic1_call_acquire_notify_sync() for the synchronous, blocking version of this method.
 */
void
bluez_gatt_characteristic1_call_acquire_notify (
    BluezGattCharacteristic1 *proxy,
    GVariant *arg_options,
    GUnixFDList *fd_list,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call_with_unix_fd_list (G_DBUS_PROXY (proxy),
    "AcquireNotify",
    g_variant_new ("(@a{sv})",
                   arg_options),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    fd_list,
    cancellable,
    callback,
    user_data);
}

/**
 * bluez_gatt_characteristic1_call_acquire_notify_finish:
 * @proxy: A #BluezGattCharacteristic1Proxy.
 * @out_fd: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_mtu: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_fd_list: (out) (optional): Return location for a #GUnixFDList or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_gatt_characteristic1_call_acquire_notify().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with bluez_gatt_characteristic1_call_acquire_notify().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_gatt_characteristic1_call_acquire_notify_finish (
    BluezGattCharacteristic1 *proxy,
    GVariant **out_fd,
    guint16 *out_mtu,
    GUnixFDList **out_fd_list,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_with_unix_fd_list_finish (G_DBUS_PROXY (proxy), out_fd_list, res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@hq)",
                 out_fd,
                 out_mtu);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_gatt_characteristic1_call_acquire_notify_sync:
 * @proxy: A #BluezGattCharacteristic1Proxy.
 * @arg_options: Argument to pass with the method invocation.
 * @fd_list: (nullable): A #GUnixFDList or %NULL.
 * @out_fd: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_mtu: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @out_fd_list: (out): Return location for a #GUnixFDList or %NULL.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-bluez-GattCharacteristic1.AcquireNotify">AcquireNotify()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See bluez_gatt_characteristic1_call_acquire_notify() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_gatt_characteristic1_call_acquire_notify_sync (
    BluezGattCharacteristic1 *proxy,
    GVariant *arg_options,
    GUnixFDList  *fd_list,
    GVariant **out_fd,
    guint16 *out_mtu,
    GUnixFDList **out_fd_list,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_with_unix_fd_list_sync (G_DBUS_PROXY (proxy),
    "AcquireNotify",
    g_variant_new ("(@a{sv})",
                   arg_options),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    fd_list,
    out_fd_list,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@hq)",
                 out_fd,
                 out_mtu);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_gatt_characteristic1_call_start_notify:
 * @proxy: A #BluezGattCharacteristic1Proxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-bluez-GattCharacteristic1.StartNotify">StartNotify()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call bluez_gatt_characteristic1_call_start_notify_finish() to get the result of the operation.
 *
 * See bluez_gatt_characteristic1_call_start_notify_sync() for the synchronous, blocking version of this method.
 */
void
bluez_gatt_characteristic1_call_start_notify (
    BluezGattCharacteristic1 *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "StartNotify",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * bluez_gatt_characteristic1_call_start_notify_finish:
 * @proxy: A #BluezGattCharacteristic1Proxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_gatt_characteristic1_call_start_notify().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with bluez_gatt_characteristic1_call_start_notify().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_gatt_characteristic1_call_start_notify_finish (
    BluezGattCharacteristic1 *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_gatt_characteristic1_call_start_notify_sync:
 * @proxy: A #BluezGattCharacteristic1Proxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-bluez-GattCharacteristic1.StartNotify">StartNotify()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See bluez_gatt_characteristic1_call_start_notify() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_gatt_characteristic1_call_start_notify_sync (
    BluezGattCharacteristic1 *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "StartNotify",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_gatt_characteristic1_call_stop_notify:
 * @proxy: A #BluezGattCharacteristic1Proxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-bluez-GattCharacteristic1.StopNotify">StopNotify()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call bluez_gatt_characteristic1_call_stop_notify_finish() to get the result of the operation.
 *
 * See bluez_gatt_characteristic1_call_stop_notify_sync() for the synchronous, blocking version of this method.
 */
void
bluez_gatt_characteristic1_call_stop_notify (
    BluezGattCharacteristic1 *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "StopNotify",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * bluez_gatt_characteristic1_call_stop_notify_finish:
 * @proxy: A #BluezGattCharacteristic1Proxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_gatt_characteristic1_call_stop_notify().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with bluez_gatt_characteristic1_call_stop_notify().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_gatt_characteristic1_call_stop_notify_finish (
    BluezGattCharacteristic1 *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_gatt_characteristic1_call_stop_notify_sync:
 * @proxy: A #BluezGattCharacteristic1Proxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-bluez-GattCharacteristic1.StopNotify">StopNotify()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See bluez_gatt_characteristic1_call_stop_notify() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_gatt_characteristic1_call_stop_notify_sync (
    BluezGattCharacteristic1 *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "StopNotify",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_gatt_characteristic1_call_confirm:
 * @proxy: A #BluezGattCharacteristic1Proxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-bluez-GattCharacteristic1.Confirm">Confirm()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call bluez_gatt_characteristic1_call_confirm_finish() to get the result of the operation.
 *
 * See bluez_gatt_characteristic1_call_confirm_sync() for the synchronous, blocking version of this method.
 */
void
bluez_gatt_characteristic1_call_confirm (
    BluezGattCharacteristic1 *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Confirm",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * bluez_gatt_characteristic1_call_confirm_finish:
 * @proxy: A #BluezGattCharacteristic1Proxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_gatt_characteristic1_call_confirm().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with bluez_gatt_characteristic1_call_confirm().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_gatt_characteristic1_call_confirm_finish (
    BluezGattCharacteristic1 *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_gatt_characteristic1_call_confirm_sync:
 * @proxy: A #BluezGattCharacteristic1Proxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-bluez-GattCharacteristic1.Confirm">Confirm()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See bluez_gatt_characteristic1_call_confirm() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_gatt_characteristic1_call_confirm_sync (
    BluezGattCharacteristic1 *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Confirm",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_gatt_characteristic1_complete_read_value:
 * @object: A #BluezGattCharacteristic1.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @value: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-bluez-GattCharacteristic1.ReadValue">ReadValue()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
bluez_gatt_characteristic1_complete_read_value (
    BluezGattCharacteristic1 *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation,
    GVariant *value)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@ay)",
                   value));
}

/**
 * bluez_gatt_characteristic1_complete_write_value:
 * @object: A #BluezGattCharacteristic1.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-bluez-GattCharacteristic1.WriteValue">WriteValue()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
bluez_gatt_characteristic1_complete_write_value (
    BluezGattCharacteristic1 *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * bluez_gatt_characteristic1_complete_acquire_write:
 * @object: A #BluezGattCharacteristic1.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @fd_list: (nullable): A #GUnixFDList or %NULL.
 * @fd: Parameter to return.
 * @mtu: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-bluez-GattCharacteristic1.AcquireWrite">AcquireWrite()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
bluez_gatt_characteristic1_complete_acquire_write (
    BluezGattCharacteristic1 *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation,
    GUnixFDList *fd_list,
    GVariant *fd,
    guint16 mtu)
{
  g_dbus_method_invocation_return_value_with_unix_fd_list (invocation,
    g_variant_new ("(@hq)",
                   fd,
                   mtu),
    fd_list);
}

/**
 * bluez_gatt_characteristic1_complete_acquire_notify:
 * @object: A #BluezGattCharacteristic1.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @fd_list: (nullable): A #GUnixFDList or %NULL.
 * @fd: Parameter to return.
 * @mtu: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-bluez-GattCharacteristic1.AcquireNotify">AcquireNotify()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
bluez_gatt_characteristic1_complete_acquire_notify (
    BluezGattCharacteristic1 *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation,
    GUnixFDList *fd_list,
    GVariant *fd,
    guint16 mtu)
{
  g_dbus_method_invocation_return_value_with_unix_fd_list (invocation,
    g_variant_new ("(@hq)",
                   fd,
                   mtu),
    fd_list);
}

/**
 * bluez_gatt_characteristic1_complete_start_notify:
 * @object: A #BluezGattCharacteristic1.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-bluez-GattCharacteristic1.StartNotify">StartNotify()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
bluez_gatt_characteristic1_complete_start_notify (
    BluezGattCharacteristic1 *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * bluez_gatt_characteristic1_complete_stop_notify:
 * @object: A #BluezGattCharacteristic1.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-bluez-GattCharacteristic1.StopNotify">StopNotify()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
bluez_gatt_characteristic1_complete_stop_notify (
    BluezGattCharacteristic1 *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * bluez_gatt_characteristic1_complete_confirm:
 * @object: A #BluezGattCharacteristic1.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-bluez-GattCharacteristic1.Confirm">Confirm()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
bluez_gatt_characteristic1_complete_confirm (
    BluezGattCharacteristic1 *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * BluezGattCharacteristic1Proxy:
 *
 * The #BluezGattCharacteristic1Proxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * BluezGattCharacteristic1ProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #BluezGattCharacteristic1Proxy.
 */

struct _BluezGattCharacteristic1ProxyPrivate
{
  GData *qdata;
};

static void bluez_gatt_characteristic1_proxy_iface_init (BluezGattCharacteristic1Iface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (BluezGattCharacteristic1Proxy, bluez_gatt_characteristic1_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (BluezGattCharacteristic1Proxy)
                         G_IMPLEMENT_INTERFACE (BLUEZ_TYPE_GATT_CHARACTERISTIC1, bluez_gatt_characteristic1_proxy_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (BluezGattCharacteristic1Proxy, bluez_gatt_characteristic1_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (BLUEZ_TYPE_GATT_CHARACTERISTIC1, bluez_gatt_characteristic1_proxy_iface_init))

#endif
static void
bluez_gatt_characteristic1_proxy_finalize (GObject *object)
{
  BluezGattCharacteristic1Proxy *proxy = BLUEZ_GATT_CHARACTERISTIC1_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (bluez_gatt_characteristic1_proxy_parent_class)->finalize (object);
}

static void
bluez_gatt_characteristic1_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 7);
  info = (const _ExtendedGDBusPropertyInfo *) _bluez_gatt_characteristic1_property_info_pointers[prop_id - 1];
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
  if (info->use_gvariant)
    {
      g_value_set_variant (value, variant);
    }
  else
    {
      if (variant != NULL)
        g_dbus_gvariant_to_gvalue (variant, value);
    }
  if (variant != NULL)
    g_variant_unref (variant);
}

static void
bluez_gatt_characteristic1_proxy_set_property_cb (GDBusProxy *proxy,
  GAsyncResult *res,
  gpointer      user_data)
{
  const _ExtendedGDBusPropertyInfo *info = user_data;
  GError *error;
  GVariant *_ret;
  error = NULL;
  _ret = g_dbus_proxy_call_finish (proxy, res, &error);
  if (!_ret)
    {
      g_warning ("Error setting property '%s' on interface org.bluez.GattCharacteristic1: %s (%s, %d)",
                 info->parent_struct.name, 
                 error->message, g_quark_to_string (error->domain), error->code);
      g_error_free (error);
    }
  else
    {
      g_variant_unref (_ret);
    }
}

static void
bluez_gatt_characteristic1_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 7);
  info = (const _ExtendedGDBusPropertyInfo *) _bluez_gatt_characteristic1_property_info_pointers[prop_id - 1];
  variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
  g_dbus_proxy_call (G_DBUS_PROXY (object),
    "org.freedesktop.DBus.Properties.Set",
    g_variant_new ("(ssv)", "org.bluez.GattCharacteristic1", info->parent_struct.name, variant),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    NULL, (GAsyncReadyCallback) bluez_gatt_characteristic1_proxy_set_property_cb, (GDBusPropertyInfo *) &info->parent_struct);
  g_variant_unref (variant);
}

static void
bluez_gatt_characteristic1_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  gsize n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_bluez_gatt_characteristic1_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], BLUEZ_TYPE_GATT_CHARACTERISTIC1);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, BLUEZ_TYPE_GATT_CHARACTERISTIC1);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
bluez_gatt_characteristic1_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  BluezGattCharacteristic1Proxy *proxy = BLUEZ_GATT_CHARACTERISTIC1_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_gatt_characteristic1_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_gatt_characteristic1_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static const gchar *
bluez_gatt_characteristic1_proxy_get_uuid (BluezGattCharacteristic1 *object)
{
  BluezGattCharacteristic1Proxy *proxy = BLUEZ_GATT_CHARACTERISTIC1_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "UUID");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
bluez_gatt_characteristic1_proxy_get_service (BluezGattCharacteristic1 *object)
{
  BluezGattCharacteristic1Proxy *proxy = BLUEZ_GATT_CHARACTERISTIC1_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Service");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static GVariant *
bluez_gatt_characteristic1_proxy_get_value (BluezGattCharacteristic1 *object)
{
  BluezGattCharacteristic1Proxy *proxy = BLUEZ_GATT_CHARACTERISTIC1_PROXY (object);
  GVariant *variant;
  GVariant *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Value");
  value = variant;
  if (variant != NULL)
    g_variant_unref (variant);
  return value;
}

static gboolean 
bluez_gatt_characteristic1_proxy_get_notifying (BluezGattCharacteristic1 *object)
{
  BluezGattCharacteristic1Proxy *proxy = BLUEZ_GATT_CHARACTERISTIC1_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Notifying");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *const *
bluez_gatt_characteristic1_proxy_get_flags (BluezGattCharacteristic1 *object)
{
  BluezGattCharacteristic1Proxy *proxy = BLUEZ_GATT_CHARACTERISTIC1_PROXY (object);
  GVariant *variant;
  const gchar *const *value = NULL;
  value = g_datalist_get_data (&proxy->priv->qdata, "Flags");
  if (value != NULL)
    return value;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Flags");
  if (variant != NULL)
    {
      value = g_variant_get_strv (variant, NULL);
      g_datalist_set_data_full (&proxy->priv->qdata, "Flags", (gpointer) value, g_free);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
bluez_gatt_characteristic1_proxy_get_write_acquired (BluezGattCharacteristic1 *object)
{
  BluezGattCharacteristic1Proxy *proxy = BLUEZ_GATT_CHARACTERISTIC1_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "WriteAcquired");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
bluez_gatt_characteristic1_proxy_get_notify_acquired (BluezGattCharacteristic1 *object)
{
  BluezGattCharacteristic1Proxy *proxy = BLUEZ_GATT_CHARACTERISTIC1_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "NotifyAcquired");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static void
bluez_gatt_characteristic1_proxy_init (BluezGattCharacteristic1Proxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = bluez_gatt_characteristic1_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, BLUEZ_TYPE_GATT_CHARACTERISTIC1_PROXY, BluezGattCharacteristic1ProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), bluez_gatt_characteristic1_interface_info ());
}

static void
bluez_gatt_characteristic1_proxy_class_init (BluezGattCharacteristic1ProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = bluez_gatt_characteristic1_proxy_finalize;
  gobject_class->get_property = bluez_gatt_characteristic1_proxy_get_property;
  gobject_class->set_property = bluez_gatt_characteristic1_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = bluez_gatt_characteristic1_proxy_g_signal;
  proxy_class->g_properties_changed = bluez_gatt_characteristic1_proxy_g_properties_changed;

  bluez_gatt_characteristic1_override_properties (gobject_class, 1);

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (BluezGattCharacteristic1ProxyPrivate));
#endif
}

static void
bluez_gatt_characteristic1_proxy_iface_init (BluezGattCharacteristic1Iface *iface)
{
  iface->get_uuid = bluez_gatt_characteristic1_proxy_get_uuid;
  iface->get_service = bluez_gatt_characteristic1_proxy_get_service;
  iface->get_value = bluez_gatt_characteristic1_proxy_get_value;
  iface->get_notifying = bluez_gatt_characteristic1_proxy_get_notifying;
  iface->get_flags = bluez_gatt_characteristic1_proxy_get_flags;
  iface->get_write_acquired = bluez_gatt_characteristic1_proxy_get_write_acquired;
  iface->get_notify_acquired = bluez_gatt_characteristic1_proxy_get_notify_acquired;
}

/**
 * bluez_gatt_characteristic1_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-bluez-GattCharacteristic1.top_of_page">org.bluez.GattCharacteristic1</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call bluez_gatt_characteristic1_proxy_new_finish() to get the result of the operation.
 *
 * See bluez_gatt_characteristic1_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
bluez_gatt_characteristic1_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (BLUEZ_TYPE_GATT_CHARACTERISTIC1_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.bluez.GattCharacteristic1", NULL);
}

/**
 * bluez_gatt_characteristic1_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_gatt_characteristic1_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with bluez_gatt_characteristic1_proxy_new().
 *
 * Returns: (transfer full) (type BluezGattCharacteristic1Proxy): The constructed proxy object or %NULL if @error is set.
 */
BluezGattCharacteristic1 *
bluez_gatt_characteristic1_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return BLUEZ_GATT_CHARACTERISTIC1 (ret);
  else
    return NULL;
}

/**
 * bluez_gatt_characteristic1_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-bluez-GattCharacteristic1.top_of_page">org.bluez.GattCharacteristic1</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See bluez_gatt_characteristic1_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type BluezGattCharacteristic1Proxy): The constructed proxy object or %NULL if @error is set.
 */
BluezGattCharacteristic1 *
bluez_gatt_characteristic1_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (BLUEZ_TYPE_GATT_CHARACTERISTIC1_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.bluez.GattCharacteristic1", NULL);
  if (ret != NULL)
    return BLUEZ_GATT_CHARACTERISTIC1 (ret);
  else
    return NULL;
}


/**
 * bluez_gatt_characteristic1_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like bluez_gatt_characteristic1_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call bluez_gatt_characteristic1_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See bluez_gatt_characteristic1_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
bluez_gatt_characteristic1_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (BLUEZ_TYPE_GATT_CHARACTERISTIC1_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.bluez.GattCharacteristic1", NULL);
}

/**
 * bluez_gatt_characteristic1_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_gatt_characteristic1_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with bluez_gatt_characteristic1_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type BluezGattCharacteristic1Proxy): The constructed proxy object or %NULL if @error is set.
 */
BluezGattCharacteristic1 *
bluez_gatt_characteristic1_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return BLUEZ_GATT_CHARACTERISTIC1 (ret);
  else
    return NULL;
}

/**
 * bluez_gatt_characteristic1_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like bluez_gatt_characteristic1_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See bluez_gatt_characteristic1_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type BluezGattCharacteristic1Proxy): The constructed proxy object or %NULL if @error is set.
 */
BluezGattCharacteristic1 *
bluez_gatt_characteristic1_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (BLUEZ_TYPE_GATT_CHARACTERISTIC1_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.bluez.GattCharacteristic1", NULL);
  if (ret != NULL)
    return BLUEZ_GATT_CHARACTERISTIC1 (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * BluezGattCharacteristic1Skeleton:
 *
 * The #BluezGattCharacteristic1Skeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * BluezGattCharacteristic1SkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #BluezGattCharacteristic1Skeleton.
 */

struct _BluezGattCharacteristic1SkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_bluez_gatt_characteristic1_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  BluezGattCharacteristic1Skeleton *skeleton = BLUEZ_GATT_CHARACTERISTIC1_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  guint num_extra;
  gsize n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], BLUEZ_TYPE_GATT_CHARACTERISTIC1);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, BLUEZ_TYPE_GATT_CHARACTERISTIC1);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_bluez_gatt_characteristic1_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  BluezGattCharacteristic1Skeleton *skeleton = BLUEZ_GATT_CHARACTERISTIC1_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_gatt_characteristic1_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_bluez_gatt_characteristic1_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  BluezGattCharacteristic1Skeleton *skeleton = BLUEZ_GATT_CHARACTERISTIC1_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_gatt_characteristic1_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _bluez_gatt_characteristic1_skeleton_vtable =
{
  _bluez_gatt_characteristic1_skeleton_handle_method_call,
  _bluez_gatt_characteristic1_skeleton_handle_get_property,
  _bluez_gatt_characteristic1_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
bluez_gatt_characteristic1_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return bluez_gatt_characteristic1_interface_info ();
}

static GDBusInterfaceVTable *
bluez_gatt_characteristic1_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_bluez_gatt_characteristic1_skeleton_vtable;
}

static GVariant *
bluez_gatt_characteristic1_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  BluezGattCharacteristic1Skeleton *skeleton = BLUEZ_GATT_CHARACTERISTIC1_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_bluez_gatt_characteristic1_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _bluez_gatt_characteristic1_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _bluez_gatt_characteristic1_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _bluez_gatt_characteristic1_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.bluez.GattCharacteristic1", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static gboolean _bluez_gatt_characteristic1_emit_changed (gpointer user_data);

static void
bluez_gatt_characteristic1_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
  BluezGattCharacteristic1Skeleton *skeleton = BLUEZ_GATT_CHARACTERISTIC1_SKELETON (_skeleton);
  gboolean emit_changed = FALSE;

  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    {
      g_source_destroy (skeleton->priv->changed_properties_idle_source);
      skeleton->priv->changed_properties_idle_source = NULL;
      emit_changed = TRUE;
    }
  g_mutex_unlock (&skeleton->priv->lock);

  if (emit_changed)
    _bluez_gatt_characteristic1_emit_changed (skeleton);
}

static void bluez_gatt_characteristic1_skeleton_iface_init (BluezGattCharacteristic1Iface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (BluezGattCharacteristic1Skeleton, bluez_gatt_characteristic1_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (BluezGattCharacteristic1Skeleton)
                         G_IMPLEMENT_INTERFACE (BLUEZ_TYPE_GATT_CHARACTERISTIC1, bluez_gatt_characteristic1_skeleton_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (BluezGattCharacteristic1Skeleton, bluez_gatt_characteristic1_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (BLUEZ_TYPE_GATT_CHARACTERISTIC1, bluez_gatt_characteristic1_skeleton_iface_init))

#endif
static void
bluez_gatt_characteristic1_skeleton_finalize (GObject *object)
{
  BluezGattCharacteristic1Skeleton *skeleton = BLUEZ_GATT_CHARACTERISTIC1_SKELETON (object);
  guint n;
  for (n = 0; n < 7; n++)
    g_value_unset (&skeleton->priv->properties[n]);
  g_free (skeleton->priv->properties);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (bluez_gatt_characteristic1_skeleton_parent_class)->finalize (object);
}

static void
bluez_gatt_characteristic1_skeleton_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  BluezGattCharacteristic1Skeleton *skeleton = BLUEZ_GATT_CHARACTERISTIC1_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 7);
  g_mutex_lock (&skeleton->priv->lock);
  g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
  g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
_bluez_gatt_characteristic1_emit_changed (gpointer user_data)
{
  BluezGattCharacteristic1Skeleton *skeleton = BLUEZ_GATT_CHARACTERISTIC1_SKELETON (user_data);
  GList *l;
  GVariantBuilder builder;
  GVariantBuilder invalidated_builder;
  guint num_changes;

  g_mutex_lock (&skeleton->priv->lock);
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
  for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
    {
      ChangedProperty *cp = l->data;
      GVariant *variant;
      const GValue *cur_value;

      cur_value = &skeleton->priv->properties[cp->prop_id - 1];
      if (!_g_value_equal (cur_value, &cp->orig_value))
        {
          variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
          g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
          g_variant_unref (variant);
          num_changes++;
        }
    }
  if (num_changes > 0)
    {
      GList *connections, *ll;
      GVariant *signal_variant;
      signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "org.bluez.GattCharacteristic1",
                                           &builder, &invalidated_builder));
      connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
      for (ll = connections; ll != NULL; ll = ll->next)
        {
          GDBusConnection *connection = ll->data;

          g_dbus_connection_emit_signal (connection,
                                         NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
                                         "org.freedesktop.DBus.Properties",
                                         "PropertiesChanged",
                                         signal_variant,
                                         NULL);
        }
      g_variant_unref (signal_variant);
      g_list_free_full (connections, g_object_unref);
    }
  else
    {
      g_variant_builder_clear (&builder);
      g_variant_builder_clear (&invalidated_builder);
    }
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  skeleton->priv->changed_properties = NULL;
  skeleton->priv->changed_properties_idle_source = NULL;
  g_mutex_unlock (&skeleton->priv->lock);
  return FALSE;
}

static void
_bluez_gatt_characteristic1_schedule_emit_changed (BluezGattCharacteristic1Skeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
  ChangedProperty *cp;
  GList *l;
  cp = NULL;
  for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
    {
      ChangedProperty *i_cp = l->data;
      if (i_cp->info == info)
        {
          cp = i_cp;
          break;
        }
    }
  if (cp == NULL)
    {
      cp = g_new0 (ChangedProperty, 1);
      cp->prop_id = prop_id;
      cp->info = info;
      skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
      g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
      g_value_copy (orig_value, &cp->orig_value);
    }
}

static void
bluez_gatt_characteristic1_skeleton_notify (GObject      *object,
  GParamSpec *pspec G_GNUC_UNUSED)
{
  BluezGattCharacteristic1Skeleton *skeleton = BLUEZ_GATT_CHARACTERISTIC1_SKELETON (object);
  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties != NULL &&
      skeleton->priv->changed_properties_idle_source == NULL)
    {
      skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
      g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
      g_source_set_callback (skeleton->priv->changed_properties_idle_source, _bluez_gatt_characteristic1_emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
      g_source_set_name (skeleton->priv->changed_properties_idle_source, "[generated] _bluez_gatt_characteristic1_emit_changed");
      g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
      g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
  g_mutex_unlock (&skeleton->priv->lock);
}

static void
bluez_gatt_characteristic1_skeleton_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  const _ExtendedGDBusPropertyInfo *info;
  BluezGattCharacteristic1Skeleton *skeleton = BLUEZ_GATT_CHARACTERISTIC1_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 7);
  info = (const _ExtendedGDBusPropertyInfo *) _bluez_gatt_characteristic1_property_info_pointers[prop_id - 1];
  g_mutex_lock (&skeleton->priv->lock);
  g_object_freeze_notify (object);
  if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
      if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL &&
          info->emits_changed_signal)
        _bluez_gatt_characteristic1_schedule_emit_changed (skeleton, info, prop_id, &skeleton->priv->properties[prop_id - 1]);
      g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
      g_object_notify_by_pspec (object, pspec);
    }
  g_mutex_unlock (&skeleton->priv->lock);
  g_object_thaw_notify (object);
}

static void
bluez_gatt_characteristic1_skeleton_init (BluezGattCharacteristic1Skeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = bluez_gatt_characteristic1_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, BLUEZ_TYPE_GATT_CHARACTERISTIC1_SKELETON, BluezGattCharacteristic1SkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
  skeleton->priv->properties = g_new0 (GValue, 7);
  g_value_init (&skeleton->priv->properties[0], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[1], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[2], G_TYPE_VARIANT);
  g_value_init (&skeleton->priv->properties[3], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[4], G_TYPE_STRV);
  g_value_init (&skeleton->priv->properties[5], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[6], G_TYPE_BOOLEAN);
}

static const gchar *
bluez_gatt_characteristic1_skeleton_get_uuid (BluezGattCharacteristic1 *object)
{
  BluezGattCharacteristic1Skeleton *skeleton = BLUEZ_GATT_CHARACTERISTIC1_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[0]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
bluez_gatt_characteristic1_skeleton_get_service (BluezGattCharacteristic1 *object)
{
  BluezGattCharacteristic1Skeleton *skeleton = BLUEZ_GATT_CHARACTERISTIC1_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[1]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static GVariant *
bluez_gatt_characteristic1_skeleton_get_value (BluezGattCharacteristic1 *object)
{
  BluezGattCharacteristic1Skeleton *skeleton = BLUEZ_GATT_CHARACTERISTIC1_SKELETON (object);
  GVariant *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_variant (&(skeleton->priv->properties[2]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
bluez_gatt_characteristic1_skeleton_get_notifying (BluezGattCharacteristic1 *object)
{
  BluezGattCharacteristic1Skeleton *skeleton = BLUEZ_GATT_CHARACTERISTIC1_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[3]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *const *
bluez_gatt_characteristic1_skeleton_get_flags (BluezGattCharacteristic1 *object)
{
  BluezGattCharacteristic1Skeleton *skeleton = BLUEZ_GATT_CHARACTERISTIC1_SKELETON (object);
  const gchar *const *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boxed (&(skeleton->priv->properties[4]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
bluez_gatt_characteristic1_skeleton_get_write_acquired (BluezGattCharacteristic1 *object)
{
  BluezGattCharacteristic1Skeleton *skeleton = BLUEZ_GATT_CHARACTERISTIC1_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[5]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
bluez_gatt_characteristic1_skeleton_get_notify_acquired (BluezGattCharacteristic1 *object)
{
  BluezGattCharacteristic1Skeleton *skeleton = BLUEZ_GATT_CHARACTERISTIC1_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[6]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static void
bluez_gatt_characteristic1_skeleton_class_init (BluezGattCharacteristic1SkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = bluez_gatt_characteristic1_skeleton_finalize;
  gobject_class->get_property = bluez_gatt_characteristic1_skeleton_get_property;
  gobject_class->set_property = bluez_gatt_characteristic1_skeleton_set_property;
  gobject_class->notify       = bluez_gatt_characteristic1_skeleton_notify;


  bluez_gatt_characteristic1_override_properties (gobject_class, 1);

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = bluez_gatt_characteristic1_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = bluez_gatt_characteristic1_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = bluez_gatt_characteristic1_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = bluez_gatt_characteristic1_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (BluezGattCharacteristic1SkeletonPrivate));
#endif
}

static void
bluez_gatt_characteristic1_skeleton_iface_init (BluezGattCharacteristic1Iface *iface)
{
  iface->get_uuid = bluez_gatt_characteristic1_skeleton_get_uuid;
  iface->get_service = bluez_gatt_characteristic1_skeleton_get_service;
  iface->get_value = bluez_gatt_characteristic1_skeleton_get_value;
  iface->get_notifying = bluez_gatt_characteristic1_skeleton_get_notifying;
  iface->get_flags = bluez_gatt_characteristic1_skeleton_get_flags;
  iface->get_write_acquired = bluez_gatt_characteristic1_skeleton_get_write_acquired;
  iface->get_notify_acquired = bluez_gatt_characteristic1_skeleton_get_notify_acquired;
}

/**
 * bluez_gatt_characteristic1_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-bluez-GattCharacteristic1.top_of_page">org.bluez.GattCharacteristic1</link>.
 *
 * Returns: (transfer full) (type BluezGattCharacteristic1Skeleton): The skeleton object.
 */
BluezGattCharacteristic1 *
bluez_gatt_characteristic1_skeleton_new (void)
{
  return BLUEZ_GATT_CHARACTERISTIC1 (g_object_new (BLUEZ_TYPE_GATT_CHARACTERISTIC1_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface org.bluez.GattDescriptor1
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:BluezGattDescriptor1
 * @title: BluezGattDescriptor1
 * @short_description: Generated C code for the org.bluez.GattDescriptor1 D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-bluez-GattDescriptor1.top_of_page">org.bluez.GattDescriptor1</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.bluez.GattDescriptor1 ---- */

static const _ExtendedGDBusArgInfo _bluez_gatt_descriptor1_method_info_read_value_IN_ARG_options =
{
  {
    -1,
    (gchar *) "options",
    (gchar *) "a{sv}",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _bluez_gatt_descriptor1_method_info_read_value_IN_ARG_pointers[] =
{
  &_bluez_gatt_descriptor1_method_info_read_value_IN_ARG_options.parent_struct,
  NULL
};

static const _ExtendedGDBusArgInfo _bluez_gatt_descriptor1_method_info_read_value_OUT_ARG_value =
{
  {
    -1,
    (gchar *) "value",
    (gchar *) "ay",
    NULL
  },
  TRUE
};

static const GDBusArgInfo * const _bluez_gatt_descriptor1_method_info_read_value_OUT_ARG_pointers[] =
{
  &_bluez_gatt_descriptor1_method_info_read_value_OUT_ARG_value.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _bluez_gatt_descriptor1_method_info_read_value =
{
  {
    -1,
    (gchar *) "ReadValue",
    (GDBusArgInfo **) &_bluez_gatt_descriptor1_method_info_read_value_IN_ARG_pointers,
    (GDBusArgInfo **) &_bluez_gatt_descriptor1_method_info_read_value_OUT_ARG_pointers,
    NULL
  },
  "handle-read-value",
  FALSE
};

static const _ExtendedGDBusArgInfo _bluez_gatt_descriptor1_method_info_write_value_IN_ARG_value =
{
  {
    -1,
    (gchar *) "value",
    (gchar *) "ay",
    NULL
  },
  TRUE
};

static const _ExtendedGDBusArgInfo _bluez_gatt_descriptor1_method_info_write_value_IN_ARG_options =
{
  {
    -1,
    (gchar *) "options",
    (gchar *) "a{sv}",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _bluez_gatt_descriptor1_method_info_write_value_IN_ARG_pointers[] =
{
  &_bluez_gatt_descriptor1_method_info_write_value_IN_ARG_value.parent_struct,
  &_bluez_gatt_descriptor1_method_info_write_value_IN_ARG_options.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _bluez_gatt_descriptor1_method_info_write_value =
{
  {
    -1,
    (gchar *) "WriteValue",
    (GDBusArgInfo **) &_bluez_gatt_descriptor1_method_info_write_value_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-write-value",
  FALSE
};

static const GDBusMethodInfo * const _bluez_gatt_descriptor1_method_info_pointers[] =
{
  &_bluez_gatt_descriptor1_method_info_read_value.parent_struct,
  &_bluez_gatt_descriptor1_method_info_write_value.parent_struct,
  NULL
};

static const _ExtendedGDBusPropertyInfo _bluez_gatt_descriptor1_property_info_uuid =
{
  {
    -1,
    (gchar *) "UUID",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "uuid",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _bluez_gatt_descriptor1_property_info_characteristic =
{
  {
    -1,
    (gchar *) "Characteristic",
    (gchar *) "o",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "characteristic",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _bluez_gatt_descriptor1_property_info_value =
{
  {
    -1,
    (gchar *) "Value",
    (gchar *) "ay",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "value",
  TRUE,
  TRUE
};

static const GDBusPropertyInfo * const _bluez_gatt_descriptor1_property_info_pointers[] =
{
  &_bluez_gatt_descriptor1_property_info_uuid.parent_struct,
  &_bluez_gatt_descriptor1_property_info_characteristic.parent_struct,
  &_bluez_gatt_descriptor1_property_info_value.parent_struct,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _bluez_gatt_descriptor1_interface_info =
{
  {
    -1,
    (gchar *) "org.bluez.GattDescriptor1",
    (GDBusMethodInfo **) &_bluez_gatt_descriptor1_method_info_pointers,
    NULL,
    (GDBusPropertyInfo **) &_bluez_gatt_descriptor1_property_info_pointers,
    NULL
  },
  "gatt-descriptor1",
};


/**
 * bluez_gatt_descriptor1_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-bluez-GattDescriptor1.top_of_page">org.bluez.GattDescriptor1</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
bluez_gatt_descriptor1_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_bluez_gatt_descriptor1_interface_info.parent_struct;
}

/**
 * bluez_gatt_descriptor1_override_properties:
 * @klass: The class structure for a #GObject derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #BluezGattDescriptor1 interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
bluez_gatt_descriptor1_override_properties (GObjectClass *klass, guint property_id_begin)
{
  g_object_class_override_property (klass, property_id_begin++, "uuid");
  g_object_class_override_property (klass, property_id_begin++, "characteristic");
  g_object_class_override_property (klass, property_id_begin++, "value");
  return property_id_begin - 1;
}



/**
 * BluezGattDescriptor1:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-bluez-GattDescriptor1.top_of_page">org.bluez.GattDescriptor1</link>.
 */

/**
 * BluezGattDescriptor1Iface:
 * @parent_iface: The parent interface.
 * @handle_read_value: Handler for the #BluezGattDescriptor1::handle-read-value signal.
 * @handle_write_value: Handler for the #BluezGattDescriptor1::handle-write-value signal.
 * @get_characteristic: Getter for the #BluezGattDescriptor1:characteristic property.
 * @get_uuid: Getter for the #BluezGattDescriptor1:uuid property.
 * @get_value: Getter for the #BluezGattDescriptor1:value property.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-bluez-GattDescriptor1.top_of_page">org.bluez.GattDescriptor1</link>.
 */

typedef BluezGattDescriptor1Iface BluezGattDescriptor1Interface;
G_DEFINE_INTERFACE (BluezGattDescriptor1, bluez_gatt_descriptor1, G_TYPE_OBJECT)

static void
bluez_gatt_descriptor1_default_init (BluezGattDescriptor1Iface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * BluezGattDescriptor1::handle-read-value:
   * @object: A #BluezGattDescriptor1.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_options: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-bluez-GattDescriptor1.ReadValue">ReadValue()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call bluez_gatt_descriptor1_complete_read_value() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-read-value",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (BluezGattDescriptor1Iface, handle_read_value),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_VARIANT);

  /**
   * BluezGattDescriptor1::handle-write-value:
   * @object: A #BluezGattDescriptor1.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_value: Argument passed by remote caller.
   * @arg_options: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-bluez-GattDescriptor1.WriteValue">WriteValue()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call bluez_gatt_descriptor1_complete_write_value() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-write-value",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (BluezGattDescriptor1Iface, handle_write_value),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_VARIANT, G_TYPE_VARIANT);

  /* GObject properties for D-Bus properties: */
  /**
   * BluezGattDescriptor1:uuid:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-GattDescriptor1.UUID">"UUID"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("uuid", "UUID", "UUID", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * BluezGattDescriptor1:characteristic:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-GattDescriptor1.Characteristic">"Characteristic"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("characteristic", "Characteristic", "Characteristic", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * BluezGattDescriptor1:value:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-GattDescriptor1.Value">"Value"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_variant ("value", "Value", "Value", G_VARIANT_TYPE ("ay"), NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

/**
 * bluez_gatt_descriptor1_get_uuid: (skip)
 * @object: A #BluezGattDescriptor1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-GattDescriptor1.UUID">"UUID"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use bluez_gatt_descriptor1_dup_uuid() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
bluez_gatt_descriptor1_get_uuid (BluezGattDescriptor1 *object)
{
  return BLUEZ_GATT_DESCRIPTOR1_GET_IFACE (object)->get_uuid (object);
}

/**
 * bluez_gatt_descriptor1_dup_uuid: (skip)
 * @object: A #BluezGattDescriptor1.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-GattDescriptor1.UUID">"UUID"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
bluez_gatt_descriptor1_dup_uuid (BluezGattDescriptor1 *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "uuid", &value, NULL);
  return value;
}

/**
 * bluez_gatt_descriptor1_set_uuid: (skip)
 * @object: A #BluezGattDescriptor1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-GattDescriptor1.UUID">"UUID"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_gatt_descriptor1_set_uuid (BluezGattDescriptor1 *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "uuid", value, NULL);
}

/**
 * bluez_gatt_descriptor1_get_characteristic: (skip)
 * @object: A #BluezGattDescriptor1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-GattDescriptor1.Characteristic">"Characteristic"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use bluez_gatt_descriptor1_dup_characteristic() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
bluez_gatt_descriptor1_get_characteristic (BluezGattDescriptor1 *object)
{
  return BLUEZ_GATT_DESCRIPTOR1_GET_IFACE (object)->get_characteristic (object);
}

/**
 * bluez_gatt_descriptor1_dup_characteristic: (skip)
 * @object: A #BluezGattDescriptor1.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-GattDescriptor1.Characteristic">"Characteristic"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
bluez_gatt_descriptor1_dup_characteristic (BluezGattDescriptor1 *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "characteristic", &value, NULL);
  return value;
}

/**
 * bluez_gatt_descriptor1_set_characteristic: (skip)
 * @object: A #BluezGattDescriptor1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-GattDescriptor1.Characteristic">"Characteristic"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_gatt_descriptor1_set_characteristic (BluezGattDescriptor1 *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "characteristic", value, NULL);
}

/**
 * bluez_gatt_descriptor1_get_value: (skip)
 * @object: A #BluezGattDescriptor1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-GattDescriptor1.Value">"Value"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use bluez_gatt_descriptor1_dup_value() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
GVariant *
bluez_gatt_descriptor1_get_value (BluezGattDescriptor1 *object)
{
  return BLUEZ_GATT_DESCRIPTOR1_GET_IFACE (object)->get_value (object);
}

/**
 * bluez_gatt_descriptor1_dup_value: (skip)
 * @object: A #BluezGattDescriptor1.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-GattDescriptor1.Value">"Value"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_variant_unref().
 */
GVariant *
bluez_gatt_descriptor1_dup_value (BluezGattDescriptor1 *object)
{
  GVariant *value;
  g_object_get (G_OBJECT (object), "value", &value, NULL);
  return value;
}

/**
 * bluez_gatt_descriptor1_set_value: (skip)
 * @object: A #BluezGattDescriptor1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-GattDescriptor1.Value">"Value"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_gatt_descriptor1_set_value (BluezGattDescriptor1 *object, GVariant *value)
{
  g_object_set (G_OBJECT (object), "value", value, NULL);
}

/**
 * bluez_gatt_descriptor1_call_read_value:
 * @proxy: A #BluezGattDescriptor1Proxy.
 * @arg_options: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-bluez-GattDescriptor1.ReadValue">ReadValue()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call bluez_gatt_descriptor1_call_read_value_finish() to get the result of the operation.
 *
 * See bluez_gatt_descriptor1_call_read_value_sync() for the synchronous, blocking version of this method.
 */
void
bluez_gatt_descriptor1_call_read_value (
    BluezGattDescriptor1 *proxy,
    GVariant *arg_options,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "ReadValue",
    g_variant_new ("(@a{sv})",
                   arg_options),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * bluez_gatt_descriptor1_call_read_value_finish:
 * @proxy: A #BluezGattDescriptor1Proxy.
 * @out_value: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_gatt_descriptor1_call_read_value().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with bluez_gatt_descriptor1_call_read_value().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_gatt_descriptor1_call_read_value_finish (
    BluezGattDescriptor1 *proxy,
    GVariant **out_value,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@ay)",
                 out_value);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_gatt_descriptor1_call_read_value_sync:
 * @proxy: A #BluezGattDescriptor1Proxy.
 * @arg_options: Argument to pass with the method invocation.
 * @out_value: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-bluez-GattDescriptor1.ReadValue">ReadValue()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See bluez_gatt_descriptor1_call_read_value() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_gatt_descriptor1_call_read_value_sync (
    BluezGattDescriptor1 *proxy,
    GVariant *arg_options,
    GVariant **out_value,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "ReadValue",
    g_variant_new ("(@a{sv})",
                   arg_options),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@ay)",
                 out_value);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_gatt_descriptor1_call_write_value:
 * @proxy: A #BluezGattDescriptor1Proxy.
 * @arg_value: Argument to pass with the method invocation.
 * @arg_options: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-bluez-GattDescriptor1.WriteValue">WriteValue()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call bluez_gatt_descriptor1_call_write_value_finish() to get the result of the operation.
 *
 * See bluez_gatt_descriptor1_call_write_value_sync() for the synchronous, blocking version of this method.
 */
void
bluez_gatt_descriptor1_call_write_value (
    BluezGattDescriptor1 *proxy,
    GVariant *arg_value,
    GVariant *arg_options,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "WriteValue",
    g_variant_new ("(@ay@a{sv})",
                   arg_value,
                   arg_options),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * bluez_gatt_descriptor1_call_write_value_finish:
 * @proxy: A #BluezGattDescriptor1Proxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_gatt_descriptor1_call_write_value().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with bluez_gatt_descriptor1_call_write_value().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_gatt_descriptor1_call_write_value_finish (
    BluezGattDescriptor1 *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_gatt_descriptor1_call_write_value_sync:
 * @proxy: A #BluezGattDescriptor1Proxy.
 * @arg_value: Argument to pass with the method invocation.
 * @arg_options: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-bluez-GattDescriptor1.WriteValue">WriteValue()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See bluez_gatt_descriptor1_call_write_value() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_gatt_descriptor1_call_write_value_sync (
    BluezGattDescriptor1 *proxy,
    GVariant *arg_value,
    GVariant *arg_options,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "WriteValue",
    g_variant_new ("(@ay@a{sv})",
                   arg_value,
                   arg_options),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_gatt_descriptor1_complete_read_value:
 * @object: A #BluezGattDescriptor1.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @value: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-bluez-GattDescriptor1.ReadValue">ReadValue()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
bluez_gatt_descriptor1_complete_read_value (
    BluezGattDescriptor1 *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation,
    GVariant *value)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@ay)",
                   value));
}

/**
 * bluez_gatt_descriptor1_complete_write_value:
 * @object: A #BluezGattDescriptor1.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-bluez-GattDescriptor1.WriteValue">WriteValue()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
bluez_gatt_descriptor1_complete_write_value (
    BluezGattDescriptor1 *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * BluezGattDescriptor1Proxy:
 *
 * The #BluezGattDescriptor1Proxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * BluezGattDescriptor1ProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #BluezGattDescriptor1Proxy.
 */

struct _BluezGattDescriptor1ProxyPrivate
{
  GData *qdata;
};

static void bluez_gatt_descriptor1_proxy_iface_init (BluezGattDescriptor1Iface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (BluezGattDescriptor1Proxy, bluez_gatt_descriptor1_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (BluezGattDescriptor1Proxy)
                         G_IMPLEMENT_INTERFACE (BLUEZ_TYPE_GATT_DESCRIPTOR1, bluez_gatt_descriptor1_proxy_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (BluezGattDescriptor1Proxy, bluez_gatt_descriptor1_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (BLUEZ_TYPE_GATT_DESCRIPTOR1, bluez_gatt_descriptor1_proxy_iface_init))

#endif
static void
bluez_gatt_descriptor1_proxy_finalize (GObject *object)
{
  BluezGattDescriptor1Proxy *proxy = BLUEZ_GATT_DESCRIPTOR1_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (bluez_gatt_descriptor1_proxy_parent_class)->finalize (object);
}

static void
bluez_gatt_descriptor1_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 3);
  info = (const _ExtendedGDBusPropertyInfo *) _bluez_gatt_descriptor1_property_info_pointers[prop_id - 1];
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
  if (info->use_gvariant)
    {
      g_value_set_variant (value, variant);
    }
  else
    {
      if (variant != NULL)
        g_dbus_gvariant_to_gvalue (variant, value);
    }
  if (variant != NULL)
    g_variant_unref (variant);
}

static void
bluez_gatt_descriptor1_proxy_set_property_cb (GDBusProxy *proxy,
  GAsyncResult *res,
  gpointer      user_data)
{
  const _ExtendedGDBusPropertyInfo *info = user_data;
  GError *error;
  GVariant *_ret;
  error = NULL;
  _ret = g_dbus_proxy_call_finish (proxy, res, &error);
  if (!_ret)
    {
      g_warning ("Error setting property '%s' on interface org.bluez.GattDescriptor1: %s (%s, %d)",
                 info->parent_struct.name, 
                 error->message, g_quark_to_string (error->domain), error->code);
      g_error_free (error);
    }
  else
    {
      g_variant_unref (_ret);
    }
}

static void
bluez_gatt_descriptor1_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 3);
  info = (const _ExtendedGDBusPropertyInfo *) _bluez_gatt_descriptor1_property_info_pointers[prop_id - 1];
  variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
  g_dbus_proxy_call (G_DBUS_PROXY (object),
    "org.freedesktop.DBus.Properties.Set",
    g_variant_new ("(ssv)", "org.bluez.GattDescriptor1", info->parent_struct.name, variant),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    NULL, (GAsyncReadyCallback) bluez_gatt_descriptor1_proxy_set_property_cb, (GDBusPropertyInfo *) &info->parent_struct);
  g_variant_unref (variant);
}

static void
bluez_gatt_descriptor1_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  gsize n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_bluez_gatt_descriptor1_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], BLUEZ_TYPE_GATT_DESCRIPTOR1);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, BLUEZ_TYPE_GATT_DESCRIPTOR1);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
bluez_gatt_descriptor1_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  BluezGattDescriptor1Proxy *proxy = BLUEZ_GATT_DESCRIPTOR1_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_gatt_descriptor1_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_gatt_descriptor1_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static const gchar *
bluez_gatt_descriptor1_proxy_get_uuid (BluezGattDescriptor1 *object)
{
  BluezGattDescriptor1Proxy *proxy = BLUEZ_GATT_DESCRIPTOR1_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "UUID");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
bluez_gatt_descriptor1_proxy_get_characteristic (BluezGattDescriptor1 *object)
{
  BluezGattDescriptor1Proxy *proxy = BLUEZ_GATT_DESCRIPTOR1_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Characteristic");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static GVariant *
bluez_gatt_descriptor1_proxy_get_value (BluezGattDescriptor1 *object)
{
  BluezGattDescriptor1Proxy *proxy = BLUEZ_GATT_DESCRIPTOR1_PROXY (object);
  GVariant *variant;
  GVariant *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Value");
  value = variant;
  if (variant != NULL)
    g_variant_unref (variant);
  return value;
}

static void
bluez_gatt_descriptor1_proxy_init (BluezGattDescriptor1Proxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = bluez_gatt_descriptor1_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, BLUEZ_TYPE_GATT_DESCRIPTOR1_PROXY, BluezGattDescriptor1ProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), bluez_gatt_descriptor1_interface_info ());
}

static void
bluez_gatt_descriptor1_proxy_class_init (BluezGattDescriptor1ProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = bluez_gatt_descriptor1_proxy_finalize;
  gobject_class->get_property = bluez_gatt_descriptor1_proxy_get_property;
  gobject_class->set_property = bluez_gatt_descriptor1_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = bluez_gatt_descriptor1_proxy_g_signal;
  proxy_class->g_properties_changed = bluez_gatt_descriptor1_proxy_g_properties_changed;

  bluez_gatt_descriptor1_override_properties (gobject_class, 1);

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (BluezGattDescriptor1ProxyPrivate));
#endif
}

static void
bluez_gatt_descriptor1_proxy_iface_init (BluezGattDescriptor1Iface *iface)
{
  iface->get_uuid = bluez_gatt_descriptor1_proxy_get_uuid;
  iface->get_characteristic = bluez_gatt_descriptor1_proxy_get_characteristic;
  iface->get_value = bluez_gatt_descriptor1_proxy_get_value;
}

/**
 * bluez_gatt_descriptor1_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-bluez-GattDescriptor1.top_of_page">org.bluez.GattDescriptor1</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call bluez_gatt_descriptor1_proxy_new_finish() to get the result of the operation.
 *
 * See bluez_gatt_descriptor1_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
bluez_gatt_descriptor1_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (BLUEZ_TYPE_GATT_DESCRIPTOR1_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.bluez.GattDescriptor1", NULL);
}

/**
 * bluez_gatt_descriptor1_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_gatt_descriptor1_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with bluez_gatt_descriptor1_proxy_new().
 *
 * Returns: (transfer full) (type BluezGattDescriptor1Proxy): The constructed proxy object or %NULL if @error is set.
 */
BluezGattDescriptor1 *
bluez_gatt_descriptor1_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return BLUEZ_GATT_DESCRIPTOR1 (ret);
  else
    return NULL;
}

/**
 * bluez_gatt_descriptor1_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-bluez-GattDescriptor1.top_of_page">org.bluez.GattDescriptor1</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See bluez_gatt_descriptor1_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type BluezGattDescriptor1Proxy): The constructed proxy object or %NULL if @error is set.
 */
BluezGattDescriptor1 *
bluez_gatt_descriptor1_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (BLUEZ_TYPE_GATT_DESCRIPTOR1_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.bluez.GattDescriptor1", NULL);
  if (ret != NULL)
    return BLUEZ_GATT_DESCRIPTOR1 (ret);
  else
    return NULL;
}


/**
 * bluez_gatt_descriptor1_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like bluez_gatt_descriptor1_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call bluez_gatt_descriptor1_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See bluez_gatt_descriptor1_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
bluez_gatt_descriptor1_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (BLUEZ_TYPE_GATT_DESCRIPTOR1_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.bluez.GattDescriptor1", NULL);
}

/**
 * bluez_gatt_descriptor1_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_gatt_descriptor1_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with bluez_gatt_descriptor1_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type BluezGattDescriptor1Proxy): The constructed proxy object or %NULL if @error is set.
 */
BluezGattDescriptor1 *
bluez_gatt_descriptor1_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return BLUEZ_GATT_DESCRIPTOR1 (ret);
  else
    return NULL;
}

/**
 * bluez_gatt_descriptor1_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like bluez_gatt_descriptor1_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See bluez_gatt_descriptor1_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type BluezGattDescriptor1Proxy): The constructed proxy object or %NULL if @error is set.
 */
BluezGattDescriptor1 *
bluez_gatt_descriptor1_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (BLUEZ_TYPE_GATT_DESCRIPTOR1_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.bluez.GattDescriptor1", NULL);
  if (ret != NULL)
    return BLUEZ_GATT_DESCRIPTOR1 (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * BluezGattDescriptor1Skeleton:
 *
 * The #BluezGattDescriptor1Skeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * BluezGattDescriptor1SkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #BluezGattDescriptor1Skeleton.
 */

struct _BluezGattDescriptor1SkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_bluez_gatt_descriptor1_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  BluezGattDescriptor1Skeleton *skeleton = BLUEZ_GATT_DESCRIPTOR1_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  guint num_extra;
  gsize n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], BLUEZ_TYPE_GATT_DESCRIPTOR1);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, BLUEZ_TYPE_GATT_DESCRIPTOR1);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_bluez_gatt_descriptor1_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  BluezGattDescriptor1Skeleton *skeleton = BLUEZ_GATT_DESCRIPTOR1_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_gatt_descriptor1_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_bluez_gatt_descriptor1_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  BluezGattDescriptor1Skeleton *skeleton = BLUEZ_GATT_DESCRIPTOR1_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_gatt_descriptor1_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _bluez_gatt_descriptor1_skeleton_vtable =
{
  _bluez_gatt_descriptor1_skeleton_handle_method_call,
  _bluez_gatt_descriptor1_skeleton_handle_get_property,
  _bluez_gatt_descriptor1_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
bluez_gatt_descriptor1_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return bluez_gatt_descriptor1_interface_info ();
}

static GDBusInterfaceVTable *
bluez_gatt_descriptor1_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_bluez_gatt_descriptor1_skeleton_vtable;
}

static GVariant *
bluez_gatt_descriptor1_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  BluezGattDescriptor1Skeleton *skeleton = BLUEZ_GATT_DESCRIPTOR1_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_bluez_gatt_descriptor1_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _bluez_gatt_descriptor1_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _bluez_gatt_descriptor1_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _bluez_gatt_descriptor1_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.bluez.GattDescriptor1", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static gboolean _bluez_gatt_descriptor1_emit_changed (gpointer user_data);

static void
bluez_gatt_descriptor1_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
  BluezGattDescriptor1Skeleton *skeleton = BLUEZ_GATT_DESCRIPTOR1_SKELETON (_skeleton);
  gboolean emit_changed = FALSE;

  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    {
      g_source_destroy (skeleton->priv->changed_properties_idle_source);
      skeleton->priv->changed_properties_idle_source = NULL;
      emit_changed = TRUE;
    }
  g_mutex_unlock (&skeleton->priv->lock);

  if (emit_changed)
    _bluez_gatt_descriptor1_emit_changed (skeleton);
}

static void bluez_gatt_descriptor1_skeleton_iface_init (BluezGattDescriptor1Iface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (BluezGattDescriptor1Skeleton, bluez_gatt_descriptor1_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (BluezGattDescriptor1Skeleton)
                         G_IMPLEMENT_INTERFACE (BLUEZ_TYPE_GATT_DESCRIPTOR1, bluez_gatt_descriptor1_skeleton_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (BluezGattDescriptor1Skeleton, bluez_gatt_descriptor1_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (BLUEZ_TYPE_GATT_DESCRIPTOR1, bluez_gatt_descriptor1_skeleton_iface_init))

#endif
static void
bluez_gatt_descriptor1_skeleton_finalize (GObject *object)
{
  BluezGattDescriptor1Skeleton *skeleton = BLUEZ_GATT_DESCRIPTOR1_SKELETON (object);
  guint n;
  for (n = 0; n < 3; n++)
    g_value_unset (&skeleton->priv->properties[n]);
  g_free (skeleton->priv->properties);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (bluez_gatt_descriptor1_skeleton_parent_class)->finalize (object);
}

static void
bluez_gatt_descriptor1_skeleton_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  BluezGattDescriptor1Skeleton *skeleton = BLUEZ_GATT_DESCRIPTOR1_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 3);
  g_mutex_lock (&skeleton->priv->lock);
  g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
  g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
_bluez_gatt_descriptor1_emit_changed (gpointer user_data)
{
  BluezGattDescriptor1Skeleton *skeleton = BLUEZ_GATT_DESCRIPTOR1_SKELETON (user_data);
  GList *l;
  GVariantBuilder builder;
  GVariantBuilder invalidated_builder;
  guint num_changes;

  g_mutex_lock (&skeleton->priv->lock);
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
  for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
    {
      ChangedProperty *cp = l->data;
      GVariant *variant;
      const GValue *cur_value;

      cur_value = &skeleton->priv->properties[cp->prop_id - 1];
      if (!_g_value_equal (cur_value, &cp->orig_value))
        {
          variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
          g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
          g_variant_unref (variant);
          num_changes++;
        }
    }
  if (num_changes > 0)
    {
      GList *connections, *ll;
      GVariant *signal_variant;
      signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "org.bluez.GattDescriptor1",
                                           &builder, &invalidated_builder));
      connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
      for (ll = connections; ll != NULL; ll = ll->next)
        {
          GDBusConnection *connection = ll->data;

          g_dbus_connection_emit_signal (connection,
                                         NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
                                         "org.freedesktop.DBus.Properties",
                                         "PropertiesChanged",
                                         signal_variant,
                                         NULL);
        }
      g_variant_unref (signal_variant);
      g_list_free_full (connections, g_object_unref);
    }
  else
    {
      g_variant_builder_clear (&builder);
      g_variant_builder_clear (&invalidated_builder);
    }
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  skeleton->priv->changed_properties = NULL;
  skeleton->priv->changed_properties_idle_source = NULL;
  g_mutex_unlock (&skeleton->priv->lock);
  return FALSE;
}

static void
_bluez_gatt_descriptor1_schedule_emit_changed (BluezGattDescriptor1Skeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
  ChangedProperty *cp;
  GList *l;
  cp = NULL;
  for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
    {
      ChangedProperty *i_cp = l->data;
      if (i_cp->info == info)
        {
          cp = i_cp;
          break;
        }
    }
  if (cp == NULL)
    {
      cp = g_new0 (ChangedProperty, 1);
      cp->prop_id = prop_id;
      cp->info = info;
      skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
      g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
      g_value_copy (orig_value, &cp->orig_value);
    }
}

static void
bluez_gatt_descriptor1_skeleton_notify (GObject      *object,
  GParamSpec *pspec G_GNUC_UNUSED)
{
  BluezGattDescriptor1Skeleton *skeleton = BLUEZ_GATT_DESCRIPTOR1_SKELETON (object);
  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties != NULL &&
      skeleton->priv->changed_properties_idle_source == NULL)
    {
      skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
      g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
      g_source_set_callback (skeleton->priv->changed_properties_idle_source, _bluez_gatt_descriptor1_emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
      g_source_set_name (skeleton->priv->changed_properties_idle_source, "[generated] _bluez_gatt_descriptor1_emit_changed");
      g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
      g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
  g_mutex_unlock (&skeleton->priv->lock);
}

static void
bluez_gatt_descriptor1_skeleton_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  const _ExtendedGDBusPropertyInfo *info;
  BluezGattDescriptor1Skeleton *skeleton = BLUEZ_GATT_DESCRIPTOR1_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 3);
  info = (const _ExtendedGDBusPropertyInfo *) _bluez_gatt_descriptor1_property_info_pointers[prop_id - 1];
  g_mutex_lock (&skeleton->priv->lock);
  g_object_freeze_notify (object);
  if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
      if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL &&
          info->emits_changed_signal)
        _bluez_gatt_descriptor1_schedule_emit_changed (skeleton, info, prop_id, &skeleton->priv->properties[prop_id - 1]);
      g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
      g_object_notify_by_pspec (object, pspec);
    }
  g_mutex_unlock (&skeleton->priv->lock);
  g_object_thaw_notify (object);
}

static void
bluez_gatt_descriptor1_skeleton_init (BluezGattDescriptor1Skeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = bluez_gatt_descriptor1_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, BLUEZ_TYPE_GATT_DESCRIPTOR1_SKELETON, BluezGattDescriptor1SkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
  skeleton->priv->properties = g_new0 (GValue, 3);
  g_value_init (&skeleton->priv->properties[0], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[1], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[2], G_TYPE_VARIANT);
}

static const gchar *
bluez_gatt_descriptor1_skeleton_get_uuid (BluezGattDescriptor1 *object)
{
  BluezGattDescriptor1Skeleton *skeleton = BLUEZ_GATT_DESCRIPTOR1_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[0]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
bluez_gatt_descriptor1_skeleton_get_characteristic (BluezGattDescriptor1 *object)
{
  BluezGattDescriptor1Skeleton *skeleton = BLUEZ_GATT_DESCRIPTOR1_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[1]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static GVariant *
bluez_gatt_descriptor1_skeleton_get_value (BluezGattDescriptor1 *object)
{
  BluezGattDescriptor1Skeleton *skeleton = BLUEZ_GATT_DESCRIPTOR1_SKELETON (object);
  GVariant *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_variant (&(skeleton->priv->properties[2]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static void
bluez_gatt_descriptor1_skeleton_class_init (BluezGattDescriptor1SkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = bluez_gatt_descriptor1_skeleton_finalize;
  gobject_class->get_property = bluez_gatt_descriptor1_skeleton_get_property;
  gobject_class->set_property = bluez_gatt_descriptor1_skeleton_set_property;
  gobject_class->notify       = bluez_gatt_descriptor1_skeleton_notify;


  bluez_gatt_descriptor1_override_properties (gobject_class, 1);

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = bluez_gatt_descriptor1_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = bluez_gatt_descriptor1_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = bluez_gatt_descriptor1_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = bluez_gatt_descriptor1_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (BluezGattDescriptor1SkeletonPrivate));
#endif
}

static void
bluez_gatt_descriptor1_skeleton_iface_init (BluezGattDescriptor1Iface *iface)
{
  iface->get_uuid = bluez_gatt_descriptor1_skeleton_get_uuid;
  iface->get_characteristic = bluez_gatt_descriptor1_skeleton_get_characteristic;
  iface->get_value = bluez_gatt_descriptor1_skeleton_get_value;
}

/**
 * bluez_gatt_descriptor1_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-bluez-GattDescriptor1.top_of_page">org.bluez.GattDescriptor1</link>.
 *
 * Returns: (transfer full) (type BluezGattDescriptor1Skeleton): The skeleton object.
 */
BluezGattDescriptor1 *
bluez_gatt_descriptor1_skeleton_new (void)
{
  return BLUEZ_GATT_DESCRIPTOR1 (g_object_new (BLUEZ_TYPE_GATT_DESCRIPTOR1_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface org.bluez.LEAdvertisement1
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:BluezLEAdvertisement1
 * @title: BluezLEAdvertisement1
 * @short_description: Generated C code for the org.bluez.LEAdvertisement1 D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-bluez-LEAdvertisement1.top_of_page">org.bluez.LEAdvertisement1</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.bluez.LEAdvertisement1 ---- */

static const _ExtendedGDBusMethodInfo _bluez_leadvertisement1_method_info_release =
{
  {
    -1,
    (gchar *) "Release",
    NULL,
    NULL,
    NULL
  },
  "handle-release",
  FALSE
};

static const GDBusMethodInfo * const _bluez_leadvertisement1_method_info_pointers[] =
{
  &_bluez_leadvertisement1_method_info_release.parent_struct,
  NULL
};

static const _ExtendedGDBusPropertyInfo _bluez_leadvertisement1_property_info_type_ =
{
  {
    -1,
    (gchar *) "Type",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "type",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _bluez_leadvertisement1_property_info_service_uuids =
{
  {
    -1,
    (gchar *) "ServiceUUIDs",
    (gchar *) "as",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "service-uuids",
  TRUE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _bluez_leadvertisement1_property_info_manufacturer_data =
{
  {
    -1,
    (gchar *) "ManufacturerData",
    (gchar *) "a{qv}",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "manufacturer-data",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _bluez_leadvertisement1_property_info_solicit_uuids =
{
  {
    -1,
    (gchar *) "SolicitUUIDs",
    (gchar *) "as",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "solicit-uuids",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _bluez_leadvertisement1_property_info_service_data =
{
  {
    -1,
    (gchar *) "ServiceData",
    (gchar *) "a{sv}",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "service-data",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _bluez_leadvertisement1_property_info_data =
{
  {
    -1,
    (gchar *) "Data",
    (gchar *) "a{yay}",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "data",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _bluez_leadvertisement1_property_info_discoverable =
{
  {
    -1,
    (gchar *) "Discoverable",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "discoverable",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _bluez_leadvertisement1_property_info_includes =
{
  {
    -1,
    (gchar *) "Includes",
    (gchar *) "as",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "includes",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _bluez_leadvertisement1_property_info_local_name =
{
  {
    -1,
    (gchar *) "LocalName",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "local-name",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _bluez_leadvertisement1_property_info_appearance =
{
  {
    -1,
    (gchar *) "Appearance",
    (gchar *) "q",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "appearance",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _bluez_leadvertisement1_property_info_min_interval =
{
  {
    -1,
    (gchar *) "MinInterval",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "min-interval",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _bluez_leadvertisement1_property_info_max_interval =
{
  {
    -1,
    (gchar *) "MaxInterval",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "max-interval",
  FALSE,
  TRUE
};

static const GDBusPropertyInfo * const _bluez_leadvertisement1_property_info_pointers[] =
{
  &_bluez_leadvertisement1_property_info_type_.parent_struct,
  &_bluez_leadvertisement1_property_info_service_uuids.parent_struct,
  &_bluez_leadvertisement1_property_info_manufacturer_data.parent_struct,
  &_bluez_leadvertisement1_property_info_solicit_uuids.parent_struct,
  &_bluez_leadvertisement1_property_info_service_data.parent_struct,
  &_bluez_leadvertisement1_property_info_data.parent_struct,
  &_bluez_leadvertisement1_property_info_discoverable.parent_struct,
  &_bluez_leadvertisement1_property_info_includes.parent_struct,
  &_bluez_leadvertisement1_property_info_local_name.parent_struct,
  &_bluez_leadvertisement1_property_info_appearance.parent_struct,
  &_bluez_leadvertisement1_property_info_min_interval.parent_struct,
  &_bluez_leadvertisement1_property_info_max_interval.parent_struct,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _bluez_leadvertisement1_interface_info =
{
  {
    -1,
    (gchar *) "org.bluez.LEAdvertisement1",
    (GDBusMethodInfo **) &_bluez_leadvertisement1_method_info_pointers,
    NULL,
    (GDBusPropertyInfo **) &_bluez_leadvertisement1_property_info_pointers,
    NULL
  },
  "leadvertisement1",
};


/**
 * bluez_leadvertisement1_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-bluez-LEAdvertisement1.top_of_page">org.bluez.LEAdvertisement1</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
bluez_leadvertisement1_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_bluez_leadvertisement1_interface_info.parent_struct;
}

/**
 * bluez_leadvertisement1_override_properties:
 * @klass: The class structure for a #GObject derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #BluezLEAdvertisement1 interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
bluez_leadvertisement1_override_properties (GObjectClass *klass, guint property_id_begin)
{
  g_object_class_override_property (klass, property_id_begin++, "type");
  g_object_class_override_property (klass, property_id_begin++, "service-uuids");
  g_object_class_override_property (klass, property_id_begin++, "manufacturer-data");
  g_object_class_override_property (klass, property_id_begin++, "solicit-uuids");
  g_object_class_override_property (klass, property_id_begin++, "service-data");
  g_object_class_override_property (klass, property_id_begin++, "data");
  g_object_class_override_property (klass, property_id_begin++, "discoverable");
  g_object_class_override_property (klass, property_id_begin++, "includes");
  g_object_class_override_property (klass, property_id_begin++, "local-name");
  g_object_class_override_property (klass, property_id_begin++, "appearance");
  g_object_class_override_property (klass, property_id_begin++, "min-interval");
  g_object_class_override_property (klass, property_id_begin++, "max-interval");
  return property_id_begin - 1;
}



/**
 * BluezLEAdvertisement1:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-bluez-LEAdvertisement1.top_of_page">org.bluez.LEAdvertisement1</link>.
 */

/**
 * BluezLEAdvertisement1Iface:
 * @parent_iface: The parent interface.
 * @handle_release: Handler for the #BluezLEAdvertisement1::handle-release signal.
 * @get_appearance: Getter for the #BluezLEAdvertisement1:appearance property.
 * @get_data: Getter for the #BluezLEAdvertisement1:data property.
 * @get_discoverable: Getter for the #BluezLEAdvertisement1:discoverable property.
 * @get_includes: Getter for the #BluezLEAdvertisement1:includes property.
 * @get_local_name: Getter for the #BluezLEAdvertisement1:local-name property.
 * @get_manufacturer_data: Getter for the #BluezLEAdvertisement1:manufacturer-data property.
 * @get_max_interval: Getter for the #BluezLEAdvertisement1:max-interval property.
 * @get_min_interval: Getter for the #BluezLEAdvertisement1:min-interval property.
 * @get_service_data: Getter for the #BluezLEAdvertisement1:service-data property.
 * @get_service_uuids: Getter for the #BluezLEAdvertisement1:service-uuids property.
 * @get_solicit_uuids: Getter for the #BluezLEAdvertisement1:solicit-uuids property.
 * @get_type_: Getter for the #BluezLEAdvertisement1:type property.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-bluez-LEAdvertisement1.top_of_page">org.bluez.LEAdvertisement1</link>.
 */

typedef BluezLEAdvertisement1Iface BluezLEAdvertisement1Interface;
G_DEFINE_INTERFACE (BluezLEAdvertisement1, bluez_leadvertisement1, G_TYPE_OBJECT)

static void
bluez_leadvertisement1_default_init (BluezLEAdvertisement1Iface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * BluezLEAdvertisement1::handle-release:
   * @object: A #BluezLEAdvertisement1.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-bluez-LEAdvertisement1.Release">Release()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call bluez_leadvertisement1_complete_release() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-release",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (BluezLEAdvertisement1Iface, handle_release),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /* GObject properties for D-Bus properties: */
  /**
   * BluezLEAdvertisement1:type:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-LEAdvertisement1.Type">"Type"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("type", "Type", "Type", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * BluezLEAdvertisement1:service-uuids:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-LEAdvertisement1.ServiceUUIDs">"ServiceUUIDs"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_variant ("service-uuids", "ServiceUUIDs", "ServiceUUIDs", G_VARIANT_TYPE ("as"), NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * BluezLEAdvertisement1:manufacturer-data:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-LEAdvertisement1.ManufacturerData">"ManufacturerData"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_variant ("manufacturer-data", "ManufacturerData", "ManufacturerData", G_VARIANT_TYPE ("a{qv}"), NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * BluezLEAdvertisement1:solicit-uuids:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-LEAdvertisement1.SolicitUUIDs">"SolicitUUIDs"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boxed ("solicit-uuids", "SolicitUUIDs", "SolicitUUIDs", G_TYPE_STRV, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * BluezLEAdvertisement1:service-data:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-LEAdvertisement1.ServiceData">"ServiceData"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_variant ("service-data", "ServiceData", "ServiceData", G_VARIANT_TYPE ("a{sv}"), NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * BluezLEAdvertisement1:data:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-LEAdvertisement1.Data">"Data"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_variant ("data", "Data", "Data", G_VARIANT_TYPE ("a{yay}"), NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * BluezLEAdvertisement1:discoverable:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-LEAdvertisement1.Discoverable">"Discoverable"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("discoverable", "Discoverable", "Discoverable", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * BluezLEAdvertisement1:includes:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-LEAdvertisement1.Includes">"Includes"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boxed ("includes", "Includes", "Includes", G_TYPE_STRV, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * BluezLEAdvertisement1:local-name:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-LEAdvertisement1.LocalName">"LocalName"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("local-name", "LocalName", "LocalName", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * BluezLEAdvertisement1:appearance:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-LEAdvertisement1.Appearance">"Appearance"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("appearance", "Appearance", "Appearance", 0, G_MAXUINT16, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * BluezLEAdvertisement1:min-interval:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-LEAdvertisement1.MinInterval">"MinInterval"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("min-interval", "MinInterval", "MinInterval", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * BluezLEAdvertisement1:max-interval:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-bluez-LEAdvertisement1.MaxInterval">"MaxInterval"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("max-interval", "MaxInterval", "MaxInterval", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

/**
 * bluez_leadvertisement1_get_type_: (skip)
 * @object: A #BluezLEAdvertisement1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-LEAdvertisement1.Type">"Type"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use bluez_leadvertisement1_dup_type_() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
bluez_leadvertisement1_get_type_ (BluezLEAdvertisement1 *object)
{
  return BLUEZ_LEADVERTISEMENT1_GET_IFACE (object)->get_type_ (object);
}

/**
 * bluez_leadvertisement1_dup_type_: (skip)
 * @object: A #BluezLEAdvertisement1.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-LEAdvertisement1.Type">"Type"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
bluez_leadvertisement1_dup_type_ (BluezLEAdvertisement1 *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "type", &value, NULL);
  return value;
}

/**
 * bluez_leadvertisement1_set_type_: (skip)
 * @object: A #BluezLEAdvertisement1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-LEAdvertisement1.Type">"Type"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_leadvertisement1_set_type_ (BluezLEAdvertisement1 *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "type", value, NULL);
}

/**
 * bluez_leadvertisement1_get_service_uuids: (skip)
 * @object: A #BluezLEAdvertisement1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-LEAdvertisement1.ServiceUUIDs">"ServiceUUIDs"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use bluez_leadvertisement1_dup_service_uuids() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
GVariant *
bluez_leadvertisement1_get_service_uuids (BluezLEAdvertisement1 *object)
{
  return BLUEZ_LEADVERTISEMENT1_GET_IFACE (object)->get_service_uuids (object);
}

/**
 * bluez_leadvertisement1_dup_service_uuids: (skip)
 * @object: A #BluezLEAdvertisement1.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-LEAdvertisement1.ServiceUUIDs">"ServiceUUIDs"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_variant_unref().
 */
GVariant *
bluez_leadvertisement1_dup_service_uuids (BluezLEAdvertisement1 *object)
{
  GVariant *value;
  g_object_get (G_OBJECT (object), "service-uuids", &value, NULL);
  return value;
}

/**
 * bluez_leadvertisement1_set_service_uuids: (skip)
 * @object: A #BluezLEAdvertisement1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-LEAdvertisement1.ServiceUUIDs">"ServiceUUIDs"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_leadvertisement1_set_service_uuids (BluezLEAdvertisement1 *object, GVariant *value)
{
  g_object_set (G_OBJECT (object), "service-uuids", value, NULL);
}

/**
 * bluez_leadvertisement1_get_manufacturer_data: (skip)
 * @object: A #BluezLEAdvertisement1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-LEAdvertisement1.ManufacturerData">"ManufacturerData"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use bluez_leadvertisement1_dup_manufacturer_data() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
GVariant *
bluez_leadvertisement1_get_manufacturer_data (BluezLEAdvertisement1 *object)
{
  return BLUEZ_LEADVERTISEMENT1_GET_IFACE (object)->get_manufacturer_data (object);
}

/**
 * bluez_leadvertisement1_dup_manufacturer_data: (skip)
 * @object: A #BluezLEAdvertisement1.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-LEAdvertisement1.ManufacturerData">"ManufacturerData"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_variant_unref().
 */
GVariant *
bluez_leadvertisement1_dup_manufacturer_data (BluezLEAdvertisement1 *object)
{
  GVariant *value;
  g_object_get (G_OBJECT (object), "manufacturer-data", &value, NULL);
  return value;
}

/**
 * bluez_leadvertisement1_set_manufacturer_data: (skip)
 * @object: A #BluezLEAdvertisement1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-LEAdvertisement1.ManufacturerData">"ManufacturerData"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_leadvertisement1_set_manufacturer_data (BluezLEAdvertisement1 *object, GVariant *value)
{
  g_object_set (G_OBJECT (object), "manufacturer-data", value, NULL);
}

/**
 * bluez_leadvertisement1_get_solicit_uuids: (skip)
 * @object: A #BluezLEAdvertisement1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-LEAdvertisement1.SolicitUUIDs">"SolicitUUIDs"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use bluez_leadvertisement1_dup_solicit_uuids() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *const *
bluez_leadvertisement1_get_solicit_uuids (BluezLEAdvertisement1 *object)
{
  return BLUEZ_LEADVERTISEMENT1_GET_IFACE (object)->get_solicit_uuids (object);
}

/**
 * bluez_leadvertisement1_dup_solicit_uuids: (skip)
 * @object: A #BluezLEAdvertisement1.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-LEAdvertisement1.SolicitUUIDs">"SolicitUUIDs"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_strfreev().
 */
gchar **
bluez_leadvertisement1_dup_solicit_uuids (BluezLEAdvertisement1 *object)
{
  gchar **value;
  g_object_get (G_OBJECT (object), "solicit-uuids", &value, NULL);
  return value;
}

/**
 * bluez_leadvertisement1_set_solicit_uuids: (skip)
 * @object: A #BluezLEAdvertisement1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-LEAdvertisement1.SolicitUUIDs">"SolicitUUIDs"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_leadvertisement1_set_solicit_uuids (BluezLEAdvertisement1 *object, const gchar *const *value)
{
  g_object_set (G_OBJECT (object), "solicit-uuids", value, NULL);
}

/**
 * bluez_leadvertisement1_get_service_data: (skip)
 * @object: A #BluezLEAdvertisement1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-LEAdvertisement1.ServiceData">"ServiceData"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use bluez_leadvertisement1_dup_service_data() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
GVariant *
bluez_leadvertisement1_get_service_data (BluezLEAdvertisement1 *object)
{
  return BLUEZ_LEADVERTISEMENT1_GET_IFACE (object)->get_service_data (object);
}

/**
 * bluez_leadvertisement1_dup_service_data: (skip)
 * @object: A #BluezLEAdvertisement1.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-LEAdvertisement1.ServiceData">"ServiceData"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_variant_unref().
 */
GVariant *
bluez_leadvertisement1_dup_service_data (BluezLEAdvertisement1 *object)
{
  GVariant *value;
  g_object_get (G_OBJECT (object), "service-data", &value, NULL);
  return value;
}

/**
 * bluez_leadvertisement1_set_service_data: (skip)
 * @object: A #BluezLEAdvertisement1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-LEAdvertisement1.ServiceData">"ServiceData"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_leadvertisement1_set_service_data (BluezLEAdvertisement1 *object, GVariant *value)
{
  g_object_set (G_OBJECT (object), "service-data", value, NULL);
}

/**
 * bluez_leadvertisement1_get_data: (skip)
 * @object: A #BluezLEAdvertisement1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-LEAdvertisement1.Data">"Data"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use bluez_leadvertisement1_dup_data() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
GVariant *
bluez_leadvertisement1_get_data (BluezLEAdvertisement1 *object)
{
  return BLUEZ_LEADVERTISEMENT1_GET_IFACE (object)->get_data (object);
}

/**
 * bluez_leadvertisement1_dup_data: (skip)
 * @object: A #BluezLEAdvertisement1.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-LEAdvertisement1.Data">"Data"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_variant_unref().
 */
GVariant *
bluez_leadvertisement1_dup_data (BluezLEAdvertisement1 *object)
{
  GVariant *value;
  g_object_get (G_OBJECT (object), "data", &value, NULL);
  return value;
}

/**
 * bluez_leadvertisement1_set_data: (skip)
 * @object: A #BluezLEAdvertisement1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-LEAdvertisement1.Data">"Data"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_leadvertisement1_set_data (BluezLEAdvertisement1 *object, GVariant *value)
{
  g_object_set (G_OBJECT (object), "data", value, NULL);
}

/**
 * bluez_leadvertisement1_get_discoverable: (skip)
 * @object: A #BluezLEAdvertisement1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-LEAdvertisement1.Discoverable">"Discoverable"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
bluez_leadvertisement1_get_discoverable (BluezLEAdvertisement1 *object)
{
  return BLUEZ_LEADVERTISEMENT1_GET_IFACE (object)->get_discoverable (object);
}

/**
 * bluez_leadvertisement1_set_discoverable: (skip)
 * @object: A #BluezLEAdvertisement1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-LEAdvertisement1.Discoverable">"Discoverable"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_leadvertisement1_set_discoverable (BluezLEAdvertisement1 *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "discoverable", value, NULL);
}

/**
 * bluez_leadvertisement1_get_includes: (skip)
 * @object: A #BluezLEAdvertisement1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-LEAdvertisement1.Includes">"Includes"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use bluez_leadvertisement1_dup_includes() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *const *
bluez_leadvertisement1_get_includes (BluezLEAdvertisement1 *object)
{
  return BLUEZ_LEADVERTISEMENT1_GET_IFACE (object)->get_includes (object);
}

/**
 * bluez_leadvertisement1_dup_includes: (skip)
 * @object: A #BluezLEAdvertisement1.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-LEAdvertisement1.Includes">"Includes"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_strfreev().
 */
gchar **
bluez_leadvertisement1_dup_includes (BluezLEAdvertisement1 *object)
{
  gchar **value;
  g_object_get (G_OBJECT (object), "includes", &value, NULL);
  return value;
}

/**
 * bluez_leadvertisement1_set_includes: (skip)
 * @object: A #BluezLEAdvertisement1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-LEAdvertisement1.Includes">"Includes"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_leadvertisement1_set_includes (BluezLEAdvertisement1 *object, const gchar *const *value)
{
  g_object_set (G_OBJECT (object), "includes", value, NULL);
}

/**
 * bluez_leadvertisement1_get_local_name: (skip)
 * @object: A #BluezLEAdvertisement1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-LEAdvertisement1.LocalName">"LocalName"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use bluez_leadvertisement1_dup_local_name() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
bluez_leadvertisement1_get_local_name (BluezLEAdvertisement1 *object)
{
  return BLUEZ_LEADVERTISEMENT1_GET_IFACE (object)->get_local_name (object);
}

/**
 * bluez_leadvertisement1_dup_local_name: (skip)
 * @object: A #BluezLEAdvertisement1.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-bluez-LEAdvertisement1.LocalName">"LocalName"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
bluez_leadvertisement1_dup_local_name (BluezLEAdvertisement1 *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "local-name", &value, NULL);
  return value;
}

/**
 * bluez_leadvertisement1_set_local_name: (skip)
 * @object: A #BluezLEAdvertisement1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-LEAdvertisement1.LocalName">"LocalName"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_leadvertisement1_set_local_name (BluezLEAdvertisement1 *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "local-name", value, NULL);
}

/**
 * bluez_leadvertisement1_get_appearance: (skip)
 * @object: A #BluezLEAdvertisement1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-LEAdvertisement1.Appearance">"Appearance"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint16 
bluez_leadvertisement1_get_appearance (BluezLEAdvertisement1 *object)
{
  return BLUEZ_LEADVERTISEMENT1_GET_IFACE (object)->get_appearance (object);
}

/**
 * bluez_leadvertisement1_set_appearance: (skip)
 * @object: A #BluezLEAdvertisement1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-LEAdvertisement1.Appearance">"Appearance"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_leadvertisement1_set_appearance (BluezLEAdvertisement1 *object, guint16 value)
{
  g_object_set (G_OBJECT (object), "appearance", value, NULL);
}

/**
 * bluez_leadvertisement1_get_min_interval: (skip)
 * @object: A #BluezLEAdvertisement1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-LEAdvertisement1.MinInterval">"MinInterval"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
bluez_leadvertisement1_get_min_interval (BluezLEAdvertisement1 *object)
{
  return BLUEZ_LEADVERTISEMENT1_GET_IFACE (object)->get_min_interval (object);
}

/**
 * bluez_leadvertisement1_set_min_interval: (skip)
 * @object: A #BluezLEAdvertisement1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-LEAdvertisement1.MinInterval">"MinInterval"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_leadvertisement1_set_min_interval (BluezLEAdvertisement1 *object, guint value)
{
  g_object_set (G_OBJECT (object), "min-interval", value, NULL);
}

/**
 * bluez_leadvertisement1_get_max_interval: (skip)
 * @object: A #BluezLEAdvertisement1.
 *
 * Gets the value of the <link linkend="gdbus-property-org-bluez-LEAdvertisement1.MaxInterval">"MaxInterval"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
bluez_leadvertisement1_get_max_interval (BluezLEAdvertisement1 *object)
{
  return BLUEZ_LEADVERTISEMENT1_GET_IFACE (object)->get_max_interval (object);
}

/**
 * bluez_leadvertisement1_set_max_interval: (skip)
 * @object: A #BluezLEAdvertisement1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-bluez-LEAdvertisement1.MaxInterval">"MaxInterval"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
bluez_leadvertisement1_set_max_interval (BluezLEAdvertisement1 *object, guint value)
{
  g_object_set (G_OBJECT (object), "max-interval", value, NULL);
}

/**
 * bluez_leadvertisement1_call_release:
 * @proxy: A #BluezLEAdvertisement1Proxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-bluez-LEAdvertisement1.Release">Release()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call bluez_leadvertisement1_call_release_finish() to get the result of the operation.
 *
 * See bluez_leadvertisement1_call_release_sync() for the synchronous, blocking version of this method.
 */
void
bluez_leadvertisement1_call_release (
    BluezLEAdvertisement1 *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Release",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * bluez_leadvertisement1_call_release_finish:
 * @proxy: A #BluezLEAdvertisement1Proxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_leadvertisement1_call_release().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with bluez_leadvertisement1_call_release().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_leadvertisement1_call_release_finish (
    BluezLEAdvertisement1 *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_leadvertisement1_call_release_sync:
 * @proxy: A #BluezLEAdvertisement1Proxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-bluez-LEAdvertisement1.Release">Release()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See bluez_leadvertisement1_call_release() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_leadvertisement1_call_release_sync (
    BluezLEAdvertisement1 *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Release",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_leadvertisement1_complete_release:
 * @object: A #BluezLEAdvertisement1.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-bluez-LEAdvertisement1.Release">Release()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
bluez_leadvertisement1_complete_release (
    BluezLEAdvertisement1 *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * BluezLEAdvertisement1Proxy:
 *
 * The #BluezLEAdvertisement1Proxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * BluezLEAdvertisement1ProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #BluezLEAdvertisement1Proxy.
 */

struct _BluezLEAdvertisement1ProxyPrivate
{
  GData *qdata;
};

static void bluez_leadvertisement1_proxy_iface_init (BluezLEAdvertisement1Iface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (BluezLEAdvertisement1Proxy, bluez_leadvertisement1_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (BluezLEAdvertisement1Proxy)
                         G_IMPLEMENT_INTERFACE (BLUEZ_TYPE_LEADVERTISEMENT1, bluez_leadvertisement1_proxy_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (BluezLEAdvertisement1Proxy, bluez_leadvertisement1_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (BLUEZ_TYPE_LEADVERTISEMENT1, bluez_leadvertisement1_proxy_iface_init))

#endif
static void
bluez_leadvertisement1_proxy_finalize (GObject *object)
{
  BluezLEAdvertisement1Proxy *proxy = BLUEZ_LEADVERTISEMENT1_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (bluez_leadvertisement1_proxy_parent_class)->finalize (object);
}

static void
bluez_leadvertisement1_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 12);
  info = (const _ExtendedGDBusPropertyInfo *) _bluez_leadvertisement1_property_info_pointers[prop_id - 1];
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
  if (info->use_gvariant)
    {
      g_value_set_variant (value, variant);
    }
  else
    {
      if (variant != NULL)
        g_dbus_gvariant_to_gvalue (variant, value);
    }
  if (variant != NULL)
    g_variant_unref (variant);
}

static void
bluez_leadvertisement1_proxy_set_property_cb (GDBusProxy *proxy,
  GAsyncResult *res,
  gpointer      user_data)
{
  const _ExtendedGDBusPropertyInfo *info = user_data;
  GError *error;
  GVariant *_ret;
  error = NULL;
  _ret = g_dbus_proxy_call_finish (proxy, res, &error);
  if (!_ret)
    {
      g_warning ("Error setting property '%s' on interface org.bluez.LEAdvertisement1: %s (%s, %d)",
                 info->parent_struct.name, 
                 error->message, g_quark_to_string (error->domain), error->code);
      g_error_free (error);
    }
  else
    {
      g_variant_unref (_ret);
    }
}

static void
bluez_leadvertisement1_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 12);
  info = (const _ExtendedGDBusPropertyInfo *) _bluez_leadvertisement1_property_info_pointers[prop_id - 1];
  variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
  g_dbus_proxy_call (G_DBUS_PROXY (object),
    "org.freedesktop.DBus.Properties.Set",
    g_variant_new ("(ssv)", "org.bluez.LEAdvertisement1", info->parent_struct.name, variant),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    NULL, (GAsyncReadyCallback) bluez_leadvertisement1_proxy_set_property_cb, (GDBusPropertyInfo *) &info->parent_struct);
  g_variant_unref (variant);
}

static void
bluez_leadvertisement1_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  gsize n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_bluez_leadvertisement1_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], BLUEZ_TYPE_LEADVERTISEMENT1);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, BLUEZ_TYPE_LEADVERTISEMENT1);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
bluez_leadvertisement1_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  BluezLEAdvertisement1Proxy *proxy = BLUEZ_LEADVERTISEMENT1_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_leadvertisement1_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_leadvertisement1_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static const gchar *
bluez_leadvertisement1_proxy_get_type_ (BluezLEAdvertisement1 *object)
{
  BluezLEAdvertisement1Proxy *proxy = BLUEZ_LEADVERTISEMENT1_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Type");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static GVariant *
bluez_leadvertisement1_proxy_get_service_uuids (BluezLEAdvertisement1 *object)
{
  BluezLEAdvertisement1Proxy *proxy = BLUEZ_LEADVERTISEMENT1_PROXY (object);
  GVariant *variant;
  GVariant *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "ServiceUUIDs");
  value = variant;
  if (variant != NULL)
    g_variant_unref (variant);
  return value;
}

static GVariant *
bluez_leadvertisement1_proxy_get_manufacturer_data (BluezLEAdvertisement1 *object)
{
  BluezLEAdvertisement1Proxy *proxy = BLUEZ_LEADVERTISEMENT1_PROXY (object);
  GVariant *variant;
  GVariant *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "ManufacturerData");
  value = variant;
  if (variant != NULL)
    g_variant_unref (variant);
  return value;
}

static const gchar *const *
bluez_leadvertisement1_proxy_get_solicit_uuids (BluezLEAdvertisement1 *object)
{
  BluezLEAdvertisement1Proxy *proxy = BLUEZ_LEADVERTISEMENT1_PROXY (object);
  GVariant *variant;
  const gchar *const *value = NULL;
  value = g_datalist_get_data (&proxy->priv->qdata, "SolicitUUIDs");
  if (value != NULL)
    return value;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "SolicitUUIDs");
  if (variant != NULL)
    {
      value = g_variant_get_strv (variant, NULL);
      g_datalist_set_data_full (&proxy->priv->qdata, "SolicitUUIDs", (gpointer) value, g_free);
      g_variant_unref (variant);
    }
  return value;
}

static GVariant *
bluez_leadvertisement1_proxy_get_service_data (BluezLEAdvertisement1 *object)
{
  BluezLEAdvertisement1Proxy *proxy = BLUEZ_LEADVERTISEMENT1_PROXY (object);
  GVariant *variant;
  GVariant *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "ServiceData");
  value = variant;
  if (variant != NULL)
    g_variant_unref (variant);
  return value;
}

static GVariant *
bluez_leadvertisement1_proxy_get_data (BluezLEAdvertisement1 *object)
{
  BluezLEAdvertisement1Proxy *proxy = BLUEZ_LEADVERTISEMENT1_PROXY (object);
  GVariant *variant;
  GVariant *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Data");
  value = variant;
  if (variant != NULL)
    g_variant_unref (variant);
  return value;
}

static gboolean 
bluez_leadvertisement1_proxy_get_discoverable (BluezLEAdvertisement1 *object)
{
  BluezLEAdvertisement1Proxy *proxy = BLUEZ_LEADVERTISEMENT1_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Discoverable");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *const *
bluez_leadvertisement1_proxy_get_includes (BluezLEAdvertisement1 *object)
{
  BluezLEAdvertisement1Proxy *proxy = BLUEZ_LEADVERTISEMENT1_PROXY (object);
  GVariant *variant;
  const gchar *const *value = NULL;
  value = g_datalist_get_data (&proxy->priv->qdata, "Includes");
  if (value != NULL)
    return value;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Includes");
  if (variant != NULL)
    {
      value = g_variant_get_strv (variant, NULL);
      g_datalist_set_data_full (&proxy->priv->qdata, "Includes", (gpointer) value, g_free);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
bluez_leadvertisement1_proxy_get_local_name (BluezLEAdvertisement1 *object)
{
  BluezLEAdvertisement1Proxy *proxy = BLUEZ_LEADVERTISEMENT1_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "LocalName");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static guint16 
bluez_leadvertisement1_proxy_get_appearance (BluezLEAdvertisement1 *object)
{
  BluezLEAdvertisement1Proxy *proxy = BLUEZ_LEADVERTISEMENT1_PROXY (object);
  GVariant *variant;
  guint16 value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Appearance");
  if (variant != NULL)
    {
      value = g_variant_get_uint16 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static guint 
bluez_leadvertisement1_proxy_get_min_interval (BluezLEAdvertisement1 *object)
{
  BluezLEAdvertisement1Proxy *proxy = BLUEZ_LEADVERTISEMENT1_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "MinInterval");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static guint 
bluez_leadvertisement1_proxy_get_max_interval (BluezLEAdvertisement1 *object)
{
  BluezLEAdvertisement1Proxy *proxy = BLUEZ_LEADVERTISEMENT1_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "MaxInterval");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static void
bluez_leadvertisement1_proxy_init (BluezLEAdvertisement1Proxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = bluez_leadvertisement1_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, BLUEZ_TYPE_LEADVERTISEMENT1_PROXY, BluezLEAdvertisement1ProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), bluez_leadvertisement1_interface_info ());
}

static void
bluez_leadvertisement1_proxy_class_init (BluezLEAdvertisement1ProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = bluez_leadvertisement1_proxy_finalize;
  gobject_class->get_property = bluez_leadvertisement1_proxy_get_property;
  gobject_class->set_property = bluez_leadvertisement1_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = bluez_leadvertisement1_proxy_g_signal;
  proxy_class->g_properties_changed = bluez_leadvertisement1_proxy_g_properties_changed;

  bluez_leadvertisement1_override_properties (gobject_class, 1);

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (BluezLEAdvertisement1ProxyPrivate));
#endif
}

static void
bluez_leadvertisement1_proxy_iface_init (BluezLEAdvertisement1Iface *iface)
{
  iface->get_type_ = bluez_leadvertisement1_proxy_get_type_;
  iface->get_service_uuids = bluez_leadvertisement1_proxy_get_service_uuids;
  iface->get_manufacturer_data = bluez_leadvertisement1_proxy_get_manufacturer_data;
  iface->get_solicit_uuids = bluez_leadvertisement1_proxy_get_solicit_uuids;
  iface->get_service_data = bluez_leadvertisement1_proxy_get_service_data;
  iface->get_data = bluez_leadvertisement1_proxy_get_data;
  iface->get_discoverable = bluez_leadvertisement1_proxy_get_discoverable;
  iface->get_includes = bluez_leadvertisement1_proxy_get_includes;
  iface->get_local_name = bluez_leadvertisement1_proxy_get_local_name;
  iface->get_appearance = bluez_leadvertisement1_proxy_get_appearance;
  iface->get_min_interval = bluez_leadvertisement1_proxy_get_min_interval;
  iface->get_max_interval = bluez_leadvertisement1_proxy_get_max_interval;
}

/**
 * bluez_leadvertisement1_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-bluez-LEAdvertisement1.top_of_page">org.bluez.LEAdvertisement1</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call bluez_leadvertisement1_proxy_new_finish() to get the result of the operation.
 *
 * See bluez_leadvertisement1_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
bluez_leadvertisement1_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (BLUEZ_TYPE_LEADVERTISEMENT1_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.bluez.LEAdvertisement1", NULL);
}

/**
 * bluez_leadvertisement1_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_leadvertisement1_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with bluez_leadvertisement1_proxy_new().
 *
 * Returns: (transfer full) (type BluezLEAdvertisement1Proxy): The constructed proxy object or %NULL if @error is set.
 */
BluezLEAdvertisement1 *
bluez_leadvertisement1_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return BLUEZ_LEADVERTISEMENT1 (ret);
  else
    return NULL;
}

/**
 * bluez_leadvertisement1_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-bluez-LEAdvertisement1.top_of_page">org.bluez.LEAdvertisement1</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See bluez_leadvertisement1_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type BluezLEAdvertisement1Proxy): The constructed proxy object or %NULL if @error is set.
 */
BluezLEAdvertisement1 *
bluez_leadvertisement1_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (BLUEZ_TYPE_LEADVERTISEMENT1_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.bluez.LEAdvertisement1", NULL);
  if (ret != NULL)
    return BLUEZ_LEADVERTISEMENT1 (ret);
  else
    return NULL;
}


/**
 * bluez_leadvertisement1_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like bluez_leadvertisement1_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call bluez_leadvertisement1_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See bluez_leadvertisement1_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
bluez_leadvertisement1_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (BLUEZ_TYPE_LEADVERTISEMENT1_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.bluez.LEAdvertisement1", NULL);
}

/**
 * bluez_leadvertisement1_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_leadvertisement1_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with bluez_leadvertisement1_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type BluezLEAdvertisement1Proxy): The constructed proxy object or %NULL if @error is set.
 */
BluezLEAdvertisement1 *
bluez_leadvertisement1_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return BLUEZ_LEADVERTISEMENT1 (ret);
  else
    return NULL;
}

/**
 * bluez_leadvertisement1_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like bluez_leadvertisement1_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See bluez_leadvertisement1_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type BluezLEAdvertisement1Proxy): The constructed proxy object or %NULL if @error is set.
 */
BluezLEAdvertisement1 *
bluez_leadvertisement1_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (BLUEZ_TYPE_LEADVERTISEMENT1_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.bluez.LEAdvertisement1", NULL);
  if (ret != NULL)
    return BLUEZ_LEADVERTISEMENT1 (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * BluezLEAdvertisement1Skeleton:
 *
 * The #BluezLEAdvertisement1Skeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * BluezLEAdvertisement1SkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #BluezLEAdvertisement1Skeleton.
 */

struct _BluezLEAdvertisement1SkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_bluez_leadvertisement1_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  BluezLEAdvertisement1Skeleton *skeleton = BLUEZ_LEADVERTISEMENT1_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  guint num_extra;
  gsize n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], BLUEZ_TYPE_LEADVERTISEMENT1);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, BLUEZ_TYPE_LEADVERTISEMENT1);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_bluez_leadvertisement1_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  BluezLEAdvertisement1Skeleton *skeleton = BLUEZ_LEADVERTISEMENT1_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_leadvertisement1_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_bluez_leadvertisement1_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  BluezLEAdvertisement1Skeleton *skeleton = BLUEZ_LEADVERTISEMENT1_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_leadvertisement1_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _bluez_leadvertisement1_skeleton_vtable =
{
  _bluez_leadvertisement1_skeleton_handle_method_call,
  _bluez_leadvertisement1_skeleton_handle_get_property,
  _bluez_leadvertisement1_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
bluez_leadvertisement1_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return bluez_leadvertisement1_interface_info ();
}

static GDBusInterfaceVTable *
bluez_leadvertisement1_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_bluez_leadvertisement1_skeleton_vtable;
}

static GVariant *
bluez_leadvertisement1_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  BluezLEAdvertisement1Skeleton *skeleton = BLUEZ_LEADVERTISEMENT1_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_bluez_leadvertisement1_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _bluez_leadvertisement1_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _bluez_leadvertisement1_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _bluez_leadvertisement1_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.bluez.LEAdvertisement1", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static gboolean _bluez_leadvertisement1_emit_changed (gpointer user_data);

static void
bluez_leadvertisement1_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
  BluezLEAdvertisement1Skeleton *skeleton = BLUEZ_LEADVERTISEMENT1_SKELETON (_skeleton);
  gboolean emit_changed = FALSE;

  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    {
      g_source_destroy (skeleton->priv->changed_properties_idle_source);
      skeleton->priv->changed_properties_idle_source = NULL;
      emit_changed = TRUE;
    }
  g_mutex_unlock (&skeleton->priv->lock);

  if (emit_changed)
    _bluez_leadvertisement1_emit_changed (skeleton);
}

static void bluez_leadvertisement1_skeleton_iface_init (BluezLEAdvertisement1Iface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (BluezLEAdvertisement1Skeleton, bluez_leadvertisement1_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (BluezLEAdvertisement1Skeleton)
                         G_IMPLEMENT_INTERFACE (BLUEZ_TYPE_LEADVERTISEMENT1, bluez_leadvertisement1_skeleton_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (BluezLEAdvertisement1Skeleton, bluez_leadvertisement1_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (BLUEZ_TYPE_LEADVERTISEMENT1, bluez_leadvertisement1_skeleton_iface_init))

#endif
static void
bluez_leadvertisement1_skeleton_finalize (GObject *object)
{
  BluezLEAdvertisement1Skeleton *skeleton = BLUEZ_LEADVERTISEMENT1_SKELETON (object);
  guint n;
  for (n = 0; n < 12; n++)
    g_value_unset (&skeleton->priv->properties[n]);
  g_free (skeleton->priv->properties);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (bluez_leadvertisement1_skeleton_parent_class)->finalize (object);
}

static void
bluez_leadvertisement1_skeleton_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  BluezLEAdvertisement1Skeleton *skeleton = BLUEZ_LEADVERTISEMENT1_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 12);
  g_mutex_lock (&skeleton->priv->lock);
  g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
  g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
_bluez_leadvertisement1_emit_changed (gpointer user_data)
{
  BluezLEAdvertisement1Skeleton *skeleton = BLUEZ_LEADVERTISEMENT1_SKELETON (user_data);
  GList *l;
  GVariantBuilder builder;
  GVariantBuilder invalidated_builder;
  guint num_changes;

  g_mutex_lock (&skeleton->priv->lock);
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
  for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
    {
      ChangedProperty *cp = l->data;
      GVariant *variant;
      const GValue *cur_value;

      cur_value = &skeleton->priv->properties[cp->prop_id - 1];
      if (!_g_value_equal (cur_value, &cp->orig_value))
        {
          variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
          g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
          g_variant_unref (variant);
          num_changes++;
        }
    }
  if (num_changes > 0)
    {
      GList *connections, *ll;
      GVariant *signal_variant;
      signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "org.bluez.LEAdvertisement1",
                                           &builder, &invalidated_builder));
      connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
      for (ll = connections; ll != NULL; ll = ll->next)
        {
          GDBusConnection *connection = ll->data;

          g_dbus_connection_emit_signal (connection,
                                         NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
                                         "org.freedesktop.DBus.Properties",
                                         "PropertiesChanged",
                                         signal_variant,
                                         NULL);
        }
      g_variant_unref (signal_variant);
      g_list_free_full (connections, g_object_unref);
    }
  else
    {
      g_variant_builder_clear (&builder);
      g_variant_builder_clear (&invalidated_builder);
    }
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  skeleton->priv->changed_properties = NULL;
  skeleton->priv->changed_properties_idle_source = NULL;
  g_mutex_unlock (&skeleton->priv->lock);
  return FALSE;
}

static void
_bluez_leadvertisement1_schedule_emit_changed (BluezLEAdvertisement1Skeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
  ChangedProperty *cp;
  GList *l;
  cp = NULL;
  for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
    {
      ChangedProperty *i_cp = l->data;
      if (i_cp->info == info)
        {
          cp = i_cp;
          break;
        }
    }
  if (cp == NULL)
    {
      cp = g_new0 (ChangedProperty, 1);
      cp->prop_id = prop_id;
      cp->info = info;
      skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
      g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
      g_value_copy (orig_value, &cp->orig_value);
    }
}

static void
bluez_leadvertisement1_skeleton_notify (GObject      *object,
  GParamSpec *pspec G_GNUC_UNUSED)
{
  BluezLEAdvertisement1Skeleton *skeleton = BLUEZ_LEADVERTISEMENT1_SKELETON (object);
  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties != NULL &&
      skeleton->priv->changed_properties_idle_source == NULL)
    {
      skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
      g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
      g_source_set_callback (skeleton->priv->changed_properties_idle_source, _bluez_leadvertisement1_emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
      g_source_set_name (skeleton->priv->changed_properties_idle_source, "[generated] _bluez_leadvertisement1_emit_changed");
      g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
      g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
  g_mutex_unlock (&skeleton->priv->lock);
}

static void
bluez_leadvertisement1_skeleton_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  const _ExtendedGDBusPropertyInfo *info;
  BluezLEAdvertisement1Skeleton *skeleton = BLUEZ_LEADVERTISEMENT1_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 12);
  info = (const _ExtendedGDBusPropertyInfo *) _bluez_leadvertisement1_property_info_pointers[prop_id - 1];
  g_mutex_lock (&skeleton->priv->lock);
  g_object_freeze_notify (object);
  if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
      if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL &&
          info->emits_changed_signal)
        _bluez_leadvertisement1_schedule_emit_changed (skeleton, info, prop_id, &skeleton->priv->properties[prop_id - 1]);
      g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
      g_object_notify_by_pspec (object, pspec);
    }
  g_mutex_unlock (&skeleton->priv->lock);
  g_object_thaw_notify (object);
}

static void
bluez_leadvertisement1_skeleton_init (BluezLEAdvertisement1Skeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = bluez_leadvertisement1_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, BLUEZ_TYPE_LEADVERTISEMENT1_SKELETON, BluezLEAdvertisement1SkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
  skeleton->priv->properties = g_new0 (GValue, 12);
  g_value_init (&skeleton->priv->properties[0], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[1], G_TYPE_VARIANT);
  g_value_init (&skeleton->priv->properties[2], G_TYPE_VARIANT);
  g_value_init (&skeleton->priv->properties[3], G_TYPE_STRV);
  g_value_init (&skeleton->priv->properties[4], G_TYPE_VARIANT);
  g_value_init (&skeleton->priv->properties[5], G_TYPE_VARIANT);
  g_value_init (&skeleton->priv->properties[6], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[7], G_TYPE_STRV);
  g_value_init (&skeleton->priv->properties[8], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[9], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[10], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[11], G_TYPE_UINT);
}

static const gchar *
bluez_leadvertisement1_skeleton_get_type_ (BluezLEAdvertisement1 *object)
{
  BluezLEAdvertisement1Skeleton *skeleton = BLUEZ_LEADVERTISEMENT1_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[0]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static GVariant *
bluez_leadvertisement1_skeleton_get_service_uuids (BluezLEAdvertisement1 *object)
{
  BluezLEAdvertisement1Skeleton *skeleton = BLUEZ_LEADVERTISEMENT1_SKELETON (object);
  GVariant *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_variant (&(skeleton->priv->properties[1]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static GVariant *
bluez_leadvertisement1_skeleton_get_manufacturer_data (BluezLEAdvertisement1 *object)
{
  BluezLEAdvertisement1Skeleton *skeleton = BLUEZ_LEADVERTISEMENT1_SKELETON (object);
  GVariant *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_variant (&(skeleton->priv->properties[2]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *const *
bluez_leadvertisement1_skeleton_get_solicit_uuids (BluezLEAdvertisement1 *object)
{
  BluezLEAdvertisement1Skeleton *skeleton = BLUEZ_LEADVERTISEMENT1_SKELETON (object);
  const gchar *const *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boxed (&(skeleton->priv->properties[3]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static GVariant *
bluez_leadvertisement1_skeleton_get_service_data (BluezLEAdvertisement1 *object)
{
  BluezLEAdvertisement1Skeleton *skeleton = BLUEZ_LEADVERTISEMENT1_SKELETON (object);
  GVariant *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_variant (&(skeleton->priv->properties[4]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static GVariant *
bluez_leadvertisement1_skeleton_get_data (BluezLEAdvertisement1 *object)
{
  BluezLEAdvertisement1Skeleton *skeleton = BLUEZ_LEADVERTISEMENT1_SKELETON (object);
  GVariant *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_variant (&(skeleton->priv->properties[5]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
bluez_leadvertisement1_skeleton_get_discoverable (BluezLEAdvertisement1 *object)
{
  BluezLEAdvertisement1Skeleton *skeleton = BLUEZ_LEADVERTISEMENT1_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[6]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *const *
bluez_leadvertisement1_skeleton_get_includes (BluezLEAdvertisement1 *object)
{
  BluezLEAdvertisement1Skeleton *skeleton = BLUEZ_LEADVERTISEMENT1_SKELETON (object);
  const gchar *const *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boxed (&(skeleton->priv->properties[7]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
bluez_leadvertisement1_skeleton_get_local_name (BluezLEAdvertisement1 *object)
{
  BluezLEAdvertisement1Skeleton *skeleton = BLUEZ_LEADVERTISEMENT1_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[8]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint16 
bluez_leadvertisement1_skeleton_get_appearance (BluezLEAdvertisement1 *object)
{
  BluezLEAdvertisement1Skeleton *skeleton = BLUEZ_LEADVERTISEMENT1_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[9]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint 
bluez_leadvertisement1_skeleton_get_min_interval (BluezLEAdvertisement1 *object)
{
  BluezLEAdvertisement1Skeleton *skeleton = BLUEZ_LEADVERTISEMENT1_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[10]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint 
bluez_leadvertisement1_skeleton_get_max_interval (BluezLEAdvertisement1 *object)
{
  BluezLEAdvertisement1Skeleton *skeleton = BLUEZ_LEADVERTISEMENT1_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[11]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static void
bluez_leadvertisement1_skeleton_class_init (BluezLEAdvertisement1SkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = bluez_leadvertisement1_skeleton_finalize;
  gobject_class->get_property = bluez_leadvertisement1_skeleton_get_property;
  gobject_class->set_property = bluez_leadvertisement1_skeleton_set_property;
  gobject_class->notify       = bluez_leadvertisement1_skeleton_notify;


  bluez_leadvertisement1_override_properties (gobject_class, 1);

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = bluez_leadvertisement1_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = bluez_leadvertisement1_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = bluez_leadvertisement1_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = bluez_leadvertisement1_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (BluezLEAdvertisement1SkeletonPrivate));
#endif
}

static void
bluez_leadvertisement1_skeleton_iface_init (BluezLEAdvertisement1Iface *iface)
{
  iface->get_type_ = bluez_leadvertisement1_skeleton_get_type_;
  iface->get_service_uuids = bluez_leadvertisement1_skeleton_get_service_uuids;
  iface->get_manufacturer_data = bluez_leadvertisement1_skeleton_get_manufacturer_data;
  iface->get_solicit_uuids = bluez_leadvertisement1_skeleton_get_solicit_uuids;
  iface->get_service_data = bluez_leadvertisement1_skeleton_get_service_data;
  iface->get_data = bluez_leadvertisement1_skeleton_get_data;
  iface->get_discoverable = bluez_leadvertisement1_skeleton_get_discoverable;
  iface->get_includes = bluez_leadvertisement1_skeleton_get_includes;
  iface->get_local_name = bluez_leadvertisement1_skeleton_get_local_name;
  iface->get_appearance = bluez_leadvertisement1_skeleton_get_appearance;
  iface->get_min_interval = bluez_leadvertisement1_skeleton_get_min_interval;
  iface->get_max_interval = bluez_leadvertisement1_skeleton_get_max_interval;
}

/**
 * bluez_leadvertisement1_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-bluez-LEAdvertisement1.top_of_page">org.bluez.LEAdvertisement1</link>.
 *
 * Returns: (transfer full) (type BluezLEAdvertisement1Skeleton): The skeleton object.
 */
BluezLEAdvertisement1 *
bluez_leadvertisement1_skeleton_new (void)
{
  return BLUEZ_LEADVERTISEMENT1 (g_object_new (BLUEZ_TYPE_LEADVERTISEMENT1_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface org.bluez.LEAdvertisingManager1
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:BluezLEAdvertisingManager1
 * @title: BluezLEAdvertisingManager1
 * @short_description: Generated C code for the org.bluez.LEAdvertisingManager1 D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-bluez-LEAdvertisingManager1.top_of_page">org.bluez.LEAdvertisingManager1</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.bluez.LEAdvertisingManager1 ---- */

static const _ExtendedGDBusArgInfo _bluez_leadvertising_manager1_method_info_register_advertisement_IN_ARG_advertisement =
{
  {
    -1,
    (gchar *) "advertisement",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _bluez_leadvertising_manager1_method_info_register_advertisement_IN_ARG_options =
{
  {
    -1,
    (gchar *) "options",
    (gchar *) "a{sv}",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _bluez_leadvertising_manager1_method_info_register_advertisement_IN_ARG_pointers[] =
{
  &_bluez_leadvertising_manager1_method_info_register_advertisement_IN_ARG_advertisement.parent_struct,
  &_bluez_leadvertising_manager1_method_info_register_advertisement_IN_ARG_options.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _bluez_leadvertising_manager1_method_info_register_advertisement =
{
  {
    -1,
    (gchar *) "RegisterAdvertisement",
    (GDBusArgInfo **) &_bluez_leadvertising_manager1_method_info_register_advertisement_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-register-advertisement",
  FALSE
};

static const _ExtendedGDBusArgInfo _bluez_leadvertising_manager1_method_info_unregister_advertisement_IN_ARG_service =
{
  {
    -1,
    (gchar *) "service",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _bluez_leadvertising_manager1_method_info_unregister_advertisement_IN_ARG_pointers[] =
{
  &_bluez_leadvertising_manager1_method_info_unregister_advertisement_IN_ARG_service.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _bluez_leadvertising_manager1_method_info_unregister_advertisement =
{
  {
    -1,
    (gchar *) "UnregisterAdvertisement",
    (GDBusArgInfo **) &_bluez_leadvertising_manager1_method_info_unregister_advertisement_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-unregister-advertisement",
  FALSE
};

static const GDBusMethodInfo * const _bluez_leadvertising_manager1_method_info_pointers[] =
{
  &_bluez_leadvertising_manager1_method_info_register_advertisement.parent_struct,
  &_bluez_leadvertising_manager1_method_info_unregister_advertisement.parent_struct,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _bluez_leadvertising_manager1_interface_info =
{
  {
    -1,
    (gchar *) "org.bluez.LEAdvertisingManager1",
    (GDBusMethodInfo **) &_bluez_leadvertising_manager1_method_info_pointers,
    NULL,
    NULL,
    NULL
  },
  "leadvertising-manager1",
};


/**
 * bluez_leadvertising_manager1_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-bluez-LEAdvertisingManager1.top_of_page">org.bluez.LEAdvertisingManager1</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
bluez_leadvertising_manager1_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_bluez_leadvertising_manager1_interface_info.parent_struct;
}

/**
 * bluez_leadvertising_manager1_override_properties:
 * @klass: The class structure for a #GObject derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #BluezLEAdvertisingManager1 interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
bluez_leadvertising_manager1_override_properties (GObjectClass *klass G_GNUC_UNUSED, guint property_id_begin)
{
  return property_id_begin - 1;
}



/**
 * BluezLEAdvertisingManager1:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-bluez-LEAdvertisingManager1.top_of_page">org.bluez.LEAdvertisingManager1</link>.
 */

/**
 * BluezLEAdvertisingManager1Iface:
 * @parent_iface: The parent interface.
 * @handle_register_advertisement: Handler for the #BluezLEAdvertisingManager1::handle-register-advertisement signal.
 * @handle_unregister_advertisement: Handler for the #BluezLEAdvertisingManager1::handle-unregister-advertisement signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-bluez-LEAdvertisingManager1.top_of_page">org.bluez.LEAdvertisingManager1</link>.
 */

typedef BluezLEAdvertisingManager1Iface BluezLEAdvertisingManager1Interface;
G_DEFINE_INTERFACE (BluezLEAdvertisingManager1, bluez_leadvertising_manager1, G_TYPE_OBJECT)

static void
bluez_leadvertising_manager1_default_init (BluezLEAdvertisingManager1Iface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * BluezLEAdvertisingManager1::handle-register-advertisement:
   * @object: A #BluezLEAdvertisingManager1.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_advertisement: Argument passed by remote caller.
   * @arg_options: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-bluez-LEAdvertisingManager1.RegisterAdvertisement">RegisterAdvertisement()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call bluez_leadvertising_manager1_complete_register_advertisement() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-register-advertisement",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (BluezLEAdvertisingManager1Iface, handle_register_advertisement),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_VARIANT);

  /**
   * BluezLEAdvertisingManager1::handle-unregister-advertisement:
   * @object: A #BluezLEAdvertisingManager1.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_service: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-bluez-LEAdvertisingManager1.UnregisterAdvertisement">UnregisterAdvertisement()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call bluez_leadvertising_manager1_complete_unregister_advertisement() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-unregister-advertisement",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (BluezLEAdvertisingManager1Iface, handle_unregister_advertisement),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

}

/**
 * bluez_leadvertising_manager1_call_register_advertisement:
 * @proxy: A #BluezLEAdvertisingManager1Proxy.
 * @arg_advertisement: Argument to pass with the method invocation.
 * @arg_options: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-bluez-LEAdvertisingManager1.RegisterAdvertisement">RegisterAdvertisement()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call bluez_leadvertising_manager1_call_register_advertisement_finish() to get the result of the operation.
 *
 * See bluez_leadvertising_manager1_call_register_advertisement_sync() for the synchronous, blocking version of this method.
 */
void
bluez_leadvertising_manager1_call_register_advertisement (
    BluezLEAdvertisingManager1 *proxy,
    const gchar *arg_advertisement,
    GVariant *arg_options,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "RegisterAdvertisement",
    g_variant_new ("(o@a{sv})",
                   arg_advertisement,
                   arg_options),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * bluez_leadvertising_manager1_call_register_advertisement_finish:
 * @proxy: A #BluezLEAdvertisingManager1Proxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_leadvertising_manager1_call_register_advertisement().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with bluez_leadvertising_manager1_call_register_advertisement().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_leadvertising_manager1_call_register_advertisement_finish (
    BluezLEAdvertisingManager1 *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_leadvertising_manager1_call_register_advertisement_sync:
 * @proxy: A #BluezLEAdvertisingManager1Proxy.
 * @arg_advertisement: Argument to pass with the method invocation.
 * @arg_options: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-bluez-LEAdvertisingManager1.RegisterAdvertisement">RegisterAdvertisement()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See bluez_leadvertising_manager1_call_register_advertisement() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_leadvertising_manager1_call_register_advertisement_sync (
    BluezLEAdvertisingManager1 *proxy,
    const gchar *arg_advertisement,
    GVariant *arg_options,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "RegisterAdvertisement",
    g_variant_new ("(o@a{sv})",
                   arg_advertisement,
                   arg_options),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_leadvertising_manager1_call_unregister_advertisement:
 * @proxy: A #BluezLEAdvertisingManager1Proxy.
 * @arg_service: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-bluez-LEAdvertisingManager1.UnregisterAdvertisement">UnregisterAdvertisement()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call bluez_leadvertising_manager1_call_unregister_advertisement_finish() to get the result of the operation.
 *
 * See bluez_leadvertising_manager1_call_unregister_advertisement_sync() for the synchronous, blocking version of this method.
 */
void
bluez_leadvertising_manager1_call_unregister_advertisement (
    BluezLEAdvertisingManager1 *proxy,
    const gchar *arg_service,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "UnregisterAdvertisement",
    g_variant_new ("(o)",
                   arg_service),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * bluez_leadvertising_manager1_call_unregister_advertisement_finish:
 * @proxy: A #BluezLEAdvertisingManager1Proxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_leadvertising_manager1_call_unregister_advertisement().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with bluez_leadvertising_manager1_call_unregister_advertisement().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_leadvertising_manager1_call_unregister_advertisement_finish (
    BluezLEAdvertisingManager1 *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_leadvertising_manager1_call_unregister_advertisement_sync:
 * @proxy: A #BluezLEAdvertisingManager1Proxy.
 * @arg_service: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-bluez-LEAdvertisingManager1.UnregisterAdvertisement">UnregisterAdvertisement()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See bluez_leadvertising_manager1_call_unregister_advertisement() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
bluez_leadvertising_manager1_call_unregister_advertisement_sync (
    BluezLEAdvertisingManager1 *proxy,
    const gchar *arg_service,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "UnregisterAdvertisement",
    g_variant_new ("(o)",
                   arg_service),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * bluez_leadvertising_manager1_complete_register_advertisement:
 * @object: A #BluezLEAdvertisingManager1.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-bluez-LEAdvertisingManager1.RegisterAdvertisement">RegisterAdvertisement()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
bluez_leadvertising_manager1_complete_register_advertisement (
    BluezLEAdvertisingManager1 *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * bluez_leadvertising_manager1_complete_unregister_advertisement:
 * @object: A #BluezLEAdvertisingManager1.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-bluez-LEAdvertisingManager1.UnregisterAdvertisement">UnregisterAdvertisement()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
bluez_leadvertising_manager1_complete_unregister_advertisement (
    BluezLEAdvertisingManager1 *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * BluezLEAdvertisingManager1Proxy:
 *
 * The #BluezLEAdvertisingManager1Proxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * BluezLEAdvertisingManager1ProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #BluezLEAdvertisingManager1Proxy.
 */

struct _BluezLEAdvertisingManager1ProxyPrivate
{
  GData *qdata;
};

static void bluez_leadvertising_manager1_proxy_iface_init (BluezLEAdvertisingManager1Iface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (BluezLEAdvertisingManager1Proxy, bluez_leadvertising_manager1_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (BluezLEAdvertisingManager1Proxy)
                         G_IMPLEMENT_INTERFACE (BLUEZ_TYPE_LEADVERTISING_MANAGER1, bluez_leadvertising_manager1_proxy_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (BluezLEAdvertisingManager1Proxy, bluez_leadvertising_manager1_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (BLUEZ_TYPE_LEADVERTISING_MANAGER1, bluez_leadvertising_manager1_proxy_iface_init))

#endif
static void
bluez_leadvertising_manager1_proxy_finalize (GObject *object)
{
  BluezLEAdvertisingManager1Proxy *proxy = BLUEZ_LEADVERTISING_MANAGER1_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (bluez_leadvertising_manager1_proxy_parent_class)->finalize (object);
}

static void
bluez_leadvertising_manager1_proxy_get_property (GObject      *object G_GNUC_UNUSED,
  guint         prop_id G_GNUC_UNUSED,
  GValue       *value G_GNUC_UNUSED,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
bluez_leadvertising_manager1_proxy_set_property (GObject      *object G_GNUC_UNUSED,
  guint         prop_id G_GNUC_UNUSED,
  const GValue *value G_GNUC_UNUSED,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
bluez_leadvertising_manager1_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  gsize n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_bluez_leadvertising_manager1_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], BLUEZ_TYPE_LEADVERTISING_MANAGER1);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, BLUEZ_TYPE_LEADVERTISING_MANAGER1);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
bluez_leadvertising_manager1_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  BluezLEAdvertisingManager1Proxy *proxy = BLUEZ_LEADVERTISING_MANAGER1_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_leadvertising_manager1_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_leadvertising_manager1_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
bluez_leadvertising_manager1_proxy_init (BluezLEAdvertisingManager1Proxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = bluez_leadvertising_manager1_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, BLUEZ_TYPE_LEADVERTISING_MANAGER1_PROXY, BluezLEAdvertisingManager1ProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), bluez_leadvertising_manager1_interface_info ());
}

static void
bluez_leadvertising_manager1_proxy_class_init (BluezLEAdvertisingManager1ProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = bluez_leadvertising_manager1_proxy_finalize;
  gobject_class->get_property = bluez_leadvertising_manager1_proxy_get_property;
  gobject_class->set_property = bluez_leadvertising_manager1_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = bluez_leadvertising_manager1_proxy_g_signal;
  proxy_class->g_properties_changed = bluez_leadvertising_manager1_proxy_g_properties_changed;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (BluezLEAdvertisingManager1ProxyPrivate));
#endif
}

static void
bluez_leadvertising_manager1_proxy_iface_init (BluezLEAdvertisingManager1Iface *iface G_GNUC_UNUSED)
{
}

/**
 * bluez_leadvertising_manager1_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-bluez-LEAdvertisingManager1.top_of_page">org.bluez.LEAdvertisingManager1</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call bluez_leadvertising_manager1_proxy_new_finish() to get the result of the operation.
 *
 * See bluez_leadvertising_manager1_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
bluez_leadvertising_manager1_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (BLUEZ_TYPE_LEADVERTISING_MANAGER1_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.bluez.LEAdvertisingManager1", NULL);
}

/**
 * bluez_leadvertising_manager1_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_leadvertising_manager1_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with bluez_leadvertising_manager1_proxy_new().
 *
 * Returns: (transfer full) (type BluezLEAdvertisingManager1Proxy): The constructed proxy object or %NULL if @error is set.
 */
BluezLEAdvertisingManager1 *
bluez_leadvertising_manager1_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return BLUEZ_LEADVERTISING_MANAGER1 (ret);
  else
    return NULL;
}

/**
 * bluez_leadvertising_manager1_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-bluez-LEAdvertisingManager1.top_of_page">org.bluez.LEAdvertisingManager1</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See bluez_leadvertising_manager1_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type BluezLEAdvertisingManager1Proxy): The constructed proxy object or %NULL if @error is set.
 */
BluezLEAdvertisingManager1 *
bluez_leadvertising_manager1_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (BLUEZ_TYPE_LEADVERTISING_MANAGER1_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.bluez.LEAdvertisingManager1", NULL);
  if (ret != NULL)
    return BLUEZ_LEADVERTISING_MANAGER1 (ret);
  else
    return NULL;
}


/**
 * bluez_leadvertising_manager1_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like bluez_leadvertising_manager1_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call bluez_leadvertising_manager1_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See bluez_leadvertising_manager1_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
bluez_leadvertising_manager1_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (BLUEZ_TYPE_LEADVERTISING_MANAGER1_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.bluez.LEAdvertisingManager1", NULL);
}

/**
 * bluez_leadvertising_manager1_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_leadvertising_manager1_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with bluez_leadvertising_manager1_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type BluezLEAdvertisingManager1Proxy): The constructed proxy object or %NULL if @error is set.
 */
BluezLEAdvertisingManager1 *
bluez_leadvertising_manager1_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return BLUEZ_LEADVERTISING_MANAGER1 (ret);
  else
    return NULL;
}

/**
 * bluez_leadvertising_manager1_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like bluez_leadvertising_manager1_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See bluez_leadvertising_manager1_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type BluezLEAdvertisingManager1Proxy): The constructed proxy object or %NULL if @error is set.
 */
BluezLEAdvertisingManager1 *
bluez_leadvertising_manager1_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (BLUEZ_TYPE_LEADVERTISING_MANAGER1_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.bluez.LEAdvertisingManager1", NULL);
  if (ret != NULL)
    return BLUEZ_LEADVERTISING_MANAGER1 (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * BluezLEAdvertisingManager1Skeleton:
 *
 * The #BluezLEAdvertisingManager1Skeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * BluezLEAdvertisingManager1SkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #BluezLEAdvertisingManager1Skeleton.
 */

struct _BluezLEAdvertisingManager1SkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_bluez_leadvertising_manager1_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  BluezLEAdvertisingManager1Skeleton *skeleton = BLUEZ_LEADVERTISING_MANAGER1_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  guint num_extra;
  gsize n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], BLUEZ_TYPE_LEADVERTISING_MANAGER1);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, BLUEZ_TYPE_LEADVERTISING_MANAGER1);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_bluez_leadvertising_manager1_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  BluezLEAdvertisingManager1Skeleton *skeleton = BLUEZ_LEADVERTISING_MANAGER1_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_leadvertising_manager1_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_bluez_leadvertising_manager1_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  BluezLEAdvertisingManager1Skeleton *skeleton = BLUEZ_LEADVERTISING_MANAGER1_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_bluez_leadvertising_manager1_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _bluez_leadvertising_manager1_skeleton_vtable =
{
  _bluez_leadvertising_manager1_skeleton_handle_method_call,
  _bluez_leadvertising_manager1_skeleton_handle_get_property,
  _bluez_leadvertising_manager1_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
bluez_leadvertising_manager1_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return bluez_leadvertising_manager1_interface_info ();
}

static GDBusInterfaceVTable *
bluez_leadvertising_manager1_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_bluez_leadvertising_manager1_skeleton_vtable;
}

static GVariant *
bluez_leadvertising_manager1_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  BluezLEAdvertisingManager1Skeleton *skeleton = BLUEZ_LEADVERTISING_MANAGER1_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_bluez_leadvertising_manager1_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _bluez_leadvertising_manager1_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _bluez_leadvertising_manager1_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _bluez_leadvertising_manager1_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.bluez.LEAdvertisingManager1", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static void
bluez_leadvertising_manager1_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton G_GNUC_UNUSED)
{
}

static void bluez_leadvertising_manager1_skeleton_iface_init (BluezLEAdvertisingManager1Iface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (BluezLEAdvertisingManager1Skeleton, bluez_leadvertising_manager1_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (BluezLEAdvertisingManager1Skeleton)
                         G_IMPLEMENT_INTERFACE (BLUEZ_TYPE_LEADVERTISING_MANAGER1, bluez_leadvertising_manager1_skeleton_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (BluezLEAdvertisingManager1Skeleton, bluez_leadvertising_manager1_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (BLUEZ_TYPE_LEADVERTISING_MANAGER1, bluez_leadvertising_manager1_skeleton_iface_init))

#endif
static void
bluez_leadvertising_manager1_skeleton_finalize (GObject *object)
{
  BluezLEAdvertisingManager1Skeleton *skeleton = BLUEZ_LEADVERTISING_MANAGER1_SKELETON (object);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (bluez_leadvertising_manager1_skeleton_parent_class)->finalize (object);
}

static void
bluez_leadvertising_manager1_skeleton_init (BluezLEAdvertisingManager1Skeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = bluez_leadvertising_manager1_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, BLUEZ_TYPE_LEADVERTISING_MANAGER1_SKELETON, BluezLEAdvertisingManager1SkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
bluez_leadvertising_manager1_skeleton_class_init (BluezLEAdvertisingManager1SkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = bluez_leadvertising_manager1_skeleton_finalize;

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = bluez_leadvertising_manager1_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = bluez_leadvertising_manager1_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = bluez_leadvertising_manager1_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = bluez_leadvertising_manager1_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (BluezLEAdvertisingManager1SkeletonPrivate));
#endif
}

static void
bluez_leadvertising_manager1_skeleton_iface_init (BluezLEAdvertisingManager1Iface *iface G_GNUC_UNUSED)
{
}

/**
 * bluez_leadvertising_manager1_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-bluez-LEAdvertisingManager1.top_of_page">org.bluez.LEAdvertisingManager1</link>.
 *
 * Returns: (transfer full) (type BluezLEAdvertisingManager1Skeleton): The skeleton object.
 */
BluezLEAdvertisingManager1 *
bluez_leadvertising_manager1_skeleton_new (void)
{
  return BLUEZ_LEADVERTISING_MANAGER1 (g_object_new (BLUEZ_TYPE_LEADVERTISING_MANAGER1_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for Object, ObjectProxy and ObjectSkeleton
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:BluezObject
 * @title: BluezObject
 * @short_description: Specialized GDBusObject types
 *
 * This section contains the #BluezObject, #BluezObjectProxy, and #BluezObjectSkeleton types which make it easier to work with objects implementing generated types for D-Bus interfaces.
 */

/**
 * BluezObject:
 *
 * The #BluezObject type is a specialized container of interfaces.
 */

/**
 * BluezObjectIface:
 * @parent_iface: The parent interface.
 *
 * Virtual table for the #BluezObject interface.
 */

typedef BluezObjectIface BluezObjectInterface;
G_DEFINE_INTERFACE_WITH_CODE (BluezObject, bluez_object, G_TYPE_OBJECT, g_type_interface_add_prerequisite (g_define_type_id, G_TYPE_DBUS_OBJECT);)

static void
bluez_object_default_init (BluezObjectIface *iface)
{
  /**
   * BluezObject:adapter1:
   *
   * The #BluezAdapter1 instance corresponding to the D-Bus interface <link linkend="gdbus-interface-org-bluez-Adapter1.top_of_page">org.bluez.Adapter1</link>, if any.
   *
   * Connect to the #GObject::notify signal to get informed of property changes.
   */
  g_object_interface_install_property (iface, g_param_spec_object ("adapter1", "adapter1", "adapter1", BLUEZ_TYPE_ADAPTER1, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));

  /**
   * BluezObject:device1:
   *
   * The #BluezDevice1 instance corresponding to the D-Bus interface <link linkend="gdbus-interface-org-bluez-Device1.top_of_page">org.bluez.Device1</link>, if any.
   *
   * Connect to the #GObject::notify signal to get informed of property changes.
   */
  g_object_interface_install_property (iface, g_param_spec_object ("device1", "device1", "device1", BLUEZ_TYPE_DEVICE1, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));

  /**
   * BluezObject:gatt-manager1:
   *
   * The #BluezGattManager1 instance corresponding to the D-Bus interface <link linkend="gdbus-interface-org-bluez-GattManager1.top_of_page">org.bluez.GattManager1</link>, if any.
   *
   * Connect to the #GObject::notify signal to get informed of property changes.
   */
  g_object_interface_install_property (iface, g_param_spec_object ("gatt-manager1", "gatt-manager1", "gatt-manager1", BLUEZ_TYPE_GATT_MANAGER1, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));

  /**
   * BluezObject:gatt-service1:
   *
   * The #BluezGattService1 instance corresponding to the D-Bus interface <link linkend="gdbus-interface-org-bluez-GattService1.top_of_page">org.bluez.GattService1</link>, if any.
   *
   * Connect to the #GObject::notify signal to get informed of property changes.
   */
  g_object_interface_install_property (iface, g_param_spec_object ("gatt-service1", "gatt-service1", "gatt-service1", BLUEZ_TYPE_GATT_SERVICE1, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));

  /**
   * BluezObject:gatt-characteristic1:
   *
   * The #BluezGattCharacteristic1 instance corresponding to the D-Bus interface <link linkend="gdbus-interface-org-bluez-GattCharacteristic1.top_of_page">org.bluez.GattCharacteristic1</link>, if any.
   *
   * Connect to the #GObject::notify signal to get informed of property changes.
   */
  g_object_interface_install_property (iface, g_param_spec_object ("gatt-characteristic1", "gatt-characteristic1", "gatt-characteristic1", BLUEZ_TYPE_GATT_CHARACTERISTIC1, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));

  /**
   * BluezObject:gatt-descriptor1:
   *
   * The #BluezGattDescriptor1 instance corresponding to the D-Bus interface <link linkend="gdbus-interface-org-bluez-GattDescriptor1.top_of_page">org.bluez.GattDescriptor1</link>, if any.
   *
   * Connect to the #GObject::notify signal to get informed of property changes.
   */
  g_object_interface_install_property (iface, g_param_spec_object ("gatt-descriptor1", "gatt-descriptor1", "gatt-descriptor1", BLUEZ_TYPE_GATT_DESCRIPTOR1, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));

  /**
   * BluezObject:leadvertisement1:
   *
   * The #BluezLEAdvertisement1 instance corresponding to the D-Bus interface <link linkend="gdbus-interface-org-bluez-LEAdvertisement1.top_of_page">org.bluez.LEAdvertisement1</link>, if any.
   *
   * Connect to the #GObject::notify signal to get informed of property changes.
   */
  g_object_interface_install_property (iface, g_param_spec_object ("leadvertisement1", "leadvertisement1", "leadvertisement1", BLUEZ_TYPE_LEADVERTISEMENT1, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));

  /**
   * BluezObject:leadvertising-manager1:
   *
   * The #BluezLEAdvertisingManager1 instance corresponding to the D-Bus interface <link linkend="gdbus-interface-org-bluez-LEAdvertisingManager1.top_of_page">org.bluez.LEAdvertisingManager1</link>, if any.
   *
   * Connect to the #GObject::notify signal to get informed of property changes.
   */
  g_object_interface_install_property (iface, g_param_spec_object ("leadvertising-manager1", "leadvertising-manager1", "leadvertising-manager1", BLUEZ_TYPE_LEADVERTISING_MANAGER1, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));

}

/**
 * bluez_object_get_adapter1:
 * @object: A #BluezObject.
 *
 * Gets the #BluezAdapter1 instance for the D-Bus interface <link linkend="gdbus-interface-org-bluez-Adapter1.top_of_page">org.bluez.Adapter1</link> on @object, if any.
 *
 * Returns: (transfer full) (nullable): A #BluezAdapter1 that must be freed with g_object_unref() or %NULL if @object does not implement the interface.
 */
BluezAdapter1 *bluez_object_get_adapter1 (BluezObject *object)
{
  GDBusInterface *ret;
  ret = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.bluez.Adapter1");
  if (ret == NULL)
    return NULL;
  return BLUEZ_ADAPTER1 (ret);
}

/**
 * bluez_object_get_device1:
 * @object: A #BluezObject.
 *
 * Gets the #BluezDevice1 instance for the D-Bus interface <link linkend="gdbus-interface-org-bluez-Device1.top_of_page">org.bluez.Device1</link> on @object, if any.
 *
 * Returns: (transfer full) (nullable): A #BluezDevice1 that must be freed with g_object_unref() or %NULL if @object does not implement the interface.
 */
BluezDevice1 *bluez_object_get_device1 (BluezObject *object)
{
  GDBusInterface *ret;
  ret = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.bluez.Device1");
  if (ret == NULL)
    return NULL;
  return BLUEZ_DEVICE1 (ret);
}

/**
 * bluez_object_get_gatt_manager1:
 * @object: A #BluezObject.
 *
 * Gets the #BluezGattManager1 instance for the D-Bus interface <link linkend="gdbus-interface-org-bluez-GattManager1.top_of_page">org.bluez.GattManager1</link> on @object, if any.
 *
 * Returns: (transfer full) (nullable): A #BluezGattManager1 that must be freed with g_object_unref() or %NULL if @object does not implement the interface.
 */
BluezGattManager1 *bluez_object_get_gatt_manager1 (BluezObject *object)
{
  GDBusInterface *ret;
  ret = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.bluez.GattManager1");
  if (ret == NULL)
    return NULL;
  return BLUEZ_GATT_MANAGER1 (ret);
}

/**
 * bluez_object_get_gatt_service1:
 * @object: A #BluezObject.
 *
 * Gets the #BluezGattService1 instance for the D-Bus interface <link linkend="gdbus-interface-org-bluez-GattService1.top_of_page">org.bluez.GattService1</link> on @object, if any.
 *
 * Returns: (transfer full) (nullable): A #BluezGattService1 that must be freed with g_object_unref() or %NULL if @object does not implement the interface.
 */
BluezGattService1 *bluez_object_get_gatt_service1 (BluezObject *object)
{
  GDBusInterface *ret;
  ret = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.bluez.GattService1");
  if (ret == NULL)
    return NULL;
  return BLUEZ_GATT_SERVICE1 (ret);
}

/**
 * bluez_object_get_gatt_characteristic1:
 * @object: A #BluezObject.
 *
 * Gets the #BluezGattCharacteristic1 instance for the D-Bus interface <link linkend="gdbus-interface-org-bluez-GattCharacteristic1.top_of_page">org.bluez.GattCharacteristic1</link> on @object, if any.
 *
 * Returns: (transfer full) (nullable): A #BluezGattCharacteristic1 that must be freed with g_object_unref() or %NULL if @object does not implement the interface.
 */
BluezGattCharacteristic1 *bluez_object_get_gatt_characteristic1 (BluezObject *object)
{
  GDBusInterface *ret;
  ret = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.bluez.GattCharacteristic1");
  if (ret == NULL)
    return NULL;
  return BLUEZ_GATT_CHARACTERISTIC1 (ret);
}

/**
 * bluez_object_get_gatt_descriptor1:
 * @object: A #BluezObject.
 *
 * Gets the #BluezGattDescriptor1 instance for the D-Bus interface <link linkend="gdbus-interface-org-bluez-GattDescriptor1.top_of_page">org.bluez.GattDescriptor1</link> on @object, if any.
 *
 * Returns: (transfer full) (nullable): A #BluezGattDescriptor1 that must be freed with g_object_unref() or %NULL if @object does not implement the interface.
 */
BluezGattDescriptor1 *bluez_object_get_gatt_descriptor1 (BluezObject *object)
{
  GDBusInterface *ret;
  ret = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.bluez.GattDescriptor1");
  if (ret == NULL)
    return NULL;
  return BLUEZ_GATT_DESCRIPTOR1 (ret);
}

/**
 * bluez_object_get_leadvertisement1:
 * @object: A #BluezObject.
 *
 * Gets the #BluezLEAdvertisement1 instance for the D-Bus interface <link linkend="gdbus-interface-org-bluez-LEAdvertisement1.top_of_page">org.bluez.LEAdvertisement1</link> on @object, if any.
 *
 * Returns: (transfer full) (nullable): A #BluezLEAdvertisement1 that must be freed with g_object_unref() or %NULL if @object does not implement the interface.
 */
BluezLEAdvertisement1 *bluez_object_get_leadvertisement1 (BluezObject *object)
{
  GDBusInterface *ret;
  ret = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.bluez.LEAdvertisement1");
  if (ret == NULL)
    return NULL;
  return BLUEZ_LEADVERTISEMENT1 (ret);
}

/**
 * bluez_object_get_leadvertising_manager1:
 * @object: A #BluezObject.
 *
 * Gets the #BluezLEAdvertisingManager1 instance for the D-Bus interface <link linkend="gdbus-interface-org-bluez-LEAdvertisingManager1.top_of_page">org.bluez.LEAdvertisingManager1</link> on @object, if any.
 *
 * Returns: (transfer full) (nullable): A #BluezLEAdvertisingManager1 that must be freed with g_object_unref() or %NULL if @object does not implement the interface.
 */
BluezLEAdvertisingManager1 *bluez_object_get_leadvertising_manager1 (BluezObject *object)
{
  GDBusInterface *ret;
  ret = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.bluez.LEAdvertisingManager1");
  if (ret == NULL)
    return NULL;
  return BLUEZ_LEADVERTISING_MANAGER1 (ret);
}


/**
 * bluez_object_peek_adapter1: (skip)
 * @object: A #BluezObject.
 *
 * Like bluez_object_get_adapter1() but doesn't increase the reference count on the returned object.
 *
 * It is not safe to use the returned object if you are on another thread than the one where the #GDBusObjectManagerClient or #GDBusObjectManagerServer for @object is running.
 *
 * Returns: (transfer none) (nullable): A #BluezAdapter1 or %NULL if @object does not implement the interface. Do not free the returned object, it is owned by @object.
 */
BluezAdapter1 *bluez_object_peek_adapter1 (BluezObject *object)
{
  GDBusInterface *ret;
  ret = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.bluez.Adapter1");
  if (ret == NULL)
    return NULL;
  g_object_unref (ret);
  return BLUEZ_ADAPTER1 (ret);
}

/**
 * bluez_object_peek_device1: (skip)
 * @object: A #BluezObject.
 *
 * Like bluez_object_get_device1() but doesn't increase the reference count on the returned object.
 *
 * It is not safe to use the returned object if you are on another thread than the one where the #GDBusObjectManagerClient or #GDBusObjectManagerServer for @object is running.
 *
 * Returns: (transfer none) (nullable): A #BluezDevice1 or %NULL if @object does not implement the interface. Do not free the returned object, it is owned by @object.
 */
BluezDevice1 *bluez_object_peek_device1 (BluezObject *object)
{
  GDBusInterface *ret;
  ret = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.bluez.Device1");
  if (ret == NULL)
    return NULL;
  g_object_unref (ret);
  return BLUEZ_DEVICE1 (ret);
}

/**
 * bluez_object_peek_gatt_manager1: (skip)
 * @object: A #BluezObject.
 *
 * Like bluez_object_get_gatt_manager1() but doesn't increase the reference count on the returned object.
 *
 * It is not safe to use the returned object if you are on another thread than the one where the #GDBusObjectManagerClient or #GDBusObjectManagerServer for @object is running.
 *
 * Returns: (transfer none) (nullable): A #BluezGattManager1 or %NULL if @object does not implement the interface. Do not free the returned object, it is owned by @object.
 */
BluezGattManager1 *bluez_object_peek_gatt_manager1 (BluezObject *object)
{
  GDBusInterface *ret;
  ret = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.bluez.GattManager1");
  if (ret == NULL)
    return NULL;
  g_object_unref (ret);
  return BLUEZ_GATT_MANAGER1 (ret);
}

/**
 * bluez_object_peek_gatt_service1: (skip)
 * @object: A #BluezObject.
 *
 * Like bluez_object_get_gatt_service1() but doesn't increase the reference count on the returned object.
 *
 * It is not safe to use the returned object if you are on another thread than the one where the #GDBusObjectManagerClient or #GDBusObjectManagerServer for @object is running.
 *
 * Returns: (transfer none) (nullable): A #BluezGattService1 or %NULL if @object does not implement the interface. Do not free the returned object, it is owned by @object.
 */
BluezGattService1 *bluez_object_peek_gatt_service1 (BluezObject *object)
{
  GDBusInterface *ret;
  ret = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.bluez.GattService1");
  if (ret == NULL)
    return NULL;
  g_object_unref (ret);
  return BLUEZ_GATT_SERVICE1 (ret);
}

/**
 * bluez_object_peek_gatt_characteristic1: (skip)
 * @object: A #BluezObject.
 *
 * Like bluez_object_get_gatt_characteristic1() but doesn't increase the reference count on the returned object.
 *
 * It is not safe to use the returned object if you are on another thread than the one where the #GDBusObjectManagerClient or #GDBusObjectManagerServer for @object is running.
 *
 * Returns: (transfer none) (nullable): A #BluezGattCharacteristic1 or %NULL if @object does not implement the interface. Do not free the returned object, it is owned by @object.
 */
BluezGattCharacteristic1 *bluez_object_peek_gatt_characteristic1 (BluezObject *object)
{
  GDBusInterface *ret;
  ret = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.bluez.GattCharacteristic1");
  if (ret == NULL)
    return NULL;
  g_object_unref (ret);
  return BLUEZ_GATT_CHARACTERISTIC1 (ret);
}

/**
 * bluez_object_peek_gatt_descriptor1: (skip)
 * @object: A #BluezObject.
 *
 * Like bluez_object_get_gatt_descriptor1() but doesn't increase the reference count on the returned object.
 *
 * It is not safe to use the returned object if you are on another thread than the one where the #GDBusObjectManagerClient or #GDBusObjectManagerServer for @object is running.
 *
 * Returns: (transfer none) (nullable): A #BluezGattDescriptor1 or %NULL if @object does not implement the interface. Do not free the returned object, it is owned by @object.
 */
BluezGattDescriptor1 *bluez_object_peek_gatt_descriptor1 (BluezObject *object)
{
  GDBusInterface *ret;
  ret = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.bluez.GattDescriptor1");
  if (ret == NULL)
    return NULL;
  g_object_unref (ret);
  return BLUEZ_GATT_DESCRIPTOR1 (ret);
}

/**
 * bluez_object_peek_leadvertisement1: (skip)
 * @object: A #BluezObject.
 *
 * Like bluez_object_get_leadvertisement1() but doesn't increase the reference count on the returned object.
 *
 * It is not safe to use the returned object if you are on another thread than the one where the #GDBusObjectManagerClient or #GDBusObjectManagerServer for @object is running.
 *
 * Returns: (transfer none) (nullable): A #BluezLEAdvertisement1 or %NULL if @object does not implement the interface. Do not free the returned object, it is owned by @object.
 */
BluezLEAdvertisement1 *bluez_object_peek_leadvertisement1 (BluezObject *object)
{
  GDBusInterface *ret;
  ret = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.bluez.LEAdvertisement1");
  if (ret == NULL)
    return NULL;
  g_object_unref (ret);
  return BLUEZ_LEADVERTISEMENT1 (ret);
}

/**
 * bluez_object_peek_leadvertising_manager1: (skip)
 * @object: A #BluezObject.
 *
 * Like bluez_object_get_leadvertising_manager1() but doesn't increase the reference count on the returned object.
 *
 * It is not safe to use the returned object if you are on another thread than the one where the #GDBusObjectManagerClient or #GDBusObjectManagerServer for @object is running.
 *
 * Returns: (transfer none) (nullable): A #BluezLEAdvertisingManager1 or %NULL if @object does not implement the interface. Do not free the returned object, it is owned by @object.
 */
BluezLEAdvertisingManager1 *bluez_object_peek_leadvertising_manager1 (BluezObject *object)
{
  GDBusInterface *ret;
  ret = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.bluez.LEAdvertisingManager1");
  if (ret == NULL)
    return NULL;
  g_object_unref (ret);
  return BLUEZ_LEADVERTISING_MANAGER1 (ret);
}


static void
bluez_object_notify (GDBusObject *object, GDBusInterface *interface)
{
  _ExtendedGDBusInterfaceInfo *info = (_ExtendedGDBusInterfaceInfo *) g_dbus_interface_get_info (interface);
  /* info can be NULL if the other end is using a D-Bus interface we don't know
   * anything about, for example old generated code in this process talking to
   * newer generated code in the other process. */
  if (info != NULL)
    g_object_notify (G_OBJECT (object), info->hyphen_name);
}

/**
 * BluezObjectProxy:
 *
 * The #BluezObjectProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * BluezObjectProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #BluezObjectProxy.
 */

static void
bluez_object_proxy__bluez_object_iface_init (BluezObjectIface *iface G_GNUC_UNUSED)
{
}

static void
bluez_object_proxy__g_dbus_object_iface_init (GDBusObjectIface *iface)
{
  iface->interface_added = bluez_object_notify;
  iface->interface_removed = bluez_object_notify;
}


G_DEFINE_TYPE_WITH_CODE (BluezObjectProxy, bluez_object_proxy, G_TYPE_DBUS_OBJECT_PROXY,
                         G_IMPLEMENT_INTERFACE (BLUEZ_TYPE_OBJECT, bluez_object_proxy__bluez_object_iface_init)
                         G_IMPLEMENT_INTERFACE (G_TYPE_DBUS_OBJECT, bluez_object_proxy__g_dbus_object_iface_init))

static void
bluez_object_proxy_init (BluezObjectProxy *object G_GNUC_UNUSED)
{
}

static void
bluez_object_proxy_set_property (GObject      *gobject,
  guint         prop_id,
  const GValue *value G_GNUC_UNUSED,
  GParamSpec   *pspec)
{
  G_OBJECT_WARN_INVALID_PROPERTY_ID (gobject, prop_id, pspec);
}

static void
bluez_object_proxy_get_property (GObject      *gobject,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec)
{
  BluezObjectProxy *object = BLUEZ_OBJECT_PROXY (gobject);
  GDBusInterface *interface;

  switch (prop_id)
    {
    case 1:
      interface = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.bluez.Adapter1");
      g_value_take_object (value, interface);
      break;

    case 2:
      interface = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.bluez.Device1");
      g_value_take_object (value, interface);
      break;

    case 3:
      interface = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.bluez.GattManager1");
      g_value_take_object (value, interface);
      break;

    case 4:
      interface = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.bluez.GattService1");
      g_value_take_object (value, interface);
      break;

    case 5:
      interface = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.bluez.GattCharacteristic1");
      g_value_take_object (value, interface);
      break;

    case 6:
      interface = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.bluez.GattDescriptor1");
      g_value_take_object (value, interface);
      break;

    case 7:
      interface = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.bluez.LEAdvertisement1");
      g_value_take_object (value, interface);
      break;

    case 8:
      interface = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.bluez.LEAdvertisingManager1");
      g_value_take_object (value, interface);
      break;

    default:
      G_OBJECT_WARN_INVALID_PROPERTY_ID (gobject, prop_id, pspec);
      break;
  }
}

static void
bluez_object_proxy_class_init (BluezObjectProxyClass *klass)
{
  GObjectClass *gobject_class = G_OBJECT_CLASS (klass);

  gobject_class->set_property = bluez_object_proxy_set_property;
  gobject_class->get_property = bluez_object_proxy_get_property;

  g_object_class_override_property (gobject_class, 1, "adapter1");
  g_object_class_override_property (gobject_class, 2, "device1");
  g_object_class_override_property (gobject_class, 3, "gatt-manager1");
  g_object_class_override_property (gobject_class, 4, "gatt-service1");
  g_object_class_override_property (gobject_class, 5, "gatt-characteristic1");
  g_object_class_override_property (gobject_class, 6, "gatt-descriptor1");
  g_object_class_override_property (gobject_class, 7, "leadvertisement1");
  g_object_class_override_property (gobject_class, 8, "leadvertising-manager1");
}

/**
 * bluez_object_proxy_new:
 * @connection: A #GDBusConnection.
 * @object_path: An object path.
 *
 * Creates a new proxy object.
 *
 * Returns: (transfer full): The proxy object.
 */
BluezObjectProxy *
bluez_object_proxy_new (GDBusConnection *connection,
  const gchar *object_path)
{
  g_return_val_if_fail (G_IS_DBUS_CONNECTION (connection), NULL);
  g_return_val_if_fail (g_variant_is_object_path (object_path), NULL);
  return BLUEZ_OBJECT_PROXY (g_object_new (BLUEZ_TYPE_OBJECT_PROXY, "g-connection", connection, "g-object-path", object_path, NULL));
}

/**
 * BluezObjectSkeleton:
 *
 * The #BluezObjectSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * BluezObjectSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #BluezObjectSkeleton.
 */

static void
bluez_object_skeleton__bluez_object_iface_init (BluezObjectIface *iface G_GNUC_UNUSED)
{
}


static void
bluez_object_skeleton__g_dbus_object_iface_init (GDBusObjectIface *iface)
{
  iface->interface_added = bluez_object_notify;
  iface->interface_removed = bluez_object_notify;
}

G_DEFINE_TYPE_WITH_CODE (BluezObjectSkeleton, bluez_object_skeleton, G_TYPE_DBUS_OBJECT_SKELETON,
                         G_IMPLEMENT_INTERFACE (BLUEZ_TYPE_OBJECT, bluez_object_skeleton__bluez_object_iface_init)
                         G_IMPLEMENT_INTERFACE (G_TYPE_DBUS_OBJECT, bluez_object_skeleton__g_dbus_object_iface_init))

static void
bluez_object_skeleton_init (BluezObjectSkeleton *object G_GNUC_UNUSED)
{
}

static void
bluez_object_skeleton_set_property (GObject      *gobject,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  BluezObjectSkeleton *object = BLUEZ_OBJECT_SKELETON (gobject);
  GDBusInterfaceSkeleton *interface;

  switch (prop_id)
    {
    case 1:
      interface = g_value_get_object (value);
      if (interface != NULL)
        {
          g_warn_if_fail (BLUEZ_IS_ADAPTER1 (interface));
          g_dbus_object_skeleton_add_interface (G_DBUS_OBJECT_SKELETON (object), interface);
        }
      else
        {
          g_dbus_object_skeleton_remove_interface_by_name (G_DBUS_OBJECT_SKELETON (object), "org.bluez.Adapter1");
        }
      break;

    case 2:
      interface = g_value_get_object (value);
      if (interface != NULL)
        {
          g_warn_if_fail (BLUEZ_IS_DEVICE1 (interface));
          g_dbus_object_skeleton_add_interface (G_DBUS_OBJECT_SKELETON (object), interface);
        }
      else
        {
          g_dbus_object_skeleton_remove_interface_by_name (G_DBUS_OBJECT_SKELETON (object), "org.bluez.Device1");
        }
      break;

    case 3:
      interface = g_value_get_object (value);
      if (interface != NULL)
        {
          g_warn_if_fail (BLUEZ_IS_GATT_MANAGER1 (interface));
          g_dbus_object_skeleton_add_interface (G_DBUS_OBJECT_SKELETON (object), interface);
        }
      else
        {
          g_dbus_object_skeleton_remove_interface_by_name (G_DBUS_OBJECT_SKELETON (object), "org.bluez.GattManager1");
        }
      break;

    case 4:
      interface = g_value_get_object (value);
      if (interface != NULL)
        {
          g_warn_if_fail (BLUEZ_IS_GATT_SERVICE1 (interface));
          g_dbus_object_skeleton_add_interface (G_DBUS_OBJECT_SKELETON (object), interface);
        }
      else
        {
          g_dbus_object_skeleton_remove_interface_by_name (G_DBUS_OBJECT_SKELETON (object), "org.bluez.GattService1");
        }
      break;

    case 5:
      interface = g_value_get_object (value);
      if (interface != NULL)
        {
          g_warn_if_fail (BLUEZ_IS_GATT_CHARACTERISTIC1 (interface));
          g_dbus_object_skeleton_add_interface (G_DBUS_OBJECT_SKELETON (object), interface);
        }
      else
        {
          g_dbus_object_skeleton_remove_interface_by_name (G_DBUS_OBJECT_SKELETON (object), "org.bluez.GattCharacteristic1");
        }
      break;

    case 6:
      interface = g_value_get_object (value);
      if (interface != NULL)
        {
          g_warn_if_fail (BLUEZ_IS_GATT_DESCRIPTOR1 (interface));
          g_dbus_object_skeleton_add_interface (G_DBUS_OBJECT_SKELETON (object), interface);
        }
      else
        {
          g_dbus_object_skeleton_remove_interface_by_name (G_DBUS_OBJECT_SKELETON (object), "org.bluez.GattDescriptor1");
        }
      break;

    case 7:
      interface = g_value_get_object (value);
      if (interface != NULL)
        {
          g_warn_if_fail (BLUEZ_IS_LEADVERTISEMENT1 (interface));
          g_dbus_object_skeleton_add_interface (G_DBUS_OBJECT_SKELETON (object), interface);
        }
      else
        {
          g_dbus_object_skeleton_remove_interface_by_name (G_DBUS_OBJECT_SKELETON (object), "org.bluez.LEAdvertisement1");
        }
      break;

    case 8:
      interface = g_value_get_object (value);
      if (interface != NULL)
        {
          g_warn_if_fail (BLUEZ_IS_LEADVERTISING_MANAGER1 (interface));
          g_dbus_object_skeleton_add_interface (G_DBUS_OBJECT_SKELETON (object), interface);
        }
      else
        {
          g_dbus_object_skeleton_remove_interface_by_name (G_DBUS_OBJECT_SKELETON (object), "org.bluez.LEAdvertisingManager1");
        }
      break;

    default:
      G_OBJECT_WARN_INVALID_PROPERTY_ID (gobject, prop_id, pspec);
      break;
  }
}

static void
bluez_object_skeleton_get_property (GObject      *gobject,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec)
{
  BluezObjectSkeleton *object = BLUEZ_OBJECT_SKELETON (gobject);
  GDBusInterface *interface;

  switch (prop_id)
    {
    case 1:
      interface = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.bluez.Adapter1");
      g_value_take_object (value, interface);
      break;

    case 2:
      interface = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.bluez.Device1");
      g_value_take_object (value, interface);
      break;

    case 3:
      interface = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.bluez.GattManager1");
      g_value_take_object (value, interface);
      break;

    case 4:
      interface = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.bluez.GattService1");
      g_value_take_object (value, interface);
      break;

    case 5:
      interface = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.bluez.GattCharacteristic1");
      g_value_take_object (value, interface);
      break;

    case 6:
      interface = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.bluez.GattDescriptor1");
      g_value_take_object (value, interface);
      break;

    case 7:
      interface = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.bluez.LEAdvertisement1");
      g_value_take_object (value, interface);
      break;

    case 8:
      interface = g_dbus_object_get_interface (G_DBUS_OBJECT (object), "org.bluez.LEAdvertisingManager1");
      g_value_take_object (value, interface);
      break;

    default:
      G_OBJECT_WARN_INVALID_PROPERTY_ID (gobject, prop_id, pspec);
      break;
  }
}

static void
bluez_object_skeleton_class_init (BluezObjectSkeletonClass *klass)
{
  GObjectClass *gobject_class = G_OBJECT_CLASS (klass);

  gobject_class->set_property = bluez_object_skeleton_set_property;
  gobject_class->get_property = bluez_object_skeleton_get_property;

  g_object_class_override_property (gobject_class, 1, "adapter1");
  g_object_class_override_property (gobject_class, 2, "device1");
  g_object_class_override_property (gobject_class, 3, "gatt-manager1");
  g_object_class_override_property (gobject_class, 4, "gatt-service1");
  g_object_class_override_property (gobject_class, 5, "gatt-characteristic1");
  g_object_class_override_property (gobject_class, 6, "gatt-descriptor1");
  g_object_class_override_property (gobject_class, 7, "leadvertisement1");
  g_object_class_override_property (gobject_class, 8, "leadvertising-manager1");
}

/**
 * bluez_object_skeleton_new:
 * @object_path: An object path.
 *
 * Creates a new skeleton object.
 *
 * Returns: (transfer full): The skeleton object.
 */
BluezObjectSkeleton *
bluez_object_skeleton_new (const gchar *object_path)
{
  g_return_val_if_fail (g_variant_is_object_path (object_path), NULL);
  return BLUEZ_OBJECT_SKELETON (g_object_new (BLUEZ_TYPE_OBJECT_SKELETON, "g-object-path", object_path, NULL));
}

/**
 * bluez_object_skeleton_set_adapter1:
 * @object: A #BluezObjectSkeleton.
 * @interface_: (nullable): A #BluezAdapter1 or %NULL to clear the interface.
 *
 * Sets the #BluezAdapter1 instance for the D-Bus interface <link linkend="gdbus-interface-org-bluez-Adapter1.top_of_page">org.bluez.Adapter1</link> on @object.
 */
void bluez_object_skeleton_set_adapter1 (BluezObjectSkeleton *object, BluezAdapter1 *interface_)
{
  g_object_set (G_OBJECT (object), "adapter1", interface_, NULL);
}

/**
 * bluez_object_skeleton_set_device1:
 * @object: A #BluezObjectSkeleton.
 * @interface_: (nullable): A #BluezDevice1 or %NULL to clear the interface.
 *
 * Sets the #BluezDevice1 instance for the D-Bus interface <link linkend="gdbus-interface-org-bluez-Device1.top_of_page">org.bluez.Device1</link> on @object.
 */
void bluez_object_skeleton_set_device1 (BluezObjectSkeleton *object, BluezDevice1 *interface_)
{
  g_object_set (G_OBJECT (object), "device1", interface_, NULL);
}

/**
 * bluez_object_skeleton_set_gatt_manager1:
 * @object: A #BluezObjectSkeleton.
 * @interface_: (nullable): A #BluezGattManager1 or %NULL to clear the interface.
 *
 * Sets the #BluezGattManager1 instance for the D-Bus interface <link linkend="gdbus-interface-org-bluez-GattManager1.top_of_page">org.bluez.GattManager1</link> on @object.
 */
void bluez_object_skeleton_set_gatt_manager1 (BluezObjectSkeleton *object, BluezGattManager1 *interface_)
{
  g_object_set (G_OBJECT (object), "gatt-manager1", interface_, NULL);
}

/**
 * bluez_object_skeleton_set_gatt_service1:
 * @object: A #BluezObjectSkeleton.
 * @interface_: (nullable): A #BluezGattService1 or %NULL to clear the interface.
 *
 * Sets the #BluezGattService1 instance for the D-Bus interface <link linkend="gdbus-interface-org-bluez-GattService1.top_of_page">org.bluez.GattService1</link> on @object.
 */
void bluez_object_skeleton_set_gatt_service1 (BluezObjectSkeleton *object, BluezGattService1 *interface_)
{
  g_object_set (G_OBJECT (object), "gatt-service1", interface_, NULL);
}

/**
 * bluez_object_skeleton_set_gatt_characteristic1:
 * @object: A #BluezObjectSkeleton.
 * @interface_: (nullable): A #BluezGattCharacteristic1 or %NULL to clear the interface.
 *
 * Sets the #BluezGattCharacteristic1 instance for the D-Bus interface <link linkend="gdbus-interface-org-bluez-GattCharacteristic1.top_of_page">org.bluez.GattCharacteristic1</link> on @object.
 */
void bluez_object_skeleton_set_gatt_characteristic1 (BluezObjectSkeleton *object, BluezGattCharacteristic1 *interface_)
{
  g_object_set (G_OBJECT (object), "gatt-characteristic1", interface_, NULL);
}

/**
 * bluez_object_skeleton_set_gatt_descriptor1:
 * @object: A #BluezObjectSkeleton.
 * @interface_: (nullable): A #BluezGattDescriptor1 or %NULL to clear the interface.
 *
 * Sets the #BluezGattDescriptor1 instance for the D-Bus interface <link linkend="gdbus-interface-org-bluez-GattDescriptor1.top_of_page">org.bluez.GattDescriptor1</link> on @object.
 */
void bluez_object_skeleton_set_gatt_descriptor1 (BluezObjectSkeleton *object, BluezGattDescriptor1 *interface_)
{
  g_object_set (G_OBJECT (object), "gatt-descriptor1", interface_, NULL);
}

/**
 * bluez_object_skeleton_set_leadvertisement1:
 * @object: A #BluezObjectSkeleton.
 * @interface_: (nullable): A #BluezLEAdvertisement1 or %NULL to clear the interface.
 *
 * Sets the #BluezLEAdvertisement1 instance for the D-Bus interface <link linkend="gdbus-interface-org-bluez-LEAdvertisement1.top_of_page">org.bluez.LEAdvertisement1</link> on @object.
 */
void bluez_object_skeleton_set_leadvertisement1 (BluezObjectSkeleton *object, BluezLEAdvertisement1 *interface_)
{
  g_object_set (G_OBJECT (object), "leadvertisement1", interface_, NULL);
}

/**
 * bluez_object_skeleton_set_leadvertising_manager1:
 * @object: A #BluezObjectSkeleton.
 * @interface_: (nullable): A #BluezLEAdvertisingManager1 or %NULL to clear the interface.
 *
 * Sets the #BluezLEAdvertisingManager1 instance for the D-Bus interface <link linkend="gdbus-interface-org-bluez-LEAdvertisingManager1.top_of_page">org.bluez.LEAdvertisingManager1</link> on @object.
 */
void bluez_object_skeleton_set_leadvertising_manager1 (BluezObjectSkeleton *object, BluezLEAdvertisingManager1 *interface_)
{
  g_object_set (G_OBJECT (object), "leadvertising-manager1", interface_, NULL);
}


/* ------------------------------------------------------------------------
 * Code for ObjectManager client
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:BluezObjectManagerClient
 * @title: BluezObjectManagerClient
 * @short_description: Generated GDBusObjectManagerClient type
 *
 * This section contains a #GDBusObjectManagerClient that uses bluez_object_manager_client_get_proxy_type() as the #GDBusProxyTypeFunc.
 */

/**
 * BluezObjectManagerClient:
 *
 * The #BluezObjectManagerClient structure contains only private data and should only be accessed using the provided API.
 */

/**
 * BluezObjectManagerClientClass:
 * @parent_class: The parent class.
 *
 * Class structure for #BluezObjectManagerClient.
 */

G_DEFINE_TYPE (BluezObjectManagerClient, bluez_object_manager_client, G_TYPE_DBUS_OBJECT_MANAGER_CLIENT)

static void
bluez_object_manager_client_init (BluezObjectManagerClient *manager G_GNUC_UNUSED)
{
}

static void
bluez_object_manager_client_class_init (BluezObjectManagerClientClass *klass G_GNUC_UNUSED)
{
}

/**
 * bluez_object_manager_client_get_proxy_type:
 * @manager: A #GDBusObjectManagerClient.
 * @object_path: The object path of the remote object (unused).
 * @interface_name: (nullable): Interface name of the remote object or %NULL to get the object proxy #GType.
 * @user_data: User data (unused).
 *
 * A #GDBusProxyTypeFunc that maps @interface_name to the generated #GDBusObjectProxy derived and #GDBusProxy derived types.
 *
 * Returns: A #GDBusProxy derived #GType if @interface_name is not %NULL, otherwise the #GType for #BluezObjectProxy.
 */
GType
bluez_object_manager_client_get_proxy_type (GDBusObjectManagerClient *manager G_GNUC_UNUSED, const gchar *object_path G_GNUC_UNUSED, const gchar *interface_name, gpointer user_data G_GNUC_UNUSED)
{
  static gsize once_init_value = 0;
  static GHashTable *lookup_hash;
  GType ret;

  if (interface_name == NULL)
    return BLUEZ_TYPE_OBJECT_PROXY;
  if (g_once_init_enter (&once_init_value))
    {
      lookup_hash = g_hash_table_new (g_str_hash, g_str_equal);
      g_hash_table_insert (lookup_hash, (gpointer) "org.bluez.Adapter1", GSIZE_TO_POINTER (BLUEZ_TYPE_ADAPTER1_PROXY));
      g_hash_table_insert (lookup_hash, (gpointer) "org.bluez.Device1", GSIZE_TO_POINTER (BLUEZ_TYPE_DEVICE1_PROXY));
      g_hash_table_insert (lookup_hash, (gpointer) "org.bluez.GattManager1", GSIZE_TO_POINTER (BLUEZ_TYPE_GATT_MANAGER1_PROXY));
      g_hash_table_insert (lookup_hash, (gpointer) "org.bluez.GattService1", GSIZE_TO_POINTER (BLUEZ_TYPE_GATT_SERVICE1_PROXY));
      g_hash_table_insert (lookup_hash, (gpointer) "org.bluez.GattCharacteristic1", GSIZE_TO_POINTER (BLUEZ_TYPE_GATT_CHARACTERISTIC1_PROXY));
      g_hash_table_insert (lookup_hash, (gpointer) "org.bluez.GattDescriptor1", GSIZE_TO_POINTER (BLUEZ_TYPE_GATT_DESCRIPTOR1_PROXY));
      g_hash_table_insert (lookup_hash, (gpointer) "org.bluez.LEAdvertisement1", GSIZE_TO_POINTER (BLUEZ_TYPE_LEADVERTISEMENT1_PROXY));
      g_hash_table_insert (lookup_hash, (gpointer) "org.bluez.LEAdvertisingManager1", GSIZE_TO_POINTER (BLUEZ_TYPE_LEADVERTISING_MANAGER1_PROXY));
      g_once_init_leave (&once_init_value, 1);
    }
  ret = (GType) GPOINTER_TO_SIZE (g_hash_table_lookup (lookup_hash, interface_name));
  if (ret == (GType) 0)
    ret = G_TYPE_DBUS_PROXY;
  return ret;
}

/**
 * bluez_object_manager_client_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusObjectManagerClientFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates #GDBusObjectManagerClient using bluez_object_manager_client_get_proxy_type() as the #GDBusProxyTypeFunc. See g_dbus_object_manager_client_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call bluez_object_manager_client_new_finish() to get the result of the operation.
 *
 * See bluez_object_manager_client_new_sync() for the synchronous, blocking version of this constructor.
 */
void
bluez_object_manager_client_new (
    GDBusConnection        *connection,
    GDBusObjectManagerClientFlags  flags,
    const gchar            *name,
    const gchar            *object_path,
    GCancellable           *cancellable,
    GAsyncReadyCallback     callback,
    gpointer                user_data)
{
  g_async_initable_new_async (BLUEZ_TYPE_OBJECT_MANAGER_CLIENT, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "flags", flags, "name", name, "connection", connection, "object-path", object_path, "get-proxy-type-func", bluez_object_manager_client_get_proxy_type, NULL);
}

/**
 * bluez_object_manager_client_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_object_manager_client_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with bluez_object_manager_client_new().
 *
 * Returns: (transfer full) (type BluezObjectManagerClient): The constructed object manager client or %NULL if @error is set.
 */
GDBusObjectManager *
bluez_object_manager_client_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return G_DBUS_OBJECT_MANAGER (ret);
  else
    return NULL;
}

/**
 * bluez_object_manager_client_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusObjectManagerClientFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates #GDBusObjectManagerClient using bluez_object_manager_client_get_proxy_type() as the #GDBusProxyTypeFunc. See g_dbus_object_manager_client_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See bluez_object_manager_client_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type BluezObjectManagerClient): The constructed object manager client or %NULL if @error is set.
 */
GDBusObjectManager *
bluez_object_manager_client_new_sync (
    GDBusConnection        *connection,
    GDBusObjectManagerClientFlags  flags,
    const gchar            *name,
    const gchar            *object_path,
    GCancellable           *cancellable,
    GError                **error)
{
  GInitable *ret;
  ret = g_initable_new (BLUEZ_TYPE_OBJECT_MANAGER_CLIENT, cancellable, error, "flags", flags, "name", name, "connection", connection, "object-path", object_path, "get-proxy-type-func", bluez_object_manager_client_get_proxy_type, NULL);
  if (ret != NULL)
    return G_DBUS_OBJECT_MANAGER (ret);
  else
    return NULL;
}


/**
 * bluez_object_manager_client_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusObjectManagerClientFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like bluez_object_manager_client_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call bluez_object_manager_client_new_for_bus_finish() to get the result of the operation.
 *
 * See bluez_object_manager_client_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
bluez_object_manager_client_new_for_bus (
    GBusType                bus_type,
    GDBusObjectManagerClientFlags  flags,
    const gchar            *name,
    const gchar            *object_path,
    GCancellable           *cancellable,
    GAsyncReadyCallback     callback,
    gpointer                user_data)
{
  g_async_initable_new_async (BLUEZ_TYPE_OBJECT_MANAGER_CLIENT, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "flags", flags, "name", name, "bus-type", bus_type, "object-path", object_path, "get-proxy-type-func", bluez_object_manager_client_get_proxy_type, NULL);
}

/**
 * bluez_object_manager_client_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to bluez_object_manager_client_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with bluez_object_manager_client_new_for_bus().
 *
 * Returns: (transfer full) (type BluezObjectManagerClient): The constructed object manager client or %NULL if @error is set.
 */
GDBusObjectManager *
bluez_object_manager_client_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return G_DBUS_OBJECT_MANAGER (ret);
  else
    return NULL;
}

/**
 * bluez_object_manager_client_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusObjectManagerClientFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like bluez_object_manager_client_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See bluez_object_manager_client_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type BluezObjectManagerClient): The constructed object manager client or %NULL if @error is set.
 */
GDBusObjectManager *
bluez_object_manager_client_new_for_bus_sync (
    GBusType                bus_type,
    GDBusObjectManagerClientFlags  flags,
    const gchar            *name,
    const gchar            *object_path,
    GCancellable           *cancellable,
    GError                **error)
{
  GInitable *ret;
  ret = g_initable_new (BLUEZ_TYPE_OBJECT_MANAGER_CLIENT, cancellable, error, "flags", flags, "name", name, "bus-type", bus_type, "object-path", object_path, "get-proxy-type-func", bluez_object_manager_client_get_proxy_type, NULL);
  if (ret != NULL)
    return G_DBUS_OBJECT_MANAGER (ret);
  else
    return NULL;
}


