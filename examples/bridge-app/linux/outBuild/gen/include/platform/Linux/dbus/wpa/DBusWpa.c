/*
 * This file is generated by gdbus-codegen, do not modify it.
 *
 * The license of this code is the same as for the D-Bus interface description
 * it was derived from. Note that it links to GLib, so must comply with the
 * LGPL linking clauses.
 */

#ifdef HAVE_CONFIG_H
#  include "BuildConfig.h"
#endif

#include "DBusWpa.h"

#include <string.h>
#ifdef G_OS_UNIX
#  include <gio/gunixfdlist.h>
#endif

typedef struct
{
  GDBusArgInfo parent_struct;
  gboolean use_gvariant;
} _ExtendedGDBusArgInfo;

typedef struct
{
  GDBusMethodInfo parent_struct;
  const gchar *signal_name;
  gboolean pass_fdlist;
} _ExtendedGDBusMethodInfo;

typedef struct
{
  GDBusSignalInfo parent_struct;
  const gchar *signal_name;
} _ExtendedGDBusSignalInfo;

typedef struct
{
  GDBusPropertyInfo parent_struct;
  const gchar *hyphen_name;
  guint use_gvariant : 1;
  guint emits_changed_signal : 1;
} _ExtendedGDBusPropertyInfo;

typedef struct
{
  GDBusInterfaceInfo parent_struct;
  const gchar *hyphen_name;
} _ExtendedGDBusInterfaceInfo;

typedef struct
{
  const _ExtendedGDBusPropertyInfo *info;
  guint prop_id;
  GValue orig_value; /* the value before the change */
} ChangedProperty;

static void
_changed_property_free (ChangedProperty *data)
{
  g_value_unset (&data->orig_value);
  g_free (data);
}

static gboolean
_g_strv_equal0 (gchar **a, gchar **b)
{
  gboolean ret = FALSE;
  guint n;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  if (g_strv_length (a) != g_strv_length (b))
    goto out;
  for (n = 0; a[n] != NULL; n++)
    if (g_strcmp0 (a[n], b[n]) != 0)
      goto out;
  ret = TRUE;
out:
  return ret;
}

static gboolean
_g_variant_equal0 (GVariant *a, GVariant *b)
{
  gboolean ret = FALSE;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  ret = g_variant_equal (a, b);
out:
  return ret;
}

G_GNUC_UNUSED static gboolean
_g_value_equal (const GValue *a, const GValue *b)
{
  gboolean ret = FALSE;
  g_assert (G_VALUE_TYPE (a) == G_VALUE_TYPE (b));
  switch (G_VALUE_TYPE (a))
    {
      case G_TYPE_BOOLEAN:
        ret = (g_value_get_boolean (a) == g_value_get_boolean (b));
        break;
      case G_TYPE_UCHAR:
        ret = (g_value_get_uchar (a) == g_value_get_uchar (b));
        break;
      case G_TYPE_INT:
        ret = (g_value_get_int (a) == g_value_get_int (b));
        break;
      case G_TYPE_UINT:
        ret = (g_value_get_uint (a) == g_value_get_uint (b));
        break;
      case G_TYPE_INT64:
        ret = (g_value_get_int64 (a) == g_value_get_int64 (b));
        break;
      case G_TYPE_UINT64:
        ret = (g_value_get_uint64 (a) == g_value_get_uint64 (b));
        break;
      case G_TYPE_DOUBLE:
        {
          /* Avoid -Wfloat-equal warnings by doing a direct bit compare */
          gdouble da = g_value_get_double (a);
          gdouble db = g_value_get_double (b);
          ret = memcmp (&da, &db, sizeof (gdouble)) == 0;
        }
        break;
      case G_TYPE_STRING:
        ret = (g_strcmp0 (g_value_get_string (a), g_value_get_string (b)) == 0);
        break;
      case G_TYPE_VARIANT:
        ret = _g_variant_equal0 (g_value_get_variant (a), g_value_get_variant (b));
        break;
      default:
        if (G_VALUE_TYPE (a) == G_TYPE_STRV)
          ret = _g_strv_equal0 (g_value_get_boxed (a), g_value_get_boxed (b));
        else
          g_critical ("_g_value_equal() does not handle type %s", g_type_name (G_VALUE_TYPE (a)));
        break;
    }
  return ret;
}

/* ------------------------------------------------------------------------
 * Code for interface fi.w1.wpa_supplicant1
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:WpaFiW1Wpa_supplicant1
 * @title: WpaFiW1Wpa_supplicant1
 * @short_description: Generated C code for the fi.w1.wpa_supplicant1 D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-fi-w1-wpa_supplicant1.top_of_page">fi.w1.wpa_supplicant1</link> D-Bus interface in C.
 */

/* ---- Introspection data for fi.w1.wpa_supplicant1 ---- */

static const _ExtendedGDBusArgInfo _wpa_fi_w1_wpa_supplicant1_method_info_create_interface_IN_ARG_args =
{
  {
    -1,
    (gchar *) "args",
    (gchar *) "a{sv}",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _wpa_fi_w1_wpa_supplicant1_method_info_create_interface_IN_ARG_pointers[] =
{
  &_wpa_fi_w1_wpa_supplicant1_method_info_create_interface_IN_ARG_args.parent_struct,
  NULL
};

static const _ExtendedGDBusArgInfo _wpa_fi_w1_wpa_supplicant1_method_info_create_interface_OUT_ARG_path =
{
  {
    -1,
    (gchar *) "path",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _wpa_fi_w1_wpa_supplicant1_method_info_create_interface_OUT_ARG_pointers[] =
{
  &_wpa_fi_w1_wpa_supplicant1_method_info_create_interface_OUT_ARG_path.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _wpa_fi_w1_wpa_supplicant1_method_info_create_interface =
{
  {
    -1,
    (gchar *) "CreateInterface",
    (GDBusArgInfo **) &_wpa_fi_w1_wpa_supplicant1_method_info_create_interface_IN_ARG_pointers,
    (GDBusArgInfo **) &_wpa_fi_w1_wpa_supplicant1_method_info_create_interface_OUT_ARG_pointers,
    NULL
  },
  "handle-create-interface",
  FALSE
};

static const _ExtendedGDBusArgInfo _wpa_fi_w1_wpa_supplicant1_method_info_remove_interface_IN_ARG_path =
{
  {
    -1,
    (gchar *) "path",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _wpa_fi_w1_wpa_supplicant1_method_info_remove_interface_IN_ARG_pointers[] =
{
  &_wpa_fi_w1_wpa_supplicant1_method_info_remove_interface_IN_ARG_path.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _wpa_fi_w1_wpa_supplicant1_method_info_remove_interface =
{
  {
    -1,
    (gchar *) "RemoveInterface",
    (GDBusArgInfo **) &_wpa_fi_w1_wpa_supplicant1_method_info_remove_interface_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-remove-interface",
  FALSE
};

static const _ExtendedGDBusArgInfo _wpa_fi_w1_wpa_supplicant1_method_info_get_interface_IN_ARG_ifname =
{
  {
    -1,
    (gchar *) "ifname",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _wpa_fi_w1_wpa_supplicant1_method_info_get_interface_IN_ARG_pointers[] =
{
  &_wpa_fi_w1_wpa_supplicant1_method_info_get_interface_IN_ARG_ifname.parent_struct,
  NULL
};

static const _ExtendedGDBusArgInfo _wpa_fi_w1_wpa_supplicant1_method_info_get_interface_OUT_ARG_path =
{
  {
    -1,
    (gchar *) "path",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _wpa_fi_w1_wpa_supplicant1_method_info_get_interface_OUT_ARG_pointers[] =
{
  &_wpa_fi_w1_wpa_supplicant1_method_info_get_interface_OUT_ARG_path.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _wpa_fi_w1_wpa_supplicant1_method_info_get_interface =
{
  {
    -1,
    (gchar *) "GetInterface",
    (GDBusArgInfo **) &_wpa_fi_w1_wpa_supplicant1_method_info_get_interface_IN_ARG_pointers,
    (GDBusArgInfo **) &_wpa_fi_w1_wpa_supplicant1_method_info_get_interface_OUT_ARG_pointers,
    NULL
  },
  "handle-get-interface",
  FALSE
};

static const _ExtendedGDBusMethodInfo _wpa_fi_w1_wpa_supplicant1_method_info_expect_disconnect =
{
  {
    -1,
    (gchar *) "ExpectDisconnect",
    NULL,
    NULL,
    NULL
  },
  "handle-expect-disconnect",
  FALSE
};

static const GDBusMethodInfo * const _wpa_fi_w1_wpa_supplicant1_method_info_pointers[] =
{
  &_wpa_fi_w1_wpa_supplicant1_method_info_create_interface.parent_struct,
  &_wpa_fi_w1_wpa_supplicant1_method_info_remove_interface.parent_struct,
  &_wpa_fi_w1_wpa_supplicant1_method_info_get_interface.parent_struct,
  &_wpa_fi_w1_wpa_supplicant1_method_info_expect_disconnect.parent_struct,
  NULL
};

static const _ExtendedGDBusArgInfo _wpa_fi_w1_wpa_supplicant1_signal_info_interface_added_ARG_path =
{
  {
    -1,
    (gchar *) "path",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _wpa_fi_w1_wpa_supplicant1_signal_info_interface_added_ARG_properties =
{
  {
    -1,
    (gchar *) "properties",
    (gchar *) "a{sv}",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _wpa_fi_w1_wpa_supplicant1_signal_info_interface_added_ARG_pointers[] =
{
  &_wpa_fi_w1_wpa_supplicant1_signal_info_interface_added_ARG_path.parent_struct,
  &_wpa_fi_w1_wpa_supplicant1_signal_info_interface_added_ARG_properties.parent_struct,
  NULL
};

static const _ExtendedGDBusSignalInfo _wpa_fi_w1_wpa_supplicant1_signal_info_interface_added =
{
  {
    -1,
    (gchar *) "InterfaceAdded",
    (GDBusArgInfo **) &_wpa_fi_w1_wpa_supplicant1_signal_info_interface_added_ARG_pointers,
    NULL
  },
  "interface-added"
};

static const _ExtendedGDBusArgInfo _wpa_fi_w1_wpa_supplicant1_signal_info_interface_removed_ARG_path =
{
  {
    -1,
    (gchar *) "path",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _wpa_fi_w1_wpa_supplicant1_signal_info_interface_removed_ARG_pointers[] =
{
  &_wpa_fi_w1_wpa_supplicant1_signal_info_interface_removed_ARG_path.parent_struct,
  NULL
};

static const _ExtendedGDBusSignalInfo _wpa_fi_w1_wpa_supplicant1_signal_info_interface_removed =
{
  {
    -1,
    (gchar *) "InterfaceRemoved",
    (GDBusArgInfo **) &_wpa_fi_w1_wpa_supplicant1_signal_info_interface_removed_ARG_pointers,
    NULL
  },
  "interface-removed"
};

static const _ExtendedGDBusArgInfo _wpa_fi_w1_wpa_supplicant1_signal_info_properties_changed_ARG_properties =
{
  {
    -1,
    (gchar *) "properties",
    (gchar *) "a{sv}",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _wpa_fi_w1_wpa_supplicant1_signal_info_properties_changed_ARG_pointers[] =
{
  &_wpa_fi_w1_wpa_supplicant1_signal_info_properties_changed_ARG_properties.parent_struct,
  NULL
};

static const _ExtendedGDBusSignalInfo _wpa_fi_w1_wpa_supplicant1_signal_info_properties_changed =
{
  {
    -1,
    (gchar *) "PropertiesChanged",
    (GDBusArgInfo **) &_wpa_fi_w1_wpa_supplicant1_signal_info_properties_changed_ARG_pointers,
    NULL
  },
  "properties-changed"
};

static const GDBusSignalInfo * const _wpa_fi_w1_wpa_supplicant1_signal_info_pointers[] =
{
  &_wpa_fi_w1_wpa_supplicant1_signal_info_interface_added.parent_struct,
  &_wpa_fi_w1_wpa_supplicant1_signal_info_interface_removed.parent_struct,
  &_wpa_fi_w1_wpa_supplicant1_signal_info_properties_changed.parent_struct,
  NULL
};

static const _ExtendedGDBusPropertyInfo _wpa_fi_w1_wpa_supplicant1_property_info_debug_level =
{
  {
    -1,
    (gchar *) "DebugLevel",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "debug-level",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _wpa_fi_w1_wpa_supplicant1_property_info_debug_timestamp =
{
  {
    -1,
    (gchar *) "DebugTimestamp",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "debug-timestamp",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _wpa_fi_w1_wpa_supplicant1_property_info_debug_show_keys =
{
  {
    -1,
    (gchar *) "DebugShowKeys",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "debug-show-keys",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _wpa_fi_w1_wpa_supplicant1_property_info_interfaces =
{
  {
    -1,
    (gchar *) "Interfaces",
    (gchar *) "ao",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "interfaces",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _wpa_fi_w1_wpa_supplicant1_property_info_eap_methods =
{
  {
    -1,
    (gchar *) "EapMethods",
    (gchar *) "as",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "eap-methods",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _wpa_fi_w1_wpa_supplicant1_property_info_capabilities =
{
  {
    -1,
    (gchar *) "Capabilities",
    (gchar *) "as",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "capabilities",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _wpa_fi_w1_wpa_supplicant1_property_info_wfdies =
{
  {
    -1,
    (gchar *) "WFDIEs",
    (gchar *) "ay",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "wfdies",
  FALSE,
  TRUE
};

static const GDBusPropertyInfo * const _wpa_fi_w1_wpa_supplicant1_property_info_pointers[] =
{
  &_wpa_fi_w1_wpa_supplicant1_property_info_debug_level.parent_struct,
  &_wpa_fi_w1_wpa_supplicant1_property_info_debug_timestamp.parent_struct,
  &_wpa_fi_w1_wpa_supplicant1_property_info_debug_show_keys.parent_struct,
  &_wpa_fi_w1_wpa_supplicant1_property_info_interfaces.parent_struct,
  &_wpa_fi_w1_wpa_supplicant1_property_info_eap_methods.parent_struct,
  &_wpa_fi_w1_wpa_supplicant1_property_info_capabilities.parent_struct,
  &_wpa_fi_w1_wpa_supplicant1_property_info_wfdies.parent_struct,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _wpa_fi_w1_wpa_supplicant1_interface_info =
{
  {
    -1,
    (gchar *) "fi.w1.wpa_supplicant1",
    (GDBusMethodInfo **) &_wpa_fi_w1_wpa_supplicant1_method_info_pointers,
    (GDBusSignalInfo **) &_wpa_fi_w1_wpa_supplicant1_signal_info_pointers,
    (GDBusPropertyInfo **) &_wpa_fi_w1_wpa_supplicant1_property_info_pointers,
    NULL
  },
  "fi-w1-wpa-supplicant1",
};


/**
 * wpa_fi_w1_wpa_supplicant1_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-fi-w1-wpa_supplicant1.top_of_page">fi.w1.wpa_supplicant1</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
wpa_fi_w1_wpa_supplicant1_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_wpa_fi_w1_wpa_supplicant1_interface_info.parent_struct;
}

/**
 * wpa_fi_w1_wpa_supplicant1_override_properties:
 * @klass: The class structure for a #GObject derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #WpaFiW1Wpa_supplicant1 interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
wpa_fi_w1_wpa_supplicant1_override_properties (GObjectClass *klass, guint property_id_begin)
{
  g_object_class_override_property (klass, property_id_begin++, "debug-level");
  g_object_class_override_property (klass, property_id_begin++, "debug-timestamp");
  g_object_class_override_property (klass, property_id_begin++, "debug-show-keys");
  g_object_class_override_property (klass, property_id_begin++, "interfaces");
  g_object_class_override_property (klass, property_id_begin++, "eap-methods");
  g_object_class_override_property (klass, property_id_begin++, "capabilities");
  g_object_class_override_property (klass, property_id_begin++, "wfdies");
  return property_id_begin - 1;
}



/**
 * WpaFiW1Wpa_supplicant1:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-fi-w1-wpa_supplicant1.top_of_page">fi.w1.wpa_supplicant1</link>.
 */

/**
 * WpaFiW1Wpa_supplicant1Iface:
 * @parent_iface: The parent interface.
 * @handle_create_interface: Handler for the #WpaFiW1Wpa_supplicant1::handle-create-interface signal.
 * @handle_expect_disconnect: Handler for the #WpaFiW1Wpa_supplicant1::handle-expect-disconnect signal.
 * @handle_get_interface: Handler for the #WpaFiW1Wpa_supplicant1::handle-get-interface signal.
 * @handle_remove_interface: Handler for the #WpaFiW1Wpa_supplicant1::handle-remove-interface signal.
 * @get_capabilities: Getter for the #WpaFiW1Wpa_supplicant1:capabilities property.
 * @get_debug_level: Getter for the #WpaFiW1Wpa_supplicant1:debug-level property.
 * @get_debug_show_keys: Getter for the #WpaFiW1Wpa_supplicant1:debug-show-keys property.
 * @get_debug_timestamp: Getter for the #WpaFiW1Wpa_supplicant1:debug-timestamp property.
 * @get_eap_methods: Getter for the #WpaFiW1Wpa_supplicant1:eap-methods property.
 * @get_interfaces: Getter for the #WpaFiW1Wpa_supplicant1:interfaces property.
 * @get_wfdies: Getter for the #WpaFiW1Wpa_supplicant1:wfdies property.
 * @interface_added: Handler for the #WpaFiW1Wpa_supplicant1::interface-added signal.
 * @interface_removed: Handler for the #WpaFiW1Wpa_supplicant1::interface-removed signal.
 * @properties_changed: Handler for the #WpaFiW1Wpa_supplicant1::properties-changed signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-fi-w1-wpa_supplicant1.top_of_page">fi.w1.wpa_supplicant1</link>.
 */

typedef WpaFiW1Wpa_supplicant1Iface WpaFiW1Wpa_supplicant1Interface;
G_DEFINE_INTERFACE (WpaFiW1Wpa_supplicant1, wpa_fi_w1_wpa_supplicant1, G_TYPE_OBJECT)

static void
wpa_fi_w1_wpa_supplicant1_default_init (WpaFiW1Wpa_supplicant1Iface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * WpaFiW1Wpa_supplicant1::handle-create-interface:
   * @object: A #WpaFiW1Wpa_supplicant1.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_args: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-fi-w1-wpa_supplicant1.CreateInterface">CreateInterface()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call wpa_fi_w1_wpa_supplicant1_complete_create_interface() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-create-interface",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (WpaFiW1Wpa_supplicant1Iface, handle_create_interface),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_VARIANT);

  /**
   * WpaFiW1Wpa_supplicant1::handle-remove-interface:
   * @object: A #WpaFiW1Wpa_supplicant1.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_path: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-fi-w1-wpa_supplicant1.RemoveInterface">RemoveInterface()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call wpa_fi_w1_wpa_supplicant1_complete_remove_interface() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-remove-interface",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (WpaFiW1Wpa_supplicant1Iface, handle_remove_interface),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /**
   * WpaFiW1Wpa_supplicant1::handle-get-interface:
   * @object: A #WpaFiW1Wpa_supplicant1.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_ifname: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-fi-w1-wpa_supplicant1.GetInterface">GetInterface()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call wpa_fi_w1_wpa_supplicant1_complete_get_interface() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-get-interface",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (WpaFiW1Wpa_supplicant1Iface, handle_get_interface),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /**
   * WpaFiW1Wpa_supplicant1::handle-expect-disconnect:
   * @object: A #WpaFiW1Wpa_supplicant1.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-fi-w1-wpa_supplicant1.ExpectDisconnect">ExpectDisconnect()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call wpa_fi_w1_wpa_supplicant1_complete_expect_disconnect() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-expect-disconnect",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (WpaFiW1Wpa_supplicant1Iface, handle_expect_disconnect),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /* GObject signals for received D-Bus signals: */
  /**
   * WpaFiW1Wpa_supplicant1::interface-added:
   * @object: A #WpaFiW1Wpa_supplicant1.
   * @arg_path: Argument.
   * @arg_properties: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-fi-w1-wpa_supplicant1.InterfaceAdded">"InterfaceAdded"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("interface-added",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (WpaFiW1Wpa_supplicant1Iface, interface_added),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    2, G_TYPE_STRING, G_TYPE_VARIANT);

  /**
   * WpaFiW1Wpa_supplicant1::interface-removed:
   * @object: A #WpaFiW1Wpa_supplicant1.
   * @arg_path: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-fi-w1-wpa_supplicant1.InterfaceRemoved">"InterfaceRemoved"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("interface-removed",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (WpaFiW1Wpa_supplicant1Iface, interface_removed),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    1, G_TYPE_STRING);

  /**
   * WpaFiW1Wpa_supplicant1::properties-changed:
   * @object: A #WpaFiW1Wpa_supplicant1.
   * @arg_properties: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-fi-w1-wpa_supplicant1.PropertiesChanged">"PropertiesChanged"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("properties-changed",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (WpaFiW1Wpa_supplicant1Iface, properties_changed),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    1, G_TYPE_VARIANT);

  /* GObject properties for D-Bus properties: */
  /**
   * WpaFiW1Wpa_supplicant1:debug-level:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-fi-w1-wpa_supplicant1.DebugLevel">"DebugLevel"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("debug-level", "DebugLevel", "DebugLevel", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * WpaFiW1Wpa_supplicant1:debug-timestamp:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-fi-w1-wpa_supplicant1.DebugTimestamp">"DebugTimestamp"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("debug-timestamp", "DebugTimestamp", "DebugTimestamp", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * WpaFiW1Wpa_supplicant1:debug-show-keys:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-fi-w1-wpa_supplicant1.DebugShowKeys">"DebugShowKeys"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("debug-show-keys", "DebugShowKeys", "DebugShowKeys", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * WpaFiW1Wpa_supplicant1:interfaces:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-fi-w1-wpa_supplicant1.Interfaces">"Interfaces"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boxed ("interfaces", "Interfaces", "Interfaces", G_TYPE_STRV, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * WpaFiW1Wpa_supplicant1:eap-methods:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-fi-w1-wpa_supplicant1.EapMethods">"EapMethods"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boxed ("eap-methods", "EapMethods", "EapMethods", G_TYPE_STRV, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * WpaFiW1Wpa_supplicant1:capabilities:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-fi-w1-wpa_supplicant1.Capabilities">"Capabilities"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boxed ("capabilities", "Capabilities", "Capabilities", G_TYPE_STRV, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * WpaFiW1Wpa_supplicant1:wfdies:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-fi-w1-wpa_supplicant1.WFDIEs">"WFDIEs"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("wfdies", "WFDIEs", "WFDIEs", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

/**
 * wpa_fi_w1_wpa_supplicant1_get_debug_level: (skip)
 * @object: A #WpaFiW1Wpa_supplicant1.
 *
 * Gets the value of the <link linkend="gdbus-property-fi-w1-wpa_supplicant1.DebugLevel">"DebugLevel"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use wpa_fi_w1_wpa_supplicant1_dup_debug_level() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
wpa_fi_w1_wpa_supplicant1_get_debug_level (WpaFiW1Wpa_supplicant1 *object)
{
  return WPA_FI_W1_WPA_SUPPLICANT1_GET_IFACE (object)->get_debug_level (object);
}

/**
 * wpa_fi_w1_wpa_supplicant1_dup_debug_level: (skip)
 * @object: A #WpaFiW1Wpa_supplicant1.
 *
 * Gets a copy of the <link linkend="gdbus-property-fi-w1-wpa_supplicant1.DebugLevel">"DebugLevel"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
wpa_fi_w1_wpa_supplicant1_dup_debug_level (WpaFiW1Wpa_supplicant1 *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "debug-level", &value, NULL);
  return value;
}

/**
 * wpa_fi_w1_wpa_supplicant1_set_debug_level: (skip)
 * @object: A #WpaFiW1Wpa_supplicant1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-fi-w1-wpa_supplicant1.DebugLevel">"DebugLevel"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
wpa_fi_w1_wpa_supplicant1_set_debug_level (WpaFiW1Wpa_supplicant1 *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "debug-level", value, NULL);
}

/**
 * wpa_fi_w1_wpa_supplicant1_get_debug_timestamp: (skip)
 * @object: A #WpaFiW1Wpa_supplicant1.
 *
 * Gets the value of the <link linkend="gdbus-property-fi-w1-wpa_supplicant1.DebugTimestamp">"DebugTimestamp"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
wpa_fi_w1_wpa_supplicant1_get_debug_timestamp (WpaFiW1Wpa_supplicant1 *object)
{
  return WPA_FI_W1_WPA_SUPPLICANT1_GET_IFACE (object)->get_debug_timestamp (object);
}

/**
 * wpa_fi_w1_wpa_supplicant1_set_debug_timestamp: (skip)
 * @object: A #WpaFiW1Wpa_supplicant1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-fi-w1-wpa_supplicant1.DebugTimestamp">"DebugTimestamp"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
wpa_fi_w1_wpa_supplicant1_set_debug_timestamp (WpaFiW1Wpa_supplicant1 *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "debug-timestamp", value, NULL);
}

/**
 * wpa_fi_w1_wpa_supplicant1_get_debug_show_keys: (skip)
 * @object: A #WpaFiW1Wpa_supplicant1.
 *
 * Gets the value of the <link linkend="gdbus-property-fi-w1-wpa_supplicant1.DebugShowKeys">"DebugShowKeys"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
wpa_fi_w1_wpa_supplicant1_get_debug_show_keys (WpaFiW1Wpa_supplicant1 *object)
{
  return WPA_FI_W1_WPA_SUPPLICANT1_GET_IFACE (object)->get_debug_show_keys (object);
}

/**
 * wpa_fi_w1_wpa_supplicant1_set_debug_show_keys: (skip)
 * @object: A #WpaFiW1Wpa_supplicant1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-fi-w1-wpa_supplicant1.DebugShowKeys">"DebugShowKeys"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
wpa_fi_w1_wpa_supplicant1_set_debug_show_keys (WpaFiW1Wpa_supplicant1 *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "debug-show-keys", value, NULL);
}

/**
 * wpa_fi_w1_wpa_supplicant1_get_interfaces: (skip)
 * @object: A #WpaFiW1Wpa_supplicant1.
 *
 * Gets the value of the <link linkend="gdbus-property-fi-w1-wpa_supplicant1.Interfaces">"Interfaces"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use wpa_fi_w1_wpa_supplicant1_dup_interfaces() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *const *
wpa_fi_w1_wpa_supplicant1_get_interfaces (WpaFiW1Wpa_supplicant1 *object)
{
  return WPA_FI_W1_WPA_SUPPLICANT1_GET_IFACE (object)->get_interfaces (object);
}

/**
 * wpa_fi_w1_wpa_supplicant1_dup_interfaces: (skip)
 * @object: A #WpaFiW1Wpa_supplicant1.
 *
 * Gets a copy of the <link linkend="gdbus-property-fi-w1-wpa_supplicant1.Interfaces">"Interfaces"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_strfreev().
 */
gchar **
wpa_fi_w1_wpa_supplicant1_dup_interfaces (WpaFiW1Wpa_supplicant1 *object)
{
  gchar **value;
  g_object_get (G_OBJECT (object), "interfaces", &value, NULL);
  return value;
}

/**
 * wpa_fi_w1_wpa_supplicant1_set_interfaces: (skip)
 * @object: A #WpaFiW1Wpa_supplicant1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-fi-w1-wpa_supplicant1.Interfaces">"Interfaces"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
wpa_fi_w1_wpa_supplicant1_set_interfaces (WpaFiW1Wpa_supplicant1 *object, const gchar *const *value)
{
  g_object_set (G_OBJECT (object), "interfaces", value, NULL);
}

/**
 * wpa_fi_w1_wpa_supplicant1_get_eap_methods: (skip)
 * @object: A #WpaFiW1Wpa_supplicant1.
 *
 * Gets the value of the <link linkend="gdbus-property-fi-w1-wpa_supplicant1.EapMethods">"EapMethods"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use wpa_fi_w1_wpa_supplicant1_dup_eap_methods() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *const *
wpa_fi_w1_wpa_supplicant1_get_eap_methods (WpaFiW1Wpa_supplicant1 *object)
{
  return WPA_FI_W1_WPA_SUPPLICANT1_GET_IFACE (object)->get_eap_methods (object);
}

/**
 * wpa_fi_w1_wpa_supplicant1_dup_eap_methods: (skip)
 * @object: A #WpaFiW1Wpa_supplicant1.
 *
 * Gets a copy of the <link linkend="gdbus-property-fi-w1-wpa_supplicant1.EapMethods">"EapMethods"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_strfreev().
 */
gchar **
wpa_fi_w1_wpa_supplicant1_dup_eap_methods (WpaFiW1Wpa_supplicant1 *object)
{
  gchar **value;
  g_object_get (G_OBJECT (object), "eap-methods", &value, NULL);
  return value;
}

/**
 * wpa_fi_w1_wpa_supplicant1_set_eap_methods: (skip)
 * @object: A #WpaFiW1Wpa_supplicant1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-fi-w1-wpa_supplicant1.EapMethods">"EapMethods"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
wpa_fi_w1_wpa_supplicant1_set_eap_methods (WpaFiW1Wpa_supplicant1 *object, const gchar *const *value)
{
  g_object_set (G_OBJECT (object), "eap-methods", value, NULL);
}

/**
 * wpa_fi_w1_wpa_supplicant1_get_capabilities: (skip)
 * @object: A #WpaFiW1Wpa_supplicant1.
 *
 * Gets the value of the <link linkend="gdbus-property-fi-w1-wpa_supplicant1.Capabilities">"Capabilities"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use wpa_fi_w1_wpa_supplicant1_dup_capabilities() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *const *
wpa_fi_w1_wpa_supplicant1_get_capabilities (WpaFiW1Wpa_supplicant1 *object)
{
  return WPA_FI_W1_WPA_SUPPLICANT1_GET_IFACE (object)->get_capabilities (object);
}

/**
 * wpa_fi_w1_wpa_supplicant1_dup_capabilities: (skip)
 * @object: A #WpaFiW1Wpa_supplicant1.
 *
 * Gets a copy of the <link linkend="gdbus-property-fi-w1-wpa_supplicant1.Capabilities">"Capabilities"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_strfreev().
 */
gchar **
wpa_fi_w1_wpa_supplicant1_dup_capabilities (WpaFiW1Wpa_supplicant1 *object)
{
  gchar **value;
  g_object_get (G_OBJECT (object), "capabilities", &value, NULL);
  return value;
}

/**
 * wpa_fi_w1_wpa_supplicant1_set_capabilities: (skip)
 * @object: A #WpaFiW1Wpa_supplicant1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-fi-w1-wpa_supplicant1.Capabilities">"Capabilities"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
wpa_fi_w1_wpa_supplicant1_set_capabilities (WpaFiW1Wpa_supplicant1 *object, const gchar *const *value)
{
  g_object_set (G_OBJECT (object), "capabilities", value, NULL);
}

/**
 * wpa_fi_w1_wpa_supplicant1_get_wfdies: (skip)
 * @object: A #WpaFiW1Wpa_supplicant1.
 *
 * Gets the value of the <link linkend="gdbus-property-fi-w1-wpa_supplicant1.WFDIEs">"WFDIEs"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use wpa_fi_w1_wpa_supplicant1_dup_wfdies() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
wpa_fi_w1_wpa_supplicant1_get_wfdies (WpaFiW1Wpa_supplicant1 *object)
{
  return WPA_FI_W1_WPA_SUPPLICANT1_GET_IFACE (object)->get_wfdies (object);
}

/**
 * wpa_fi_w1_wpa_supplicant1_dup_wfdies: (skip)
 * @object: A #WpaFiW1Wpa_supplicant1.
 *
 * Gets a copy of the <link linkend="gdbus-property-fi-w1-wpa_supplicant1.WFDIEs">"WFDIEs"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
wpa_fi_w1_wpa_supplicant1_dup_wfdies (WpaFiW1Wpa_supplicant1 *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "wfdies", &value, NULL);
  return value;
}

/**
 * wpa_fi_w1_wpa_supplicant1_set_wfdies: (skip)
 * @object: A #WpaFiW1Wpa_supplicant1.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-fi-w1-wpa_supplicant1.WFDIEs">"WFDIEs"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
wpa_fi_w1_wpa_supplicant1_set_wfdies (WpaFiW1Wpa_supplicant1 *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "wfdies", value, NULL);
}

/**
 * wpa_fi_w1_wpa_supplicant1_emit_interface_added:
 * @object: A #WpaFiW1Wpa_supplicant1.
 * @arg_path: Argument to pass with the signal.
 * @arg_properties: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-fi-w1-wpa_supplicant1.InterfaceAdded">"InterfaceAdded"</link> D-Bus signal.
 */
void
wpa_fi_w1_wpa_supplicant1_emit_interface_added (
    WpaFiW1Wpa_supplicant1 *object,
    const gchar *arg_path,
    GVariant *arg_properties)
{
  g_signal_emit_by_name (object, "interface-added", arg_path, arg_properties);
}

/**
 * wpa_fi_w1_wpa_supplicant1_emit_interface_removed:
 * @object: A #WpaFiW1Wpa_supplicant1.
 * @arg_path: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-fi-w1-wpa_supplicant1.InterfaceRemoved">"InterfaceRemoved"</link> D-Bus signal.
 */
void
wpa_fi_w1_wpa_supplicant1_emit_interface_removed (
    WpaFiW1Wpa_supplicant1 *object,
    const gchar *arg_path)
{
  g_signal_emit_by_name (object, "interface-removed", arg_path);
}

/**
 * wpa_fi_w1_wpa_supplicant1_emit_properties_changed:
 * @object: A #WpaFiW1Wpa_supplicant1.
 * @arg_properties: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-fi-w1-wpa_supplicant1.PropertiesChanged">"PropertiesChanged"</link> D-Bus signal.
 */
void
wpa_fi_w1_wpa_supplicant1_emit_properties_changed (
    WpaFiW1Wpa_supplicant1 *object,
    GVariant *arg_properties)
{
  g_signal_emit_by_name (object, "properties-changed", arg_properties);
}

/**
 * wpa_fi_w1_wpa_supplicant1_call_create_interface:
 * @proxy: A #WpaFiW1Wpa_supplicant1Proxy.
 * @arg_args: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-fi-w1-wpa_supplicant1.CreateInterface">CreateInterface()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call wpa_fi_w1_wpa_supplicant1_call_create_interface_finish() to get the result of the operation.
 *
 * See wpa_fi_w1_wpa_supplicant1_call_create_interface_sync() for the synchronous, blocking version of this method.
 */
void
wpa_fi_w1_wpa_supplicant1_call_create_interface (
    WpaFiW1Wpa_supplicant1 *proxy,
    GVariant *arg_args,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "CreateInterface",
    g_variant_new ("(@a{sv})",
                   arg_args),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * wpa_fi_w1_wpa_supplicant1_call_create_interface_finish:
 * @proxy: A #WpaFiW1Wpa_supplicant1Proxy.
 * @out_path: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to wpa_fi_w1_wpa_supplicant1_call_create_interface().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with wpa_fi_w1_wpa_supplicant1_call_create_interface().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
wpa_fi_w1_wpa_supplicant1_call_create_interface_finish (
    WpaFiW1Wpa_supplicant1 *proxy,
    gchar **out_path,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(o)",
                 out_path);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wpa_fi_w1_wpa_supplicant1_call_create_interface_sync:
 * @proxy: A #WpaFiW1Wpa_supplicant1Proxy.
 * @arg_args: Argument to pass with the method invocation.
 * @out_path: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-fi-w1-wpa_supplicant1.CreateInterface">CreateInterface()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See wpa_fi_w1_wpa_supplicant1_call_create_interface() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
wpa_fi_w1_wpa_supplicant1_call_create_interface_sync (
    WpaFiW1Wpa_supplicant1 *proxy,
    GVariant *arg_args,
    gchar **out_path,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "CreateInterface",
    g_variant_new ("(@a{sv})",
                   arg_args),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(o)",
                 out_path);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wpa_fi_w1_wpa_supplicant1_call_remove_interface:
 * @proxy: A #WpaFiW1Wpa_supplicant1Proxy.
 * @arg_path: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-fi-w1-wpa_supplicant1.RemoveInterface">RemoveInterface()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call wpa_fi_w1_wpa_supplicant1_call_remove_interface_finish() to get the result of the operation.
 *
 * See wpa_fi_w1_wpa_supplicant1_call_remove_interface_sync() for the synchronous, blocking version of this method.
 */
void
wpa_fi_w1_wpa_supplicant1_call_remove_interface (
    WpaFiW1Wpa_supplicant1 *proxy,
    const gchar *arg_path,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "RemoveInterface",
    g_variant_new ("(o)",
                   arg_path),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * wpa_fi_w1_wpa_supplicant1_call_remove_interface_finish:
 * @proxy: A #WpaFiW1Wpa_supplicant1Proxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to wpa_fi_w1_wpa_supplicant1_call_remove_interface().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with wpa_fi_w1_wpa_supplicant1_call_remove_interface().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
wpa_fi_w1_wpa_supplicant1_call_remove_interface_finish (
    WpaFiW1Wpa_supplicant1 *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wpa_fi_w1_wpa_supplicant1_call_remove_interface_sync:
 * @proxy: A #WpaFiW1Wpa_supplicant1Proxy.
 * @arg_path: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-fi-w1-wpa_supplicant1.RemoveInterface">RemoveInterface()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See wpa_fi_w1_wpa_supplicant1_call_remove_interface() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
wpa_fi_w1_wpa_supplicant1_call_remove_interface_sync (
    WpaFiW1Wpa_supplicant1 *proxy,
    const gchar *arg_path,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "RemoveInterface",
    g_variant_new ("(o)",
                   arg_path),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wpa_fi_w1_wpa_supplicant1_call_get_interface:
 * @proxy: A #WpaFiW1Wpa_supplicant1Proxy.
 * @arg_ifname: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-fi-w1-wpa_supplicant1.GetInterface">GetInterface()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call wpa_fi_w1_wpa_supplicant1_call_get_interface_finish() to get the result of the operation.
 *
 * See wpa_fi_w1_wpa_supplicant1_call_get_interface_sync() for the synchronous, blocking version of this method.
 */
void
wpa_fi_w1_wpa_supplicant1_call_get_interface (
    WpaFiW1Wpa_supplicant1 *proxy,
    const gchar *arg_ifname,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "GetInterface",
    g_variant_new ("(s)",
                   arg_ifname),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * wpa_fi_w1_wpa_supplicant1_call_get_interface_finish:
 * @proxy: A #WpaFiW1Wpa_supplicant1Proxy.
 * @out_path: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to wpa_fi_w1_wpa_supplicant1_call_get_interface().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with wpa_fi_w1_wpa_supplicant1_call_get_interface().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
wpa_fi_w1_wpa_supplicant1_call_get_interface_finish (
    WpaFiW1Wpa_supplicant1 *proxy,
    gchar **out_path,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(o)",
                 out_path);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wpa_fi_w1_wpa_supplicant1_call_get_interface_sync:
 * @proxy: A #WpaFiW1Wpa_supplicant1Proxy.
 * @arg_ifname: Argument to pass with the method invocation.
 * @out_path: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-fi-w1-wpa_supplicant1.GetInterface">GetInterface()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See wpa_fi_w1_wpa_supplicant1_call_get_interface() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
wpa_fi_w1_wpa_supplicant1_call_get_interface_sync (
    WpaFiW1Wpa_supplicant1 *proxy,
    const gchar *arg_ifname,
    gchar **out_path,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "GetInterface",
    g_variant_new ("(s)",
                   arg_ifname),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(o)",
                 out_path);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wpa_fi_w1_wpa_supplicant1_call_expect_disconnect:
 * @proxy: A #WpaFiW1Wpa_supplicant1Proxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-fi-w1-wpa_supplicant1.ExpectDisconnect">ExpectDisconnect()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call wpa_fi_w1_wpa_supplicant1_call_expect_disconnect_finish() to get the result of the operation.
 *
 * See wpa_fi_w1_wpa_supplicant1_call_expect_disconnect_sync() for the synchronous, blocking version of this method.
 */
void
wpa_fi_w1_wpa_supplicant1_call_expect_disconnect (
    WpaFiW1Wpa_supplicant1 *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "ExpectDisconnect",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * wpa_fi_w1_wpa_supplicant1_call_expect_disconnect_finish:
 * @proxy: A #WpaFiW1Wpa_supplicant1Proxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to wpa_fi_w1_wpa_supplicant1_call_expect_disconnect().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with wpa_fi_w1_wpa_supplicant1_call_expect_disconnect().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
wpa_fi_w1_wpa_supplicant1_call_expect_disconnect_finish (
    WpaFiW1Wpa_supplicant1 *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wpa_fi_w1_wpa_supplicant1_call_expect_disconnect_sync:
 * @proxy: A #WpaFiW1Wpa_supplicant1Proxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-fi-w1-wpa_supplicant1.ExpectDisconnect">ExpectDisconnect()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See wpa_fi_w1_wpa_supplicant1_call_expect_disconnect() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
wpa_fi_w1_wpa_supplicant1_call_expect_disconnect_sync (
    WpaFiW1Wpa_supplicant1 *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "ExpectDisconnect",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wpa_fi_w1_wpa_supplicant1_complete_create_interface:
 * @object: A #WpaFiW1Wpa_supplicant1.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @path: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-fi-w1-wpa_supplicant1.CreateInterface">CreateInterface()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
wpa_fi_w1_wpa_supplicant1_complete_create_interface (
    WpaFiW1Wpa_supplicant1 *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation,
    const gchar *path)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(o)",
                   path));
}

/**
 * wpa_fi_w1_wpa_supplicant1_complete_remove_interface:
 * @object: A #WpaFiW1Wpa_supplicant1.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-fi-w1-wpa_supplicant1.RemoveInterface">RemoveInterface()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
wpa_fi_w1_wpa_supplicant1_complete_remove_interface (
    WpaFiW1Wpa_supplicant1 *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * wpa_fi_w1_wpa_supplicant1_complete_get_interface:
 * @object: A #WpaFiW1Wpa_supplicant1.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @path: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-fi-w1-wpa_supplicant1.GetInterface">GetInterface()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
wpa_fi_w1_wpa_supplicant1_complete_get_interface (
    WpaFiW1Wpa_supplicant1 *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation,
    const gchar *path)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(o)",
                   path));
}

/**
 * wpa_fi_w1_wpa_supplicant1_complete_expect_disconnect:
 * @object: A #WpaFiW1Wpa_supplicant1.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-fi-w1-wpa_supplicant1.ExpectDisconnect">ExpectDisconnect()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
wpa_fi_w1_wpa_supplicant1_complete_expect_disconnect (
    WpaFiW1Wpa_supplicant1 *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * WpaFiW1Wpa_supplicant1Proxy:
 *
 * The #WpaFiW1Wpa_supplicant1Proxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * WpaFiW1Wpa_supplicant1ProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #WpaFiW1Wpa_supplicant1Proxy.
 */

struct _WpaFiW1Wpa_supplicant1ProxyPrivate
{
  GData *qdata;
};

static void wpa_fi_w1_wpa_supplicant1_proxy_iface_init (WpaFiW1Wpa_supplicant1Iface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (WpaFiW1Wpa_supplicant1Proxy, wpa_fi_w1_wpa_supplicant1_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (WpaFiW1Wpa_supplicant1Proxy)
                         G_IMPLEMENT_INTERFACE (WPA_TYPE_FI_W1_WPA_SUPPLICANT1, wpa_fi_w1_wpa_supplicant1_proxy_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (WpaFiW1Wpa_supplicant1Proxy, wpa_fi_w1_wpa_supplicant1_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (WPA_TYPE_FI_W1_WPA_SUPPLICANT1, wpa_fi_w1_wpa_supplicant1_proxy_iface_init))

#endif
static void
wpa_fi_w1_wpa_supplicant1_proxy_finalize (GObject *object)
{
  WpaFiW1Wpa_supplicant1Proxy *proxy = WPA_FI_W1_WPA_SUPPLICANT1_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (wpa_fi_w1_wpa_supplicant1_proxy_parent_class)->finalize (object);
}

static void
wpa_fi_w1_wpa_supplicant1_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 7);
  info = (const _ExtendedGDBusPropertyInfo *) _wpa_fi_w1_wpa_supplicant1_property_info_pointers[prop_id - 1];
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
  if (info->use_gvariant)
    {
      g_value_set_variant (value, variant);
    }
  else
    {
      if (variant != NULL)
        g_dbus_gvariant_to_gvalue (variant, value);
    }
  if (variant != NULL)
    g_variant_unref (variant);
}

static void
wpa_fi_w1_wpa_supplicant1_proxy_set_property_cb (GDBusProxy *proxy,
  GAsyncResult *res,
  gpointer      user_data)
{
  const _ExtendedGDBusPropertyInfo *info = user_data;
  GError *error;
  GVariant *_ret;
  error = NULL;
  _ret = g_dbus_proxy_call_finish (proxy, res, &error);
  if (!_ret)
    {
      g_warning ("Error setting property '%s' on interface fi.w1.wpa_supplicant1: %s (%s, %d)",
                 info->parent_struct.name, 
                 error->message, g_quark_to_string (error->domain), error->code);
      g_error_free (error);
    }
  else
    {
      g_variant_unref (_ret);
    }
}

static void
wpa_fi_w1_wpa_supplicant1_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 7);
  info = (const _ExtendedGDBusPropertyInfo *) _wpa_fi_w1_wpa_supplicant1_property_info_pointers[prop_id - 1];
  variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
  g_dbus_proxy_call (G_DBUS_PROXY (object),
    "org.freedesktop.DBus.Properties.Set",
    g_variant_new ("(ssv)", "fi.w1.wpa_supplicant1", info->parent_struct.name, variant),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    NULL, (GAsyncReadyCallback) wpa_fi_w1_wpa_supplicant1_proxy_set_property_cb, (GDBusPropertyInfo *) &info->parent_struct);
  g_variant_unref (variant);
}

static void
wpa_fi_w1_wpa_supplicant1_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  gsize n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_wpa_fi_w1_wpa_supplicant1_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], WPA_TYPE_FI_W1_WPA_SUPPLICANT1);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, WPA_TYPE_FI_W1_WPA_SUPPLICANT1);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
wpa_fi_w1_wpa_supplicant1_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  WpaFiW1Wpa_supplicant1Proxy *proxy = WPA_FI_W1_WPA_SUPPLICANT1_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_wpa_fi_w1_wpa_supplicant1_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_wpa_fi_w1_wpa_supplicant1_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static const gchar *
wpa_fi_w1_wpa_supplicant1_proxy_get_debug_level (WpaFiW1Wpa_supplicant1 *object)
{
  WpaFiW1Wpa_supplicant1Proxy *proxy = WPA_FI_W1_WPA_SUPPLICANT1_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "DebugLevel");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
wpa_fi_w1_wpa_supplicant1_proxy_get_debug_timestamp (WpaFiW1Wpa_supplicant1 *object)
{
  WpaFiW1Wpa_supplicant1Proxy *proxy = WPA_FI_W1_WPA_SUPPLICANT1_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "DebugTimestamp");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
wpa_fi_w1_wpa_supplicant1_proxy_get_debug_show_keys (WpaFiW1Wpa_supplicant1 *object)
{
  WpaFiW1Wpa_supplicant1Proxy *proxy = WPA_FI_W1_WPA_SUPPLICANT1_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "DebugShowKeys");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *const *
wpa_fi_w1_wpa_supplicant1_proxy_get_interfaces (WpaFiW1Wpa_supplicant1 *object)
{
  WpaFiW1Wpa_supplicant1Proxy *proxy = WPA_FI_W1_WPA_SUPPLICANT1_PROXY (object);
  GVariant *variant;
  const gchar *const *value = NULL;
  value = g_datalist_get_data (&proxy->priv->qdata, "Interfaces");
  if (value != NULL)
    return value;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Interfaces");
  if (variant != NULL)
    {
      value = g_variant_get_objv (variant, NULL);
      g_datalist_set_data_full (&proxy->priv->qdata, "Interfaces", (gpointer) value, g_free);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *const *
wpa_fi_w1_wpa_supplicant1_proxy_get_eap_methods (WpaFiW1Wpa_supplicant1 *object)
{
  WpaFiW1Wpa_supplicant1Proxy *proxy = WPA_FI_W1_WPA_SUPPLICANT1_PROXY (object);
  GVariant *variant;
  const gchar *const *value = NULL;
  value = g_datalist_get_data (&proxy->priv->qdata, "EapMethods");
  if (value != NULL)
    return value;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "EapMethods");
  if (variant != NULL)
    {
      value = g_variant_get_strv (variant, NULL);
      g_datalist_set_data_full (&proxy->priv->qdata, "EapMethods", (gpointer) value, g_free);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *const *
wpa_fi_w1_wpa_supplicant1_proxy_get_capabilities (WpaFiW1Wpa_supplicant1 *object)
{
  WpaFiW1Wpa_supplicant1Proxy *proxy = WPA_FI_W1_WPA_SUPPLICANT1_PROXY (object);
  GVariant *variant;
  const gchar *const *value = NULL;
  value = g_datalist_get_data (&proxy->priv->qdata, "Capabilities");
  if (value != NULL)
    return value;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Capabilities");
  if (variant != NULL)
    {
      value = g_variant_get_strv (variant, NULL);
      g_datalist_set_data_full (&proxy->priv->qdata, "Capabilities", (gpointer) value, g_free);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
wpa_fi_w1_wpa_supplicant1_proxy_get_wfdies (WpaFiW1Wpa_supplicant1 *object)
{
  WpaFiW1Wpa_supplicant1Proxy *proxy = WPA_FI_W1_WPA_SUPPLICANT1_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "WFDIEs");
  if (variant != NULL)
    {
      value = g_variant_get_bytestring (variant);
      g_variant_unref (variant);
    }
  return value;
}

static void
wpa_fi_w1_wpa_supplicant1_proxy_init (WpaFiW1Wpa_supplicant1Proxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = wpa_fi_w1_wpa_supplicant1_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, WPA_TYPE_FI_W1_WPA_SUPPLICANT1_PROXY, WpaFiW1Wpa_supplicant1ProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), wpa_fi_w1_wpa_supplicant1_interface_info ());
}

static void
wpa_fi_w1_wpa_supplicant1_proxy_class_init (WpaFiW1Wpa_supplicant1ProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = wpa_fi_w1_wpa_supplicant1_proxy_finalize;
  gobject_class->get_property = wpa_fi_w1_wpa_supplicant1_proxy_get_property;
  gobject_class->set_property = wpa_fi_w1_wpa_supplicant1_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = wpa_fi_w1_wpa_supplicant1_proxy_g_signal;
  proxy_class->g_properties_changed = wpa_fi_w1_wpa_supplicant1_proxy_g_properties_changed;

  wpa_fi_w1_wpa_supplicant1_override_properties (gobject_class, 1);

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (WpaFiW1Wpa_supplicant1ProxyPrivate));
#endif
}

static void
wpa_fi_w1_wpa_supplicant1_proxy_iface_init (WpaFiW1Wpa_supplicant1Iface *iface)
{
  iface->get_debug_level = wpa_fi_w1_wpa_supplicant1_proxy_get_debug_level;
  iface->get_debug_timestamp = wpa_fi_w1_wpa_supplicant1_proxy_get_debug_timestamp;
  iface->get_debug_show_keys = wpa_fi_w1_wpa_supplicant1_proxy_get_debug_show_keys;
  iface->get_interfaces = wpa_fi_w1_wpa_supplicant1_proxy_get_interfaces;
  iface->get_eap_methods = wpa_fi_w1_wpa_supplicant1_proxy_get_eap_methods;
  iface->get_capabilities = wpa_fi_w1_wpa_supplicant1_proxy_get_capabilities;
  iface->get_wfdies = wpa_fi_w1_wpa_supplicant1_proxy_get_wfdies;
}

/**
 * wpa_fi_w1_wpa_supplicant1_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-fi-w1-wpa_supplicant1.top_of_page">fi.w1.wpa_supplicant1</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call wpa_fi_w1_wpa_supplicant1_proxy_new_finish() to get the result of the operation.
 *
 * See wpa_fi_w1_wpa_supplicant1_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
wpa_fi_w1_wpa_supplicant1_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (WPA_TYPE_FI_W1_WPA_SUPPLICANT1_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "fi.w1.wpa_supplicant1", NULL);
}

/**
 * wpa_fi_w1_wpa_supplicant1_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to wpa_fi_w1_wpa_supplicant1_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with wpa_fi_w1_wpa_supplicant1_proxy_new().
 *
 * Returns: (transfer full) (type WpaFiW1Wpa_supplicant1Proxy): The constructed proxy object or %NULL if @error is set.
 */
WpaFiW1Wpa_supplicant1 *
wpa_fi_w1_wpa_supplicant1_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return WPA_FI_W1_WPA_SUPPLICANT1 (ret);
  else
    return NULL;
}

/**
 * wpa_fi_w1_wpa_supplicant1_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-fi-w1-wpa_supplicant1.top_of_page">fi.w1.wpa_supplicant1</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See wpa_fi_w1_wpa_supplicant1_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type WpaFiW1Wpa_supplicant1Proxy): The constructed proxy object or %NULL if @error is set.
 */
WpaFiW1Wpa_supplicant1 *
wpa_fi_w1_wpa_supplicant1_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (WPA_TYPE_FI_W1_WPA_SUPPLICANT1_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "fi.w1.wpa_supplicant1", NULL);
  if (ret != NULL)
    return WPA_FI_W1_WPA_SUPPLICANT1 (ret);
  else
    return NULL;
}


/**
 * wpa_fi_w1_wpa_supplicant1_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like wpa_fi_w1_wpa_supplicant1_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call wpa_fi_w1_wpa_supplicant1_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See wpa_fi_w1_wpa_supplicant1_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
wpa_fi_w1_wpa_supplicant1_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (WPA_TYPE_FI_W1_WPA_SUPPLICANT1_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "fi.w1.wpa_supplicant1", NULL);
}

/**
 * wpa_fi_w1_wpa_supplicant1_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to wpa_fi_w1_wpa_supplicant1_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with wpa_fi_w1_wpa_supplicant1_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type WpaFiW1Wpa_supplicant1Proxy): The constructed proxy object or %NULL if @error is set.
 */
WpaFiW1Wpa_supplicant1 *
wpa_fi_w1_wpa_supplicant1_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return WPA_FI_W1_WPA_SUPPLICANT1 (ret);
  else
    return NULL;
}

/**
 * wpa_fi_w1_wpa_supplicant1_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like wpa_fi_w1_wpa_supplicant1_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See wpa_fi_w1_wpa_supplicant1_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type WpaFiW1Wpa_supplicant1Proxy): The constructed proxy object or %NULL if @error is set.
 */
WpaFiW1Wpa_supplicant1 *
wpa_fi_w1_wpa_supplicant1_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (WPA_TYPE_FI_W1_WPA_SUPPLICANT1_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "fi.w1.wpa_supplicant1", NULL);
  if (ret != NULL)
    return WPA_FI_W1_WPA_SUPPLICANT1 (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * WpaFiW1Wpa_supplicant1Skeleton:
 *
 * The #WpaFiW1Wpa_supplicant1Skeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * WpaFiW1Wpa_supplicant1SkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #WpaFiW1Wpa_supplicant1Skeleton.
 */

struct _WpaFiW1Wpa_supplicant1SkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_wpa_fi_w1_wpa_supplicant1_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  WpaFiW1Wpa_supplicant1Skeleton *skeleton = WPA_FI_W1_WPA_SUPPLICANT1_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  guint num_extra;
  gsize n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], WPA_TYPE_FI_W1_WPA_SUPPLICANT1);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, WPA_TYPE_FI_W1_WPA_SUPPLICANT1);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_wpa_fi_w1_wpa_supplicant1_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  WpaFiW1Wpa_supplicant1Skeleton *skeleton = WPA_FI_W1_WPA_SUPPLICANT1_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_wpa_fi_w1_wpa_supplicant1_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_wpa_fi_w1_wpa_supplicant1_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  WpaFiW1Wpa_supplicant1Skeleton *skeleton = WPA_FI_W1_WPA_SUPPLICANT1_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_wpa_fi_w1_wpa_supplicant1_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _wpa_fi_w1_wpa_supplicant1_skeleton_vtable =
{
  _wpa_fi_w1_wpa_supplicant1_skeleton_handle_method_call,
  _wpa_fi_w1_wpa_supplicant1_skeleton_handle_get_property,
  _wpa_fi_w1_wpa_supplicant1_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
wpa_fi_w1_wpa_supplicant1_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return wpa_fi_w1_wpa_supplicant1_interface_info ();
}

static GDBusInterfaceVTable *
wpa_fi_w1_wpa_supplicant1_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_wpa_fi_w1_wpa_supplicant1_skeleton_vtable;
}

static GVariant *
wpa_fi_w1_wpa_supplicant1_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  WpaFiW1Wpa_supplicant1Skeleton *skeleton = WPA_FI_W1_WPA_SUPPLICANT1_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_wpa_fi_w1_wpa_supplicant1_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _wpa_fi_w1_wpa_supplicant1_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _wpa_fi_w1_wpa_supplicant1_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _wpa_fi_w1_wpa_supplicant1_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "fi.w1.wpa_supplicant1", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static gboolean _wpa_fi_w1_wpa_supplicant1_emit_changed (gpointer user_data);

static void
wpa_fi_w1_wpa_supplicant1_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
  WpaFiW1Wpa_supplicant1Skeleton *skeleton = WPA_FI_W1_WPA_SUPPLICANT1_SKELETON (_skeleton);
  gboolean emit_changed = FALSE;

  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    {
      g_source_destroy (skeleton->priv->changed_properties_idle_source);
      skeleton->priv->changed_properties_idle_source = NULL;
      emit_changed = TRUE;
    }
  g_mutex_unlock (&skeleton->priv->lock);

  if (emit_changed)
    _wpa_fi_w1_wpa_supplicant1_emit_changed (skeleton);
}

static void
_wpa_fi_w1_wpa_supplicant1_on_signal_interface_added (
    WpaFiW1Wpa_supplicant1 *object,
    const gchar *arg_path,
    GVariant *arg_properties)
{
  WpaFiW1Wpa_supplicant1Skeleton *skeleton = WPA_FI_W1_WPA_SUPPLICANT1_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(o@a{sv})",
                   arg_path,
                   arg_properties));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "fi.w1.wpa_supplicant1", "InterfaceAdded",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_wpa_fi_w1_wpa_supplicant1_on_signal_interface_removed (
    WpaFiW1Wpa_supplicant1 *object,
    const gchar *arg_path)
{
  WpaFiW1Wpa_supplicant1Skeleton *skeleton = WPA_FI_W1_WPA_SUPPLICANT1_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(o)",
                   arg_path));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "fi.w1.wpa_supplicant1", "InterfaceRemoved",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_wpa_fi_w1_wpa_supplicant1_on_signal_properties_changed (
    WpaFiW1Wpa_supplicant1 *object,
    GVariant *arg_properties)
{
  WpaFiW1Wpa_supplicant1Skeleton *skeleton = WPA_FI_W1_WPA_SUPPLICANT1_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(@a{sv})",
                   arg_properties));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "fi.w1.wpa_supplicant1", "PropertiesChanged",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void wpa_fi_w1_wpa_supplicant1_skeleton_iface_init (WpaFiW1Wpa_supplicant1Iface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (WpaFiW1Wpa_supplicant1Skeleton, wpa_fi_w1_wpa_supplicant1_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (WpaFiW1Wpa_supplicant1Skeleton)
                         G_IMPLEMENT_INTERFACE (WPA_TYPE_FI_W1_WPA_SUPPLICANT1, wpa_fi_w1_wpa_supplicant1_skeleton_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (WpaFiW1Wpa_supplicant1Skeleton, wpa_fi_w1_wpa_supplicant1_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (WPA_TYPE_FI_W1_WPA_SUPPLICANT1, wpa_fi_w1_wpa_supplicant1_skeleton_iface_init))

#endif
static void
wpa_fi_w1_wpa_supplicant1_skeleton_finalize (GObject *object)
{
  WpaFiW1Wpa_supplicant1Skeleton *skeleton = WPA_FI_W1_WPA_SUPPLICANT1_SKELETON (object);
  guint n;
  for (n = 0; n < 7; n++)
    g_value_unset (&skeleton->priv->properties[n]);
  g_free (skeleton->priv->properties);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (wpa_fi_w1_wpa_supplicant1_skeleton_parent_class)->finalize (object);
}

static void
wpa_fi_w1_wpa_supplicant1_skeleton_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  WpaFiW1Wpa_supplicant1Skeleton *skeleton = WPA_FI_W1_WPA_SUPPLICANT1_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 7);
  g_mutex_lock (&skeleton->priv->lock);
  g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
  g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
_wpa_fi_w1_wpa_supplicant1_emit_changed (gpointer user_data)
{
  WpaFiW1Wpa_supplicant1Skeleton *skeleton = WPA_FI_W1_WPA_SUPPLICANT1_SKELETON (user_data);
  GList *l;
  GVariantBuilder builder;
  GVariantBuilder invalidated_builder;
  guint num_changes;

  g_mutex_lock (&skeleton->priv->lock);
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
  for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
    {
      ChangedProperty *cp = l->data;
      GVariant *variant;
      const GValue *cur_value;

      cur_value = &skeleton->priv->properties[cp->prop_id - 1];
      if (!_g_value_equal (cur_value, &cp->orig_value))
        {
          variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
          g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
          g_variant_unref (variant);
          num_changes++;
        }
    }
  if (num_changes > 0)
    {
      GList *connections, *ll;
      GVariant *signal_variant;
      signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "fi.w1.wpa_supplicant1",
                                           &builder, &invalidated_builder));
      connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
      for (ll = connections; ll != NULL; ll = ll->next)
        {
          GDBusConnection *connection = ll->data;

          g_dbus_connection_emit_signal (connection,
                                         NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
                                         "org.freedesktop.DBus.Properties",
                                         "PropertiesChanged",
                                         signal_variant,
                                         NULL);
        }
      g_variant_unref (signal_variant);
      g_list_free_full (connections, g_object_unref);
    }
  else
    {
      g_variant_builder_clear (&builder);
      g_variant_builder_clear (&invalidated_builder);
    }
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  skeleton->priv->changed_properties = NULL;
  skeleton->priv->changed_properties_idle_source = NULL;
  g_mutex_unlock (&skeleton->priv->lock);
  return FALSE;
}

static void
_wpa_fi_w1_wpa_supplicant1_schedule_emit_changed (WpaFiW1Wpa_supplicant1Skeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
  ChangedProperty *cp;
  GList *l;
  cp = NULL;
  for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
    {
      ChangedProperty *i_cp = l->data;
      if (i_cp->info == info)
        {
          cp = i_cp;
          break;
        }
    }
  if (cp == NULL)
    {
      cp = g_new0 (ChangedProperty, 1);
      cp->prop_id = prop_id;
      cp->info = info;
      skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
      g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
      g_value_copy (orig_value, &cp->orig_value);
    }
}

static void
wpa_fi_w1_wpa_supplicant1_skeleton_notify (GObject      *object,
  GParamSpec *pspec G_GNUC_UNUSED)
{
  WpaFiW1Wpa_supplicant1Skeleton *skeleton = WPA_FI_W1_WPA_SUPPLICANT1_SKELETON (object);
  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties != NULL &&
      skeleton->priv->changed_properties_idle_source == NULL)
    {
      skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
      g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
      g_source_set_callback (skeleton->priv->changed_properties_idle_source, _wpa_fi_w1_wpa_supplicant1_emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
      g_source_set_name (skeleton->priv->changed_properties_idle_source, "[generated] _wpa_fi_w1_wpa_supplicant1_emit_changed");
      g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
      g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
  g_mutex_unlock (&skeleton->priv->lock);
}

static void
wpa_fi_w1_wpa_supplicant1_skeleton_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  const _ExtendedGDBusPropertyInfo *info;
  WpaFiW1Wpa_supplicant1Skeleton *skeleton = WPA_FI_W1_WPA_SUPPLICANT1_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 7);
  info = (const _ExtendedGDBusPropertyInfo *) _wpa_fi_w1_wpa_supplicant1_property_info_pointers[prop_id - 1];
  g_mutex_lock (&skeleton->priv->lock);
  g_object_freeze_notify (object);
  if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
      if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL &&
          info->emits_changed_signal)
        _wpa_fi_w1_wpa_supplicant1_schedule_emit_changed (skeleton, info, prop_id, &skeleton->priv->properties[prop_id - 1]);
      g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
      g_object_notify_by_pspec (object, pspec);
    }
  g_mutex_unlock (&skeleton->priv->lock);
  g_object_thaw_notify (object);
}

static void
wpa_fi_w1_wpa_supplicant1_skeleton_init (WpaFiW1Wpa_supplicant1Skeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = wpa_fi_w1_wpa_supplicant1_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, WPA_TYPE_FI_W1_WPA_SUPPLICANT1_SKELETON, WpaFiW1Wpa_supplicant1SkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
  skeleton->priv->properties = g_new0 (GValue, 7);
  g_value_init (&skeleton->priv->properties[0], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[1], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[2], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[3], G_TYPE_STRV);
  g_value_init (&skeleton->priv->properties[4], G_TYPE_STRV);
  g_value_init (&skeleton->priv->properties[5], G_TYPE_STRV);
  g_value_init (&skeleton->priv->properties[6], G_TYPE_STRING);
}

static const gchar *
wpa_fi_w1_wpa_supplicant1_skeleton_get_debug_level (WpaFiW1Wpa_supplicant1 *object)
{
  WpaFiW1Wpa_supplicant1Skeleton *skeleton = WPA_FI_W1_WPA_SUPPLICANT1_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[0]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
wpa_fi_w1_wpa_supplicant1_skeleton_get_debug_timestamp (WpaFiW1Wpa_supplicant1 *object)
{
  WpaFiW1Wpa_supplicant1Skeleton *skeleton = WPA_FI_W1_WPA_SUPPLICANT1_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[1]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
wpa_fi_w1_wpa_supplicant1_skeleton_get_debug_show_keys (WpaFiW1Wpa_supplicant1 *object)
{
  WpaFiW1Wpa_supplicant1Skeleton *skeleton = WPA_FI_W1_WPA_SUPPLICANT1_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[2]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *const *
wpa_fi_w1_wpa_supplicant1_skeleton_get_interfaces (WpaFiW1Wpa_supplicant1 *object)
{
  WpaFiW1Wpa_supplicant1Skeleton *skeleton = WPA_FI_W1_WPA_SUPPLICANT1_SKELETON (object);
  const gchar *const *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boxed (&(skeleton->priv->properties[3]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *const *
wpa_fi_w1_wpa_supplicant1_skeleton_get_eap_methods (WpaFiW1Wpa_supplicant1 *object)
{
  WpaFiW1Wpa_supplicant1Skeleton *skeleton = WPA_FI_W1_WPA_SUPPLICANT1_SKELETON (object);
  const gchar *const *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boxed (&(skeleton->priv->properties[4]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *const *
wpa_fi_w1_wpa_supplicant1_skeleton_get_capabilities (WpaFiW1Wpa_supplicant1 *object)
{
  WpaFiW1Wpa_supplicant1Skeleton *skeleton = WPA_FI_W1_WPA_SUPPLICANT1_SKELETON (object);
  const gchar *const *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boxed (&(skeleton->priv->properties[5]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
wpa_fi_w1_wpa_supplicant1_skeleton_get_wfdies (WpaFiW1Wpa_supplicant1 *object)
{
  WpaFiW1Wpa_supplicant1Skeleton *skeleton = WPA_FI_W1_WPA_SUPPLICANT1_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[6]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static void
wpa_fi_w1_wpa_supplicant1_skeleton_class_init (WpaFiW1Wpa_supplicant1SkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = wpa_fi_w1_wpa_supplicant1_skeleton_finalize;
  gobject_class->get_property = wpa_fi_w1_wpa_supplicant1_skeleton_get_property;
  gobject_class->set_property = wpa_fi_w1_wpa_supplicant1_skeleton_set_property;
  gobject_class->notify       = wpa_fi_w1_wpa_supplicant1_skeleton_notify;


  wpa_fi_w1_wpa_supplicant1_override_properties (gobject_class, 1);

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = wpa_fi_w1_wpa_supplicant1_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = wpa_fi_w1_wpa_supplicant1_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = wpa_fi_w1_wpa_supplicant1_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = wpa_fi_w1_wpa_supplicant1_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (WpaFiW1Wpa_supplicant1SkeletonPrivate));
#endif
}

static void
wpa_fi_w1_wpa_supplicant1_skeleton_iface_init (WpaFiW1Wpa_supplicant1Iface *iface)
{
  iface->interface_added = _wpa_fi_w1_wpa_supplicant1_on_signal_interface_added;
  iface->interface_removed = _wpa_fi_w1_wpa_supplicant1_on_signal_interface_removed;
  iface->properties_changed = _wpa_fi_w1_wpa_supplicant1_on_signal_properties_changed;
  iface->get_debug_level = wpa_fi_w1_wpa_supplicant1_skeleton_get_debug_level;
  iface->get_debug_timestamp = wpa_fi_w1_wpa_supplicant1_skeleton_get_debug_timestamp;
  iface->get_debug_show_keys = wpa_fi_w1_wpa_supplicant1_skeleton_get_debug_show_keys;
  iface->get_interfaces = wpa_fi_w1_wpa_supplicant1_skeleton_get_interfaces;
  iface->get_eap_methods = wpa_fi_w1_wpa_supplicant1_skeleton_get_eap_methods;
  iface->get_capabilities = wpa_fi_w1_wpa_supplicant1_skeleton_get_capabilities;
  iface->get_wfdies = wpa_fi_w1_wpa_supplicant1_skeleton_get_wfdies;
}

/**
 * wpa_fi_w1_wpa_supplicant1_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-fi-w1-wpa_supplicant1.top_of_page">fi.w1.wpa_supplicant1</link>.
 *
 * Returns: (transfer full) (type WpaFiW1Wpa_supplicant1Skeleton): The skeleton object.
 */
WpaFiW1Wpa_supplicant1 *
wpa_fi_w1_wpa_supplicant1_skeleton_new (void)
{
  return WPA_FI_W1_WPA_SUPPLICANT1 (g_object_new (WPA_TYPE_FI_W1_WPA_SUPPLICANT1_SKELETON, NULL));
}

