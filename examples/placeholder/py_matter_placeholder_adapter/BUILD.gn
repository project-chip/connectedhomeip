# SPDX-FileCopyrightText: 2023 Project CHIP Authors
#
# SPDX-License-Identifier: Apache-2.0

import("//build_overrides/build.gni")
import("//build_overrides/chip.gni")

import("//build_overrides/pigweed.gni")
import("$dir_pw_build/python.gni")

pw_python_package("matter_placeholder_adapter") {
  setup = [
    "setup.py",
    "setup.cfg",
    "pyproject.toml",
  ]

  sources = [
    "matter_placeholder_adapter/__init__.py",
    "matter_placeholder_adapter/adapter.py",
  ]

  # TODO: at a future time consider enabling all (* or missing) here to get
  #       pylint checking these files
  static_analysis = []
}
