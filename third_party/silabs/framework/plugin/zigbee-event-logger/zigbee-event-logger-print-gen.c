/**
 *
 *    Copyright (c) 2020 Project CHIP Authors
 *
 *    Licensed under the Apache License, Version 2.0 (the "License");
 *    you may not use this file except in compliance with the License.
 *    You may obtain a copy of the License at
 *
 *        http://www.apache.org/licenses/LICENSE-2.0
 *
 *    Unless required by applicable law or agreed to in writing, software
 *    distributed under the License is distributed on an "AS IS" BASIS,
 *    WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 *    See the License for the specific language governing permissions and
 *    limitations under the License.
 */

/**
 *
 *    Copyright (c) 2020 Silicon Labs
 *
 *    Licensed under the Apache License, Version 2.0 (the "License");
 *    you may not use this file except in compliance with the License.
 *    You may obtain a copy of the License at
 *
 *        http://www.apache.org/licenses/LICENSE-2.0
 *
 *    Unless required by applicable law or agreed to in writing, software
 *    distributed under the License is distributed on an "AS IS" BASIS,
 *    WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 *    See the License for the specific language governing permissions and
 *    limitations under the License.
 */
// GENERATED FILE DO NOT MODIFY
// Generated by: generate-zigbee-logger-files.py -y zigbee_logger.yaml

#include "../../include/af.h"
#include "stack/include/zigbee-event-logger-gen.h"
#include "zigbee-event-logger-internal.h"
#include "zigbee-event-logger-print-gen.h"
#include "app/framework/util/print.h"

bool emberAfPluginZigBeeEventLoggerPrintHexData(uint8_t *data, uint16_t dataLength)
{
  emberAfPrintBuffer(0xFFFF, data, dataLength, FALSE);
  emberAfCorePrint("\n");
  return true;
}

#ifdef EMBER_AF_PLUGIN_ZIGBEE_EVENT_LOGGER_INCLUDE_STRINGS_IN_LOGS

#define SHORT 0
#define LONG 1

#if EMBER_AF_PLUGIN_ZIGBEE_EVENT_LOGGER_PRINT_STRING_LENGTH == LONG
  #define EMBER_LOGGER_ZIGBEE_LONG_STRINGS
#elif EMBER_AF_PLUGIN_ZIGBEE_EVENT_LOGGER_PRINT_STRING_LENGTH == SHORT
  #define EMBER_LOGGER_ZIGBEE_SHORT_STRINGS
#endif

bool emberAfPluginZigBeeEventLoggerPrintBootEvent(uint8_t *data, uint16_t dataLength);
bool emberAfPluginZigBeeEventLoggerPrintJoinNetwork(uint8_t *data, uint16_t dataLength);
bool emberAfPluginZigBeeEventLoggerPrintTrustCenterRejoin(uint8_t *data, uint16_t dataLength);
bool emberAfPluginZigBeeEventLoggerPrintSecureRejoin(uint8_t *data, uint16_t dataLength);
bool emberAfPluginZigBeeEventLoggerPrintLeaveWithoutRejoin(uint8_t *data, uint16_t dataLength);
bool emberAfPluginZigBeeEventLoggerPrintLeaveWithRejoin(uint8_t *data, uint16_t dataLength);
bool emberAfPluginZigBeeEventLoggerPrintTrustCenterLinkKeyChange(uint8_t *data, uint16_t dataLength);
bool emberAfPluginZigBeeEventLoggerPrintNetworkKeySequenceChange(uint8_t *data, uint16_t dataLength);
bool emberAfPluginZigBeeEventLoggerPrintReset(uint8_t *data, uint16_t dataLength);
bool emberAfPluginZigBeeEventLoggerPrintChannelChange(uint8_t *data, uint16_t dataLength);
bool emberAfPluginZigBeeEventLoggerPrintParentChange(uint8_t *data, uint16_t dataLength);
bool emberAfPluginZigBeeEventLoggerPrintDutyCycleChange(uint8_t *data, uint16_t dataLength);
bool emberAfPluginZigBeeEventLoggerPrintChildAdded(uint8_t *data, uint16_t dataLength);
bool emberAfPluginZigBeeEventLoggerPrintChildRemoved(uint8_t *data, uint16_t dataLength);
bool emberAfPluginZigBeeEventLoggerPrintChildTimedOut(uint8_t *data, uint16_t dataLength);
bool emberAfPluginZigBeeEventLoggerPrintStackStatus(uint8_t *data, uint16_t dataLength);
bool emberAfPluginZigBeeEventLoggerPrintTimeSync(uint8_t *data, uint16_t dataLength);
bool emberAfPluginZigBeeEventLoggerPrintTrustCenterJoinDecision(uint8_t *data, uint16_t dataLength);

typedef struct {
  EmberZigbeeLoggerEventTypes type;
  printFunction *pfunc;
  const char* string;
} EmberZigbeeLoggerIdToPrint;

#ifdef EMBER_LOGGER_ZIGBEE_LONG_STRINGS
static EmberZigbeeLoggerIdToPrint idToPrint[] = {
  { EMBER_LOGGER_ZIGBEE_BOOT_EVENT_ID,
    emberAfPluginZigBeeEventLoggerPrintBootEvent,
    "Boot event. Boot Count: %d. Type:0x%2X" },
  { EMBER_LOGGER_ZIGBEE_JOIN_NETWORK_ID,
    emberAfPluginZigBeeEventLoggerPrintJoinNetwork,
    "Joining the network on PAN ID: 0x%2x, channel: %d, page: %d, extPanId: 0x%x%x%x%x%x%x%x%x" },
  { EMBER_LOGGER_ZIGBEE_TRUST_CENTER_REJOIN_ID,
    emberAfPluginZigBeeEventLoggerPrintTrustCenterRejoin,
    "Trust Center Rejoin channel mask: 0x%4x, reason: 0x%x" },
  { EMBER_LOGGER_ZIGBEE_SECURE_REJOIN_ID,
    emberAfPluginZigBeeEventLoggerPrintSecureRejoin,
    "Secure Rejoin channel mask: 0x%4x, reason: 0x%x" },
  { EMBER_LOGGER_ZIGBEE_LEAVE_WITHOUT_REJOIN_ID,
    emberAfPluginZigBeeEventLoggerPrintLeaveWithoutRejoin,
    "Leave Network Without Rejoin from node ID 0x%2x, reason: 0x%x" },
  { EMBER_LOGGER_ZIGBEE_LEAVE_WITH_REJOIN_ID,
    emberAfPluginZigBeeEventLoggerPrintLeaveWithRejoin,
    "Leave Network With Rejoin from node ID 0x%2x, reason: 0x%x" },
  { EMBER_LOGGER_ZIGBEE_TRUST_CENTER_LINK_KEY_CHANGE_ID,
    emberAfPluginZigBeeEventLoggerPrintTrustCenterLinkKeyChange,
    "Trust Center Link Key Change" },
  { EMBER_LOGGER_ZIGBEE_NETWORK_KEY_SEQUENCE_NUMBER_CHANGE_ID,
    emberAfPluginZigBeeEventLoggerPrintNetworkKeySequenceChange,
    "Network Key changed to sequence num: %d" },
  { EMBER_LOGGER_ZIGBEE_RESET_ID,
    emberAfPluginZigBeeEventLoggerPrintReset,
    "Reset due to Ember Reset Info: %d, extended reset: %d" },
  { EMBER_LOGGER_ZIGBEE_CHANNEL_CHANGE_ID,
    emberAfPluginZigBeeEventLoggerPrintChannelChange,
    "Changed channel to page: %d, channel: %d" },
  { EMBER_LOGGER_ZIGBEE_PARENT_CHANGE_ID,
    emberAfPluginZigBeeEventLoggerPrintParentChange,
    "Changed to new parent 0x%2x, eui: 0x%x%x%x%x%x%x%x%x" },
  { EMBER_LOGGER_ZIGBEE_DUTY_CYCLE_CHANGE_ID,
    emberAfPluginZigBeeEventLoggerPrintDutyCycleChange,
    "Duty Cycle Change state %d, limit %d, critical %d, susp %d" },
  { EMBER_LOGGER_ZIGBEE_CHILD_ADDED_ID,
    emberAfPluginZigBeeEventLoggerPrintChildAdded,
    "Child Added 0x%2x, eui: 0x%x%x%x%x%x%x%x%x" },
  { EMBER_LOGGER_ZIGBEE_CHILD_REMOVED_ID,
    emberAfPluginZigBeeEventLoggerPrintChildRemoved,
    "Child Removed 0x%2x, eui: 0x%x%x%x%x%x%x%x%x" },
  { EMBER_LOGGER_ZIGBEE_CHILD_TIMED_OUT_ID,
    emberAfPluginZigBeeEventLoggerPrintChildTimedOut,
    "Child Timed Out 0x%2x eui: 0x%x%x%x%x%x%x%x%x" },
  { EMBER_LOGGER_ZIGBEE_STACK_STATUS_ID,
    emberAfPluginZigBeeEventLoggerPrintStackStatus,
    "Stack Status Event 0x%x" },
  { EMBER_LOGGER_ZIGBEE_TIME_SYNC_ID,
    emberAfPluginZigBeeEventLoggerPrintTimeSync,
    "UTC Time Sync'd, ZigBee UTC Time %d, seconds since boot %d" },
  { EMBER_LOGGER_ZIGBEE_TRUST_CENTER_JOIN_DECISION_ID,
    emberAfPluginZigBeeEventLoggerPrintTrustCenterJoinDecision,
    "Trust Center Join Decision for 0x%2x: %x" },
  { EMBER_LOGGER_ZIGBEE_NULL_ID, NULL, NULL }
};
#elif defined EMBER_LOGGER_ZIGBEE_SHORT_STRINGS
static EmberZigbeeLoggerIdToPrint idToPrint[] = {
  { EMBER_LOGGER_ZIGBEE_BOOT_EVENT_ID,
    emberAfPluginZigBeeEventLoggerPrintBootEvent,
    "Boot Cnt: %d. Type: 0x%2X" },
  { EMBER_LOGGER_ZIGBEE_JOIN_NETWORK_ID,
    emberAfPluginZigBeeEventLoggerPrintJoinNetwork,
    "Join panId:0x%2x, ch:%d, pg:%d, epan:0x%x%x%x%x%x%x%x%x" },
  { EMBER_LOGGER_ZIGBEE_TRUST_CENTER_REJOIN_ID,
    emberAfPluginZigBeeEventLoggerPrintTrustCenterRejoin,
    "TC Rejoin ch mask:0x%4x, rsn:0x%x" },
  { EMBER_LOGGER_ZIGBEE_SECURE_REJOIN_ID,
    emberAfPluginZigBeeEventLoggerPrintSecureRejoin,
    "Sec Rejoin ch mask:0x%4x, rsn:0x%x" },
  { EMBER_LOGGER_ZIGBEE_LEAVE_WITHOUT_REJOIN_ID,
    emberAfPluginZigBeeEventLoggerPrintLeaveWithoutRejoin,
    "Leave No Rejoin node:0x%2x, rsn:0x%x" },
  { EMBER_LOGGER_ZIGBEE_LEAVE_WITH_REJOIN_ID,
    emberAfPluginZigBeeEventLoggerPrintLeaveWithRejoin,
    "Leave Rejoin node:0x%2x, rsn:0x%x" },
  { EMBER_LOGGER_ZIGBEE_TRUST_CENTER_LINK_KEY_CHANGE_ID,
    emberAfPluginZigBeeEventLoggerPrintTrustCenterLinkKeyChange,
    "TC Link Key Change" },
  { EMBER_LOGGER_ZIGBEE_NETWORK_KEY_SEQUENCE_NUMBER_CHANGE_ID,
    emberAfPluginZigBeeEventLoggerPrintNetworkKeySequenceChange,
    "Nwk Key seq num changed: %d" },
  { EMBER_LOGGER_ZIGBEE_RESET_ID,
    emberAfPluginZigBeeEventLoggerPrintReset,
    "Reset code: %d, ext: %d" },
  { EMBER_LOGGER_ZIGBEE_CHANNEL_CHANGE_ID,
    emberAfPluginZigBeeEventLoggerPrintChannelChange,
    "Channel Change pg: %d chan: %d" },
  { EMBER_LOGGER_ZIGBEE_PARENT_CHANGE_ID,
    emberAfPluginZigBeeEventLoggerPrintParentChange,
    "Parent Change: 0x%2x, 0x%x%x%x%x%x%x%x%x" },
  { EMBER_LOGGER_ZIGBEE_DUTY_CYCLE_CHANGE_ID,
    emberAfPluginZigBeeEventLoggerPrintDutyCycleChange,
    "Duty Cycle Change, st %d lim %d cr %d susp %d" },
  { EMBER_LOGGER_ZIGBEE_CHILD_ADDED_ID,
    emberAfPluginZigBeeEventLoggerPrintChildAdded,
    "Child Added 0x%2x, 0x%x%x%x%x%x%x%x%x" },
  { EMBER_LOGGER_ZIGBEE_CHILD_REMOVED_ID,
    emberAfPluginZigBeeEventLoggerPrintChildRemoved,
    "Child Removed 0x%2x, 0x%x%x%x%x%x%x%x%x" },
  { EMBER_LOGGER_ZIGBEE_CHILD_TIMED_OUT_ID,
    emberAfPluginZigBeeEventLoggerPrintChildTimedOut,
    "Child Tmout 0x%2x, 0x%x%x%x%x%x%x%x%x" },
  { EMBER_LOGGER_ZIGBEE_STACK_STATUS_ID,
    emberAfPluginZigBeeEventLoggerPrintStackStatus,
    "Stack Status 0x%x" },
  { EMBER_LOGGER_ZIGBEE_TIME_SYNC_ID,
    emberAfPluginZigBeeEventLoggerPrintTimeSync,
    "Time Sync, utcTime %d, sec %d" },
  { EMBER_LOGGER_ZIGBEE_TRUST_CENTER_JOIN_DECISION_ID,
    emberAfPluginZigBeeEventLoggerPrintTrustCenterJoinDecision,
    "TC Join Decision for 0x%2x: %x" },
  { EMBER_LOGGER_ZIGBEE_NULL_ID, NULL, NULL }
};
#endif

static const char* lookupFormatString(EmberZigbeeLoggerEventTypes type)
{
#if defined(EMBER_LOGGER_ZIGBEE_LONG_STRINGS) || defined(EMBER_LOGGER_ZIGBEE_SHORT_STRINGS)
  uint16_t i = 0;
  for (i = 0; i < EMBER_LOGGER_ZIGBEE_NULL_ID; i++) {
    if (type == idToPrint[i].type) {
      return idToPrint[i].string;
    }
    if (idToPrint[i].type == EMBER_LOGGER_ZIGBEE_NULL_ID) {
      return NULL;
    }
  }
#endif
  return NULL;
}

printFunction *emberAfPluginZigBeeEventLoggerLookupPrintFunction(EmberZigbeeLoggerEventTypes type)
{
#if defined EMBER_LOGGER_ZIGBEE_LONG_STRINGS || defined EMBER_LOGGER_ZIGBEE_SHORT_STRINGS
  uint16_t i = 0;
  for (i = 0; i < EMBER_LOGGER_ZIGBEE_NULL_ID; i++) {
    if (type == idToPrint[i].type) {
      return idToPrint[i].pfunc;
    }
    if (idToPrint[i].type == EMBER_LOGGER_ZIGBEE_NULL_ID) {
      return NULL;
    }
  }
#endif
  return emberAfPluginZigBeeEventLoggerPrintHexData;
}

bool emberAfPluginZigBeeEventLoggerPrintBootEvent(uint8_t *data, uint16_t dataLength)
{
  uint32_t bootCount;
  uint16_t resetType;
  const char *formattedString = lookupFormatString(EMBER_LOGGER_ZIGBEE_BOOT_EVENT_ID);
  if (formattedString == NULL || dataLength != EMBER_LOGGER_ZIGBEE_BOOT_EVENT_LENGTH) {
    return false;
  }
  bootCount = parseUint32_t(&data);
  resetType = parseUint16_t(&data);

  emberAfCorePrintln(formattedString, bootCount, resetType);
  return true;
}

bool emberAfPluginZigBeeEventLoggerPrintJoinNetwork(uint8_t *data, uint16_t dataLength)
{
  EmberPanId panId;
  uint8_t channelNumber;
  uint8_t page;
  uint8_t extendedPanId[EXTENDED_PAN_ID_SIZE];
  const char *formattedString = lookupFormatString(EMBER_LOGGER_ZIGBEE_JOIN_NETWORK_ID);
  if (formattedString == NULL || dataLength != EMBER_LOGGER_ZIGBEE_JOIN_NETWORK_LENGTH) {
    return false;
  }
  panId = parseUint16_t(&data);
  channelNumber = parseUint8_t(&data);
  page = parseUint8_t(&data);
  parseArray(extendedPanId, &data, EXTENDED_PAN_ID_SIZE);

  emberAfCorePrintln(formattedString, panId, channelNumber, page, extendedPanId[7], extendedPanId[6], extendedPanId[5], extendedPanId[4], extendedPanId[3], extendedPanId[2], extendedPanId[1], extendedPanId[0]);
  return true;
}

bool emberAfPluginZigBeeEventLoggerPrintTrustCenterRejoin(uint8_t *data, uint16_t dataLength)
{
  uint32_t channelMask;
  EmberRejoinReason reason;
  const char *formattedString = lookupFormatString(EMBER_LOGGER_ZIGBEE_TRUST_CENTER_REJOIN_ID);
  if (formattedString == NULL || dataLength != EMBER_LOGGER_ZIGBEE_TRUST_CENTER_REJOIN_LENGTH) {
    return false;
  }
  channelMask = parseUint32_t(&data);
  reason = parseUint8_t(&data);

  emberAfCorePrintln(formattedString, channelMask, reason);
  return true;
}

bool emberAfPluginZigBeeEventLoggerPrintSecureRejoin(uint8_t *data, uint16_t dataLength)
{
  uint32_t channelMask;
  EmberRejoinReason reason;
  const char *formattedString = lookupFormatString(EMBER_LOGGER_ZIGBEE_SECURE_REJOIN_ID);
  if (formattedString == NULL || dataLength != EMBER_LOGGER_ZIGBEE_SECURE_REJOIN_LENGTH) {
    return false;
  }
  channelMask = parseUint32_t(&data);
  reason = parseUint8_t(&data);

  emberAfCorePrintln(formattedString, channelMask, reason);
  return true;
}

bool emberAfPluginZigBeeEventLoggerPrintLeaveWithoutRejoin(uint8_t *data, uint16_t dataLength)
{
  EmberNodeId sourceNode;
  EmberLeaveReason reason;
  const char *formattedString = lookupFormatString(EMBER_LOGGER_ZIGBEE_LEAVE_WITHOUT_REJOIN_ID);
  if (formattedString == NULL || dataLength != EMBER_LOGGER_ZIGBEE_LEAVE_WITHOUT_REJOIN_LENGTH) {
    return false;
  }
  sourceNode = parseUint16_t(&data);
  reason = parseUint8_t(&data);

  emberAfCorePrintln(formattedString, sourceNode, reason);
  return true;
}

bool emberAfPluginZigBeeEventLoggerPrintLeaveWithRejoin(uint8_t *data, uint16_t dataLength)
{
  EmberNodeId sourceNode;
  EmberLeaveReason reason;
  const char *formattedString = lookupFormatString(EMBER_LOGGER_ZIGBEE_LEAVE_WITH_REJOIN_ID);
  if (formattedString == NULL || dataLength != EMBER_LOGGER_ZIGBEE_LEAVE_WITH_REJOIN_LENGTH) {
    return false;
  }
  sourceNode = parseUint16_t(&data);
  reason = parseUint8_t(&data);

  emberAfCorePrintln(formattedString, sourceNode, reason);
  return true;
}

bool emberAfPluginZigBeeEventLoggerPrintTrustCenterLinkKeyChange(uint8_t *data, uint16_t dataLength)
{
  const char *formattedString = lookupFormatString(EMBER_LOGGER_ZIGBEE_TRUST_CENTER_LINK_KEY_CHANGE_ID);
  if (formattedString == NULL || dataLength != EMBER_LOGGER_ZIGBEE_TRUST_CENTER_LINK_KEY_CHANGE_LENGTH) {
    return false;
  }

  emberAfCorePrintln(formattedString);
  return true;
}

bool emberAfPluginZigBeeEventLoggerPrintNetworkKeySequenceChange(uint8_t *data, uint16_t dataLength)
{
  uint8_t sequence;
  const char *formattedString = lookupFormatString(EMBER_LOGGER_ZIGBEE_NETWORK_KEY_SEQUENCE_NUMBER_CHANGE_ID);
  if (formattedString == NULL || dataLength != EMBER_LOGGER_ZIGBEE_NETWORK_KEY_SEQUENCE_NUMBER_CHANGE_LENGTH) {
    return false;
  }
  sequence = parseUint8_t(&data);

  emberAfCorePrintln(formattedString, sequence);
  return true;
}

bool emberAfPluginZigBeeEventLoggerPrintReset(uint8_t *data, uint16_t dataLength)
{
  uint8_t resetCode;
  uint8_t extendedReason;
  const char *formattedString = lookupFormatString(EMBER_LOGGER_ZIGBEE_RESET_ID);
  if (formattedString == NULL || dataLength != EMBER_LOGGER_ZIGBEE_RESET_LENGTH) {
    return false;
  }
  resetCode = parseUint8_t(&data);
  extendedReason = parseUint8_t(&data);

  emberAfCorePrintln(formattedString, resetCode, extendedReason);
  return true;
}

bool emberAfPluginZigBeeEventLoggerPrintChannelChange(uint8_t *data, uint16_t dataLength)
{
  uint8_t page;
  uint8_t channel;
  const char *formattedString = lookupFormatString(EMBER_LOGGER_ZIGBEE_CHANNEL_CHANGE_ID);
  if (formattedString == NULL || dataLength != EMBER_LOGGER_ZIGBEE_CHANNEL_CHANGE_LENGTH) {
    return false;
  }
  page = parseUint8_t(&data);
  channel = parseUint8_t(&data);

  emberAfCorePrintln(formattedString, page, channel);
  return true;
}

bool emberAfPluginZigBeeEventLoggerPrintParentChange(uint8_t *data, uint16_t dataLength)
{
  EmberNodeId parentNodeId;
  EmberEUI64 parentEui64;
  const char *formattedString = lookupFormatString(EMBER_LOGGER_ZIGBEE_PARENT_CHANGE_ID);
  if (formattedString == NULL || dataLength != EMBER_LOGGER_ZIGBEE_PARENT_CHANGE_LENGTH) {
    return false;
  }
  parentNodeId = parseUint16_t(&data);
  parseArray(parentEui64, &data, EUI64_SIZE);

  emberAfCorePrintln(formattedString, parentNodeId, parentEui64[7], parentEui64[6], parentEui64[5], parentEui64[4], parentEui64[3], parentEui64[2], parentEui64[1], parentEui64[0]);
  return true;
}

bool emberAfPluginZigBeeEventLoggerPrintDutyCycleChange(uint8_t *data, uint16_t dataLength)
{
  EmberDutyCycleState state;
  EmberDutyCycleHectoPct limitThreshold;
  EmberDutyCycleHectoPct criticalThreshold;
  EmberDutyCycleHectoPct suspendedLimit;
  const char *formattedString = lookupFormatString(EMBER_LOGGER_ZIGBEE_DUTY_CYCLE_CHANGE_ID);
  if (formattedString == NULL || dataLength != EMBER_LOGGER_ZIGBEE_DUTY_CYCLE_CHANGE_LENGTH) {
    return false;
  }
  state = parseUint8_t(&data);
  limitThreshold = parseUint16_t(&data);
  criticalThreshold = parseUint16_t(&data);
  suspendedLimit = parseUint16_t(&data);

  emberAfCorePrintln(formattedString, state, limitThreshold, criticalThreshold, suspendedLimit);
  return true;
}

bool emberAfPluginZigBeeEventLoggerPrintChildAdded(uint8_t *data, uint16_t dataLength)
{
  EmberNodeId childNodeId;
  EmberEUI64 childEui64;
  const char *formattedString = lookupFormatString(EMBER_LOGGER_ZIGBEE_CHILD_ADDED_ID);
  if (formattedString == NULL || dataLength != EMBER_LOGGER_ZIGBEE_CHILD_ADDED_LENGTH) {
    return false;
  }
  childNodeId = parseUint16_t(&data);
  parseArray(childEui64, &data, EUI64_SIZE);

  emberAfCorePrintln(formattedString, childNodeId, childEui64[7], childEui64[6], childEui64[5], childEui64[4], childEui64[3], childEui64[2], childEui64[1], childEui64[0]);
  return true;
}

bool emberAfPluginZigBeeEventLoggerPrintChildRemoved(uint8_t *data, uint16_t dataLength)
{
  EmberNodeId childNodeId;
  EmberEUI64 childEui64;
  const char *formattedString = lookupFormatString(EMBER_LOGGER_ZIGBEE_CHILD_REMOVED_ID);
  if (formattedString == NULL || dataLength != EMBER_LOGGER_ZIGBEE_CHILD_REMOVED_LENGTH) {
    return false;
  }
  childNodeId = parseUint16_t(&data);
  parseArray(childEui64, &data, EUI64_SIZE);

  emberAfCorePrintln(formattedString, childNodeId, childEui64[7], childEui64[6], childEui64[5], childEui64[4], childEui64[3], childEui64[2], childEui64[1], childEui64[0]);
  return true;
}

bool emberAfPluginZigBeeEventLoggerPrintChildTimedOut(uint8_t *data, uint16_t dataLength)
{
  EmberNodeId childNodeId;
  EmberEUI64 childEui64;
  const char *formattedString = lookupFormatString(EMBER_LOGGER_ZIGBEE_CHILD_TIMED_OUT_ID);
  if (formattedString == NULL || dataLength != EMBER_LOGGER_ZIGBEE_CHILD_TIMED_OUT_LENGTH) {
    return false;
  }
  childNodeId = parseUint16_t(&data);
  parseArray(childEui64, &data, EUI64_SIZE);

  emberAfCorePrintln(formattedString, childNodeId, childEui64[7], childEui64[6], childEui64[5], childEui64[4], childEui64[3], childEui64[2], childEui64[1], childEui64[0]);
  return true;
}

bool emberAfPluginZigBeeEventLoggerPrintStackStatus(uint8_t *data, uint16_t dataLength)
{
  EmberStatus status;
  const char *formattedString = lookupFormatString(EMBER_LOGGER_ZIGBEE_STACK_STATUS_ID);
  if (formattedString == NULL || dataLength != EMBER_LOGGER_ZIGBEE_STACK_STATUS_LENGTH) {
    return false;
  }
  status = parseUint8_t(&data);

  emberAfCorePrintln(formattedString, status);
  return true;
}

bool emberAfPluginZigBeeEventLoggerPrintTimeSync(uint8_t *data, uint16_t dataLength)
{
  uint32_t utcTime;
  uint32_t secondsSinceBoot;
  const char *formattedString = lookupFormatString(EMBER_LOGGER_ZIGBEE_TIME_SYNC_ID);
  if (formattedString == NULL || dataLength != EMBER_LOGGER_ZIGBEE_TIME_SYNC_LENGTH) {
    return false;
  }
  utcTime = parseUint32_t(&data);
  secondsSinceBoot = parseUint32_t(&data);

  emberAfCorePrintln(formattedString, utcTime, secondsSinceBoot);
  return true;
}

bool emberAfPluginZigBeeEventLoggerPrintTrustCenterJoinDecision(uint8_t *data, uint16_t dataLength)
{
  EmberNodeId nodeId;
  uint8_t decision;
  const char *formattedString = lookupFormatString(EMBER_LOGGER_ZIGBEE_TRUST_CENTER_JOIN_DECISION_ID);
  if (formattedString == NULL || dataLength != EMBER_LOGGER_ZIGBEE_TRUST_CENTER_JOIN_DECISION_LENGTH) {
    return false;
  }
  nodeId = parseUint16_t(&data);
  decision = parseUint8_t(&data);

  emberAfCorePrintln(formattedString, nodeId, decision);
  return true;
}

#else

printFunction *emberAfPluginZigBeeEventLoggerLookupPrintFunction(EmberZigbeeLoggerEventTypes type)
{
  return emberAfPluginZigBeeEventLoggerPrintHexData;
}

#endif  // #ifdef EMBER_AF_PLUGIN_ZIGBEE_EVENT_LOGGER_INCLUDE_STRINGS_IN_LOGS
