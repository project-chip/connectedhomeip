name = ZCL-IP

category=IP

qualityString=Production Ready 
# This is one of the following:  productionReady, testTool, extensionNeeded
quality=production

# Description of the plugin
description=This plugin provides functionality to encapsulate and extract ZCL packets over IP to a remote internet connected host.

implementedCallbacks=emberAfPluginZclIpInitCallback

sourceFiles=zcl-ip.c, zcl-ip-cli.c

options=maxPayloadSize

released=FALSE

maxPayloadSize.name=Max Payload Size
mayPayloadSize.description=Maximum size of ZCL messages accepted by the device.
maxPayloadSize.type=NUMBER:1,65535
maxPayloadSize.default=150

