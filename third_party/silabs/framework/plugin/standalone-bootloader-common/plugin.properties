name=Standalone Bootloader Common 
category=Standalone Bootloader
description=Code common to both the client and server of the Ember standalone bootload protocol.

# Any string is allowable here.  Generally it is either: Production Ready, Test Tool, or Requires Extending
qualityString=Production Ready
# This must be one of the following:  productionReady, testTool, extensionNeeded
quality=production

implementedCallbacks=ezspIncomingBootloadMessageHandler,ezspBootloadTransmitCompleteHandler,emberIncomingBootloadMessageHandler,emberBootloadTransmitCompleteHandler

sourceFiles=bootloader-protocol.c
sourceFilesSoc=bootloader-message-soc.c
sourceFilesHost=bootloader-message-host.c

includedByDefault=false
