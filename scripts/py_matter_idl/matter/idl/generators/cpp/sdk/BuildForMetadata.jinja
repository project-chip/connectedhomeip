# DO NOT EDIT MANUALLY - Generated file
#
# Cluster generated code for constants and metadata based on {{input_name}}

source_set("ids") {
  sources = [ "{{cluster.name}}Ids.h" ]
  deps = [
    "${chip_root}/src/app/data-model-provider:metadata",
    "${chip_root}/src/lib/core:types",
  ]
  configs = [ "${chip_root}/zzz_generated/clusters:includes" ]
}

source_set("metadata") {
  sources = [ "{{cluster.name}}Metadata.h" ]
  deps = [
    "${chip_root}/src/app/data-model-provider:metadata",
    "${chip_root}/src/lib/core:types",
  ]
  public_deps = [ ":ids" ]
  configs = [ "${chip_root}/zzz_generated/clusters:includes" ]
}

source_set("{{cluster.name}}") {
  public_deps = [
    ":ids",
    ":metadata",
  ]
}
